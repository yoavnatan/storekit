User-agent: *
Allow: /
Disallow: /admin
Disallow: /checkout
# Block the API surface, but the product feed under it MUST stay fetchable —
# Google Merchant Center / Meta Catalog crawl it and honour robots.txt. Google
# resolves the longest matching rule, so this Allow wins for the feed path only.
Allow: /api/feed/
Disallow: /api

# AI answer engines (AIO / "AI Optimization"): we explicitly WELCOME the AI
# crawlers so our stores + products can be surfaced and cited in AI shopping
# answers (ChatGPT, Perplexity, Gemini, Apple Intelligence, Claude, ...). A
# named group replaces the '*' rules for that bot, so the same protections are
# repeated here. Google-Extended / Applebot-Extended are grounding opt-ins —
# listing them Allow is a deliberate "yes, use our catalog" (see /llms.txt).
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: Claude-Web
User-agent: Google-Extended
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: Meta-ExternalAgent
User-agent: CCBot
Allow: /
Disallow: /admin
Disallow: /checkout
Allow: /api/feed/
Disallow: /api

# Update the host below to your real domain.
# Two sitemaps (Google merges multiple Sitemap directives): the static one for
# build-time public routes, and the dynamic one enumerating all store + product
# pages (SSR — absent from the static sitemap).
Sitemap: https://dezabin.com/sitemap-index.xml
Sitemap: https://dezabin.com/sitemap-content.xml
