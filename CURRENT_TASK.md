# Current Task

Keep it simple. 

## Your instruction


## Next

*יום שישי*


- לשייך תמונה לגירסה (שתהיה את האופציה)
- שיפור פיד חנויות ראשי + סקלטונים!
- לשקול להציג את העגלה בצד, כאשר נמצאים בדסקטופ.

*יום ראשון*

- שיפור דף חנות, תצוגת קטגוריות נכונה יותר.
- אני מרגיש שהאתגר פה הוא איך להעלות הרבה מוצרים בבת אחת.
- לשפר את שורת החיפוש (לוודא שהתוצאות שמוצגות שם הן ברורות)
- דשבורד אדמין לי כמנהל של כל הפלפטורמה — דיווח בלבד (הכנסות למוכר, סה"כ פלטפורמה); אין תור תשלומים/כפתור "שולם" — ראה "תשלומים — ארכיטקטורה" למטה.
- לשפר סינונים ופלטורים בכל מקום שיש בו רשימות/פריטים
- צריך לחשוב: האתר צריך להיות בעצם עם שם נפרד לכל חנות ב- seo כלומר חיפוש בגוגל יביא את החנות עצמה בתוך הפלטפורמה. צריך להדגיש את זה.
- לשונית פרסום בדשבורד מוכרים

*לאחר מכן*
- לדבר על אוטומציות (מוכר, קונה, אני)
- לוגו
- חיבור תשלום אמיתי — split payment (SUMIT/תקבול, ספק סופי טרם נבחר), `/api/payment/confirm` webhook, עדכון SellerBalance (דיווח בלבד)
- שליחת מייל לקונה ומוכר אחרי הזמנה (SendGrid/Brevo)
- ניהול מלאי: `decrementStock` אטומי עם mutex, אינטגרציית Sendit לציטוט משלוח דינמי
- Checkout tracking: `fbq InitiateCheckout` בעמוד checkout, `fbq Purchase` ב-webhook
- צריך להבין איך לאסוף דאטה רלוונטי כבר מההתחלה.



### תשלומים — ארכיטקטורה  (ישראל בלבד)
> ✅ ההחלטה התקבלה (עודכן 2026-07-06) — **Split payment דרך חברת הסליקה**. הפלטפורמה לא מחזיקה כסף של מוכרים בכלל — לכל מוכר חשבון עסק/sub-merchant משלו אצל הספק, והתשלום מתפצל אוטומטית ב-checkout: חלק המוכר → המוכר, עמלה → הפלטפורמה. סיבה: החזקת כסף מוכרים + חלוקה ידנית = שירות תשלום מפוקח (רישיון PSP מבנק ישראל). כך הפלטפורמה נשארת שכבת תוכנה בלבד; המוכרים אחראים על המוצר/פולפילמנט.
- **ספק — טרם נבחר סופית.** נבדקו לעומק: **SUMIT** ו-**תקבול (Takbull)** — שניהם היחידים בשוק הישראלי שמאשרים מודל מרקטפלייס עם split אמיתי (אושר גם ע"י תיעוד ישיר, לא רק חיפוש). Cardcom/Payme/Tranzila/PayPlus/Meshulam/יעד שריג/iCredit — אין עדות לתמיכה ב-split/sub-merchant; כנראה לא רלוונטיים למודל הזה.
  - **תקבול** — כל מוכר = ישות עצמאית לגמרי, ניסוח מפורש: "בעל המרקטפלייס אינו צד בעסקה" — ההתאמה הכי חדה למטרה הרגולטורית (למנוע סיווג כשירות תשלום). API/עמלות לא מתועדים פומבית — צריך שיחה ישירה.
  - **SUMIT** — מנהל המרקטפלייס יוצר ומנהל את חשבון-העסק של כל מוכר (יותר "מנוהל"), אך הכסף זורם ישירות לחשבון המוכר. יתרון: REST API פומבי ומתועד היטב (`/website/companies/create/` ליצירת עסק חדש) — מתאים יותר לזרימת הרשמת-מוכר-עצמי אוטומטית שאנחנו רוצים. חשבון שנוצר דרך API מחויב מיד, אין חודש ניסיון.
  - **מה לשאול את שתיהן:** מבנה עמלות מדויק (% + עמלה קבועה, דמי חשבון חודשי למוכר), API להרשמת מוכר עצמי, זמן הפעלה בפועל לחשבון מוכר חדש, ואישור מפורש שהמודל לא הופך את הפלטפורמה לספק שירותי תשלום מפוקח.
- **צ׳ק אאוט (ללא חובת הרשמה)** — כל קונה, מחובר או לא, יכול לקנות. מחובר → פרטים ממולאים מראש; לא מחובר → ממלא ידנית. שם + מייל + טלפון + כתובת → העברה לדף תשלום → חזרה עם אישור → יצירת Order ב-`data/orders.json`.
- **webhook אישור תשלום** — `/api/payment/confirm`: מאמת חתימה מהספק, יוצר Order, מעדכן `SellerBalance` (לדיווח בלבד — הכסף כבר הגיע ישירות למוכר דרך ה-split), שולח מייל לקונה ולמוכר.
- **`data/orders.json`** — יצירת קובץ + `src/lib/orders.ts` עם interface Order (ראה AI_INSTRUCTIONS לפרטי השדות).
- **`data/seller-balances.json`** — יצירת קובץ + `src/lib/seller-balances.ts`: totalEarned per seller/store (דיווח בלבד, לא ניהול תשלומים — אין totalPaidOut/תור תשלומים כי הספק משלם למוכר ישירות).
- **סקשן הזמנות בדשבורד מוכרים** — טבלת הזמנות נכנסות: תאריך, קונה, פריטים, סכום, סטטוס משלוח. מוכר מעדכן סטטוס + מספר מעקב.
- **דשבורד אדמין (`/admin`)** — תצוגת דיווח: הכנסות שהרוויח כל מוכר, סה"כ הכנסות פלטפורמה. **אין** יתרות-לתשלום/תור תשלומים/כפתור "שולם" — הספק כבר שילם למוכר ישירות.
- **התראות מוכר** — email כשנכנסת הזמנה (SMTP: SendGrid / Brevo).
- **commission %** — להגדיר ב-`store.config.ts` (`checkout.commissionPercent`). מנוכה אוטומטית ע"י הספק בעת ה-split.

### משלוחים — ישראל בלבד
> ⚠️ ספקי משלוח ישראליים בלבד.
- **הגדרות בדשבורד מוכר** — flatRate, freeAbove, processingDays (שדות חדשים ב-`Store.shipping`).
- **חישוב מחיר משלוח ב-checkout** — קריאה מ-`store.shipping` לפי סל המוצרים של כל חנות.
- **`StoreProduct.weight`** — שדה חדש (גרמים). נדרש לציטוט Sendit. אופציונלי בשלב ראשון (flatRate מספיק).
- **אינטגרציה Sendit** (שלב שני) — `/api/shipping/quote` מחזיר מחיר דינמי לפי כתובת + משקל.
- **מעקב משלוח** — מוכר מזין מספר מעקב בדשבורד → נשלח ב-email לקונה.

### SEO (קטן, מהיר)
- **`store.url`** ב-`store.config.ts` → להחליף `https://example.com` בדומיין האמיתי (Canonical, sitemap, OG לא נכונים עד אז).
- **`store.locale`** → `he_IL` (כרגע `en_US`, משפיע על `og:locale`).
- **`robots.txt`** — להוסיף ב-`public/robots.txt` (כרגע לא קיים).

### שיווק / Tracking — מודל שתי-שכבות (הוחלט 2026-07-06)
> רעיון הליבה: (1) **בסיס** — כל מוצר מכל חנות מפורסם כברירת מחדל, ממומן ע"י הפלטפורמה (לא ע"י המוכר) — כדי להוכיח למוכרים שהפלטפורמה מביאה קונים עוד לפני שהם משלמים אקסטרה. (2) **בוסט** — מוכר שרוצה יכול לשלם תוספת ולקבל תקציב פרסום גדול יותר ספציפית למוצרים שלו.
- **הפער הטכני הדחוף ביותר: אין feed מוצרים.** כדי שקמפיין בסיס יעבוד (Performance Max / Advantage+ Catalog) צריך endpoint שמייצא את **כל** ה-`StoreProduct`ים בפורמט שגוגל (Merchant Center XML/CSV) ומטא (Catalog) דורשים. היום קיים רק OG `product:price:*` פר-דף (`Seo.astro`) — לא feed מרוכז. זה הצעד הבא הכי קונקרטי אם רוצים להריץ פרסום בסיס בקרוב.
- **בוסט = קמפיין נפרד, לא "משקל" בתוך קמפיין משותף** — Performance Max/Advantage+ לא תומכים בהעדפת מוצר בודד בתוך קמפיין אחד. מימוש: קמפיין ייעודי נוסף פר חנות/מוצר (`adCampaigns` על Store), נוצר דרך Google Ads API / Meta Marketing API, מחויב דרך ספק ה-split payment.
- **GTM + Meta Pixel IDs** — להגדיר ב-`store.config.ts`. `ViewContent` ו-`AddToCart` כבר מוזרקים ומחכים.
- **`fbq InitiateCheckout`** — להוסיף בעמוד checkout לפני תשלום.
- **`fbq Purchase`** — להוסיף ב-`/api/payment/confirm` אחרי אישור תשלום (server-side).
- **Conversions API (Meta server-side)** — שליחת Purchase מהשרת לפי Meta CAPI. נדרש: `access_token`, `pixel_id` ב-env vars.
- **GTM container setup** — triggers: `view_item` → GA4, `add_to_cart` → GA4. ה-dataLayer כבר מוכן.

## Recommended next step
כדאי לסגור את אכיפת המלאי האטומית בצ'ק-אאוט עצמו (decrementStock) — היא עדיין לא קיימת, רק צד הלקוח מכוסה.
