# תוכנית מעבר למסד נתונים אמיתי

> מסמך תוכנית (לא משימה מיידית). נכתב 2026-07-27 אחרי סריקה של כל שכבת הנתונים בפועל.
> מופנה מ-`AI_INSTRUCTIONS.md` → Hard rules → Scalability, ומ-`GO_LIVE_CHECKLIST.md` סעיף 6.
> משלים את `CONTEXTUAL_SEARCH_STRATEGY.md` — החיפוש הסמנטי תלוי במעבר הזה.

**בשורה אחת:** Postgres, סכימה מנורמלת חוץ ממה שבאמת לא צריך, אינדקסים ואילוצים נכתבים
באותה מיגרציה שיוצרת את הטבלה, והמעבר נעשה מודול-מודול מאחורי חתימות הפונקציות הקיימות.

---

## 1. מה יש היום (מצב בפועל, נמדד)

19 קבצי JSON ב-`data/`, 25 מודולי `lib/*.ts` שקוראים/כותבים אותם. הגדולים:
`store-products.json` (834 מוצרים, 720K), `orders.json` (210 הזמנות, 296K),
`stores.json` (42), `sellers.json` (46), `messages.json` (72), `store-categories.json` (130).

הקבצים מתחלקים לשני סוגים, וזה משנה איך מהגרים אותם:

| סוג | קבצים | הופך ל- |
|---|---|---|
| **מערך רשומות** | stores, store-products, orders, sellers, messages, notifications, store-categories, ad-campaigns, admin-messages, error-log | טבלה רגילה, שורה לרשומה |
| **אובייקט לפי מפתח** (דלי) | store-pageviews, product-pageviews, analytics-events, wishlist-counts, store-favorite-counts, user-carts, admin-tab-views, platform-ads | לא טבלה אחת-לאחת — ראה §5 |

**מה כבר עובד לטובתנו:** כל הגישה לנתונים כבר מרוכזת ב-`lib/*.ts` כפונקציות adapter נקיות,
ה-IDs כבר `crypto.randomUUID()` (לא מונים רצים — אין התנגשות בייבוא), ופריטי ההזמנה כבר
שומרים snapshot של שם/מחיר/תמונה. שלושת אלה חוסכים את רוב הכאב.

---

## 2. איזה DB — Postgres

**לא SQLite.** שתי סיבות שנוגעות ישירות לקוד הקיים:
1. **כתיבות בכל בקשה.** ה-middleware כותב היום ב-3 מקומות על כל טעינת עמוד
   (`recordPageView`, `recordProductView`, `recordAnalyticsEvent`). SQLite הוא כותב-יחיד;
   זה בדיוק דפוס העומס שיחנוק אותו.
2. **חיפוש וקטורי.** `CONTEXTUAL_SEARCH_STRATEGY.md` דורש embeddings + vector search.
   `pgvector` הוא הרחבה של Postgres — בחירה ב-DB בלי זה תכפה מיגרציה שנייה בעוד חצי שנה.

בונוס: `JSONB` נותן לשמור בדיוק את השדות שלא שווה לנרמל (`specs`, `variants`) בלי לוותר
על שאילתות עליהם, ו-full-text search בעברית זמין מיידית.

### 2.1 אירוח — נסגר 2026-08-02: **Neon, אזור `eu-central-1` (פרנקפורט)**

**קודם כל אי-הבנה שעולה הרבה כסף אם לא מפרקים אותה: מה זה "DB מהיר בישראל".**
ההשהיה שחשובה היא בין **שרת האפליקציה ל-DB**, לא בין הגולש ל-DB. עמוד אחד מריץ
5–15 שאילתות; אם ה-DB במקום אחר מהשרת, כל אחת מהן משלמת את המרחק שוב. הגולש, לעומת
זאת, משלם את המרחק **פעם אחת** לכל בקשה. פרנקפורט↔תל-אביב = ~63ms
([מדידה](https://www.economize.cloud/resources/aws/latency/eu-central-1-vs-il-central-1/)):
שרת+DB שניהם בפרנקפורט = 63ms פעם אחת לגולש הישראלי, ו-<1ms לכל שאילתה.
שרת בפרנקפורט ו-DB בתל-אביב = 63ms **כפול 10 שאילתות**. כלומר הכלל היחיד שאסור להפר
הוא **שרת ו-DB באותו אזור**; איזה אזור זה — הרבה פחות משנה.

| אפשרות | עלות התחלה | אזור | pgvector | הערה |
|---|---|---|---|---|
| **Neon** ✅ | $0 (Free), Launch לפי שימוש — ~$5–15/חודש, ללא רצפה חודשית; אחסון $0.35/GB | פרנקפורט | מובנה | pooler מובנה, scale-to-zero, branching לסביבת בדיקות |
| Supabase | $25/חודש קבוע (Pro) | פרנקפורט | מובנה | משלמים גם על Auth/Storage/Realtime שלא נשתמש בהם |
| AWS RDS `il-central-1` | ~$30–60/חודש מינימום ריאלי | תל-אביב | ידני | הכי קרוב פיזית, אבל תוספת מחיר על האזור ואפס scale-to-zero |

**למה Neon:** הכי זול בהתחלה בפער גדול (בלי רצפה חודשית — משלמים על שימוש בפועל),
pgvector ו-pooler בלי התקנה, וסביבת בדיקות היא branch של הפרודקשן בשנייה. הן Neon והן
Supabase לא מציעות אזור בישראל, ולשתיהן פרנקפורט הוא האזור המומלץ לישראל.

**מה זה לא נועל:** אנחנו כותבים Postgres סטנדרטי עם דרייבר `pg` רגיל — בלי SDK של הספק
ובלי תחביר קנייני. מעבר ל-RDS בתל-אביב (או לכל ספק אחר) הוא `pg_dump` + `pg_restore`
ושינוי `DATABASE_URL`, לא כתיבה מחדש. **ההחלטה הזו הפיכה ביום.** מתי כן לעבור: כשהשרת
עצמו יעבור לישראל, או כשחשבון Neon יעבור באופן עקבי את המחיר של RDS.

**החלטה נגזרת:** שרת האפליקציה חייב לשבת גם הוא בפרנקפורט (`eu-central-1`) —
GO_LIVE §7.

**חובה pooler** (§7 סעיף 16) — ב-Neon זה מחרוזת החיבור עם `-pooler` בשם ההוסט.

---

## 3. העיקרון: אותן פונקציות, אותם טיפוסים — אבל אסינכרוניות

כל מודול ב-`lib/` נשאר עם אותן פונקציות מיוצאות ואותם טיפוסים. הפנימיות מתחלפות:
`readFileSync` + `filter` → שאילתה.

> **תיקון לניסוח המקורי (2026-08-02, נמדד).** כתוב כאן קודם "החתימות לא משתנות", וזה לא נכון
> בציר היחיד שבאמת עולה עבודה: **כל 68 הפונקציות המיוצאות ב-`stores.ts`/`store-products.ts`/
> `orders.ts`/`seller-auth.ts` הן היום סינכרוניות** (`getStoreBySlug(slug): Store`), ושאילתה היא
> `Promise`. כלומר כל אחת מהן הופכת ל-`async`, וכל קורא שלה צריך `await` — 47 קבצים ב-`src`
> מייבאים רק את שני המודולים הראשונים. זה שינוי מכני וגדול, לא "רק הפנימיות".
> מה שכן מקל: ב-Astro אפשר `await` ישירות ב-frontmatter של עמוד, ו-`/api/*` ממילא async.
> **מסקנה מעשית:** שלב 2 מתבצע מודול-מודול **ביחד עם הקוראים שלו**, לא מודול ואז תיקון גורף.

```
getStoreBySlug(slug)  →  SELECT * FROM stores WHERE slug = $1
```

**חריג אחד שחייב לשבור את הכלל** — פונקציות `getAll*()` שמחזירות הכל:
`getAllOrders()`, `getAllStores()`, `readProducts()`, `getMessagesBySeller()`.
היום זה בסדר (210 הזמנות); ב-100,000 הזמנות פונקציה שמחזירה הכל לזיכרון היא באג.
**במעבר הן חייבות לקבל פרמטרי סינון/עימוד ולדחוף אותם לשאילתה.** רשימת הקוראים שלהן
כבר קצרה — `admin-stats.ts`, `seller-performance.ts`, `platform-performance.ts` — וכולם
ממילא עושים אגרגציה שה-DB יעשה טוב יותר (`SUM`/`GROUP BY` במקום `reduce`).

> **✅ בוצע 2026-08-03. הפירוט המלא — מה הפך לשאילתה, מה נמחק, ומה נשאר בכוונה — ב-§8, בסוף.**
> הכלל שהחליט הכול, ושווה לקרוא לפני שנוגעים בשכבת דיווח כלשהי בהמשך: **`GROUP BY` שנכתב
> כלולאת `for` שייך למסד; אריתמטיקה מקבלת את הנתונים כפרמטר ונשארת סינכרונית.** כל פונקציה
> טהורה שירדה מנתיב הרינדור **לא נמחקה** — היא התאום הניתן-לבדיקה של השאילתה שהחליפה אותה,
> ו-`tests/reporting-invariants.test.ts` מריץ את שתיהן על אותן שורות ודורש אותה תשובה.

---

## 4. סכימה — הטבלאות

### ליבה
```
sellers(id uuid pk, name, email citext unique, password_hash, google_id unique,
        tier text, created_at timestamptz)

stores(id uuid pk, seller_id → sellers, slug citext unique, name, tagline, description,
       colors jsonb, categories text[], shipping jsonb, banner_image, profile_image,
       address, address_visible bool, hours jsonb, hours_visible bool,
       blocked bool default false, promo_weight int default 0,
       bg_colors text[], feed_sync jsonb, feed_export_token,
       custom_domain_hostname citext unique, custom_domain_status, custom_domain_added_at,
       created_at timestamptz)

store_previous_slugs(slug citext pk, store_id → stores, replaced_at)
```
**`previousSlugs` יוצא מהמערך לטבלה משלו** — זה מה שהופך את חיפוש ה-301 לשליפה רגילה
עם אינדקס רגיל, במקום אינדקס GIN על מערך. גם `isSlugTaken` נהיה בדיקה אחת מול שתי טבלאות.
**`customDomain` נפרס לשלוש עמודות** ולא נשאר JSONB — כי `hostname` הוא השליפה הכי חמה
באפליקציה (§6), ואי אפשר לאנדקס אותו טוב בתוך JSONB.

### קטלוג
```
store_categories(id uuid pk, store_id → stores, parent_id → store_categories, name, position)

store_products(id uuid pk, store_id → stores, slug citext, name, description,
               price_agorot bigint, stock int, sku, category_id → store_categories,
               hidden bool default false, blocked bool default false,
               tags text[], specs jsonb, variants jsonb, created_at,
               UNIQUE (store_id, slug),
               UNIQUE (store_id, sku) WHERE sku IS NOT NULL)

product_images(product_id → store_products, position int, url, cloudinary_public_id,
               PRIMARY KEY (product_id, position))

product_variant_stock(product_id → store_products, combo_key text, stock int,
                      PRIMARY KEY (product_id, combo_key))
```
**`variantStock` חייב לצאת מה-JSON לטבלה** — זו הטבלה היחידה שבה `UPDATE` אטומי מחליף
את ה-Mutex (§7 סעיף 5). `variants` (הגדרת המימדים) נשאר JSONB כי אף פעם לא שולפים לפיו.

### כסף
```
orders(id uuid pk, buyer_name, buyer_email, buyer_phone, buyer_address, buyer_id → sellers,
       shipping_agorot bigint, total_agorot bigint,
       payment_ref text UNIQUE, payment_status, shipping_status, tracking_number,
       created_at, updated_at)

order_items(id uuid pk, order_id → orders, product_id, product_name, product_slug,
            store_slug, store_name, price_agorot bigint, qty int, image,
            selected_variants jsonb)

order_stores(order_id → orders, store_slug, store_name,
             subtotal_agorot bigint, shipping_agorot bigint, delivery_method,
             seller_note, PRIMARY KEY (order_id, store_slug))
```
**`order_items` שומר snapshot ולא FK למוצר** — זה כבר המצב היום ו**אסור לתקן את זה**
במעבר. אם ההזמנה תצביע על המוצר החי, שינוי מחיר ישכתב היסטוריה פיננסית.
זו הטעות הקלאסית ב"נרמול" של הזמנות.

**`*_agorot bigint` — כאן זה נסגר סופית.** היום הסכומים הם `number` עם 2 ספרות
אחרי הנקודה, ו-`src/lib/money.ts` מעגל בכל גבול כדי שהזנב הבינארי לא יצטבר. זה
פתרון ביניים מוצהר: העמודות למעלה כבר מוגדרות `bigint` באגורות, וזו ההזדמנות
היחידה לעשות את זה בלי להגר את אותם נתונים פעמיים. במעבר — `money.ts` הופך
מ"עיגול" ל"המרה", וכל קריאה/כתיבה עוברת דרכו. אין סכום חלקי בשום מקום בקוד.

```
money_events(id uuid pk, at timestamptz, type, order_id → orders, checkout_ref,
             store_slug, amount_agorot bigint, from_value text, to_value text,
             actor text, detail text)
```
**append-only.** בלי UPDATE ובלי DELETE — תיקון נרשם כשורה חדשה. שווה לאכוף את זה
ברמת ההרשאות (`REVOKE UPDATE, DELETE`), לא רק בקוד: יומן שאפשר לשכתב לא מוכיח כלום.
אינדקס על `(order_id, at)` לציר-זמן של הזמנה, ועל `(at DESC)` לתצוגת האדמין.
מאז סשן ג׳ לשונית היומן באדמין גם **מחפשת טקסט חופשי** (מספר הזמנה, אסמכתא, חנות,
פירוט) בתוך חלון תאריכים — היום זו סריקה בזיכרון על כל היומן
(`src/lib/admin-moneylog-filter.ts`). ב-DB זה הופך ל-`WHERE at BETWEEN … AND (…)`,
ולכן צריך גם אינדקס trigram (`pg_trgm` על `order_id/checkout_ref/store_slug/detail`)
או `tsvector` — בלעדיו החיפוש הופך ל-seq scan על טבלה שרק גדלה ולא נמחקת ממנה כלום.

```
checkout_idempotency(key text PRIMARY KEY, status, checkout_ref, order_ids uuid[],
                     at timestamptz)
```
✅ **נעשה (2026-08-02) — השער על יותר מ-instance אחד נפתח.** `checkout-idempotency.ts` הסתמך על
`Mutex` בתוך תהליך node יחיד: שני instances = שני mutexes = שני חיובים לאותה קנייה. היום ה-PK על
`key` הוא הפוסק — `INSERT … ON CONFLICT DO UPDATE … WHERE <הרשומה ישנה/נטושה>`, ובקשה שמקבלת 0
שורות יודעת לשחזר או לסרב במקום לחייב. `money-events.ts` עבר באותו דיף מאותה סיבה.

### שאר
```
messages, admin_messages, notifications, ad_campaigns, brand_campaigns, error_log
```
כולם המרה ישירה אחת-לאחת של המערך הקיים.

---

## 5. הדליים — מה שלא ממירים אחד-לאחד

ארבעה קבצים הם דלי יומי, ותרגום נאיבי שלהם יוצר בעיה:

**`store-pageviews` / `product-pageviews` / `analytics-events`** — היום כל דלי יומי מחזיק
**מערך של כל מזהי המבקרים** של אותו יום (`visitors: [...]`), כדי לספור מבקרים ייחודיים.
מדדתי: יום עמוס אחד כבר מחזיק 359 מזהים. **זה גדל בלי גבול** — ב-10,000 מבקרים ליום זו
שורה עם 10,000 ערכים שנקראת ונכתבת מחדש בכל טעינת עמוד.

הפתרון:
```
page_view_events(store_id → stores, product_id, visitor_id, day date, occurred_at,
                 UNIQUE (store_id, product_id, visitor_id, day))
page_view_daily(store_id, product_id, day, total int, uniques int,
                PRIMARY KEY (store_id, product_id, day))
```
הכתיבה נהיית `INSERT ... ON CONFLICT DO NOTHING` (שורה אחת, בלי לקרוא כלום),
והספירה נהיית `COUNT(*)` / `COUNT(DISTINCT visitor_id)`. `page_view_daily` הוא
rollup לילי שמאפשר למחוק את ה-events הגולמיים אחרי 90 יום בלי לאבד היסטוריה.

**`wishlist-counts` / `store-favorite-counts`** — היום מונים שמורים. מונה שמור **תמיד**
נסחף מהאמת בסופו של דבר. במעבר הם הופכים ל-`COUNT(*)` על טבלאות ה-wishlist/favorites
האמיתיות; אם זה יאט (לא יאט בסקייל הזה) — `COUNT` עם אינדקס, ורק אם באמת צריך, מונה
מתוחזק בטריגר.

> **נסגר 2026-08-02 — שני הקבצים האלה לא מיובאים, וזו לא השמטה.** מיפוי של כל צרכני
> ה-JSON העלה ששניהם כבר לא מה שהם נראים:
>
> · **`store-favorite-counts.json` — דאטה מת. להתעלם ולא להעביר.** אין לו קורא חי:
>   `store-favorite-counts.ts` כבר מחשב את המספר מ-`user-carts.json` בזמן אמת, ורק
>   `scripts/seed-demo-data.mjs` עוד כותב לקובץ. הוא נמחק בשלב 3 עם השאר, בלי יעד ב-DB.
>
> · **`wishlist-counts.json` — הפיצ'ר עובר, הקובץ לא.** המודול חי (3 צרכנים), אבל
>   הקובץ ממופתח ב-**slug של מוצר בלבד**, בלי חנות — וזה בדיוק §7.1: 47 slugs חוזרים
>   בין חנויות, כלומר שתי חנויות חולקות מונה אחד והמספר שגוי כבר היום. לייבא אותו זה
>   לייבא מספר לא נכון. היעד כבר בנוי — `wishlist_items` + `COUNT(*)` על
>   `wishlist_items_product_idx` — ושלב 2 מחליף את שלושת הצרכנים לשאילתה.
>
> **תלוי בתיקון של אותו יום:** עד 2026-08-02 הייבוא הפיל את *כל* פריטי ה-wishlist
> (שני צדי המפתח `productKey` נכתבו בנפרד עם מפרידים שונים), ולכן `COUNT(*)` היה מחזיר
> 0 בכל מקום. `verify-import.mjs` מעגן עכשיו את שלוש טבלאות מצב-הקונה מול ה-JSON.

**`user-carts`** → `cart_items(user_id, store_slug, product_id, variant_key, qty, added_at)`
ו-`wishlist_items(user_id, product_id)`. עגלה כאובייקט מקונן אחד לכל משתמש היא כתיבה של
כל העגלה בכל שינוי כמות.

---

## 6. אינדקסים — הרשימה המדויקת

נכתבים **באותה מיגרציה שיוצרת את הטבלה**. להוסיף אינדקס לטבלה ריקה זה חינם; להוסיף
אותו לטבלה עמוסה בפרודקשן זו פעולה חוסמת.

**החמים ביותר — רצים על כל בקשת עמוד** (מ-`middleware.ts`):
| אינדקס | מי משתמש |
|---|---|
| `stores(custom_domain_hostname)` unique | `getStoreByCustomDomain` — כל בקשה, כולל בקשות שאינן חנות |
| `stores(slug)` unique | `getStoreBySlug` — כל טעינת חנות/מוצר |
| `store_previous_slugs(slug)` pk | פתרון 301 + `isSlugTaken` |
| `store_products(store_id, slug)` unique | כל טעינת עמוד מוצר |

**תכופים:**
`store_products(store_id) WHERE NOT hidden AND NOT blocked` (חלקי — כל רשת מוצרים) ·
`order_items(order_id)` · `order_stores(store_slug)` (לוח המוכר) · `orders(created_at DESC)` ·
`orders(buyer_email)` · `messages(seller_id, read)` · `notifications(user_id, read)` ·
`store_categories(store_id, parent_id)` · `page_view_daily(store_id, day)` ·
`cart_items(user_id)`.

**איך מוודאים שהם באמת נתפסים:** `EXPLAIN ANALYZE` על השאילתה. הפלט אומר `Index Scan`
(תקין) או `Seq Scan` (סורק הכל — האינדקס לא בשימוש). להריץ על ארבע השורות החמות אחרי
המיגרציה. זו בדיקה של חמש דקות שמונעת אתר איטי עם משתמשים אמיתיים.

---

## 7. מקרי קצה — למנוע מראש, לא לגלות אחר כך

1–11 נבדקו מול הדאטה בפועל. 12–15 נוספו מאוחר יותר מקריאה בקוד (לא ממדידה על הנתונים) —
כולם מאותה משפחה: כשל שלא מפיל כלום ולכן מתגלה מאוחר.

1. **סלאג של מוצר ייחודי רק בתוך חנות.** מדדתי: **47 סלאגים כפולים** בין חנויות
   (`product`, `product-2`, `-`), **אפס** כפילויות בתוך אותה חנות. אילוץ `UNIQUE(slug)`
   גלובלי **יפיל את הייבוא בשורה הראשונה**. חייב להיות `UNIQUE(store_id, slug)`.
2. **SKU רק 2 מוצרים מתוך 834 מחזיקים.** האילוץ חייב להיות חלקי
   (`WHERE sku IS NOT NULL`), אחרת NULL-ים מתנגשים בחלק מהמנועים.
3. **שורות ישנות בפורמט אחר.** `store-pageviews` מחזיק גם `64` (מספר) וגם
   `{total, visitors[]}` (אובייקט) — שתי גרסאות של אותו שדה. סקריפט הייבוא חייב לנרמל,
   לא להניח. אותו סוג בעיה בכל שדה שנוסף מאוחר (`hidden`, `categoryId`, `sku` חסרים ברוב המוצרים).
4. **התנגשות סלאג בין שני מוכרים בו-זמנית.** היום `createStore` בודק-ואז-כותב — מרוץ אמיתי
   שקיים בקוד. **לתקן במעבר:** לא לבדוק מראש, אלא לנסות `INSERT`, לתפוס את הפרת ה-unique
   ולנסות `foo-2`. ה-DB הוא הסמכות היחידה שלא יכולה לפספס.
5. **מלאי — כאן ה-Mutex מת.** `decrementStock` היום מוגן ב-Mutex בתהליך אחד; זה נשבר ברגע
   שרצים שני שרתים. במעבר:
   ```sql
   UPDATE product_variant_stock SET stock = stock - $qty
   WHERE product_id = $id AND combo_key = $k AND stock >= $qty
   ```
   מספר השורות שהושפעו הוא התשובה: 0 = אין מלאי, דחה. אין מרוץ, בלי נעילה, בכל מספר שרתים.
   זה גם מה שמאפשר לבטל את `mutex.ts`.
6. **כפילות תשלום.** `UNIQUE(payment_ref)` על `orders`. webhook שנשלח פעמיים (וזה קורה)
   ייכשל על האילוץ במקום ליצור הזמנה שנייה. זה הבסיס לסעיף 2 ב-`CURRENT_TASK`.
7. **כסף בשלמים, לא בשברים.** היום המחירים הם `number` של JS ויש `Math.round(x*100)/100`
   פזור בקוד. **לאחסן אגורות כמספר שלם** (`price_agorot`). זה מוחק מחלקה שלמה של באגי
   עיגול שמתגלים רק כשמישהו מתלונן על שקל.
8. **אזור זמן.** הדליים היומיים ממופתחים במחרוזת תאריך מקומית. אם ה-DB ב-UTC והחישוב
   בישראל, גבול היום זז ב-3 שעות והדוחות לא יסתדרו. **לקבע `Asia/Jerusalem`** בהמרה
   לתאריך ולתעד את זה.
9. **מחיקות מדורגות — להחליט במפורש לכל קשר.** מחיקת חנות: מוצרים/קטגוריות/קמפיינים
   `ON DELETE CASCADE`. **הזמנות לעולם לא** — `RESTRICT`, זו רשומה פיננסית. חנות שנמחקת
   ויש לה הזמנות → סימון `deleted_at`, לא מחיקה.
10. **תמונות — 20,000 ומעלה.** התמונות יושבות ב-Cloudinary; ה-DB מחזיק רק כתובות
    (1,997 היום, מקסימום 4 למוצר). המגבלה האמיתית היא תוכנית Cloudinary ומספר
    הטרנספורמציות, לא ה-DB. **לשמור גם `cloudinary_public_id` ולא רק URL** — בלעדיו אי
    אפשר למחוק תמונה יתומה או להחליף טרנספורמציה בהמשך.
11. **`citext` לסלאגים ולמיילים.** אחרת `Acme` ו-`acme` הן שתי חנויות, ומייל בהרשמה
    יוצר חשבון כפול.
12. **`NULL` מול `false` בדגלים אופציונליים — הכשל השקט הכי סביר במעבר הזה (נוסף 2026-07-29).**
    `isProductVisible` הוא `!blocked && !hidden`, ורוב המוצרים לא מחזיקים את השדות האלה בכלל.
    ב-JS "חסר" הוא falsy ולכן המוצר גלוי; ב-SQL התנאי הופך ל-`WHERE hidden = false`, ושורה עם
    `NULL` **לא עונה עליו** — המוצר נעלם מהחנות בלי שגיאה, בלי טסט אדום. חל על כל דגל אופציונלי
    (`blocked`, `hidden`, `addressVisible`, `hoursVisible`, `selfPickup`…). **כל עמודת דגל מוגדרת
    `NOT NULL DEFAULT false` במיגרציה עצמה**, והייבוא כותב `false` ולא `NULL`.
13. **סדר בלי `ORDER BY` (נוסף 2026-07-29).** ל-DB אין סדר טבעי. `rankDefault` מחזיר 0 על תיקו,
    ועד היום התיקו נשבר בסדר הקובץ — יציב במקרה. ב-DB אותם מוצרים יחזרו בסדר אחר בין טעינות
    ואחרי כל `UPDATE`, כלומר החנות "מתערבבת" מול הקונה בלי סיבה. **לשבור תיקו בשדה יציב**
    (`created_at DESC, id`) בכל שליפת רשימה, ולא להסתמך על סדר החזרה.
14. **שדות מקוננים — בדיקות §9 לא מכסות אותם (נוסף 2026-07-29).** ספירת שורות וסכום הכסף
    שניהם עוברים גם אם `variants` / `variant_stock` / `variant_images` / `hours` נכתבו ריקים.
    המוצר ייראה תקין ופשוט לא יהיה לו מלאי לכל וריאנט. **להוסיף ספירה משלהם:** כמה מוצרים
    מחזיקים וריאנטים לפני הייבוא מול אחריו, וכנ"ל לחנויות עם שעות פתיחה.
15. **הדליים — היחידים בלי עוגן להשוואה (נוסף 2026-07-29).** מוני הצפיות והאנליטיקס אינם כסף
    ואינם שורות, כך ששתי בדיקות §9 לא רואות אותם; ייבוא חלקי שלהם נראה בגרפים כמו "שבוע חלש",
    לא כמו תקלה. **לסכם את כל המונים בקובץ מול הסכום ב-DB**, אותו עיקרון כמו סכום הכסף.
16. **Connection pooling — הסיבה מספר 1 ל"עבד בפיתוח, נפל בפרודקשן".** שרת Node שפותח
    חיבור לכל בקשה ימצה את מגבלת החיבורים של Postgres תוך דקות. חובה pool בגודל קבוע
    (או pgBouncer בהוסטינג serverless). להגדיר את זה **ביום הראשון**, לא אחרי הנפילה.

---

## 8. סדר העבודה

**✅ שלב 0 — תשתית (בוצע 2026-08-02).** `docker-compose.yml` (pgvector/pg17, TZ=UTC),
`src/lib/db.ts` (pool עצל + `withTransaction`), `DATABASE_URL`/`DB_POOL_MAX` ב-`.env.example`,
ו-`scripts/db-migrate.mjs` כמריץ מיגרציות (~100 שורות במקום framework — ה-SQL עצמו הוא ההחלטה
וצריך להיקרא כפי שהוא; מיגרציה שהורצה נעולה ב-checksum, כלומר עריכה שלה היא שגיאה ולא no-op).
**וגם: להקים CI כאן** — קובץ אחד ב-`.github/workflows/` שמריץ `npm ci` + `npx tsc --noEmit` + `npm test`
על כל push. עד היום הבדיקות רצו כי Claude מריץ אותן ידנית לפני דיווח, וזה מספיק כשיש סשן אחד
שנוגע בהכל; שלב 2 הוא ענף ארוך על פני הרבה סשנים, ושם "מישהו זכר לבדוק" מפסיק להיות אמין.
CI גם מתקין מאפס, כלומר תופס תלות שקיימת מקומית אבל חסרה ב-`package.json`. ~15 שורות, פעם אחת.

**✅ שלב 1 — סכימה + ייבוא (בוצע 2026-08-02; ה-CLI נמחק בשלב 3).** `migrations/0001_init.sql`
יוצר את כל 31 הטבלאות **כולל האינדקסים והאילוצים**, ו-`npm run db:import` ייבא את `data/*.json`
בטרנזקציה אחת והריץ מיד אחר כך את בדיקות §9. **הפקודה הזו כבר לא קיימת** — הריצה החד-פעמית
שלה הסתיימה, המסד הוא מקור האמת מאז שלב 2, וייבוא חוזר היה דורס ערכים חיים בישנים. הלוגיקה
עצמה (`scripts/lib/db-import.mjs`) נשארה, כי מסתבר שיש לה תפקיד שני: היא בונה את המסד שכל
קובצי הבדיקה רצים מולו, מתוך `tests/fixtures/db-data`.

**מה שהמדידה מול הדאטה האמיתי שינתה בתוכנית הזו:** `money_events.order_id` הוא `text` ולא
`uuid` (232 מתוך 233 שורות הן `order-1` וכו׳ — §7.3), `analytics_products.product_id` הוא
`text` בלי FK (מזהה של מוצר שנמחק הוא עדיין היסטוריה), ו-109 דליי-צפיות של מוצרים שנמחקו
**נדחים ומדווחים** במקום להיעלם בשקט.

**מה שמאמת את זה בלי להתקין כלום:** `tests/db-import.test.ts` מריץ את המיגרציה ואת הייבוא
מול Postgres בתוך התהליך (PGlite — Postgres שקומפל ל-WASM, אותם אילוצים ממש), בתוך `npm test`.

הוא רץ **פעמיים**: תמיד על `tests/fixtures/db-data/`, ובנוסף על `data/*.json` האמיתיים כשהם
קיימים. הפיצול נולד מכישלון CI ב-2026-08-02 — `data/*.json` ב-`.gitignore` (הם state של ריצה
שמשתנה תחת שרת פיתוח רץ), כלומר ב-CI התיקייה ריקה והסוויטה ייבאה כלום ונפלה
בחמישה מקומות. **הפתרון הוא לא לדלג כשאין קבצים:** ייבוא שאף אחד לא מאמת במכונה היחידה שרצה
על כל push הוא ייבוא שאף אחד לא מאמת. ה-fixture נושא את אותן מלכודות שהדאטה האמיתי לימד —
§7.1, §7.12, §7.3, §7.7, §7.11, ויתום אחד מכל סוג כדי שנתיבי ה-drop ייבדקו. הוא מצא שלושה
עוגנים שגויים ב-`verify-import.mjs` בדקה הראשונה שרץ, כולם מאותו מין: חיסור מונה-drop של טבלה
שלמה מתוך תת-קבוצה מסוננת. הריצה על הדאטה האמיתי נשארת, כי fixture מכיל רק מלכודות שמישהו
חשב עליהן.

**מה שזה לא מכסה:** §9.2 (`EXPLAIN ANALYZE`) ו-§9.5 (50 רכישות מקבילות) —
שניהם דורשים שרת אמיתי ושייכים ל-Postgres המקומי ב-docker.

**שלב 2 — החלפת מודולים, אחד-אחד (החלק הארוך).** ענף `db-stage-2`. לפי סדר תלות:
~~`sellers`~~ → ~~`stores`~~ → ~~`store-categories`~~ → ~~`store-products`~~ → ~~`orders`~~ →
~~`messages`/`notifications`~~ → ~~הדליים~~ (~~צפיות~~ → ~~אנליטיקס~~ → ~~מצב-קונה~~) →
~~השאר~~ (~~קמפיינים+פרסום~~ → ~~`error-log`/`admin-tab-views`~~). **✅ שלב 2 הושלם 2026-08-02.**
אחרי כל מודול: `npm run verify -- --all`. **קו חוצה ברשימה הזו אינו "שלב 2 נגמר"** — היא מסתיימת
במילה אחת ("השאר") שהחזיקה חמישה מודולים, וזה התגלה רק כשמדדו. הרשימה למטה היא המצב.

**✅ עבודת הכנה + `sellers` (בוצע 2026-08-02).**

*ההכנה, בלעדיה כל בדיקה בפרויקט נשברת ברגע שהמודול הראשון מתחלף:* `db.ts` קיבל תפר —
`Database`/`Queryable`/`setDatabase` — ו-`lib/` מדבר מולו, לא מול טיפוסים של `pg`.
`tests/helpers/` מרים Postgres בתוך התהליך (PGlite), בונה image פעם אחת לכל `npm test`
ונותן לכל קובץ בדיקה עותק פרטי; הטעינה עצלה, כך שקובץ שלא שולח שאילתה לא משלם כלום.
**המקור הוא `tests/fixtures/db-data` ולעולם לא `data/`** — אחרת הבדיקות שופטות שורות אחרות
במכונה של המפתח ובמכונה ששומרת על כל push, ו-`data/` ממילא ב-gitignore ומשתנה תחת שרת פיתוח רץ.

*המודול:* כל תשע הפונקציות של `seller-auth.ts` הן שאילתות, ו-21 הקוראים שלהן `await`.
שלוש דרישות חדשות שנגזרו מזה: `DATABASE_URL` הפך **חובה** (שער האתחול חוסם הפעלה בלעדיו —
בלי קובץ ליפול אליו, שרת בלי מסד עולה נראה בריא ונופל על העמוד הראשון שקורא חשבון);
`getSellerById` בודק צורת UUID לפני השאילתה, כי Postgres *דוחה* ליטרל פסול ולא סתם לא מתאים
לו, כלומר עוגייה ישנה הייתה הופכת "אתה מנותק" ל-500; ו-`ON CONFLICT` החליף את
בדוק-ואז-כתוב בהרשמה ובעריכת פרופיל (§7.4).

*מה שהמעבר תיקן דרך אגב:* שתי הרשמות בו-זמנית לאותה כתובת כבר לא יוצרות שני חשבונות,
עריכת פרופיל כותבת רק שדות שהבקשה נשאה (`COALESCE`, כלל ה-record-rev), ושתי כניסות Google
בו-זמנית מסתיימות בחשבון אחד במקום בשתיים (בקבצים) או ב-500 (מול שני אינדקסים ייחודיים).

*מה שהתגלה ושווה לדעת למודול הבא:*
· **המודול הזה לא היה מכוסה בכלל.** הבדיקות הקיימות בדקו את *טקסט המקור* של seller-auth,
  לא את התנהגותו — כלומר "כל הטסטים עוברים" (§9.1) היה יכול להישאר ירוק גם אם ההחלפה
  הייתה מחזירה null לכל חשבון. לפני שמכריזים שמודול הוחלף נכון: לבדוק שיש מה שייפול.
· **שני הדרייברים לא מסכימים על `rowCount`.** ב-PGlite `affectedRows` הוא **0** ל-`SELECT`
  (לא חסר, כך ש-`??` לא מציל), ו-`pg` מחזיר שם את מספר השורות. הכלל שמשחזר את `pg`:
  `Math.max(affectedRows, rows.length)`. זה בדיוק המספר ששלב §7.5 מסתמך עליו כפסק דין.
· **תקציב זמן הדוק בבדיקה הוא רעש.** שלוש בדיקות ReDoS נכשלו בריצה מלאה ועברו לבד;
  התקרות (20/50/100ms) היו בתוך טווח של GC. הכלל עבר ל-`tests/helpers/redos-budget.ts`.

*מה שנשאר פתוח מהמודול הזה:* `getAllSellers()` עדיין מחזירה הכל (§3) — היא במצב של
`getAllStores`/`getAllOrders` שלצידה, ותטופל כשהן יטופלו. ובנתיב השגיאה של ה-middleware
יש עכשיו שאילתה, כך שנפילת מסד מוסיפה שם המתנה עד timeout — לבדוק בבדיקת העומס של שלב 3.

**✅ `stores` (בוצע 2026-08-02).** 20 פונקציות, 58 קבצים ב-`src`, ו-`tests/stores-db.test.ts`
(31 בדיקות) — שוב נכתבו מאפס, כי הכיסוי הקיים היה של החצי הטהור בלבד (`normalizeSlug`,
`isReservedSlug`, `computeNextPreviousSlugs`) ולא נגע באף שליפה.

*מה שהמעבר תיקן:* שני מוכרים שפותחים חנות באותו רגע כבר לא מקבלים אותו slug — האילוץ הוא
הפוסק, לא קריאה שקדמה לכתיבה (§7.4); `previousSlugs` עבר לטבלה משלו, כך ש-301 הוא שליפה
לפי מפתח ראשי ולא סריקת מערך על כל החנויות, **ו-slug שחנות פרשה ממנו כבר לא ניתן לחנות
חדשה** (ה-`INSERT` נושא `NOT EXISTS` מול הטבלה הזו לצד ה-`ON CONFLICT`); ו-`deleted_at`
מסונן בכל קריאה, כך ש"נמחקה" ו"לא קיימת" נשארות אותה תשובה בלי למחוק שורה שהזמנות מצביעות
אליה (§7.9).

*ההחלטה שדרשה מחשבה:* `slug` הוא `citext`, כלומר **הפתרון של ייחודיות הוא לא הפתרון של
פתרון-כתובת.** ה-`citext` קיים כדי ש-`Acme` ו-`acme` לא יהיו שתי חנויות; אבל אם `/MyStore`
גם היה מגיש את העמוד, אותה חנות הייתה יושבת בכל אופן איות של שמה — תוכן כפול שמצביע על
canonical אחד. לכן `getStoreBySlug` משווה גם `::text`, והייחודיות מקפלת רישיות בזמן
ש-`/MyStore` ממשיך להחטיא, כפי שהיה.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **`tsc` תופס כמעט כל קורא — חוץ מהאחד שמסוכן.** `Promise` **במקום בוליאני הוא תמיד אמת**,
  ואין שם שגיאת טיפוס. באותו diff: `hidden={!sellerHasAnyAlert(id)}` הדליק נקודת התראה
  אדומה לכל מוכר בכל עמוד, ו-`if (!ownsStore(...)) return 403` הפסיק להחזיר 403 בשני נתיבים.
  `astro check` דיווח 0 שגיאות על שלושתם. הכלל הזה מכני עכשיו: `tests/async-lib-awaited.test.ts`
  גוזר את שמות ה-`export async function` מתוך `src/lib/` ודורש `await` / `return` / `void`
  בכל קריאה. הוא יכסה את `orders` ו-`store-products` לבד ברגע שהן יעברו.
· **`updateStore` חייב להבחין בין מפתח חסר למפתח שערכו `undefined`.** ה-spread נתן את זה
  בחינם; שאילתה חייבת לבנות את ה-`SET` מ-`Object.keys` ולא מהערכים — אחרת `resumeStore`,
  שמנקה שני תאריכים ע"י `{ pausedAt: undefined }`, הופך ל-no-op בשקט.
· **`unnest(...) WITH ORDINALITY`** הוא מה ששומר על סדר של רשימה שנכתבת לטבלה: ל-`replaced_at`
  אין משמעות כתאריך, הוא רק נותן לקבוצה סדר, וכל השורות נכתבות באותו `now()` — בלי ההיסט
  של מיקרו-שנייה לפי מיקום, הקריאה חוזרת בסדר אלפביתי ו-`previousSlugs` מאבד את סדר השינויים.

*מה שנשאר פתוח מהמודול הזה:* `getAllStores()` מחזירה הכל, בדיוק כמו `getAllSellers()` (§3).
(שני ה-seeders נשארו שבורים כאן בכוונה — הם כותבים גם מוצרים, ותיקון חלקי היה יוצר חנות ראווה
בלי מוצרים. תורגמו יחד עם `store-products`, ראה למטה.)

**✅ `store-categories` (בוצע 2026-08-02).** שבע פונקציות-שאילתה (שמינית, `seedCategories`, נמחקה —
לא היה לה קורא בשום מקום בריפו), 21 קבצים ב-`src`, ו-`tests/store-categories-db.test.ts` (28 בדיקות) — שוב מאפס: הכיסוי הקיים היה
`countProductsPerCategory` בלבד, וכל שאר הבדיקות שנגעו בקטגוריה **דימו** (`mock`) את
`getCategoriesByStoreId`. כלומר החלפה שמחזירה עץ ריק לכל חנות הייתה נשארת ירוקה ב-125 קבצים.

*החלוקה שקובעת את כל הקובץ:* **הקוראים והכותבים הם שאילתות ולכן `async`; כל חישובי-העץ שמתחתם
טהורים ונשארו סינכרוניים.** קורא שולף את העץ פעם אחת ואז מטייל בו — `[storeSlug]/index.astro`
עושה בדיוק את זה, ו-`getAncestorChain` כשאילתה היה הופך עמוד אחד לתריסר סיבובים על דאטה שכבר בידו.

*מה שהמעבר תיקן:* שם כפול בין אחים נדחה ע"י אינדקס (`migrations/0002_category_sibling_name.sql`)
ולא ע"י קריאה שקדמה לכתיבה — שתי לשוניות דשבורד שהוסיפו "חולצות" באותו רגע שתיהן עברו את הבדיקה;
`position` נקבע **בתוך** ה-`INSERT` (`MAX(position) + 1`), כך ששתי יצירות בו-זמנית לא נוחתות על
אותו מקום; ותנאי "יש תתי-קטגוריות" עבר לתוך ה-`DELETE` עצמו — `parent_id` הוא `ON DELETE CASCADE`,
כלומר תת-קטגוריה שנוצרה בין הבדיקה למחיקה הייתה נמחקת איתה בלי שאיש ידווח.

*ההחלטה שדרשה מחשבה:* `COALESCE(parent_id, uuid אפסים)` באינדקס. בלעדיו — `UNIQUE (store_id,
parent_id, name)` פשוט — שני `NULL` אינם שווים ב-SQL, והכלל היה חל על כל רמות העץ **חוץ מהעליונה**,
זו שהמוכר רואה ראשונה. וברמת ה-`ORDER BY`: `unnest(...) WITH ORDINALITY` ממספר מחדש את כל שורת
האחים במקום להחליף שני ערכים, כי החלפה היא no-op כששני הערכים זהים — וזה בדיוק מה שכל שורה שיובאה
מתקופת ה-JSON מחזיקה.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **הייבוא עצמו היה צריך להשתנות באותו diff.** `db-import.mjs` הכניס את כל הקטגוריות בלי הורה
  ואז קישר ב-`UPDATE` — שיטה שהופכת את כל העץ לשורש לאורך הייבוא, ולכן חנות עם "חולצות" גם תחת
  "גברים" וגם תחת "נשים" הייתה מתנגשת על צמד שם חוקי לחלוטין. היום הוא ממיין הורים-לפני-ילדים
  (`parentsFirst`). הדאטה של היום לא מכיל את המקרה — ולכן הכשל היה מגיע מאוחר, על קטלוג אמיתי.
· **`now()` בתוך טרנזקציה זהה לכל השורות שנכתבו בה.** בדיקה שהניחה סדר החזרה של שלוש רמות
  שנוצרו ב-`resolveOrCreateCategoryPaths` אחת עברה לבד ונפלה בריצה מלאה: `position` 0 לכולן,
  `created_at` זהה, והשובר-תיקו האחרון הוא ה-uuid האקראי. סדר בין רמות אינו הבטחה של המודול.
· **`isUuid` לפני כל שאילתה, שוב.** מזהה קטגוריה מגיע מ-URL של דשבורד ומעוגייה — בלי בדיקת
  הצורה, "לא קיים" היה הופך ל-500.
· **`await` באמצע read-modify-write על קובץ JSON הוא עדכון אבוד — וזו המלכודת של כל מודול שנשאר.**
  `bulkUpsertProducts` היה `readProducts()` → שינוי → `writeProducts()` באופן סינכרוני, כלומר אטומי
  בתוך התהליך; הוספת `await resolveOrCreateCategoryPaths` באמצע פתחה חלון שבו ייבוא מקביל (שתי
  לשוניות, או סנכרון פיד לצד העלאה ידנית) דורס את כל הקטלוג של הראשון — שניהם מדווחים הצלחה.
  התיקון הוא סידור, לא מנגנון: **כל ה-`await`-ים עולים מעל הקריאה**, וכל מה שמתחת נשאר סינכרוני
  (`store-products-import.ts` קיבל את אותו טיפול). `tests/store-products-bulk.test.ts` משחזר את זה
  ברמת microtask ונופל בלי התיקון. **לבדוק את זה בכל מודול הבא שממירים לפני ש-`store-products` עובר.**
· **תקרת אורך לשם קטגוריה (`MAX_CATEGORY_NAME_LENGTH`, 40) הפכה מחובה של UI לחובה של שרת.**
  `maxlength="40"` בשני השדות היה הכלל היחיד, ו-POST ידני או תא ב-CSV לא עוברים דרכו. עם האינדקס
  החדש שם ענק כבר לא נשמר כערך מגוחך — לרשומת btree יש תקרת גודל, כלומר 500 בטופס שעבד קודם.
  העורך דוחה; נתיב ה-CSV **גוזם**, כי להפיל העלאה של אלף שורות בגלל תא אחד ארוך זו תשובה גרועה יותר.

*מה שנשאר פתוח מהמודול הזה:* אין. `/api/feed/products.xml` שולף עץ לכל חנות בלולאה — זו הייתה
קריאת-קובץ לחנות קודם ונשארה באותה צורה, ומתוכננת ממילא להפוך ל-artifact מקושש (ראה הערה בקובץ).

**✅ `store-products` + שני ה-seeders (בוצע 2026-08-02).** 13 פונקציות-שאילתה, ~35 קבצים ב-`src`,
`tests/store-products-db.test.ts` (36 בדיקות) ו-`tests/seed-db.test.ts` (5) — ושוב מאפס, והפעם
זה היה המקרה הגרוע ביותר: הכיסוי הקיים היה `slugify` ו-`getEffectiveStock` (טהורים) ועוד קבוצת
`decrementStock` מול `node:fs` מדומה. כל שאר מה שנוגע במוצר — `product-listing`, `checkout`,
`product-feed`, `sale-scope`, `product-stock-cas`, `seller-products-query` — בונה אובייקטי מוצר ביד
או מדמה את המודול כולו. החלפה שמחזירה קטלוג ריק לכל חנות, או כזו שמאבדת כל תמונה וכל מלאי-וריאנט,
הייתה נשארת ירוקה ב-123 קבצים.

*מה שהמעבר תיקן:* **`decrementStock` הוא משפט אחד** — `UPDATE … WHERE stock >= $qty` שמספר השורות
שלו הוא פסק הדין (§7.5), כלומר ה-Mutex שהגן עליו (ורק בתוך תהליך node אחד — שני instances = שני
mutexes = יחידה שנמכרה פעמיים בלי שאיש ידווח) כבר לא נחוץ כאן; סלאג מוצר ייחודי לפי אינדקס ולא לפי
קריאה שקדמה לכתיבה, ו-`UNIQUE (store_id, slug)` ולא גלובלי (§7.1 — 47 סלאגים חוזרים בין חנויות);
ומחירים הם אגורות שלמות (§7.7) עם המרה בקצה המודול (`money.ts#toAgorot/fromAgorot`) — היחידה
מתהפכת בכל האפליקציה פעם אחת, עם `orders`, לא מודול-מודול.

*ההחלטה שדרשה שינוי סכימה (`migrations/0003_variant_stock_optional.sql`):* `variantStock` הוא מפה
**חלקית** — צירוף בלי ערך נמכר מהמאגר המשותף — ו-`variantSku` היא מפה שנייה על אותם מפתחות. 0001
נתן לשתיהן שורה אחת לצירוף, כלומר צירוף שנושא רק ברקוד נאלץ להיכתב עם `stock = 0`, וזה לא "אין
override" אלא "אזל". `stock` הוא `NULL`-able עכשיו, וזו המשמעות. הבדיקה על זה נופלת בלי התיקון —
נבדק.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **שתי מפות חלקיות בטבלה אחת חייבות להיכתב יחד.** עורך המוצר כותב את מפת המלאי ולעולם לא את
  הקודים (`record-rev.ts` משאיר את `variantSku` מחוץ לטופס בכוונה), ולכן כתיבה שבונה מחדש את
  `product_variant_stock` מהמלאי בלבד הייתה מוחקת כל קוד פר-צירוף שייבוא CSV הגדיר — בשקט,
  בשמירה רגילה. `updateProduct` קורא את זו שלא סופקה תחת `FOR UPDATE` וכותב אותה חזרה.
· **מה שהחליף את "כל ה-`await`-ים מעל הקריאה" הוא טרנזקציה, לא סידור.** `bulkUpsertProducts` היא
  טרנזקציה אחת שקוראת את הקטלוג **בתוכה**; לולאה של `createProduct`/`updateProduct` הייתה אלף
  טרנזקציות, כלומר קובץ שנכשל בשורה 600 משאיר 599 מוצרים בלי דרך לזהות אותם. הבדיקה על זה קיימת.
· **סדר ההחזרה השתנה במכוון ל-`created_at DESC, id`** — זה בדיוק מה ש-`store_products_visible_idx`
  מחזיר בלי מיון, ו-`rankDefault` שובר תיקו לפי `createdAt` ממילא. סדר הקובץ (הישן→החדש) היה יציב
  במקרה.
· **`seed-db.mjs` הוא החצי-DB של שני ה-seeders, והם עדיין חצי-קבצים.** הזמנות, דליי צפיות ומוני
  מועדפים/wishlist נכתבים ל-JSON כי המודולים שלהם לא עברו — כל חצי זז כשהמודול שלו זז.
  ה-purge וה-write הם טרנזקציה אחת: שניהם מושכים תמונות מהרשת קודם, וpurge שהיה נסגר לבד היה
  מוחק את חנויות הראווה ולא שם כלום במקומן כשההרצה נכשלת.

*מה שנשאר פתוח מהמודול הזה:* `getAllProducts()` מחזירה הכל, בדיוק כמו `getAllStores()` /
`getAllSellers()` שלצידה (§3) — שלושת הקוראים (חיפוש האתר, משפך המוכרים, לשונית האדמין) יסוננו
ב-DB כשהן יטופלו. ו-`data/store-products.json` עדיין קיים בדיסק ונמחק בשלב 3 עם השאר.

**✅ `orders` + `checkout-idempotency` + `money-events` + החצי השני של seeder הדמו (בוצע 2026-08-02).**
שלושת המודולים בדיף אחד, כי מבחינת הקונה הם פעולה אחת: לחייב, לכתוב את שורות ההזמנה, לרשום
שהמפתח נוצל. פנקס בקובץ לצד הזמנות בטרנזקציה = תהליך שמת ביניהם משאיר הזמנה ששולמה עם מפתח
שאומר שהצ׳קאאוט לא הושלם, והניסיון הבא מחייב שוב. `tests/orders-db.test.ts` (29),
`tests/money-events-db.test.ts` (10), ושני קובצי ה-idempotency נכתבו מחדש מול Postgres אמיתי.

*הכיסוי הקודם, שוב:* כל מה שהיה זה הפרדיקטים הטהורים שלצד המודול — `countsAsRevenue`,
`purchasedCountsFrom`, `orderBelongsToStore`, מסנני החיפוש — וכולם על אובייקטי `Order` בנויים ביד.
אף בדיקה לא קראה ולא כתבה הזמנה אחת. שני קובצי ה-idempotency דימו `node:fs`, כלומר בדקו את המוק.

*החלטה 1 — יחידת הכסף (האופציה שנבחרה: צינור-הכסף בלבד).* §7.7 נסגר סופית **בצינור**: הזמנות,
שורות, סכומי-חנות, הנחות-כפי-שיושמו, היומן, וכל דמות revenue/GMV/עמלה שנגזרת מהם הם מספרים
שלמים באגורות. מה שנשאר בשקלים הם שני הקצוות שהיחידה מעולם לא הייתה העניין בהם — מה שהמוכר
**מקליד** ומה שאדם **קורא**. **השדות שונו שם (`totalAgorot`, `priceAgorot`, `subtotalAgorot`,
`appliedAgorot`, `amountAgorot`), וזה כל מנגנון הבטיחות:** שינוי יחידה תחת שם זהה הוא שגיאת
פי-100 שקטה ש-`number → number` עובר עליה בשלום, ואין בדיקה שהייתה תופסת כל קורא שנשכח. השינוי
הפך את כולם לשגיאות קומפילציה — 19 קבצי `src` ו-24 קובצי בדיקה, כולם נמצאו ע"י `tsc`, אף אחד
בחיפוש. הגבול היחיד הוא `money.ts#formatAgorot` (ולמנוע-הסליקה: `fromAgorot` פעם אחת בקצה, כי
API של סליקה מדבר שקלים).

*מה שהמעבר תיקן:* ה-`Mutex` מת בשני המקומות שנשארו — `claimCheckout` הוא משפט אחד
(`INSERT … ON CONFLICT DO UPDATE … WHERE <רשומה בת-השתלטות>`) שמספר השורות שלו הוא פסק הדין,
כלומר **השער על הרצת יותר מ-instance אחד נפתח** (§4); `recordMoneyEvent` הוא `INSERT` בודד, ולכן
שני instances כבר לא יכולים לכתוב זה על היומן של זה. `getAllOrders().filter(buyer)` — שלושה
משטחים שקראו את כל ההזמנות בפלטפורמה לזיכרון כדי להציג עמוד של אדם אחד — הפך ל-`WHERE`
(`getOrdersByBuyer`), ושתי לולאות N+1 (הפיד, נקודות ההתראה של המוכר) הפכו לשאילתה אחת מרובת-חנויות.
`openOrderCount` הוא `COUNT` במקום שליפה-ואז-סינון.

*החלטה 2 — מה `p.stock` אומר (האופציה שנבחרה: לשמור על ההבטחה בזמן מכירה).* ה-OPEN BUG שמעל
`countStockAlerts` נסגר. `p.stock` ממשיך לומר שני דברים, אבל השני נשאר **אמיתי**: מכירה מדלי
גוזרת מחדש את `p.stock` מסכום הדליים, בדיוק כפי שהעורך עושה בשמירה (`syncPooledStock`). אף קורא
לא זז ואף שורה לא הוגרה — זה מה שמבדיל את זה מהגדרה-מחדש של `p.stock` כמאגר-בלבד, שנדחתה כגדולה
יותר (נוגעת ב-`isProductInStock`, בחזית, ב-JSON-LD וב-`availability` של הפיד, ודורשת מיגרציית
דאטה). **החצי שההערה המקורית לא הגיעה אליו:** אותו `p.stock` הקפוא מזין את `availability` בפיד,
כלומר הפיד המשיך לפרסם מוצר שאזל כ-`in_stock` — בעיה של חשבון מודעות, לא של דשבורד.

*מה שדרש מיגרציה (`0004_orders_lookups.sql`):* `order_items.position`. שורות ההזמנה היו מערך,
ולמערך יש סדר — הסדר שהקונה בנה בו את הסל, שהוא הסדר שהמייל שלו מפרט ושהמוכר אורז לפיו. לטבלה
אין דבר כזה (§7.13) וכל מפתח-מיון חלופי שגוי באותה שקט: ה-`id` הוא hash של רצף הייבוא. ועוד שלושה
אינדקסים ש-0001 השאיר: `order_items(store_slug)` (חצי ה-OR של `orderBelongsToStore` — הפרדיקט
ששומר על גישת מוכר להזמנה — סרק כל שורת פריט שנמכרה אי-פעם), `money_events(type, at DESC)`, ושני
trgm שהתוכנית ביקשה ב-§4 ו-0001 לא כלל.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **אילוץ CHECK הופך את מה שהקובץ שמר בשקט ל-500 — וזה קרה בשלושה מקומות שונים.** סכום שלילי,
  `qty` אפס, ואחוז-הנחה שברי (`discount_percent` הוא `integer`, ו-12.5 היה נשמר כ-12 **בלי מילה**
  בזמן ש-`applied` חושב מ-12.5, כלומר העריכה הבאה של אותה הזמנה משנה לה את הסכום בשקט). הכלל:
  נתיב-כתיבה מהדק (`nonNegative`/`qtyOf`/`Math.round`), ו-`reconcile.ts` הוא מי שמדווח על השטות.
· **`bigint` חוזר מ-`pg` כמחרוזת ומ-PGlite כמספר.** `'1250' + 500` הוא `'1250500'` — שרשור, בלי
  שגיאה, סכום שגוי בארבעה סדרי גודל במסך היחיד שבו זה חשוב. כל קריאה של עמודת `*_agorot` עוברת
  דרך המרה אחת.
· **הסרת `roundMoney` היא שינוי משמעות, לא ניקוי.** `reconcile.ts` השווה שני צדדים מעוגלים, מה
  שהסתיר כל אי-התאמה קטנה מחצי אגורה. עכשיו שני הצדדים שלמים וההשוואה מדויקת — המודול מדווח
  **יותר**, וכל מה שנוסף הוא אמיתי.
· **הזמנה שיובאה מתקופת השקלים לא יכולה לסגור על עצמה לאגורה, וזו לא באג.** הקובץ שמר מחיר-לשורה
  וסכום-חנות כשני עשרוניים נפרדים, והמרת כל אחד בנפרד לא מתחלפת עם כפל: `toAgorot(1.005)×2 = 202`
  מול `toAgorot(2.01) = 201`. הסכום השמור הוא הנכון (הוא מה שהקונה חויב), ולכן הייבוא שומר אותו
  ולא גוזר מחדש. `tests/reporting-invariants.test.ts` מפריד עכשיו בין מה שחייב להתקיים בכל שורה
  (אין כסף שלילי, אין הנחה שגדולה ממה שהיא מנכה) לבין זהות-הסגירה המלאה, שנבדקת על הזמנה
  שהקוד עצמו כתב — בלי סובלנות.
· **`syncPooledStock` נועל את שורת המוצר לפני שהוא סוכם את הדליים.** בלי זה `SET stock = (SELECT
  SUM(...))` מוערך על ה-snapshot של המשפט, ו-`READ COMMITTED` בודק מחדש רק את ה-`WHERE` — שתי
  מכירות באותו רגע היו כותבות את אותו סכום. הדלי (המלאי האמיתי) לעולם לא נפגע, אבל "מלאי שגוי
  לרגע" הוא בדיוק מה שהפונקציה נכתבה למנוע.
· **הבדיקה שתפסה את הבאג האמיתי היחיד בדיף הייתה `tests/async-lib-awaited.test.ts`.** שינוי שם
  של שדה תופס `tsc`; `Promise` שנזרק לרצפה לא. `renameStoreSlugInOrders` נשאר בלי `await`, כלומר
  שינוי כתובת חנות היה מחזיר 200 לפני שההזמנות הופנו מחדש — לשונית הזמנות ריקה, ושגיאה שנבלעת.

*מה שנשאר פתוח מהמודול הזה:* `getAllOrders()` מחזירה הכל, בדיוק כמו `getAllStores()` /
`getAllSellers()` / `getAllProducts()` שלצידה (§3) — שלושת הקוראים שנשארו (סקירת האדמין, משפך
המוכרים, ביצועי הפלטפורמה) כולם עושים אגרגציה על כל הפלטפורמה ורוצים `SUM`/`GROUP BY`; זו עבודה
אחת לארבעתם יחד. חיפוש הטקסט החופשי ביומן הכסף עדיין בזיכרון מעל החלון שהשאילתה כבר צמצמה (הוא
מתאים גם את התווית העברית של סוג האירוע, שלא קיימת ב-DB) — אינדקסי ה-trgm מחכים לו.
ו-`purgeExpiredCheckouts` קיים ונבדק אבל אין scheduler שיקרא לו (שלב 4).

**✅ `messages` + `admin-messages` + `notifications` (בוצע 2026-08-02).** שלושת המודולים בדיף אחד,
כי לשונית ההודעות של המוכר **ממזגת** שרשורי קונים ושרשורי אדמין לרשימה אחת ואז ממיינת/מסננת/מעמדת
עליהם ביחד (`seller-messages-query.ts`), וכל כתיבה בשניים הראשונים היא חצי של זוג — הודעה וההתראה
שמודיעה עליה. פיצול היה משאיר עמוד אחד חצי-טבלה חצי-קובץ, ממוין על פני שני מקורות שאי אפשר למיין
ביחד. `tests/messages-db.test.ts` (32), `tests/messages-api.test.ts` (9), `tests/notifications-db.test.ts` (18),
`tests/admin-messages-db.test.ts` (18) — כולם חדשים.

*הכיסוי הקודם, שוב:* `seller-messages-query.test.ts` ו-`admin-messages.test.ts` בודקים את הפונקציות
הטהורות שמעל המודולים (`groupAdminThreads`, `buildSellerMessageRows`, המסננים) על מערכים בנויים ביד,
ו-`order-notify.test.ts` את `buildOrderStatusNotification`. **אף בדיקה לא קראה ולא כתבה הודעה או
התראה אחת**, וארבעת הקבצים שכן מייבאים `notifications` (`checkout`, `product-stock-cas`,
`store-products-import`, `csv-pooled-stock-roundtrip`) **מדמים את המודול כולו** — כלומר בודקים את המוק.

*המלכודת שהייתה הופכת כל תגובה ל-500:* `to_store_id` הוא `uuid`, ותגובה מופנית לאדם ולא לחזית חנות —
כלומר **68 מתוך 72 השורות השמורות נושאות `toStoreId: ''`**. Postgres *דוחה* `''` כליטרל uuid, ולכן
העברה ישירה הייתה שוברת את כל נתיב התגובות. שתי עמודות הזיהוי נפתרות עכשיו דרך תת-שאילתה בכתיבה
(`(SELECT id FROM stores WHERE id = $5::uuid)`) — ערך שאינו מזהה של שורה קיימת נוחת `NULL`, בדיוק
כפי ש-`db-import.mjs` עשה לאותן שורות, וחוזר כ-`''`. זה מכסה גם את ה-FK: מוכר שמשיב לקונה שחשבונו
נמחק, או למזהה טרום-uuid כמו `u1`, עדיין יכול לענות.

*ה-N+1, בחמישה מקומות:* `getMessageReplies(m.id)` בתוך לולאה על שרשורים — שני הדשבורדים, העימוד
ה-AJAX של המוכר, ה-poll של ה-header (רץ בכל טעינת עמוד), ונקודות ההתראה של מחליף החנויות (שם זה
מוכפל במספר החנויות). שתי קריאות-אצווה מחליפות את כולן: `getRepliesForMessages` לנתיבי הרינדור,
ו-`getUnreadThreadIds*` / `getStoreIdsWithUnreadMessages` למי שרק צריך לדעת אם משהו ממתין.
`seller-alerts.ts` ירד משתי שאילתות ועוד N לשלוש, קבוע.

*מה שהמעבר תיקן:* **הנמען נגזר מהחנות ולא נלקח מהבקשה** — ה-body נשא `toSellerId` ו-`toStoreName`
לצד מזהה החנות, כלומר POST ידני יכול היה לתייק הודעה תחת שם חנות אחת ולמסור אותה למוכר אחר (ועכשיו,
מול FK, גם לנקוב בחשבון שלא קיים ולהפיל את הבקשה); `markThreadReadByBuyer` נכבל לכותב השרשור בתוך
המשפט עצמו — הגרסה בקובץ קיבלה רק מזהה שרשור והאמינה לו, כלומר כל חשבון מחובר יכול היה לנקות דגלי
"לא נקרא" של כל שיחה בפלטפורמה; `replyToAdminThread` הוא `INSERT … SELECT` אחד שקורא את השורש
וכותב באותו משפט, כך ש"השרשור נמחק בין הבדיקה לכתיבה" לא קיים; ו-`getUnreadAdminThreadIdsForSeller`
מחזיר `DISTINCT`, כי שרשור עם שלוש הודעות שלא נקראו הופיע שלוש פעמים ברשימה שהלקוח מתייחס אליה כקבוצה.

*ההחלטה שדרשה מחשבה — מתי כישלון של התראה מפיל את הפעולה שהיא מבשרת.* הודעה בלי התראה משאירה את
הנמען בלי סימון, והתראה בלי הודעה היא קישור-עומק לכלום; לכן **הזוג נכתב בטרנזקציה אחת** (שני נתיבי
`/api/messages`, שני נתיבי `/api/admin/messages`, וההודעה על חסימה ב-`moderation.ts`). אבל בצ׳קאאוט
ובצינור סטטוס-ההזמנה הכיוון הפוך: שם החיוב, שורת ההזמנה או הסטטוס **כבר נשמרו**, ו-`createNotification`
שנכשל היה הופך רכישה שהושלמה ל-500 אצל הקונה. שם, ורק שם, יש `.catch()` בנקודת הקריאה — במקום שבו
ההחלטה נראית, במקום שהמודול יבלע שגיאות לכולם.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **`tests/async-lib-awaited.test.ts` תפס שוב באג אמיתי** — `deleteNotificationsByRelatedIds`
  בלי `await` ב-`store-products-import.ts`, כלומר העלאת CSV שעדכנה מלאי החזירה 200 לפני שההתראות
  על "אזל" נוקו. הבדיקה גם למדה **צורה רביעית**: קריאה בתוך `Promise.all([...])` מוּמְתָנת ע"י
  ההמתנה של המערך. בלי זה הכלל היה אוסר את הצורה הנכונה — עמוד שצריך שתי שליפות בלתי-תלויות צריך
  לשלוח אותן יחד, ומול מסד באזור אחר ההפרש הזה הוא מהירות העמוד. שתי בדיקות חדשות מוודאות שההקלה
  נגמרת בדיוק איפה שהסוגריים נגמרים, ושׁ-`Promise.all` שאיש לא ממתין לו עדיין מדווח.
· **מוק סינכרוני שובר `.catch()` בנתיב שהפך אסינכרוני.** `checkout.test.ts` החזיר `undefined`
  מ-`createNotification`, ו-13 בדיקות נפלו על `undefined.catch` — בקוד שעובד בפרודקשן. מוק של פונקציה
  אסינכרונית חייב להיות `async`, אחרת הוא בודק חוזה שאינו קיים.
· **סמן ה-poll מגיע מ-`localStorage` ולכן יכול להיות כל דבר.** `?since=` נכנס ל-`timestamptz`;
  ערך שלא נפרס היה 500 בנתיב שרץ כל 15 שניות. הוא מאומת עכשיו, ו**מתעלמים** ממנו כשהוא פסול במקום
  לענות "אין חדש" — התשובה השנייה קבועה, כי הלקוח כותב את הסמן מחדש רק משורה שהנקודה הזו החזירה.
· **`COUNT` הוא `bigint`** — מחרוזת מ-`pg`, מספר מ-PGlite (§8, `orders`). כל מונה עובר המרה אחת.
· **ארבעה ייצוא מתים נמחקו** (`getUnreadCountBySeller`, `markMessageReadBySeller`,
  `markMessageReadByBuyer`, `getUnreadCountForSellerFromAdmin`) — אף קורא בריפו, כמו `seedCategories`
  ב-`store-categories`. פונקציה שאין לה קורא היא פונקציה שאין לה בדיקה.
· **`verify-import.mjs` קיבל עוגן ל-`admin_messages`**, שלא היה לו אחד. נבדק שהוא חי: בלי חיסור
  ה-drop הוא נופל על ה-fixture (2 מול 1).
· **סקירת הדיף (`review-diff`) מצאה שני חורים שקדמו למעבר, ושניהם תוקנו כאן.** (1) **לגודל הודעה
  בין קונה למוכר לא הייתה שום תקרה בשרת** — `maxlength="120"/"2000"` בטופס היה כל הכלל, ותיבות
  התגובה נושאות אפס; זו בדיוק הנפילה של `MAX_CATEGORY_NAME_LENGTH`. עכשיו יש `MAX_MESSAGE_*_LEN`
  ב-`messages.ts`, ה-API מחזיר 400, המודול גוזם בשער האחרון לפני העמודה, ו-`admin-messages.ts`
  **מייצא מחדש** את אותם שניים במקום להחזיק עותק. גם `productRef` מצטמצם לארבעת השדות המוצהרים
  ומוגבל באורך — הוא נכנס ל-`jsonb` ישר מהבקשה. (2) **`?repliesFor=` החזיר שרשור לכל חשבון מחובר**
  שנקב במזהה, כלומר שיחה פרטית בין קונה למוכר. עכשיו הוא נכבל למשתתף. `tests/messages-api.test.ts`
  (9 בדיקות) מריץ את הנתיב האמיתי מול Postgres אמיתי; שתי הבדיקות האלה **נופלות בלי התיקונים** — נבדק.

*מה שנשאר פתוח מהמודול הזה:* `/api/messages` בלי פרמטרים מחזיר את כל ההודעות של אדם אחד בלי עימוד
(`getMessagesBySeller`/`getMessagesByBuyer`), **ואין לו קורא בצד הלקוח** — כל הלקוחות משתמשים ב-
`?repliesFor=` או ב-`?unread=1`. השארתי אותו: זו נקודת-קצה מאומתת ומוגבלת לאדם עצמו, וייתכן שלה
קורא מחוץ לריפו (סוכני AI, §CURRENT_TASK), אבל היא הקריאה הבלתי-חסומה האחרונה של המודול הזה.
ו-`getAllAdminThreads()` מצטרפת ל-`getAllOrders`/`getAllStores`/`getAllSellers`/`getAllProducts` (§3).

**✅ הדליים, דיף 1 מתוך 3 — `store-pageviews` + `product-pageviews` (בוצע 2026-08-02).**
החלוקה של הדליים לשלושה דיפים נגזרה מהקוראים בפועל, לא מהקבצים: `seller-performance.ts` מייבא את
שני מודולי הצפיות ולכן הם דיף אחד; ל-`analytics` **אין אף קורא משותף איתם** (`admin/index.astro`,
`seller-funnel.ts`) והוא דיף שני; ושלושת מודולי מצב-הקונה חולקים קובץ אחד ממש
(`store-favorite-counts.ts` קורא את `data/user-carts.json`) והם דיף שלישי.
`tests/store-pageviews-db.test.ts` (14) ו-`tests/product-pageviews-db.test.ts` (10) חדשים.

*הכיסוי הקודם:* `store-pageviews-cache.test.ts` היה **הבדיקה האמיתית היחידה בכל ששת הדליים** — הוא
באמת כתב וקרא דרך המודול — אבל כל מה שהוא אימת היה תקפות של קאש-mtime מעל קובץ JSON, והוא כתב
ל-`data/` שריק ב-CI. הקאש הלך עם הקובץ. `product-pageviews` לא היה מכוסה כלל, ו-`seller-performance.test.ts`
**דימה את שני המודולים**, כלומר החלפה שמחזירה אפס צפיות לכל חנות הייתה נשארת ירוקה.

*מה שזה לא היה — המרה אחת-לאחת (§5).* הדלי היומי החזיק `visitors[]` — מערך של כל מזהי המבקרים של
אותו יום, נקרא ונכתב **במלואו בכל טעינת עמוד** (173 מזהים ביום העמוס שנמדד). ההעברה נשענת על שלושה
דברים שאף אחד מהם אינו העתקה: כתיבה היא **משפט אחד** (CTE שמשנה נתונים) של שני `INSERT … ON CONFLICT`
בלי `SELECT` שקודם לו; `COUNT(DISTINCT visitor_id)` נשאר **בתוך** ה-DB; ו-**ה-API של הקריאה ממופתח
בדלי ולא ביום**, כי מוני-ייחודיים יומיים לא ניתנים לסיכום לחודש — להחזיר שורות-יום ולתת לקורא לאחד
היה רק מעביר את המערך חסר-הגבול מהקובץ לתוצאת השאילתה. `totalUniqueVisitors` הוא `COUNT(DISTINCT)`
משלו על כל הטווח, וזו הבדיקה שנופלת מול כל מימוש שמסכם את הדליים.

*ה-N+1 שנסגר:* `platform-performance.ts` קרא ל-`buildPerformanceSummary` בלולאה על כל חנות (45),
וכל קריאה קראה את הקובץ. **הפתרון היה לא batch לצד הלולאה אלא הפיכת הצפיות לקלט:** `views` הוא
פרמטר של `buildPerformanceSummary`/`buildProductPerformance` בדיוק כמו `orders`, הקורא שולף
`getStoreViewStats(ids…)` **פעם אחת לכל החנויות**, והפונקציות שבהן יושבת כל אריתמטיקת הדיווח נשארו
טהורות וסינכרוניות — כלומר עדיין נבדקות בלי מסד.

*מה שהמעבר תיקן דרך אגב:* `renameStoreSlugInPageviews` **נמחקה** — ההיסטוריה ממופתחת ב-`store_id`,
ולכן שינוי כתובת חנות לא יכול לפצל אותה ואין מה להעביר. ומזהי המבקרים כבר לא עוזבים את ה-DB בכלל.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **`/^\d{4}-\d{2}-\d{2}$/` אינו בדיקת תאריך, וזו מחלקה שלמה שנפתחה ברגע שדוחות קוראים עמודת `date`.**
  הוא מקבל `9999-99-99`, `0000-00-00` ו-`2026-02-30`. כל עוד טווח נסרק ב-JS אלה ייצרו `Invalid Date`,
  הלולאה רצה אפס פעמים והתשובה הייתה גרף ריק; מול `::date` **Postgres זורק** ו-הבקשה מחזירה 500.
  **והשומר שיושב לידם לא תופס את זה:** `spanDays` הוא `NaN`, ו-**גם** `NaN < 0` **וגם** `NaN > MAX_DAYS`
  הם false, כלומר תאריך בלתי-אפשרי עובר דרך חסם שנכתב בדיוק בשבילו. הביטוי הועתק ידנית ב-8 מקומות.
  היום יש `business-day.ts#isDayISO` (בדיקת round-trip, לא טווח — זה מה שתופס את ה-30 בפברואר),
  שלוש נקודות-הקצה של הדוחות מחזירות 400, `coerceRange` נופל לברירת המחדל, והמודולים עצמם מסננים
  לפני השאילתה כמו `isUuid`. `tests/day-iso.test.ts` הוא guard-test שנופל אם מישהו יגלגל עותק תשיעי.
· **אותה מחלקה כבר הייתה חיה במקום שני, מהדיף של `orders`:** `?mfrom=2026-02-30` ביומן הכסף של
  האדמין נכנס ל-`getMoneyEvents` ומשם ל-`(at AT TIME ZONE $4)::date >= $2::date` — 500 על **כל
  עמוד האדמין**, לא על נקודת-קצה. תוקן באותו סבב, עם בדיקה שנופלת בלעדיו.
· **פיקסצ'ר שבו `id === slug` מאמת כלום.** הבדיקות של `platform-performance` השתמשו בשם החנות כמזהה,
  כלומר מיזוג שמחפש `viewsByStoreId.get(store.slug)` היה עובר את כולן. יש עכשיו מקרה שבו השניים שונים.
· **הדבר היחיד שנשאר מצומד בין שני ארגומנטים הוא ה-granularity:** `views` חייב להיבנות באותו גודל
  דלי שמועבר לצידו. כל ששת הקוראים מעבירים את אותו משתנה פעמיים; אי-התאמה תיתן עמודות אפס לצד
  KPI תקין — נראה לעין, לא נובע משום קלט, ומתועד בחתימה.

*החצי של ה-seeder זז עם המודול:* `seed-demo-data.mjs` כתב `data/store-pageviews.json` שכבר אף אחד
לא קורא — כלומר חנות דמו הייתה נזרעת עם לשונית ביצועים ריקה, והסקריפט היה מדפיס הצלחה. הצפיות הן
עכשיו שורות בתוך אותה טרנזקציה של `writeCatalog`, ו-`tests/seed-db.test.ts` מאמת אותן דרך הקורא
של האפליקציה — אותה הגנה בדיוק שהקובץ הזה נכתב בשבילה. מוני המועדפים/wishlist נשארים JSON עד דיף 3.

**✅ הדליים, דיף 2 מתוך 3 — `analytics` (בוצע 2026-08-02).** מודול אחד, שישה קוראים,
ו-`tests/analytics-db.test.ts` (18 בדיקות) לצד `tests/analytics.test.ts` שנכתב מחדש (8).

*הכיסוי הקודם — נמדד, לא הוערך:* שלוש פונקציות ה-I/O הוחלפו בזמניות שמחזירות משפך ריק, אפס
סשנים ואפס כתיבה, והסוויטה הראשה **1706 מתוך 1707**. תשע הבדיקות שהיו בנו `AnalyticsData` ביד
ו-`checkout.test.ts` דימה את המודול כולו. (הכשל היחיד היה תיעוד: `AI_INSTRUCTIONS.md` עוד הפנה
ל-`tests/store-pageviews-cache.test.ts` שנמחק בדיף 1 — תוקן כאן, יחד עם שלוש שורות מבנה שנשארו
מתארות את עידן ה-JSON של שני מודולי הצפיות.)

*החלוקה שקובעת את הקובץ, והיא בדיוק זו של דיף 1:* הפונקציות הטהורות קיבלו **קלט חדש** במקום
להפוך לאסינכרוניות. `buildFunnel`/`buildAnalyticsRates` מקבלות `EventTotals` — שורה אחת לכל
אירוע, כפי שה-DB החזיר — ונשארו סינכרוניות עם כל אריתמטיקת השיעורים והקלמפים. `datesInRange`
ו-`aggregateEvent` נמחקו: הטווח הוא `day BETWEEN $1::date AND $2::date`, וסריקת לוח־שנה ב-JS
הייתה בדיוק מה שהחזיק את חור התאריכים. **`topProductsByEvent`/`topAbandonedProducts` הפכו
לשאילתות במלואן** — בגופן אין אריתמטיקה מלבד חיסור, מיון וחיתוך, ולהחזיר את כל שורות המוצרים
ל-JS כדי לבחור שמונה זה אותו מערך חסר־גבול בתחפושת.

*המלכודת שלא הייתה בקובץ בכלל:* קריאה אחת יכולה לנקוב באותו מוצר פעמיים — צ׳קאאוט עם שתי שורות
של אותו מוצר בווריאנטים שונים — ו-Postgres **דוחה** `ON CONFLICT DO UPDATE` שנוגע באותה שורה
פעמיים בפקודה אחת. הספירה מקובצת בתוך ה-`SELECT` (`GROUP BY pid`), מה שגם שומר על ההתנהגות
הישנה, שבה הלולאה פשוט הגדילה פעמיים. בלי זה כל רכישה כזו הייתה נבלעת ב-`catch` ונעלמת.

*מה שהמעבר תיקן דרך אגב:* `RecordOpts.date` נמחקה (אף קורא — הכלל מ-`messages`), ו-`sessions`
של כל שלב הוא `COUNT(DISTINCT visitor_id)` על **הטווח**, כך שסשן שחוזר מחר סופר פעם אחת. הבדיקה
שנופלת מול כל מימוש שמסכם ימים היא הראשונה בקובץ.

*החסם על הנקודה הלא־מאומתת (אושר במפורש):* `/api/analytics/event` מקבל `productId` מהלקוח.
בעידן הקובץ מזהה מומצא הוסיף מפתח לדלי; מול ה-DB הוא **שורה קבועה** ב-`analytics_products` —
טבלה בלי FK במכוון, מנקודת קצה בלי אימות. עכשיו נדרש `isUuid` (כל מוצר שהאפליקציה יוצרת הוא
uuid; המזהים שקדמו ל-uuid קיימים רק בהיסטוריה מיובאת, שהנתיב הזה לא כותב). מזהה פסול **נופל
בשקט ולא מוחזר 400** — ההוספה לעגלה כבר קרתה, ו-400 רק היה גורם ללקוח לנסות שוב.

*מה ש-`review-diff` מצא, ושוב קדם למעבר:* **תקרת גודל הגוף בשתי נקודות הקצה הלא־מאומתות נבדקה
מול הכותרת `Content-Length`** ואז `await request.json()` — ובקשה שפשוט לא שולחת את הכותרת (כל
בקשת chunked) נקראה כ-0, עברה את הבדיקה, ונקראה במלואה לזיכרון. ההערה מעל אחת מהן אף הצהירה
שזה לא יכול לקרות. הסקירה גילתה גם ששתי נקודות קצה **נוספות** בלי אימות (`/api/wishlist`,
`/api/cart/prices`) קראו גוף בלי שום תקרה. `src/lib/request-body.ts#readJsonBody` סופר את
הבתים שמגיעים ומבטל את הזרם בתקרה; `BODY_LIMIT` הוא אוצר־מילים בן ארבע (control/form/
collection/upload) במקום 28 מספרים; **כל 28 קריאות ה-`request.json()` בנתיבי ה-API הוחלפו**;
ו-`tests/request-body.test.ts` הוא guard-test כפול — אף קובץ מלבד המודול לא קורא את הכותרת,
ואף נתיב לא קורא `request.json()`. הבדיקה שנופלת בלי התיקון היא בקשת chunked של 50KB מול תקרה
של 2KB — נבדק.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **מוק סינכרוני של פונקציה אסינכרונית — שוב.** `checkout.test.ts` דימה את `recordAnalyticsEvent`
  כ-`() => {}`. `void` סובל את זה, בניגוד ל-`.catch()` שהפיל 13 בדיקות ב-`messages`, אבל המוק
  עדיין בודק חוזה שאינו קיים. תוקן ל-`async`.
· **בדיקה שנראית כמו הבדיקה הנכונה ואינה היא.** "ה-`LIMIT` חל אחרי החיסור" עבר גם כשהמיון
  שוּנה ל-`ORDER BY added` — כי המוצר שהסיח את הדעת סונן קודם ע"י ה-`WHERE`. שני מוצרים עם
  פער אמיתי, שאחד מהם מתווסף פי עשרה, הם מה שבאמת מבדיל. **סבוטאז' על כל מנגנון בנפרד הוא מה
  שגילה את זה** — חמישה מנגנונים נשברו אחד־אחד ונבדק שהאדום נוחת במקום הנכון.
· **הכתיבה היא עדיין משפט אחד לכל אירוע, אבל טעינת עמוד יורה עד שניים** (`page_view` ועוד שלב
  צר) לצד שתי כתיבות הצפיות — כלומר עד ארבע כתיבות לא־ממתינות לבקשה. לא נמדד תחת עומס; שייך
  לבדיקת העומס של שלב 3 יחד עם §9.2.

*ה-seeder לא זז כאן:* אף אחד משני ה-seeders לא כתב `analytics-events.json` מלכתחילה, ולשונית
הנתונים של האדמין הייתה ריקה בדמו גם לפני המעבר. שני העוגנים ב-`verify-import.mjs` נבדקו
בשבירה ונמצאו חיים ולא ריקים (fixture 10/3, דאטה אמיתי 10286/584).

**✅ הדליים, דיף 3 מתוך 3 — מצב-הקונה (בוצע 2026-08-02). שלב 2 סגר את הדליים — אבל לא את שלב 2.**
שלושת המודולים (`user-carts` + `wishlist-counts` + `store-favorite-counts`) הפכו למודול אחד מעל
ארבע טבלאות, ושני האחרונים **נמחקו** — הם לא עברו, הם מתו (§5). `tests/user-carts-db.test.ts` (37)
ו-`tests/user-cart-api.test.ts` (12) חדשים.

*הכיסוי הקודם — נמדד:* שלושת המודולים הוחלפו בגרסאות שמחזירות עגלה ריקה, wishlist ריק ואפס
מונים, והסוויטה נשארה **1732 מתוך 1732** ב-143 קבצים. לא בדיקה אחת. זה הגרוע מבין כל המודולים
עד כה — אפילו `analytics` הפיל אחת (על תיעוד).

*העניין המרכזי — זהות.* האפליקציה מדברת slugs, שתיים מהטבלאות ממופתחות ב-uuid, וההמרה
`(storeSlug, slug)→id` נעשית **בתוך המשפט** ולא בסיבוב נוסף. היא כיסתה את כל הקוראים חוץ מאחד,
וזה מה שהחיפוש גילה: **`/api/wishlist` נשא רק `productSlug` — בלי חנות ובלי אימות.** אין לו מאיפה
לגזור מזהה מוצר (§7.1) ואין לו מאיפה לדעת של מי ה-wishlist, כלומר כל אחד יכול היה להזיז ב-curl
מספר שהמוכר קורא בדשבורד. הוא **נמחק** עם שלוש קריאות ה-fetch שלו: ה-wishlist מסונכרן ממילא
דרך `/api/user-cart` המאומת, והמונה הוא `COUNT(*)` על מה שהסנכרון הזה כותב. משמעות המספר
השתנתה מ"כמה לחיצות מכל העולם" ל"כמה חשבונות", וזה המספר הנכון.

*מה שהמעבר תיקן:* **קנייה הפסיקה למחוק את "ביקרת לאחרונה" של הקונה.** `saveUserCart` בצ׳קאאוט
החזיר `{cart, wishlist, favoriteStores}` בלי `recentStores`, כלומר כל רכישה איפסה את הרשימה
החוצה-מכשירים — באג שקיים היום ולא נובע מהמעבר. שלושת נתיבי הכתיבה צרים עכשיו:
`removeCartLines` הוא `DELETE` אחד של השורות שנקנו, `toggleFavoriteStore` הוא משפט אחד שמספר
השורות שלו הוא פסק הדין, ו-`replaceUserCart` (נקודת הסנכרון, שהחוזה שלה בחוט באמת הוא snapshot
שלם — כי ה-`localStorage` הוא הסמכות השנייה) נכתב כ-**diff**: `ON CONFLICT … DO UPDATE … WHERE
IS DISTINCT FROM`, כלומר שינוי כמות הוא `UPDATE` אחד לשורה אחת (§5) גם כשהבקשה נשאה את כל
העגלה. הבדיקה שמוכיחה את זה בודקת ש-`added_at` לא זז. עוד שניים: `getAllWishlistCounts()` —
כל מוני ה-wishlist בפלטפורמה, שנקראו כדי לתייג את המוצרים של חנות אחת — הפכה לשאילתה מקובצת
לחנות, והדף של חנות שולף שורה אחת (`isStoreFavorited`) במקום את כל רשימת המועדפים של הקונה.

*ההחלטה שנשמרה במכוון:* `cart_items` ממופתחת ב-`store_slug` + `cart_key` טקסטואליים **בלי FK**.
`ON DELETE CASCADE` היה מעלים שורת עגלה של מוצר שנמחק — וזו בדיוק ההתנהגות ש-`CartItem.unavailable`
נכתב כדי למנוע. `favorite_stores`/`wishlist_items` ב-uuid, ולכן `renameStoreSlugInUserData` נוגעת
היום רק בשתי טבלאות במקום בארבע.

*מה ש-`review-diff` מצא — ארבע קריסות 500 שכל חשבון מחובר יכול היה להרים, שתי מחלקות מוכרות:*
**(1) טיפוס העמודה הוא גבול.** `qty` הוא `integer` והכסף `bigint`, כלומר `{"qty": 1e30}` אינו עגלה
גדולה אלא `value out of range` — אותה צורה בדיוק כמו אילוצי ה-CHECK שהפכו שלושה ערכים שהקובץ
שמר בשקט לשגיאות ב-`orders`. **(2) גם האינדקס הוא גבול.** `cart_items` ו-`recent_stores`
ממופתחות בעמודות הטקסט שלהן, ורשומת btree לא יכולה לעבור 2704 בתים —
`index row size 8072 exceeds btree version 4 maximum`. זה `MAX_CATEGORY_NAME_LENGTH` שוב.
החלוקה בתיקון: **שדה זהות מפיל את השורה, שדה תצוגה נגזם** — לקצר `cart_key` זה לשנות איזו שורה
היא, כלומר הסנכרון הבא היה מוסיף עותק שני לצידה. **ומלכודת בבדיקה עצמה:** `'x'.repeat(10000)`
עובר את האינדקס גם בלי התיקון, כי pglz מכווץ אותו — הבדיקה חייבת מחרוזת אקראית.

*מה שהתגלה ושווה לדעת:*
· **סבוטאז' על כל מנגנון בנפרד גילה שתי בדיקות שמאמתות כלום** (הכלל מדיף 2, והפעם הוא תפס
  בפועל): `ON CONFLICT DO NOTHING` **סובל** שורה כפולה בפקודה אחת בניגוד ל-`DO UPDATE`, ולכן
  ה-`DISTINCT` שהגנתי עליו אינו מה שמציל; ו-`COUNT` כ-bigint אינו נבדק בסוויטה הזו כלל, כי
  PGlite מחזיר מספר. הראשונה תוקנה בהערה, השנייה בהחלפת הדרייבר לאחד שעונה כמו `pg` בבדיקה אחת.
· **`bigint` בכסף אינו בסיכון, וזה נבדק ולא הונח.** הסרת ההמרה שם משאירה את הקובץ ירוק, כי
  `fromAgorot` **מחלק** וחילוק ממיר מחרוזת מספרית. מה שמשרשר זה חיבור, והדבר היחיד שהמודול
  מחבר הוא המונה. ההמרה נשארת בשני הנתיבים; רק לאחד יש בדיקה שיכולה ליפול.
· **`getFavoriteStoresForUser` יצאה מ-`buildHomeFeed` והפכה לקלט** — אותה חלוקה של דיפים 1-2:
  הקורא שולף פעם אחת, וכל כללי המדפים נשארים טהורים ונבדקים בלי מסד.
· **קריאת האיחוד של "ביקרת לאחרונה" ירדה לתוך הטרנזקציה.** היא ישבה בנתיב ה-API לפני הכתיבה —
  בדיוק צורת ה-`await`-באמצע שהפכה את `bulkUpsertProducts` לעדכון אבוד.

*חצי ה-seeder זז, והפעם הוא באמת ייצר דאטה:* `seed-demo-data.mjs` **כן** זרע מוני מועדפים
(0–180 לחנות) ו-wishlist לשני קבצי JSON — אחד בלי קורא חי והשני ממופתח ב-slug שגוי. הם שורות
עכשיו, בתוך אותה טרנזקציה של `writeCatalog`, ו-`tests/seed-db.test.ts` מאמת אותן דרך הקוראים של
האפליקציה. עם זה הסקריפט **נגמל מקבצים לגמרי** — `fs`/`path` ושתי עזרות ב-`seed-db.mjs` נמחקו.
· **חמשת עוגני §9.7 נבדקו בשבירה ולא בנוכחות:** שבירת כל אחד מחמשת נתיבי הייבוא של מצב-הקונה
  (שורות עגלה, wishlist לפי slug בלבד — הבאג של 2026-08-02 עצמו, מועדפים, recent, וכמויות)
  מפילה את `tests/db-import.test.ts`. חיים.

*מה שנשאר פתוח מהמודול הזה:* אין. שני קבצי המונים נמחקו, `data/user-carts.json` נמחק בשלב 3
עם השאר.

**⚠️ "השאר" הוא לא ריק — נמדד 2026-08-02.** קל לקרוא את הרשימה המסומנת למעלה כאילו שלב 2 נגמר;
הוא לא. חמישה מודולים עדיין קראו וכתבו קבצים. **קבוצת הכסף (השלושה הראשונים) עברה 2026-08-02:**

| מודול | קובץ | יעד | מצב |
|---|---|---|---|
| `ad-campaigns` | `data/ad-campaigns.json` | `ad_campaigns` | ✅ עבר |
| `brand-campaigns` | `data/brand-campaigns.json` | `brand_campaigns` | ✅ עבר |
| `platform-ads` | `data/platform-ads.json` | `app_settings['platform_ads']` | ✅ עבר |
| `error-log` | `data/error-log.json` | `error_log` | ✅ עבר |
| `admin-tab-views` | `data/admin-tab-views.json` | `app_settings['admin_tab_views']` | ✅ עבר |

**שלב 2 נגמר 2026-08-02.** אין מודול ב-`src/lib/` שקורא או כותב `data/*.json`. הקבצים עצמם עדיין
על הדיסק ונמחקים בשלב 3, יחד עם שני הקוראים שנשארו להם — `db-import.mjs` ו-`verify-import.mjs`,
שזו כל עבודתם.

**רמת הזהירות בשלב 3, ושתי הצורות שאסור לבלבל ביניהן.** אין עדיין משתמש אמיתי אחד ואין הזמנה
אמיתית אחת — הכל דאטת דמו. אבל "דמו" כאן הוא שני דברים שונים לגמרי, ורק הראשון הוא נתוני זבל:

· **קטלוג הדמו של הפיתוח** (`npm run seed:demo`, `scripts/seed-demo-data.mjs`) — נזרע מחדש
  בפקודה אחת. מחיקתו פעולה משעממת שלא דורשת טקס גיבוי. `-- --clean` מסיר **רק** שורות דמו.
· **שלוש חנויות הראווה** (`showcase-fashion`/`tech`/`home`, `npm run seed:showcase`) — **אלה
  לא נתוני זבל אלא תוכן מוצר לתקופת ההשקה.** הן קיימות כדי שהספוטלייט יוכל לעמד
  (`spotlight.length > 1`), נושאות `Store.demo`, מסומנות תמיד, חסומות בצ׳קאאוט (403 בשרת),
  מחוץ ל-sitemap/פיד/IndexNow, ו**אינן נספרות** ל-`isLaunchMode` — ראה `lib/demo-stores.ts`
  ו-`lib/launch-mode.ts`. GO_LIVE §6.2 מחייב להריץ את הזורע שלהן **על הסביבה האמיתית**, כי הן
  לא נוצרות שם מעצמן. כלומר: מותר למחוק ולזרוע מחדש, אסור להשאיר את הסביבה בלעדיהן.

הנגזרת המעשית: מספר החנויות ה**אמיתיות** הוא מה ש-`launch-mode.ts` קורא, ולכן כל זריעה או
מחיקה משנה מה שהעמוד הראשי מרנדר — לבדוק את העמוד אחרי, לא רק את ספירת השורות.

**התיאור הקודם כאן ("שמות וכתובות של קונים אמיתיים") היה שגוי ותוקן** — הוא הוליך סשן להערכת
סיכון מנופחת ב-2026-08-02.

**✅ `ad-campaigns` + `brand-campaigns` + `platform-ads` (בוצע 2026-08-02).** דיף אחד לשלושתם
בכוונה: `project_brand_boost_twin_drift` מתעד ששני סוגי הקמפיינים מזינים כסף במודולים נפרדים,
ותיקון באחד בלי השני הוא בדיוק הסחיפה. 22 קבצי `src`, `tests/ad-campaigns-db.test.ts` (23),
`tests/platform-ads-db.test.ts` (9), ו-`brand-campaigns.test.ts` נכתב מחדש.

*הכיסוי, לפני שנגענו — נמדד בהחלפת ה-I/O בזמניות שמחזירות כלום:* `ad-campaigns` השאיר **1784
מתוך 1785** ירוקות, `brand-campaigns` 1781, ו-`platform-ads` **1785 — הכל**. הקובץ היחיד שכיסה
את `ad-campaigns` בדק שני פרסרים טהורים; `ad-campaign-health.test.ts` החליף את המודול כולו
ב-`vi.doMock` ו-`store-lifecycle.test.ts` ב-`vi.mock('node:fs')`, כלומר שניהם בדקו את הפיקסצ׳ר
של עצמם. **שניהם נכתבו מחדש מול הטבלה האמיתית, בלי mock בכלל.**

*ההחלטה שהתגלתה כבר-קיימת:* ל-`platform-ads` יש יעד — `app_settings(key, value jsonb)` נוצר
ב-0001 עם הערה שמפרטת אותו ואת `admin-tab-views` בשם, וה-import כבר כתב לשם. הטבלה הישנה כאן
("❌ אין") הייתה פרוזה שהתיישנה. מה שזה עולה: **ל-jsonb אין טיפוס עמודה ואין CHECK**, ולכן
`coerce()` הוא האילוץ — כל קריאה וכל כתיבה עוברות דרכו, ועד הדיף הזה ה-route קיבל כל מספר סופי,
כלומר `1e30` היה תקציב חוקי.

*הכסף:* שלושת התקציבים היו `number` של שקלים צפים. עכשיו `monthlyBudgetAgorot` /
`lifetimeBudgetAgorot` — **השם השתנה יחד עם היחידה, וזה העיקר:** שינוי יחידה תחת שם זהה הוא
שגיאת פי-100 ש-tsc עובר עליה בשלום, והשם הוא מה שגרם למהדר למצוא את כל הקוראים. הגוף של הבקשה
נשאר בשקלים (זה מה שהמוכר מקליד), וההמרה היא נקודה אחת בכניסה ואחת ביציאה — `ad-metrics.ts`
נשאר ILS, בדיוק כמו הגבול ש-`platform-revenue.ts` כבר מתעד. שלושה עוגני §9.4 נוספו
ל-`verify-import.mjs`.

*מה שהתגלה ושווה לדעת למודול הבא:*
· **`mutex.ts` כבר היה מת.** התוכנית ספרה שבעה קבצים שמייבאים אותו; שבעתם הזכירו אותו **בהערה**
  (`grep -l` על המילה, לא על ייבוא). אף מודול לא נעל כלום. הקובץ נמחק בדיף הזה.
· **`+=` הוא הסיכון היחיד ב-bigint, וגם כאן.** `admin-ads.ts` מסכם תקציבים של קמפיינים —
  `'30000' + '20000'` הוא `'3000020000'`. `fromAgorot` מחלק ולכן בטוח; רק החיבור מסוכן, ולכן
  יש בדיוק בדיקה אחת עם דרייבר שמחזיר מחרוזות כמו `pg`.
· **`[]` אינו `undefined`.** `product_ids`/`category_ids` הם `NOT NULL DEFAULT '{}'`, והקוראים
  נכתבו מול צורת הקובץ שבה רשימה לא-בשימוש פשוט נעדרת. הן ממופות חזרה ל-`undefined`.
· **הכתיבה-בתוך-לולאה היא הצורה שנוצרה במעבר.** `getCampaignsForStore` מחליט בזיכרון וכותב
  פעם אחת לכל **סוג** (4 משפטים לכל היותר, לא אחד לקמפיין), וסגירת חנות מארכבת הכל במשפט אחד.
· **סבוטאז' על 23 מנגנונים בנפרד — 23 נתפסו.** אחד "שרד" בהרצת האצווה והתברר כ-cache של Vite
  בין שתי עריכות באותה שנייה; בהרצה נפרדת הוא נתפס. אצווה כזו צריכה השהיה בין מקרים.
· **מה ש-review-diff מצא:** התאומים לא הסכימו על תקציב פסול. `updateBrandCampaign` התעלם ממנו,
  `updateCampaign` העביר אותו לעמודה — כלומר הפרת CHECK, כלומר 500 בדשבורד במקום שדה שנדחה.
  אותו `project_brand_boost_twin_drift` שוב, בתוך הדיף שנועד למנוע אותו.

*מה שנשאר פתוח:* `getAllCampaigns()` עדיין מחזירה הכל (§3), כמו `getAllStores`/`getAllOrders`
שלצידה — תזוז איתן. ו-`data/*.json` של השלושה עדיין על הדיסק, לשלב 3.

**✅ `error-log` + `admin-tab-views` (בוצע 2026-08-02) — "השאר" ריק, ושלב 2 נגמר.** דיף אחד
לשניהם: אף אחד מהם אינו כסף, לשניהם היה יעד מוכן ב-0001, ושניהם נצרכים בעמוד אחד (`admin/index.astro`).
`tests/error-log-db.test.ts` (26) ו-`tests/admin-tab-views-db.test.ts` (13) חדשים.

*הכיסוי, לפני שנגענו — נמדד ולא הוערך:* החלפת ה-I/O של שניהם בזמניות שמחזירות כלום השאירה
**1825 מתוך 1825** ב-147 קבצים. אפס. שני קובצי הבדיקה שהיו נוגעים רק בפונקציות הטהורות
(`truncateStack`, `filterAndSortErrors`, `getErrorStoreNames`, `countSince`) על מערכים בנויים ביד.

*החלטה 1 — `logError` הופכת לשאילתה בתוך מטפל השגיאות עצמו (האופציה שנבחרה: לבלוע ולוותר מהר).*
`middleware.ts` תופס כל 500 וקורא לה, כלומר **הדבר שמדווחים עליו הוא לעיתים קרובות המסד**.
ההמתנה שם לא קונה דבר — איש לא קורא את הערך המוחזר, והכתיבה שמחכים לה תיכשל מאותה סיבה שהבקשה
נכשלה — ועולה את המשאב היחיד שבאמת נדיר: מול pool של 10 ומסד שנפל, **כל** בקשה היא 500, כלומר כל
בקשה הייתה יושבת במטפל הזה ומחזיקה חיבור עד `connectionTimeoutMillis`+`statement_timeout`. נפילת
מסד הייתה הופכת את עצמה להקפאה מלאה של האתר. לכן: `logError` לעולם לא דוחה, כל עשרת אתרי הקריאה הם
`void logError(…)` (**כולל `checkout.ts` — הזמנה שכבר חויבה לא נופלת כי שורת לוג נכשלה**, הלקח מדיף
ההודעות), ו-`MAX_CONCURRENT_WRITES=2` חוסם כמה מה-pool המודול הזה רשאי להחזיק בבת אחת. מעבר לתקרה
רשומה **נזרקת ולא מצטברת בתור** — ועדיין מגיעה ל-`console.error`, שזה ממילא מאיפה שניטור חיצוני קורא
(CURRENT_TASK → סשן ג׳). **timeout לא היה עושה את העבודה:** `Promise.race` נוטש את ה-promise בזמן
שהשאילתה ממשיכה להחזיק את החיבור. ובאותה תנועה `resolveErrorContext` (שלוש שאילתות, ה-OPEN שנפתח
במודול הראשון) ירדה **לתוך** `logError`, כך שאחרי הדיף הזה אין `await` אחד בנתיב השגיאה.

*החלטה 2 — `MAX_ENTRIES=500` (האופציה שנבחרה: התקרה נשארת על הטבלה, ונשארת 500).* `slice(-500)`
בקובץ פירושו שהאחסון עצמו לא יכול היה לגדול; `LIMIT` בקריאה זו שאלה אחרת לגמרי — הוא חוסם את מה
שהאדמין רואה, לא את מה שהטבלה עולה — ו-`POST /api/log-client-error` הוא **לא מאומת ולא מוגבל בקצב**,
כלומר נתיב הכתיבה היחיד באפליקציה שכל אחד רשאי לקרוא לו בחינם. לכן התקרה נאכפת בתוך משפט הכתיבה.
**והמספר נשאר 500 כי להעלות אותו זה לא חינם:** `admin/index.astro` גוזר את תפריט סינון-החנויות
(`getErrorStoreNames`) ואת תג "(N) חדש" (`countSince`) מתוך המערך ש-`getRecentErrors(MAX_ENTRIES)`
מחזיר — בתקרה הזו המערך *הוא* כל היומן ושניהם מדויקים, ובכל תקרה גבוהה יותר שניהם הופכים בשקט
ל"מעל ה-N האחרונים". להעלות = להעביר את שניהם לשאילתות משלהם קודם.

*מה שדרש מיגרציה בכל זאת (`0005_error_log_seq.sql`) — ובדיקה היא שגילתה אותו, לא קריאת קוד:*
היומן היה מערך, כלומר סדר ההגעה היה חינם. כטבלה זה היה `ORDER BY created_at DESC, id`, ול-`now()`
יש רזולוציה סופית — **נמדד: שלוש כתיבות רצופות נוחתות על אותה מילישנייה**, והשובר-תיקו מאחוריהן הוא
uuid אקראי. שני קוראים תלויים בסדר הזה: לשונית ההתראות (קוסמטי), ו**התקרה, שמוחקת לפיו** — כלומר
"הישן ביותר" שנקבע ע"י uuid אקראי, בזמן שאצווה יכולה לפנות רשומה **חדשה יותר** מאחת שנשמרה. ואצווה
היא בדיוק מתי שהתקרה פועלת. `seq` הוא `bigint … AS IDENTITY`, המיון והמחיקה עברו אליו,
ו-`error_log_recent_idx` נמחק כי לא נשאר לו קורא.

*מה שהתגלה ושווה לדעת:*
· **`OFFSET` בתוך CTE שמשנה נתונים הוא `MAX_ENTRIES - 1`, לא `MAX_ENTRIES`.** כל חלק ב-CTE כזה רואה
  את **אותו snapshot**, כלומר ה-`DELETE` אינו רואה את השורה שה-`INSERT` שלצידו כותב. גיזום לאחד
  מתחת לתקרה על הטבלה שלפני ההוספה משאיר בדיוק את התקרה אחריה. יש בדיקה שנופלת על ההפרש הזה.
· **אילוץ CHECK לא היה מלכודת חיה כאן, ונבדק ולא הונח.** כל עשרת אתרי הקריאה מעבירים ליטרל
  (`'server'`/`'client'`, `'buyer'`/`'seller'`), וגם 326 השורות בדאטה האמיתי מסכימות. השער נוסף
  בכל זאת: ערך מחוץ לתחום היה 500 **בתוך מטפל השגיאות**, המקום הגרוע ביותר באפליקציה לשגיאה שנייה.
· **ל-`message` לא הייתה תקרת אורך בשום מקום** — לא בקוד ולא בעמודה — וב-`middleware.ts` הוא
  `err.message`, שנושא קלט של בקשה (שגיאת Postgres מצטטת את הערך שדחתה). לא מלכודת ה-2704 (אף
  עמודה כאן אינה מאונדקסת), אבל כן צמיחה בלי גבול משולח לא מאומת.
· **`tests/async-lib-awaited.test.ts` תפס את כל 15 אתרי הקריאה, וזה נבדק בהרצה ולא הונח** — כולל
  `const ok = id ? setErrorResolved(…) : false` ב-`api/admin/errors.ts`, שבו Promise תמיד אמת:
  ה-404 היה מת בשקט ו-`tsc` לא אומר מילה.
· **סבוטאז' על 21 מנגנונים בנפרד — 21 נתפסו, אבל שניים רק בסבב שני.** אחד "שרד" והתברר כתקלה
  בסקריפט הסבוטאז' עצמו (ההחלפה פגעה במופע הראשון של המחרוזת, שהיה **בהערה** מעל ה-SQL); השני חשף
  **בדיקה שמאמתת כלום** — הטענה על `undefined` מפורש נכתבה על שדה שה-context לא פותר ממילא, כלומר
  היא עברה גם בלי המנגנון. שניהם תוקנו ואומתו בהרצה נפרדת. זה בדיוק הכלל שנולד ב-`analytics`.
· **`review-diff` מצא את מה שהחיפוש שלי לא:** כתבתי guard-test שאוסר עותק שני של רשימת הלשוניות
  (`['sellers','stores','orders','alerts']`) — והוא מצא **עותק שלישי** ב-`src/scripts/admin/tab-nav.ts`,
  קוד דפדפן, שאף אחד לא חיפש. הוא גם מסביר למה הרשימה לא יכולה לשבת ב-`admin-tab-views.ts`: הקובץ
  ההוא מייבא את המסד. היא ב-`lib/admin-tabs.ts` עכשיו, בלי ייבוא אחד, ושלושת הצדדים מייבאים ממנה.
· **`app_settings` הוא jsonb, כלומר `coerce` הוא טיפוס העמודה** — הכלל מ-`platform-ads`, וכאן הוא
  לא תיאורטי: ה-fixture שהייבוא קורא מחזיק `{"orders": 12}`, מספר במקום חותמת. מועבר כמו שהוא,
  `createdAt > 12` משווה מחרוזת למספר — false לכל שורה, כלומר תג שתקוע על אפס לנצח.
· **ה-N+1 נסגר:** `getAllLastViewedAt()` אחת במקום ארבע קריאות ל-`getLastViewedAt` לשורת jsonb אחת.
· **`verify-import.mjs` קיבל עוגן ל-`admin_tab_views`**, שלא היה לו אחד.

*מה שנשאר פתוח מהמודול הזה:* `GET /api/admin/errors` **אין לו קורא בצד הלקוח** — סקריפט לשונית
ההתראות משתמש רק ב-POST (resolve/clear), והעמוד מרונדר בשרת. השארתי אותו, מאותו נימוק כמו
`/api/messages` בלי פרמטרים: נקודת-קצה מאומתת מול אדמין ומוגבלת, שסביר שיש לה שימוש ידני. וגם
`console.error` בנתיב הזריקה יורה פעם לכל בקשה תחת נפילת מסד — זה מכוון (זריקה שקטה גרועה יותר),
אבל נקודה לבדיקת העומס של שלב 3.

**שלב 3 — ניקוי.** מחיקת `data/*.json` והקוראים שלהם (`mutex.ts` כבר נמחק בדיף הקמפיינים),
הרצת `EXPLAIN ANALYZE` על ארבע השאילתות החמות, בדיקת עומס בסיסית.

**✅ השער ב-`purge()` (בוצע 2026-08-03) — הבטיחות עברה מהקוראים לפונקציה.** `purge` קיבלה
WHERE והריצה אותו; שני הזורעים העבירו פרדיקט שחנות אמיתית לא יכולה לעמוד בו, ולכן ההתנהגות
הייתה נכונה — אבל **הפונקציה** הייתה בטוחה רק כל עוד כל קורא במקרה כזה. עכשיו קורא **נוקב בשם
scope ולא כותב אחד** (`purge(db, 'demo')`), ו-scope נבדק שהוא **תת-קבוצה** של `DISPOSABLE_*`
לפני שרץ DELETE כלשהו. שתי השכבות לא נגזרות זו מזו בכוונה: חיפוש-השם עוצר קורא שלישי, בדיקת
ההכלה עוצרת עריכה של קבוע. שלישית, guard-test גורף חוסם סקריפט שעוקף את `purge` לגמרי
(`DELETE FROM stores|sellers|orders` ב-`scripts/`). כל שכבה סובוטז'ה בנפרד ונתפסה ע"י הבדיקה שלה.

*באג שנמצא בדרך:* `purgeOrdersOfStores` מחקה **הזמנה שלמה** משום שנגעה בחנות דמו אחת. סל אחד
יכול להשתרע על כמה חנויות, ולכן הזמנה של מוכר אמיתי הלכה יחד עם הדמו. נמחקות עכשיו רק הזמנות
שכל החנויות שלהן ניתנות למחיקה; השאר נספרות ומדווחות. slug שאין לו שורת חנות נחשב **לא**
בר-מחיקה — לא נשאר דבר שיוכיח שהוא נזרע.

*אומת read-only מול הדאטה החי:* demo — 35 חנויות / 35 חשבונות / 176 הזמנות, אף שורה שאינה
בת-מחיקה, אפס הזמנות משותפות. showcase — 3 / 1 / 0. השער לא חוסם `--clean` של היום.

**✅ §9.2 `EXPLAIN ANALYZE` (בוצע 2026-08-03, מול Neon).** ארבעת האינדקסים קיימים ובני-שימוש.
`store_products(store_id, slug)` ו-`store_previous_slugs(slug)` נבחרים **לבד** כבר היום.
`stores(slug)` ו-`stores(custom_domain_hostname)` נופלים ל-Seq Scan ב-45 שורות — **וזו הבחירה
הנכונה**, לא כשל: עם `enable_seqscan = off` שניהם עוברים ל-`Index Scan using stores_slug_key` /
`stores_custom_domain_hostname_key`, כלומר האינדקס תקין והמתכנן פשוט לא זקוק לו בגודל הזה.
**נקודת המעבר נמדדה** (אותו מתכנן, PGlite, כדי לא לכתוב לשרת החי): Seq Scan ב-45 וב-200 שורות,
`Index Scan` מ-1,000 והלאה — והעלות **שטוחה** משם: 8.29 באלף מול 8.44 במאה אלף. זה בדיוק מה
שהאינדקס קיים בשבילו.

**✅ §9.5 מלאי בו-זמנית (בוצע 2026-08-03, מול Neon) — ומצאה באג.** 50 קונים במקביל למוצר עם
מלאי 10: בדיוק 10 עברו, כל אחד תפס יחידה **אחרת** (`after` 0..9), המלאי נחת על 0. §7.5 מוכח מול
יותר מ-process אחד — מה ש-PGlite לא יכול לענות עליו, ולכן הבדיקה חיכתה לשלב הזה
(`tests/stock-concurrency-live.test.ts`, מדלגת על עצמה בלי `DATABASE_URL`).
*מה שהיא גילתה:* **7 מתוך 40 הסירובים דיווחו עד 8 יחידות במלאי ברגע שבו המלאי היה 0.** ה-CTE
בשם `target` נקרא מה-snapshot של תחילת המשפט; ה-`UPDATE` שלצידו לא — הוא חסום על נעילת השורה
של צ׳קאאוט מתחרה וקורא מחדש כשהיא משתחררת. תחת תחרות השניים חלוקים, והסירוב נשא את המספר הישן —
וזה המספר שעמוד הקונה **מגביל לפיו את בורר הכמות**, כלומר הוצע לקונה מלאי שכבר לא קיים והוא נדחה
שוב בניסיון החוזר. שום דבר לא נמכר ביתר (ה-`UPDATE` עדיין הוא הפסיקה). התיקון: סירוב קורא את
המונה מחדש במשפט משלו — round-trip אחד, **רק בנתיב האזל-מהמלאי**; המסלול החם נשאר המשפט
היחיד וחסר-הנעילה של §7.5. אחרי התיקון כל 40 הסירובים מדווחים 0.

**✅ בדיקת עומס בסיסית (בוצע 2026-08-03, מול Neon).** טעינת עמוד = קריאה אחת + ארבע כתיבות
לא-ממתינות (`recordPageView`, `recordProductView`, שני `recordAnalyticsEvent`) = **5 שאילתות**.
מ-laptop מול פרנקפורט, RTT נמדד `SELECT 1` = **66ms** (median, יציב):

| מקבילות | wall | נכשלו | p50 מלא | p95 מלא | תפוקה |
|---|---|---|---|---|---|
| 10 | 755ms | 0 | 632ms | 755ms | 13/s |
| 50 | 1.7s | 0 | 1,074ms | 1,666ms | 29/s |
| 200 | 6.4s | 0 | 4,110ms | 6,330ms | 31/s |
| 500 | 8.5s | 0 | 6,733ms | 8,242ms | 59/s |

**אפס כשלים, אפס timeout על חיבור, ה-pool לא התרוקן — גם ב-500 במקביל.** התקרה מוסברת במלואה
ע"י `pool(10) ÷ RTT(66ms) ÷ 5 שאילתות ≈ 30 טעינות/s` — כלומר **הרשת, לא המסד**. בפרודקשן שרת
האפליקציה יושב באותו אזור כמו Neon (RTT ~1-2ms), ואותו pool נותן סדר גודל של ~1,000 טעינות/s.
**המסקנה המעשית:** ארבע הכתיבות הן 4 מתוך 5 ה-round-trips של טעינת עמוד. איחודן למשפט אחד
יוריד את העלות ל-2 — פי 2.5 headroom, וזו הפעולה היחידה עם ההחזר הגבוה ביותר כאן. **טרם בוצע.**

**✅ תקרת המקביליות בנתיב השגיאה (בוצע 2026-08-03) — הייתה בלי בדיקה בכלל.**
`tests/error-log-pool-guard.test.ts` מריץ 50 בקשות נכשלות מול מסד שלא עונה: מוחזקים לכל היותר
**2** חיבורים, 48 נזרקות (`dropped (busy)`) ולא מצטברות בתור, והסלוט משוחרר גם כשהכתיבה **זורקת**
— אחרת נפילת מסד הייתה משתיקה את היומן לצמיתות. שני סבוטאז'ים (ביטול התקרה, דליפת הסלוט) נתפסו.
*`console.error` בנתיב הזה נמדד ולא הונח:* ~4.5µs לקריאה, **~200,000/s** — בכל קצב בקשות סביר
זה מתחת לאחוז מליבה אחת. הוא נשאר; זריקה שקטה גרועה יותר.

**✅ מחיקת `data/*.json` (בוצע 2026-08-03).** 21 קבצים + `scripts/db-import.mjs` + הסקריפט
`db:import` ב-package.json + `reportChecks` (שאיבד את הקורא היחיד שלו יחד עם ה-CLI) + מסלול
הדאטה-האמיתי ב-`tests/db-import.test.ts`. `dataDir` ב-`verifyImport` **הפך לחובה** — ברירת מחדל
שמצביעה על תיקייה מחוקה הייתה משווה 0 מול 0 ומדווחת "עבר" בביטחון.

*ההחלטה, ומה החליף את השומר: שום דבר, וזה לא ויתור.* `tests/db-import.test.ts` הוא אחד השומרים
החזקים בסוויטה — מיגרציה + ייבוא מול Postgres אמיתי, וכל עוגני §9 חיים בתוך `verify-import`.
הוא רץ פעמיים: על ה-fixture, ועל `data/` כשהיה קיים. **הצד השני מעולם לא רץ בשום push** —
`data/` הוא gitignored, כלומר ב-CI הוא ריק. מה שגידר כל commit הוא ה-fixture, וזה עדיין הוא.
מה שאבד הוא הסיכוי להפתעה **חדשה** בקובץ בן שנה, וזה לא אותו דבר ככיסוי.
`scripts/lib/db-import.mjs` **לא נמחק ולא יכול היה** — `tests/helpers/test-db.ts` בונה איתו את
המסד של ~150 קובצי הבדיקה מתוך ה-fixture.

**§3 — שבע `getAll*`: שלב נפרד, לא שלב 3 (הוחלט 2026-08-03).** שלב 3 הוא ניקוי ואימות — הפיך,
בלי סיכון למספרים. §3 הוא שינוי עיצוב-שאילתה בחמישה משטחים שמציגים כסף לאדמין, דורש כיסוי חדש
ב-`reporting-invariants`, ושלושת האגרגטורים הגדולים (`admin/index.astro`,
`api/admin/platform-performance.ts`, `buyer/dashboard.astro`) רוצים SUM/GROUP BY — עבודה **אחת**
לכולם. לערבב = שלב 3 לא נסגר.

**✅ §3 בוצע 2026-08-03.** ענף `db-stage-getall`. מודול חדש אחד (`lib/order-reporting.ts` — כל
שאלה על הזמנות ברמת הפלטפורמה), מיגרציה אחת (`0006`), ושלושה קובצי בדיקה חדשים.

*הכלל שהחליט כל שורה בדיף, והוא בדיוק זה שחזר על עצמו בשלב 2:* **או שזו אריתמטיקה — ואז היא
מקבלת את הנתונים כפרמטר, נשארת סינכרונית וניתנת לבדיקה בלי מסד — או שזה `GROUP BY` שנכתב
כלולאת `for`, ואז זו שאילתה.** אף פונקציה טהורה לא נמחקה בדרך: היא הפכה **לתאום** של השאילתה
שהחליפה אותה, ו-`tests/reporting-invariants.test.ts` §7 מריץ את שתיהן על אותן שורות ודורש אותה
תשובה — headline הפלטפורמה, הכנסה לחנות, הזמנות פתוחות, מכירות לתקופה, המשפך, הפיוס, מוני
המוצרים, מוני הקמפיינים והמנויים. זו הסיבה ש-`filterAndSortOrders`, `getStoreRevenueMap`,
`reconcileOrders`, `buildSellerFunnel` ו-`campaignTotalsOf` עדיין קיימות.

| היה | עכשיו |
|---|---|
| `getAllOrders()` | **נמחקה.** `getAdminOrdersPage` (WHERE/ORDER BY/LIMIT) + `order-reporting.ts` |
| `getAllProducts()` | **נמחקה.** `getProductCountsByStore` (GROUP BY), `getProductsByStoreIds`, `getProductNames`, `searchVisibleProducts` |
| `getAllAdminThreads()` | **נמחקה.** `getAdminThreadsPage` — עימוד ברמת השרשור |
| `getMessagesBySeller/ByBuyer` | **נמחקו** יחד עם `GET /api/messages` בלי פרמטרים |
| `getAllCampaigns()` | **נמחקה.** `getCampaignsInRange` + `getCampaignTotals` (וכך גם התאום ב-brand) |
| `getAllStores()` / `getAllSellers()` | **נשארו — זו הרשימה של האדמין.** ראה למטה |

*מה שנשאר בכוונה, וזו החלטה ולא שארית:* **הרשימה** — כל החנויות וכל המוכרים — נקראת עדיין
במלואה בדשבורד האדמין, כי כל לשונית שם היא רשימה של אותה רשימה או חלוקה שלה (מוכרים, חנויות,
לתשומת לב, פרסום), עם חיפוש שחוצה שם-מוכר ומיון `localeCompare('he')` שקולציה של Postgres אינה.
**אבל שום דבר שנגזר ממנה כבר לא סורק טבלה שנייה:** הכנסה לחנות, מוני מוצרים, הזמנות פתוחות,
מכירות לתקופה ו-headline הפלטפורמה כולם `GROUP BY` עכשיו. מה שנשאר הוא O(פלטפורמה) ולא
O(קטלוג × חנויות). ב-100,000 חנויות המסך הזה יצטרך עימוד ב-SQL, החלטת קולציה ושאילתה ללשונית —
עבודה גדולה מ-§3, ובמכוון לא התחילה כאן באמצע.

*המדידה שהצדיקה את הסדר:* `site-search` היה הדחוף — הוא קרא את כל הקטלוג **בכל הקשה** בתיבת
החיפוש של ה-header, בנתיב של קונה ולא של אדמין. הוא היום `LIKE` מאונדקס אחד לכל מילה מול
`store_products.search_text`.

*מה שדרש מיגרציה (`0006_product_search_text.sql`):* ההתאמה בחיפוש אינה תת-מחרוזת על הטקסט הגולמי —
`normalizeHe` מסיר ניקוד, מקפל את חמש האותיות הסופיות, מוריד גרש/גרשיים והופך פיסוק לרווח, ורק
אז דורש שכל מילה תופיע. **הקיפול אינו קוסמטי:** "ספר טלפונים" נורמל ל-"ספר טלפונימ", וזה מה
שמאפשר לחיפוש "טלפון" למצוא אותו — `ILIKE` גולמי היה מפסיק למצוא בדיוק את המקרים העבריים
שהנרמול קיים בשבילם. לכן עמודה מחושבת (`GENERATED … STORED`) עם אינדקס trgm, והנרמול הוא פורט
מדויק של `normalizeHe`. **הוא חייב לשבת בפונקציה ולא בביטוי:** ביטוי של עמודה מחושבת חייב להיות
`IMMUTABLE` ו-`array_to_string` אינו מסומן כזה. `tests/product-search-normalize.test.ts` משווה
תו-בתו בין שני הצדדים על קורפוס מלכודות; סבוטאז' על הקיפול מפיל אותו.

*מה שהתגלה ושווה לדעת:*
· **`astro check` תפס מה ש-`tsc` לא, ושוב.** גוף התבנית של `.astro` אינו חלק מ-`tsc --noEmit`:
  אחרי שכל ה-frontmatter היה נקי, `astro check` מצא 12 הפניות מתות בתבנית (`allOrders.length`,
  `messageThreadsAll`, ששת משתני הפילטר של ההזמנות). **`npm run verify -- --all` הוא הבדיקה, לא
  `tsc`.**
· **פרמטר שהמשפט אינו מזכיר הוא שגיאת bind, לא עודף.** `getAdminOrdersPage` בנתה רשימת פרמטרים
  אחת לשתי שאילתות, ושאילתת ה-`COUNT` לא הזכירה את `$6` — `bind message supplies 6 parameters,
  but prepared statement requires 5`, על כל טעינה של לשונית ההזמנות.
· **`tests/money-guards.test.ts` תפס רשימת סטטוסים כתובה ביד** שהוספתי למיון. היא נגזרת עכשיו
  מהטבלה (`SHIPPING_PIPELINE_ORDER` — שורות שאינן `terminal`), כלומר סטטוס חדש נכנס למיון לבד.
· **מספר שנשאר בעמוד אחרי שהרשימה הפכה לעמוד הוא באג שקט.** `totalAll` היה `allOrders.length`,
  ו-`products.length` היה מונה המוצרים בכרטיס מוכר — שניהם היו קורסים בשקט ל"גודל העמוד" ברגע
  שהשליפה הצטמצמה. שניהם `COUNT` נפרד עכשיו (`totalUnfiltered`, `productCount`).
· **חיפוש טקסט חופשי לא מוגבל באורך בנקודת קצה לא מאומתת** — `/api/search`. היה כך גם קודם (הוא
  הזין את המתאם ב-JS), אבל עכשיו הוא הופך לתבנית `LIKE`. תוקן: 200 תווים ו-6 מילים, אותה תקרה
  ואותו נימוק כמו בחיפוש יומן הכסף.
· **`::date` על `timestamptz` קורא את אזור הזמן של ה-session.** בסינון טווח הקמפיינים ה-JS משווה
  `createdAt.slice(0,10)` שהוא UTC, אז הצמצום נכתב `(created_at AT TIME ZONE 'UTC')::date` במפורש
  ולא הושאר תלוי בהגדרת שרת.
· **צמצום הקמפיינים הוא על-קבוצה במכוון.** הכלל המדויק לכמה ימים קמפיין רץ בתוך חלון הוא
  `ad-metrics.ts#overlapDays`, ועותק שני שלו ב-SQL הוא עותק שיכול לסחוף — על מספרים שהבעלים
  קורא ככסף. השאילתה רק חייבת לא **להשמיט** קמפיין; מה שעובר מיותר מקבל אפס ב-JS ולא משנה סכום.
· **הפיוס מחזיר רק שורות שחולקות.** `reconcilePlatform` הוא שש בדיקות כשאילתות, עם תקרה של 50
  שורות ודגל `truncated` — באג מערכתי מפיל כל הזמנה, ובאנר ששקט מציג את חמישים הראשונות היה
  ממעיט בדיוק במצב שהוא קיים בשבילו.

*החלטה — `GET /api/messages` בלי פרמטרים: **נמחק, לא עומד.*** הנימוק שהשאיר אותו ב-§8 היה שייתכן
קורא חיצוני (סוכני AI). אין: הנקודה מאומתת ב**עוגיית session**, ולפלטפורמה אין אימות טוקן ואין API
ציבורי, כלומר לקורא החיצוני ההיפותטי אין דרך להיכנס. כל לקוח בריפו משתמש ב-`?repliesFor=` או
ב-`?unread=1`. לעמד במקום זה היה לתכנן cursor וצורת תשובה לצרכן שאינו קיים — ולנקודת קצה בלי
קורא אין בדיקה. היא מחזירה 400 שמפרט מה כן לבקש; `getMessagesBySeller`/`getMessagesByBuyer` נמחקו
איתה.

*החלטה — **חיפוש הטקסט החופשי ביומן הכסף נשאר בזיכרון**, ולא בגלל עצלות:*
1. ההתאמה כוללת את **התווית העברית** של סוג האירוע, ש-`MONEY_EVENT_LABELS` מחזיק בקוד לצד אוצר
   המילים בדיוק כדי שלא יהיו שני עותקים. לדחוף אותה ל-SQL = להעתיק את התוויות למסד.
2. היא כוללת גם את **הסכום כפי שהאדמין רואה אותו** (`formatAgorot`, כלומר `Intl` עברי) — אין לזה
   מקבילה נאמנה ב-SQL.
3. דחיפה חלקית (העמודות בלבד) אינה על-קבוצה אלא תת-קבוצה, כי שני החצאים מחוברים ב-OR — היא
   הייתה **מפילה** שורות שמתאימות רק לפי תווית.
**✅ והחצי שכן היה פתוח נסגר באותו יום — 30 יום, החלטת הבעלים.** החיפוש רץ מעל חלון שהשאילתה
צמצמה, אבל אדמין שפתח את היומן **בלי טווח** קרא כל אירוע שנרשם אי-פעם — הקריאה הלא-חסומה האחרונה
בדשבורד. עכשיו יש חלון ברירת מחדל של 30 יום (`MONEY_LOG_DEFAULT_DAYS`). שלושה דברים שהיו נשברים
בשקט בלעדיהם:
· **`?mev=` לשורה ישנה יותר מ-30 יום.** הקישור היה עונה "האירוע לא נמצא בסינון הנוכחי" — בדיוק
  הכשל ש-`moneyLogTargetMissing` נכתב כדי לדווח עליו, מגיע כברירת מחדל. `widenToEvent` מרחיב את
  החלון אחורה עד היום של השורה עצמה (`getMoneyEventDay`, ביום העסקי ולא ב-UTC).
· **"אין אירועים" הפך לשקר.** המסך הריק אמר "עדיין לא נרשמו אירועים כספיים" — לא נכון ליומן שפשוט
  ישן מ-30 יום, ומסתיר את הפקד היחיד שמתקן את זה. שלוש תשובות עכשיו, לא שתיים.
· **ברירת מחדל אינה סינון פעיל.** "נקה סינון" לא מציע להסיר אותה (זה היה או שקר או חזרה לקריאה
  הלא-חסומה), והכפתור **קורא לה בשם** ("30 הימים האחרונים") במקום להציג שני תאריכים שאיש לא בחר.
טווח שהאדמין בוחר גובר תמיד, וגבול אחד בלבד (`mfrom` לבד) נשאר חצי-פתוח כפי שנכתב.
אינדקסי ה-trgm ממשיכים לחכות לפורט של החיפוש עצמו, אם וכאשר.

*תיקון להנחה שהייתה רשומה כאן:* `getAllProducts` **אינה** מועמדת למחיקה. אין לה קורא מחוץ
ל-`lib`, אבל יש לה שלושה **בתוך** `lib` — `site-search.ts`, `seller-funnel.ts`, `admin-stats.ts`.
מחיקתה שוברת את חיפוש הפלטפורמה. `site-search` הוא הדחוף שבהם: הוא טוען את כל המוצרים בכל חיפוש.
*(נסגר: שלושתם קיבלו שאילתה משלהם והיא נמחקה.)*

*מה שנשאר פתוח מהשלב הזה:* `getAllStores()`/`getAllSellers()` כרשימת האדמין (למעלה); חלון ברירת
המחדל של יומן הכסף (למעלה); ו-`buildPerformanceSummary` של מוכר יחיד עדיין מקבל את כל ההזמנות של
החנות שלו — חסום לפי חנות ולא לפי פלטפורמה, ולכן מחוץ ל-§3, אבל חנות עם 100,000 הזמנות תרצה
את אותו טיפול ש-`getPlatformSales` קיבל.

**שלב 4 — מפוצל לשניים במפורש (2026-08-03).** התוכנית ספרה כאן שני דברים כשלב אחד, והם לא:
4א בוצע באותו יום, 4ב חסום על החלטת הבעלים.

**✅ 4א — scheduler. בוצע 2026-08-03, ענף `db-stage-4a-scheduler`.** שלוש פעולות שהקוד שלהן היה
כתוב, בדוק ועובד, ואיש לא קרא להן — `purgeExpiredCheckouts()`, סנכרון מלאי מפיד חיצוני, וסריקת
בריאות הקמפיינים — רצות עכשיו על טיימר. מיגרציה אחת (`0007_job_runs.sql`), מודול חדש אחד
(`src/lib/jobs/` — טריגר, תביעה, רשימה), חילוץ אחד (`lib/store-feed-sync.ts`), ושלושה קובצי בדיקה.

*ההחלטה, ולמה שתי האחרות הפסידו.* השאלה לא הייתה "איזה טיימר" אלא **איפה מותר לטיימר להחליט**,
כי ה-instances כבר לא חסומים (§7.5/§4). `pg_advisory_lock` נותן מניעת-בו-זמניות ותו לא: הוא חי
כל עוד ה-session חי, כלומר הוא יודע לענות "האם מישהו רץ עכשיו" ולא "מתי זה רץ לאחרונה" — ובלי
התשובה השנייה **כל אתחול מריץ הכל מחדש**, ודיפלוי שמרים שלושה instances היה מושך את הפיד של כל
מוכר שלוש פעמים בדקה. cron חיצוני נכון, וזו התשובה הסטנדרטית בפלטפורמה שהתהליכים שלה נרדמים —
אבל הוא מערכת שנייה להגדיר, סוד לסובב, וצעד ידני ב-GO_LIVE ש**נכשל בשקט**: cron שהפסיק לירות לא
אומר כלום, והתסמין מגיע שבועות אחר כך כסחיפת מלאי. הכלל כאן הוא zero-touch, והאפליקציה היא
תהליך ארוך-חיים. לכן: **הטיימר מקומי, ההחלטה טבלה עמידה** — `job_runs`, משפט אחד שמספר השורות
המושפעות שלו הוא כל התשובה, בדיוק כמו `claimCheckout` וההפחתה של §7.5. שני התנאים ב-`WHERE` עונים
על שתי שאלות שונות ושניהם נחוצים: `next_run_at <= now()` (האם הגיע הזמן — החצי שעמיד לאתחול)
ותפוגת `lease_until` (האם מישהו רץ עכשיו — כי ריצה יכולה לעלות על המחזור של עצמה). אין עמודת
`state`: "רץ" הוא `lease_until > now()`, ושדה שני שאומר אותו דבר הוא שדה שיכול לחלוק עליו אחרי
קריסה. הטיימר עצמו הוא seam: `runDueJobs()` בטוח לקריאה מכל מקום, ולכן מעבר ל-cron חיצוני ביום
שהאירוח ישתנה הוא החלפת **טריגר** בלבד.

*איפה הטיימר נדלק:* ל-node adapter של Astro אין hook של "השרת עלה", ותופעת-לוואי ברמת המודול
הייתה נורית בכל פעם שה-bundler טוען את הקובץ — כולל בבדיקה שייבאה ממנו פונקציה טהורה. לכן
`ensureSchedulerStarted()` בשורה הראשונה של ה-middleware: השוואה בודדת אחרי הפעם הראשונה, בתהליך
שבוודאות מגיש בקשות. כבוי בלי `DATABASE_URL`, ו-`SCHEDULER_ENABLED=0` הוא פתח המילוט למכונה
שחולקת את המסד ואסור לה לפעול עליו.

*idempotency — לא נסמך, נבדק.* ה-lease **מקטין** ריצה כפולה ולא מבטל אותה (תהליך שנעצר מעבר
ל-lease שלו מספיק), אז כל job חייב להיות בטוח תחתיה: הטיהור הוא `DELETE … WHERE`, הסריקה היא
פונקציה של מצב ולא של מעבר, והפיד הוא **כתיבה מוחלטת ולא דלתא** — חמישה סנכרונים של פיד שאומר 3
משאירים 3, ומשיכה שהייתה מוסיפה דלתא הייתה נראית תקינה בריצה אחת ומוכרת ביתר ברגע שה-lease נחצה
פעם אחת. `reporting-invariants` §8 שם מעבר מלא על שלושתם בין שתי קריאות של סכומי הכסף והמלאי של
הפלטפורמה, ומוסיף שהסריקה מותר לה לשנות **סטטוס** של קמפיין ולא שום דבר אחר בו.

*מה שנמצא בדרך, ושווה לדעת:*
· **`last_ok boolean` הפיל את שומר §7.12.** התחום הכן הוא שלוש-ערכי — מעולם לא הסתיים, הסתיים
  טוב, הסתיים רע — ו-boolean שמותר בו NULL הוא בדיוק הצורה שהסכימה אוסרת (`WHERE flag = false`
  משמיט את שורות ה-NULL). `NOT NULL DEFAULT false` היה גרוע יותר: כל job שמעולם לא רץ היה מדווח
  ככישלון. עכשיו `last_status text CHECK (…)`, ו-NULL הוא "עוד לא הסתיים".
· **הצמדת ה-IP הוציאה את `feed-fetch.ts` מ-`fetch`.** ההצמדה היא hook של `lookup` על ה-socket,
  ול-`fetch` של Node אין דרך נתמכת להעביר אחד (ה-dispatcher דורש את חבילת `undici`, שאינה תלות
  כאן). `http.request`/`https.request` מקבלים `lookup` באופן טבעי — וזה קנה עוד שלושה דברים
  שהיו נחוצים ממילא: תקרת הגודל עוצרת **תוך כדי הזרם** במקום לצבור ואז למדוד, הפניה נדחית בלי
  חריגה, ולא נשלח `Accept-Encoding` — כלומר פצצת gzip לא יכולה להתנפח אחרי שהתקבלה. TLS לא נפגע:
  רק פתרון הכתובת נדרס, אז השם עדיין מה ש-SNI נושא ומה שהתעודה נבדקת מולו.
· **הוכחת ההצמדה חייבת להיות כזו שאי אפשר לזייף.** הבקשה בבדיקה מכוונת לשם תחת `.invalid` (TLD
  שמור ב-RFC 2606) שאף resolver בעולם לא יענה עליו. אם היא מתחברת — הכתובת לא הגיעה מ-DNS. החצי
  השלילי נבדק גם הוא (אותה בקשה בלי ההצמדה נכשלת ב-ENOTFOUND), אחרת בדיקה שמראה רק את המקרה הטוב
  הייתה עוברת גם אם השם היה נפתר במקרה.
· **הסריקה הייתה נכונה וגם בלתי-נגישה.** `getCampaignsForStore` הוא כבר הסוחף, אבל הוא רץ
  **בקריאה** — כשהמוכר פותח את עמוד הפרסום. ברגע שיש חיוב אמיתי זה שני כיווני כסף: קמפיין שאזל
  ממלאי ממשיך לקנות קליקים עד שמישהו במקרה טוען את העמוד, וקמפיין שהתחדש נשאר מושהה — הפלטפורמה
  מבטיחה בכרטיס שהוא חוזר לבד, ולא חוזר. ה-job קורא **לאותה פונקציה** ולא גוזר מחדש את הכללים,
  כי עותק שני של "מתי מותר לקמפיין לחזור" הוא עותק שסוחף, על מספרים שהבעלים קורא ככסף.
· **חנות חסומה/סגורה מסוננת ב-JS ולא ב-`WHERE`.** השאילתה מחזירה כל חנות עם כתובת פיד; מי "רשאית
  למכור" נשאר `canStoreSell` בלבד, אחרת לטבלת מחזור-החיים היה נולד עותק שני ב-SQL.
· **`GET /api/store-product/feed-sync` לא נקרא מ-job.** ה-job שהיה שולח בקשת HTTP לשרת של עצמו
  היה צריך עוגיית session שאין לו, והיה עותק שני של רצף המיפוי והייבוא ברגע שאחד הצדדים נערך.
  הרצף ירד ל-`lib/store-feed-sync.ts`; המסלול שמר רק את מה שרק מסלול יכול לעשות.

*שלושה דברים שסקירת הדיף תפסה, וכולם אותה טעות: לקחת התנהגות שנכונה לאדם ולהריץ אותה בטיימר.*
1. **ניחוש העמודות היה הופך את הפיד המרוחק לבעל דעה על המחירים.** `guessMapping` נופל לסינונימים
   לכל כותרת שהמיפוי השמור אינו מזכיר — והמיפוי השמור רושם רק את מה שמופה, כלומר עמודה שהמוכר
   **התעלם** ממנה נראית בדיוק כמו עמודה שעוד לא הייתה. אצל אדם זה נוחות, כי הוא רואה תצוגה מקדימה
   לפני הכתיבה. בטיימר זה אומר שספק שמוסיף עמודת `Price` לייצוא שלו מתחיל לשכתב את מחירי החנות, כל
   שעה, בלי איש בלולאה. נוסף `confirmedMapping` — המיפוי המאושר ותו לא — וההרצה המתוזמנת משתמשת בו.
2. **פיד בלי עמודת זיהוי היה משכפל את הקטלוג כל שעה.** בלי `sku`/`id` כל שורה נראית מוצר חדש
   (`resolveSkuMatches` לא מוצא מול מה להתאים), כלומר הייבוא **יוצר** את כל הפיד. אדם רואה תצוגה
   מקדימה מלאה ב"יצירה" ונעצר; טיימר לא. הרצה מתוזמנת נעצרת עכשיו עם `no-matcher-column`.
3. **ה-job היחיד שזמן הריצה שלו גדל עם הפלטפורמה.** בקשה יוצאת אחת לכל חנות, בטור: באלף חנויות עם
   פיד הריצה הייתה עוברת את ה-lease של 30 דקות ו-instance שני היה מתחיל מעבר כפול. שום דבר לא
   נשבר (הכל idempotent) — פשוט מבזבזים את השעה פעמיים. יש עכשיו תקרה של 200 לריצה, בסדר
   `lastSyncAt` עולה עם NULL ראשון, כלומר התקרה היא **רוטציה ולא גזירה**: מי שלא נכנס הוא הראשון
   בריצה הבאה, ומי שמעולם לא סונכרן קודם לכולם. ה-job **אומר בקול** כשהתקרה חתכה — קיצוץ שקט נקרא
   כמו "כולם סונכרנו".

**4ב — `pgvector` + embeddings לחיפוש הסמנטי. ⏸ נדחה במפורש (החלטת הבעלים, 2026-08-03).**
לא בגלל תלות טכנית — ה-DB תומך — אלא בגלל **החלטה אחת שרק הבעלים יכול לסגור: מי מחשב את
ה-embeddings.** ספק חיצוני (טקסט המוצרים ושאילתות הקונים עוזבים את השרת) מול מודל self-hosted
(לא עוזב, אבל צריך תחזוקה וזיכרון). זו שאלת פרטיות מול תפעול ואין לה תשובה נכונה בלי החלטה.
**הטריגר לפתיחה מחדש:** הבעלים בוחר ספק. עד אז אין מה להתחיל — בחירת הספק קובעת את ממד הווקטור,
ולכן גם את הסכימה של העמודה ואת האינדקס.
*מה שכבר לא צריך להיבנות שם:* **החצי הלקסיקלי של ה-hybrid קיים חלקית מ-§3** —
`store_products.search_text` + אינדקס trgm (מיגרציה 0006). זה לא BM25, ולכן נשארת שאלה אמיתית
אם ה-hybrid נשען עליו או על `tsvector` (שאין לו stemmer עברי בקונפיגורציה סטנדרטית). לא להחליט
מראש — למדוד כשבונים. שאר ההחלטות ב-`CONTEXTUAL_SEARCH_STRATEGY.md` §6 אינן חוסמות: משקלול
ה-hybrid הוא כיול אמפירי תוך כדי בנייה, ועל חוצה-פלטפורמה מול per-store המסמך כבר עונה.

**לעבוד בענף.** באמצע שלב 2 האפליקציה שבורה; `git checkout main` מחזיר גרסה עובדת
תוך שנייה. ראה גם `GO_LIVE_CHECKLIST.md` §7 (deploy בלי נפילות) — כלל התאימות-לאחור
של הסכימה חל מהרגע שיש פרודקשן.

---

## 9. איך יודעים שזה יצא חלק

1. **כל הטסטים עוברים בלי שינוי** (655 נכון ל-2026-07-29, וממשיך לגדול — מדדו בפועל לפני
   שמתחילים, אל תסמכו על המספר הכתוב כאן). אם טסט נשבר, המודול לא הוחלף נכון (או שנמצא באג אמיתי).
2. ✅ **`EXPLAIN ANALYZE` מראה `Index Scan`** על ארבע השליפות החמות של §6 — **בוצע 2026-08-03,
   הפלט והמלכודת ב-§8.** אזהרה למי שיריץ שוב: בגדלים של היום המתכנן בוחר `Seq Scan` על `stores`
   **בצדק**, ובדיקה נאיבית תדווח כישלון בלי שיש בעיה. להריץ פעמיים — פעם כרגיל ופעם עם
   `SET enable_seqscan = off`, ולדווח את הפלט עצמו ולא סיכום שלו.
3. **השוואת ספירות** אחרי הייבוא: מספר הרשומות בכל טבלה = מספר הרשומות בקובץ המקור.
   סקריפט של 20 שורות, תופס ייבוא חלקי מיד.
4. **סכום כסף זהה.** `SELECT SUM(total_agorot) FROM orders` מול הסכום מה-JSON —
   הבדיקה היחידה שמוכיחה שהמרת האגורות לא איבדה כלום.
5. ✅ **בדיקת מלאי בו-זמנית.** לירות 50 רכישות מקבילות של מוצר עם מלאי 10 ולוודא
   שבדיוק 10 הצליחו. זה הטסט שמוכיח שסעיף 7.5 עובד. **בוצע 2026-08-03 מול Neon —
   `tests/stock-concurrency-live.test.ts`**, ומצאה באג במספר שסירוב מחזיר (§8). לא מספיק לספור
   10 הצלחות: לוודא גם ש-10 ערכי `after` **שונים** — עשר הצלחות שכולן מדווחות אותו מספר
   פירושן שאותה יחידה נמכרה עשר פעמים, וספירה לבדה לא רואה את זה.
6. **ספירת שדות מקוננים** (§7.14) — כמה מוצרים מחזיקים וריאנטים, וכמה חנויות מחזיקות שעות
   פתיחה, לפני הייבוא מול אחריו. בדיקות 3 ו-4 עוברות גם כשהשדות האלה נכתבו ריקים.
7. **סכום המונים** (§7.15) — סכום כל הצפיות/האנליטיקס בקבצים מול הסכום ב-DB. זה העוגן
   היחיד לדליים, שאין להם לא כסף ולא שורות להשוות מולם.
8. **ספירת מוצרים גלויים** (§7.12) — כמה מוצרים עוברים את `isProductVisible` היום מול כמה
   מחזירה השליפה החדשה. המספר הזה תופס את באג ה-`NULL` באותה דקה, לא כשמוכר יתלונן.
