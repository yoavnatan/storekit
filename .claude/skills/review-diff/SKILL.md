---
name: review-diff
description: Review the working diff against this repo's own failure history — the bug classes that have actually shipped here. Use before reporting done on any change touching money, auth, inventory, cart, checkout, or admin/seller data. The Stop hook triggers this automatically; it can also be invoked directly.
---

# Review the working diff

`/code-review` and `/security-review` cannot be launched by the model — they are user-triggered only.
This is the model-side substitute, and it is deliberately **not** generic review advice: it is the list
of things that have actually broken in *this* codebase. A generic reviewer re-derives nothing; this
list is the accumulated cost of past sessions.

## How to run it

1. Get the diff. `git diff` plus `git status --short` for untracked files — much of this repo's work is
   uncommitted, so a diff against HEAD alone misses whole files.
2. Read every changed hunk. Not the summary, not the filenames — the hunks.
3. Walk the checklist below against what the diff actually touches. Skip sections the diff cannot
   reach, and say which you skipped and why.
4. For each finding: state the concrete failure (inputs → wrong result), fix it, and add or extend a
   test. Per AI_INSTRUCTIONS.md, a gap is never "out of scope" — fix on sight, then check whether the
   whole class has the same hole elsewhere.
5. Verify: `astro check`, `npm run lint`, `npm test`.
6. Record the review so the Stop hook lets the turn end:
   `bash .claude/hooks/record-review.sh`
7. Report what you checked, what you found, and what you skipped. If you found nothing, say that
   plainly — do not invent findings to look thorough.

## Checklist

### Money and reporting
- **A status value that excludes an order from revenue must be honoured by every reader.** Cancelled
  orders counted as revenue for seven sessions because the rule lived in several modules. Adding or
  changing a status means grepping every reader of that field. `lib/reconcile.ts` computes the same
  figures a second way — if it disagrees, trust it over the code you just wrote.
- **Prices are recomputed server-side, never taken from the client.** The cart is client state and
  every API route is directly callable.
- **Anything that moves money or decrements stock needs a Vitest test in the same change.** Not
  follow-up work — the same change.
- **Rounding goes through `lib/money.ts`.** Float equality on money is a defect, not a style choice.

### Untrusted input
- **Every request-supplied destination goes through `lib/safe-redirect.ts`** — `?next=`, a cookie
  carrying one, a `Referer`. This was copy-pasted into four routes and missing from a fifth, which is
  how `/api/lang` became an open redirect.
- **Every email intake goes through `lib/email-address.ts`.** Length cap before pattern.
- **A regex with two `+`/`*` runs that can match the same character, applied to request data, is a
  denial-of-service vector.** Measure it — double the input, watch the time quadruple. Do not
  eyeball it. See `lib/url-base.ts` for why an anchored `X+$` is not safe on a request path.
- **Bounds and types are checked on the server even when the UI already checks them.** A disabled
  button is not a rule.

### Escaping and injection
- **An escaper used inside `attr="…"` must escape `"`.** Skipping it is the exact hole fixed at
  `escapeHtml`/`escH`.
- **`set:html={JSON.stringify(data)}` inside a `<script>` is an XSS sink** — `</script>` is not
  escaped by `JSON.stringify`. Use `lib/json-script.ts`.
- **Every image URL from a request goes through `lib/image-url.ts`** (`sanitizeImageUrl` /
  `sanitizeImageUrls`, or `parseImages` for a product form). It validates by SHAPE — https or
  site-relative — and stores the URL parser's own serialization, so `"`, `<`, `>` and space come
  back percent-encoded and an attribute breakout is impossible even if a call site forgets to
  escape. `tests/image-url.test.ts` greps `src/` and fails if a new route assigns an image field
  straight out of a request. A URL is still not a promise about the bytes behind it, so treat the
  fetched content as untrusted.

### Authorization and scoping
- **A record replayed or returned to a caller must be bound to that caller.** A hard-to-guess
  identifier is not authorization — see `checkoutOwner` in `lib/checkout-idempotency.ts`.
- **Multi-store orders must never leak one seller's data to another.** `scopeOrder` exists because
  the whole `sellerNotes` map used to go to the client.
- **A seller may not act on a store they do not own.** Check ownership from the session, not from a
  slug in the request.

### Architecture
- **A rule that appears in two modules is the next bug.** Extract it and add a grep guard test that
  fails if anyone hand-rolls it again — `tests/safe-redirect.test.ts` and
  `tests/email-address.test.ts` are the pattern.
- **Astro built-ins and existing `src/lib` modules before a new parallel mechanism.** Two systems for
  one job fight each other.
- **JSON files are dev-only and must stay swap-ready for the DB.** No shared write state. Ask whether
  it breaks at 1000 sellers.

### Correctness traps specific to this codebase
- **`.astro` attribute expressions**: `attr="text" + (expr)` is not valid — it silently becomes junk
  attributes and the expression vanishes. Use `attr={`…${expr}`}`. This shipped once.
- **`toggle('hidden')` is a no-op on a flex element here** — use `'!hidden'`.
- **A save must never revert a field the seller did not touch** (`lib/record-rev.ts`, per-field merge).
- **New dashboard forms needing a real browser POST must carry `data-native-submit`**, or
  `FormFallbackGuard` blocks them.

## Area audit — one row per session (user's decision 2026-08-06, in force from the session after it)

Everything above reviews a **diff**. That is a net for code being written now, and it has never once
looked at code nobody has touched since it shipped — which is where the 2026-08-06 feed bugs lived:
the feed worked, its own tests passed, and four attributes had been wrong on **every variant row**
from the day it was written. No diff ever contained them, so no review could have.

So each session also does the row for the area it worked in. **Side-work, not the main task** — if a
row turns out to be a project, say so in the summary and leave it unmarked rather than half-doing it.

**What "audited" requires**, all three:
1. Read the area's modules **end to end** — not the diff.
2. Check them against what the **outside world** demands: a published spec, a permission rule, a
   browser constraint, a payment-provider contract. The failure shape here is always the same — each
   side internally consistent, only the JOIN wrong — so testing what we *meant* cannot find it.
3. Leave a **guard test that scans the whole tree**, so the class cannot return. The pattern:
   `money-guards`, `image-optimization`, `safe-redirect`, `secret-compare`, `feed-spec-conformance`.

Then mark the row here, in the same session, with the date and the test that now holds it.

| # | Area | Audited |
|---|---|---|
| 1 | Feed + advertising — the Merchant/Catalog contract | ✅ 2026-08-06 · `feed-spec-conformance.test.ts` (Google AND Meta; they are not one list). Same day, the campaign side of it: a boost read "פעיל" while advertising nothing — now a third self-healing pause reason, held by `resumeBlockCode`'s guard test across both routes and both locales |
| 2 | **Money: orders, commissions, balances** | ✅ 2026-08-07 · `money-owed-guards.test.ts` (tree scan) + `checkout-e2e.test.ts` (16 cases against a real DB). Read end to end against ONE outside contract — **money leaves a real person's card, so something must always say where it stands.** The arithmetic was already sound and `reconcile.ts` already proved it two ways; what nothing could see was money that MOVED with no row behind it, because both of its routes read the same order tables. The journal is the independent record, so the audit's output is a second reconciliation family comparing the two. Three states, each reachable and each silent: (a) a seller cancels a PAID order — stock back, order out of every revenue sum, buyer told "cancelled", and the captured money simply stays with us, its only trace a status row whose meaning a reader had to infer; `refund-owed.ts` writes `refund_due` the moment the debt exists, and `PAYMENT_STATUS_RULES.moneyWasTaken` is the column that makes "was a person's money taken" a different question from "does this earn anyone anything today"; (b) the capture never completes — rows sit `pending` forever with stock off the shelf, no notification, and possibly a real charge; (c) an authorization with no order and no void, the 0017 shape, now a live check rather than a story. Also found: capture succeeded and the seller notification failed with **no trace anywhere** — both notification catches log now, under `notify:*` rather than `/api/checkout`, so a badge failure cannot page a person for a checkout that worked. `refund_settled` exists and nothing writes it **on purpose**: it needs the provider's refund call, so every obligation stays open and visible instead of quietly closing itself — ⚠️ GO_LIVE §3 carries the row, and until a provider is wired each line on that card is money to return by hand. History, unchanged: `orders.payment_ref` was UNIQUE while one charge becomes one order row PER STORE, so every multi-store checkout 500'd (0017), and the flow charged before writing orders — now authorize → `pending` rows → capture (`lib/payment.ts`). No test caught it because nothing ran `/api/checkout` against a real database. |
| 3 | **Inventory + checkout** | ✅ 2026-08-12 · `variant-selection-guard.test.ts` (two tree scans) + the route cases in `checkout.test.ts`. Read against one outside rule — **every field of a cart line that decides money or inventory is re-derived from the server's own record, because the cart is the buyer's** — and the checkout obeyed it for all of them but one. The price comes from the product row, the quantity is floored, the slug is resolved through renames, visibility and store status are gates; `selectedVariants` went from the request body into `decrementStock` untouched. **That is an oversell, and it is one because "no bucket matched this key" is a LEGITIMATE state down in `adjustStock`** — it is how a combo the seller never counted separately draws on the shared pool (`ComboStock` explains why that partiality is a fact about the product, not a gap). An invented selection is indistinguishable from that, so it took the same path; and on a product whose combos are ALL counted the shared pool is the SUM of the buckets (`syncPooledStock`), so a hand-posted checkout naming a colour that does not exist — or naming nothing at all — bought against the total. The sale was accepted, no real bucket moved, the seller got an order for stock they did not have, and the next per-combo sale re-derived `p.stock` from the buckets and erased the trace. Fixed at the one place that can know: `resolveSelection` resolves a buyer's selection against the product's own dimensions or refuses, and returns the DECLARED strings so whitespace cannot mint a second `comboKey` for one combo. **Swept as a class, not an instance:** the read side did it too — `/api/cart/prices` answered `getEffectiveStock` for an unrecognised combo, quoting the shared pool as that line's ceiling, and now marks the line `gone` (the word it already had for "checkout will refuse this"); `ProductQuickView` defaulted an option-less rubric to `''`, a phantom dimension that would have refused the whole purchase; and `realDimensions` is now the single definition of "what counts as a dimension" that `generateCombos`, `comboCount` and the resolver had each spelled out separately — defensively, because `variants` is a JSONB column and a malformed row read through the checkout is a 500 mid-purchase, not a broken dashboard row. The rollback path itself came out clean: `abort()` restocks every earlier decrement, releases the coupon claims and the idempotency claim on every exit, and the refusal re-reads the count (`stockAfterRefusal`). **Not audited here:** the cart modules themselves (`cart.ts`, `user-carts.ts`, `cart-sync.ts`) and `checkout-idempotency.ts` — this pass followed the line from a cart item to a stock bucket, not the storage the item sat in |
| 4 | **Authorization — who may READ and who may WRITE what** | ✅ 2026-08-16 — a RE-READ of the 2026-08-06 pass, taken because 37 files had moved under it (the drift report's worst row) · `credential-url-tracking.test.ts` (tree scan over `src/pages`). The 36 changed API routes and both session modules re-read against the same rule and all correct: every seller route still resolves the store through `ownedStore`/`getStoresBySellerId` rather than trusting an id in the request, `markBuyerInvoiceProvided` scopes its UPDATE with `AND seller_id`, and the two routes with no auth at all (`store-search-index`, `report`) are public by decision with CSRF + a rate limiter. **The finding was in code the audit had never seen:** the forgot-password flow was built 2026-08-14, eight days after the ✅ that appeared to cover it — which is the expiry rule in the table above doing exactly what it was added for. The protocol itself is sound (token hashed at rest, spent in one UPDATE so two clicks cannot both win, enumeration-safe, and the link built from `store.url` rather than the `Host` header, which is the takeover this flow is usually lost to). What nobody had connected is that the token has to travel as `?token=` — a mail can only carry a link — and `BaseLayout` puts GTM and the Meta Pixel on **every** page: not a `Referer` leak, which browsers already trim to the origin, but GA4's `page_location` and the Pixel's `dl`, which are the href itself. Every reset token would have been copied into Google's and Meta's logs inside its own 60-minute life, readable by anyone with access to either account and invisible from here. Latent only because both tag ids are still empty — wiring them is GO_LIVE §2.5, a launch blocker — i.e. correct today and an account-takeover path the day an unrelated switch is thrown. Fixed with a `noTracking` flag that kills both tags AND the dataLayer push, and guarded by SHAPE rather than by filename: a page that reads a credential-shaped param out of its own URL must pass it, so the next mailed link fails on the day it is written. **Not re-read here:** the seller/admin/buyer PAGES (only the API routes and the two session modules moved enough to matter), and the original ✅ 2026-08-06 pass stands for them · `store-ownership.test.ts` (two tree scans) + `safe-redirect.test.ts` (`_next`) + `oauth-verified-email.test.ts`. All 46 `/api` routes, both session modules, middleware/CSRF and every seller/admin/buyer page read end to end against one rule: **a session proves which STORES an account owns, never which id it may name.** Three holes, all fixed. (a) `seller/dashboard.astro`'s no-JS fallback POSTs authorized NOTHING — any signed-in seller could rewrite another store's price and stock, delete its products, or file products into it. The `/api/*` twins had checked all along, which is precisely why no diff review could ever have found it, and why this row exists. (b) Google OAuth fetched `verified_email` and never read it, while the branch below links a matching address into an existing account — takeover. (c) The hidden `_next` field bypassed `safe-redirect` on login + register while `?next=` did not. `?? stores[0]` on both save-settings paths also let an unrecognised id overwrite a different shop of the seller's own |
| 5 | Domains + the origin boundary | ✅ 2026-08-16 · read end to end against one outside rule — **a hostname is not yours because you typed it; it is yours when DNS says so.** Both consequences of owning one were being settled at REGISTRATION, i.e. on a string any logged-in seller can type into a form. **The one that reached another store:** `claimCustomDomainHostname` (`DELETE FROM store_previous_domains WHERE hostname = $1`) ran in `/api/store` on `set-custom-domain`. That row belongs to a DIFFERENT store — the one whose old links 301 from that host — and `stores.custom_domain_hostname UNIQUE` does not protect it, because a store that has MOVED off a hostname no longer holds it in `stores`, which is exactly the set migration 0015 exists for. So: type a competitor's old hostname, and every link, bookmark and indexed page they earned there stops being a 301 and becomes a 404, permanently, with no error on either side and nothing that restores the row — the destruction of precisely what that feature was built to preserve. Moved to the pending→active transition (`custom-domain-verify.ts`), which is the provider confirming DNS resolves to us and the certificate issued. Nothing is lost by waiting: `getStoreByCustomDomain` matches `'active'` only and the middleware asks it FIRST, so a promoted domain shadows the old row from its first request either way. **The second finding is the two known reasons COMBINING**, which is where a swept class usually hides: `unclaimedHostRedirect` handled a previous domain, and handled the www/apex twin of a LIVE one, and 404'd the twin of a PREVIOUS one — the seller owns both spellings (that is the premise of the twin branch) but only one is ever stored, so moving away killed the half of their brand that older links and printed material use most. It now answers with the store's canonical DIRECTLY rather than chaining through the other spelling, since engines drop chains and there is nothing at the intermediate hop. Extracted to `src/lib/unclaimed-host.ts` to be testable against real rows — every rule in it is a claim about ROWS, and the bug was a query nobody thought to run, which no amount of reading the branch would have shown. **Third finding, and it needed the schema: a pending claim SQUATTED the hostname.** `custom_domain_hostname` was globally `citext UNIQUE` (0001), which made an assertion exclusive — type a hostname you do not own, it stores as pending, it can never verify because you do not control the DNS, and its real owner is answered `domain-taken` from then on with no way to see why or by whom. Free, permanent, available to every seller. Narrowing `isCustomDomainTaken` alone is NOT the fix and was tried first: with the constraint still global it converts a readable 409 into a duplicate-key 500. Migration **0029** drops the constraint and rebuilds it `WHERE custom_domain_status = 'active'` — a pending claim excludes nobody, two stores may both claim, and whichever VERIFIES becomes real. It also adds back the plain lookup index the dropped constraint had been providing, which the partial one cannot serve (every request on a custom host resolves Host → store through that column, pending rows included). The promotion then has to choose between two claimants, so it re-asks before writing `'active'` — without that it would hit the partial index and throw, and `reverifyCustomDomain` is contractually a function that never throws, since the hourly job runs it over every store in a loop. Guards: `tests/unclaimed-host-db.test.ts` (6), `tests/custom-domain-promotion-db.test.ts` (3), plus the previous-domain case in `stores-db.test.ts` — all three proved to FAIL against the pre-fix code rather than assumed to. **Read and found correct:** per-host `robots.txt` (falls through to platform rules on an unrecognised host, and its DB lookup cannot fail the response), the sitemap index + shard host split, `previousDomainRedirectUrl` for both store shapes, `normalizeHostname`'s platform-host refusal, `hostnameAlias`'s single-label guard, `isPlatformHost`'s IPv6 handling. **Still unaudited:** `Seo.astro`'s `storeOwned` / `BaseLayout`'s `storeOwnsThisPage` (2026-08-13, row 20's identity claim) were read but not driven — the gate is `!!storeSlug && !sellerMode`, i.e. host-INDEPENDENT by decision, and whether that decision holds on every one of the thirteen pages that pass `storeMode` was not checked page by page |
| 6 | SEO surfaces: sitemap, canonical, robots, structured data | ☐ per-file tests exist; never audited AS one surface |
| 7 | **Dashboard: forms, parallel tabs, draft recovery** | ✅ 2026-08-09 · `field-repaint-guard.test.ts` (tree scan). The owner reported one symptom — a recovered draft put an image URL back and the picture did not return — and the sweep found it was a CLASS, not a card. `announceValueChange(field)` is a widget saying "I wrote this without firing an event", which is only ever true of a widget keeping its state IN a field and its picture in the DOM; exactly those widgets break when the traffic runs the other way and the form replaces the field (`dash:fieldsrewritten`). Four were missing the listener — the header-logo card, the sale-scope picker, the product multi-picker and the product gallery — all correct in `store-image.ts`, where the rule was learned, and missing in every place it was learned again. **The multi-picker's was the one that could lose data rather than merely look wrong:** it reads the field into a `Set` once at init, so the seller's next tick wrote that stale set back over the value the restore had just put there, silently undoing the recovery. Also fixed from the same report: "restore" scrolled to the FORM, and the settings form is the whole tab, so `block:'center'` landed near its midpoint — a confident movement to a place with nothing to see, which teaches a seller to distrust the restore. `apply()` now reports the first field it actually CHANGED (compared before the write — a draft carries every field, so most of that loop assigns what is already there) and the scroll goes to that field's own card; a hidden input resolves to its nearest labelled container, and with none the page does not move at all. Not audited here: parallel-tab conflict merging (`record-rev.ts`), which this session only extended by two fields |
| 8 | Graceful degradation — no secondary service in a critical path | ☐ |
| 12 | **The SCHEMA a migration actually produced** | ✅ 2026-08-10 · the column comparison in `scripts/db-migrate.mjs`, which runs on every `verify`. Not a diff and not a module — the outside contract here is the DATABASE, and the question nothing was asking is whether it matches what `migrations/` describes. The checksum guard watches the FILE and was right about that: `seller_payouts` was missing `commission_agorot` while 0023, the migration declaring it, sat in the ledger as applied with a checksum matching byte for byte. Nothing had been edited. `CREATE TABLE IF NOT EXISTS` skipped the whole statement because the table already existed from an earlier attempt, and the run was recorded as successful because every statement really had run. **`createPayout` INSERTs into that column, so the first real payout would have thrown on the day money was due to leave the company's account** — and the tests could not have caught it, because they build their own database from the same files where the column has always existed (memory `project_migration_not_applied_class`). The guard compares declared column NAMES against `information_schema`; deliberately not types, constraints or indexes, because a missing column is the failure this class produces and a guard trying to be a schema differ is one people learn to skip. It found the drift on its first run and named its own repair, which migration 0024 is. **Not audited: the rest of the schema against its READERS** — a column present with the wrong type, an index the query plan needs and does not have |
| 11 | **What a failure LOOKS like — every surface that can tell a user something went wrong** | ✅ 2026-08-10 · `silent-failure-guard.test.ts` (tree scan over `src/scripts/**` + every `.astro` client script). Read against one outside rule rather than against our own intent: **a person who pressed a button is owed an answer, and a request that never arrived has not earned the right to make a claim about the store.** All 27 `fetch` call sites walked. Sixteen were right; eleven were the same habit — a `catch` written while getting the happy path working and never revisited — and from the user's side every one of them is the same event: the button re-enables, the screen looks idle and correct, and they carry on believing it worked. Two classes. **Dead actions:** four message sends, an order status save (both entry points), a CANCELLATION (which also restocks and drops the order out of every revenue figure), a note save and a note delete, filter/sort/page on orders+products+messages, and the ad campaign list, which also threw an unhandled rejection. **Failures dressed as facts about the catalogue:** the store grid's "טען עוד" hid its own button (announcing "that is the whole catalogue"), a category filter blanked the grid, header search said "no results", the coupons list rendered its empty state, the performance breakdown said "no sales in this period". **The one that mattered most was structural:** `ConfirmModal` is the funnel EVERY destructive action goes through and ran `try { await action() } finally { close() }` with no `catch` — a delete whose fetch was dropped closed the dialog exactly as a success does, with the only trace an unhandled rejection. One catch covers twenty-one actions. Bulk product delete counted nothing and claimed everything ("the products were deleted" for twenty when three were refused); it now tallies and leaves the failures selected, so pressing delete again retries exactly them. Deleting a notification removed the card first and swallowed the result, so it reappeared on the next load looking like the site undoing the user's action. **The guard's shape is the durable part:** it does not ban silence — a background poll, an enrichment, a return whose caller renders the error — it bans silence that was never DECIDED, so each of the thirteen legitimate ones now carries a `silent:` marker and a reason. Also pinned: the ConfirmModal catch, and one wording (`showActionFailedToast`) for an event that had six hand-written Hebrew sentences. Not audited here: the server side — what an API route does when its own dependency fails |
| 9 | **Behaviour under load** | ✅ 2026-08-09 · read end to end — `db.ts` (pool, three timeouts, wake retry, saturation), `shutdown.ts`, `single-flight.ts`, `health.ts`, `jobs/scheduler.ts` + `job-runs.ts`, `process-errors.ts`, `error-log.ts`, `page-view-tap.ts`, `middleware.ts` — against what the outside actually promises rather than what we assumed: Neon's plan page (Free PITR is **6 hours**, and Neon has moved to **Postgres 18.4**), R2's pricing, and undici's real behaviour. **The operational layer held up**; what did not was two things nobody had looked at because no diff contained them. (a) `analytics_visitors` and `store_page_view_visitors` grew with traffic and nothing deleted them — fixed, `purge-visitor-detail` + a tree scan in `visitor-retention-db.test.ts` so a second `DELETE` cannot skip the `AUX_EVENTS` carve-out. (b) **Closed later the same day, and it was the whole reason this row sat at partial:** `/api/feed/products.xml` and `/sitemap-content.xml` built the whole catalogue in memory per request on one event loop — `products.xml.ts`'s own note said this becomes a cached artifact once the DB migration lands, the migration landed 2026-08-03, and it did not. `single-flight` + a 1h `Cache-Control` restrained it, which is why it read fine at 45 stores and would not have at a thousand. **The shape of the fix is the part worth remembering: moving a build off the request path is only half of it.** The jobs run in the SAME process, so a job that built the document in one allocation would block the same event loop, just on a schedule — it is the chunking that fixes that, and the chunking is what bounds the memory too. So: 20 stores per window, a flush every ~256KB, and every flush an `await` that hands the loop back (`lib/artifacts.ts`, migration 0022). Two things make it hold: the guard this row asked for — `public-route-unbounded-build.test.ts` scans `src/pages/` for the SHAPE (enumerate every store + read a catalogue keyed by that list), not for the two files that were fixed — and a byte-for-byte equality test against the serialiser it replaced, composed from the same per-store functions the build uses so it cannot quietly stop asserting. Measured on the built server after getting the read side wrong once: a part per query served a 2.5MB feed in 3.3s, a window of eight took it to 2.1–2.5s, and the remainder is the document crossing the network from Neon rather than latency. GO_LIVE §7. **And the second bug the first one uncovered, fixed the same day:** one sitemap FILE may hold no more than 50,000 URLs (checked against sitemaps.org, not recalled) and is rejected WHOLE above that — so at the very scale this work was for, the symptom would have been a platform with no content sitemap and no error anywhere. `/sitemap-content.xml` is a `<sitemapindex>` now, over shards cut at 45,000; the index is written even for one shard, because a document shape that changes itself the first time a threshold is crossed arrives unannounced at the worst possible moment. A shard a rebuild did not write is deleted, not left serving a slice of a catalogue that no longer exists, and an out-of-range shard is 404 rather than a retry-forever 503. Also never measured: the Node process itself under HTTP load (memory, event loop, TTFB) — §9's 500-concurrent test was queries against Neon, not requests to a server, and it cannot be run before a host exists |
| 13 | **Payouts and the platform statement — money LEAVING the company** | ☐ built 2026-08-10→12, i.e. entirely AFTER row 2's audit, which read orders, commissions and balances and stopped where the money was still ours. `payouts.ts`, `payout-run.ts`, `payout-hold.ts`, `payout-schedule.ts`, `payout-details.ts`, `order-payout-line.ts`, `platform-ledger.ts`, `platform-statement.ts`, `platform-statement-csv.ts`, `admin-statement-load.ts`, `seller_payouts` + `seller_ledger_adjustments`. **Outside contract to read it against:** an agent holding a third party's money (AI_INSTRUCTIONS *Payment architecture*, `docs/legal-brief-agent-model.md`) — the seller's balance is their money in our hands, so every figure on their statement must be reproducible from the orders behind it, and a run that is retried after a crash must not pay twice. The area is demonstrably live: the hold predicate was three-valued on 2026-08-12 and its negation silently lost held money. **What no existing row covers:** row 2 stops at "what we owe"; row 12 checked that `seller_payouts` has its columns, not that the numbers written into them are right |
| 14 | **The seller dashboard's LOADING model — one panel per request** | ☐ built 2026-08-11, two days after row 7 audited the dashboard as a single document, which is exactly what it stopped being. `panel-swap.ts`, `panel-freshness.ts`, the dashboard's fetch-then-wire split, the `?tab=` restore, the per-panel unsaved check, the draft rescan that now runs per arrival. **Outside contract:** the browser — a document that arrives in pieces has no single load event, so everything that ran once at load is now the question "which piece, and when": listeners, focus, the draft recovery, the alert beacon, the arrow-key strip, `announceValueChange`. Row 7's guard tests WIDGETS (`field-repaint-guard.test.ts`), not arrival, and an href built before its panel exists is the class memory `project_href_frozen_at_render` already names. Also unmeasured: whether the 865KB/page this was built to fix actually fell (memory `project_dashboard_html_weight`) |
| 15 | **Messaging — the only channel between two strangers** | ☐ never had a row. `messages.ts`, `seller-messages-query.ts`, `admin-messages.ts`, `message-flood.ts`, `spam-filter.ts`, `notification-copy.ts`, `notification-link.ts`, both thread renderers. **Outside contract:** there is no SMTP yet (memory `project_messaging_email`), so this IS the channel — a message that fails to send is a buyer who believes they were ignored and a seller who never knew. Two rules to read it against: one party's data may never reach the other (the `scopeOrder` rule, row 4, applied to threads), and a flood control that silences a real customer is worse than the spam it stops. Row 11 fixed four message sends that failed silently, which is evidence the area is thin, not that it was audited |
| 16 | **Background jobs — what runs with nobody watching** | ☐ `jobs/scheduler.ts`, `jobs/job-runs.ts`, `jobs/registry.ts` and every job in it: `order-sla-run`, the custom-domain re-check, the merchant-status monitor, `purge-visitor-detail`, the artifact rebuild. Row 9 read the scheduler for LOAD — the claim, the timer, the event loop — and never asked whether the jobs are CORRECT. **Outside contract:** a job must INFER NOTHING (memory `project_scheduler`) — its claim survives a restart, an unrecognised answer from an external service is loud and never a clean report (memory `project_merchant_status_monitor`), and a job that cancels an order owes the same refund row a human cancelling it owes (row 2's rule, applied to code with no user in front of it). This is where a bug is invisible by construction: nothing is watching, and the only symptom is a number that was quietly never updated |
| 17 | **Notifications and the alert beacons — a dot is a promise** | ☐ `notifications.ts`, `seller-alerts.ts`, `notification-copy.ts`, `notification-link.ts`, the tab-strip beacon (memory `project_tab_strip_alert_beacon`), the store-switcher dot (2026-08-12), the admin's own alerts. **Outside contract:** a badge is a claim that something needs doing, so the set of things that can raise one must equal the set of things the person must act on, and it must clear on the ACT rather than on the look. Both halves have already failed here — the switcher dotted two of the four things a shop waits on until 2026-08-12, and that was found by the owner rather than by a test. Read against memory `feedback_no_standing_screen_prose` too: a beacon that is always on is furniture |
| 18 | **The buyer's invoice — a document the platform does NOT issue** | ☐ built 2026-08-12→13, entirely after rows 2 and 13. `lib/invoicing/` (adapter, `buyer-invoice.ts`, `documents.ts`), `order-invoice-row.ts`, the PDF preset + its `/raw/upload` path, the chip and its undo, `terms.astro`'s clauses. **Read the row this replaced as the warning it is:** it was written on 2026-08-13 saying "a tax document issued in somebody else's name", which was the SUPERSEDED plan (AI_INSTRUCTIONS still carried it too, and has been corrected). The owner caught it by asking. **The actual contract, from `buyer-invoice.ts`:** the platform invoices only revenue that is ITS own — commission, subscription, ad margin — and the sale is the seller's, so the buyer's tax invoice is his to produce out of his own tax file. Nothing here creates a document; it records that he SAYS he provided one (`seller_upload` with a file, or `seller_handover` with the goods) and reminds him while it is owed. **Outside contract:** the platform is making a claim, in public, about a tax obligation that is not its own — so every surface must state whose obligation it is and must never imply verification we cannot do. Specific things nothing has checked: that `terms.astro` still describes what the code does (it was wrong once already — commit `899d3dd`), that the invoice total excludes shipping the platform charged, on every order shape including a multi-store cart; that an undone mark leaves no orphan file, stale chip or settled row; and that the `invoice_documents` reuse (a `seller_to_buyer` row planned at checkout, settled by the seller) cannot leave two answers to "does this order have an invoice yet" |
| 19 | **Every write path a stranger can reach without an account** | ☐ never had a row, and it GREW on 2026-08-13. `/api/report` (new), `/api/log-client-error`, `/api/analytics/event`, `/api/cart/*`, the guest half of `/api/messages`, `/api/lang`. **Outside contract:** an unauthenticated write path belongs to the internet, not to us — so each one must be bounded (body size, field length, rate), unable to be an amplifier (no unbounded fan-out, no outbound fetch it chooses), and unable to spend a scarce resource on the caller's say-so (pool connections, add-on quota, an alert channel). Row 4 audited who may act on WHAT; it never asked which paths need no identity at all, or what the SET of them costs when one caller hits all of them at once. `api-route-guards.test.ts` names them but only asserts that each declared a reason |
| 20 | **What leaves the platform as a LINK somebody forwards** | ☐ built 2026-08-12→13. `ShareMenu.astro` + `share-menu.ts`, the OG/Twitter card a scraper reads, `Seo.astro`'s new `storeOwned` split, store identity on a custom domain. **Outside contract:** each network's own scraper and card rules (WhatsApp, Facebook, Telegram) plus the Merchant/canonical rules row 1 holds for the FEED — a forwarded product URL has to resolve, render a picture, name the right site, and not cross the origin boundary row 5 guards. One bug in this area already shipped and was fixed on sight (a forwarded product arriving with no image), which is evidence the seam is live, not that it was audited |
| 21 | **The browser-side image pipeline — everything before the upload finishes** | ☐ `image-downscale.ts`, `bg-worker.ts` + `workers/bg-removal.ts`, `crop-modal.ts`, `cleanup-modal.ts`, `gallery.ts`, `cloudinary.ts`, `image-moderation.ts`. **Outside contract:** the BROWSER — Worker lifecycle, `OffscreenCanvas`/WebGPU availability, and the memory a phone will actually give a model — plus the provider's unsigned-preset limits. This is the one area where our own code runs on hardware we do not control and cannot measure. Its thinness is already demonstrated: on 2026-08-13 a worker the browser KILLED left the button spinning forever, because every path assumed it would answer. That was found by a question, not by a test, and the same assumption may sit in the crop and cleanup paths |
| 10 | **RTL + i18n** | ✅ 2026-08-07 · `i18n-hardcoded-strings.test.ts` (two tree scans). `translations.ts` + `getT` + the `#i18n-data` island + every consumer read end to end against two outside rules: **a page that declares `lang`/`dir` must render in that language**, and **a control that names a direction on screen must mirror with `dir`**. The gap was never an untranslated screen — those fail loudly. It was surfaces that are 95% translated: the notification panel rendered from `t.notifications` while the rows its script wrote into it were inline Hebrew; the same shape in the compose dialog, the thread row (BOTH renderers), the store/product pages' client toggles, checkout's quantity stepper and the seller's whole edit-order modal. **In three of them the dictionary already held the exact string** — each side right, only the join missing. Also the product lightbox's arrows, the last horizontal pair still on physical `left`/`right`, so in Hebrew they sat opposite the arrow keys (`arrow-step.ts`) and opposite every other carousel here. The guard's discriminator is the thing that made this a project rather than side-work: a file that calls `getT(` is a bilingual surface and every literal in it must be a lookup, a `??` fallback (188 of those are legitimate) or a `lang` conditional; a file with no `getT(` is Hebrew-only by construction, so it needs no allowlist. **Left open, and it is a measurement job rather than a translation one:** `BrandLogo.astro`'s drawn tagline. Its `aria-label` — the homepage `<h1>`'s whole accessible name, so what a crawler and a screen reader get — now follows `t.home.startSelling` like any other string. The *drawn* line under the wordmark stays Hebrew because its size and margin were bisected against a real build across 11 viewports × DPR 1/2/3 to sit flush with the mark: it is a solution for those 19 glyphs, not text, and a Latin run needs all three values re-solved (the instruction is above the span) |

**Rows 13–17 were added 2026-08-12, and rows 18–21 on 2026-08-13, both at the owner's request, for
areas built in the days AFTER the rows that would otherwise seem to cover them.** That is the pattern worth naming: row 2 audited the
money on 08-07 and the payout layer was written on 08-10, row 7 audited the dashboard on 08-09 and
its loading model changed on 08-11. **A ✅ is a statement about code that existed on that date**, so
the useful question at session start is not only "which row is open" but "what has been built since
the last ✅ next to it". They sit at the END of the queue — the hook below takes the first open row
in FILE order, so 5, 6 and 8 come first — and that is deliberate: an area finished last week is
still the one a session is most likely to remember, which is exactly the bias this table exists to
correct.

**Which row to take is not the session's choice any more (2026-08-10).** Every ✅ above was
marked by the session that happened to be working in that area — which is the cheap half, and the
half that finds the least: an area you have just spent a day inside is the one you least need to
re-read. The rows that stayed open are the ones nobody had a reason to open, which is precisely
the condition the feed bugs lived under. `.claude/hooks/next-area-audit.sh` now names the
lowest-numbered open row at session start, before any work has created an opinion about which
area is interesting. It does not block; it removes the discretion that was quietly selecting for
the areas least worth auditing.

**A ✅ EXPIRES, and that is measured now rather than remembered (owner, 2026-08-16).** The table has
always said a ✅ is a statement about code that existed on that date; nothing acted on it. Row 7
audited the dashboard's forms on 08-09, the panel-loading model underneath them was replaced on
08-11, and the row still read ✅ while five separate bugs came out of exactly that change — every
one of them reported by the owner rather than caught here. `npm run audit:drift` asks git which
rows have had code move under them since the day they were marked, ranked by how many files; the
session-start hook names the worst three. **Re-running every audit on a schedule was the obvious
answer and it is the wrong one** — it never finishes and it spends most of its time re-reading code
nobody touched. Re-opening a row when its subject moves does finish, and a re-read is far cheaper
than the audit was: only what changed has to be read again. The paths each row watches live in
`.claude/audit-areas.json`, kept in lockstep with this table by `tests/audit-areas.test.ts`; when
you finish an audit, write the paths you actually read.

**A ☐ does not mean untested.** The repo has 209 test files and ~2,690 tests, and every area above is
covered by some of them. It means nobody has read that area *as a whole against its outside
contract* — which is the specific gap this table closes, and the one the feed's own passing tests
did not.

## What this cannot do

It reads a checklist; it does not read intent the way a person does, and it will miss a class nobody
has written down yet. When a change is large or genuinely novel, say so and suggest the user run
`/code-review` for an independent pass. Never claim that ran.
