# https://payme.stoplight.io/docs/Products-Customers/tyhsvz950lrdz-update-customer

Update Customer
PATCH
https://sandbox.payme.io/api/customers/

Update customer details.

Request
Path Parameters
guid
string
required

The unique PayMe customer identifier.

Headers
Authorization
string
required

Bearer <ACCESS_TOKEN>

Body
application/json
application/json
customer_remote_id
string

The unique identifier for the customer.

first_name
string

The customer's first name.

middle_name
string

The customer's middle name.

last_name
string

The customer's last name.

email
string

The customer's email.

phone_number
string

The customer's mobile number.

additional_phone_number
string

Additional phone number of the customer.

country
string

The customer's country.

city
string

The customer's city.

street
string

The customer's street.

building_number
string

The customer's building number.

floor
string

The customer's floor.

zip
string

The customer's ZIP code.

company_name
string

The customer's company name.

gender
string

The customer's gender.

description
string

The customer description.

birthday
string<date>

The customer's birthday.

payment_tokens
object
payment_method
string

The payment method of the token. Allowed values:

Credit Card
IL Direct Debit
token_guid
string

The unique identifier of the token in PayMe.

Responses
200
400
401
500

Customer updated successfully.

Body
application/json
application/json
responses
/
200
created_at
string<date-time>

The timestamp of the update.

Example:
04-20-2024 13:00:00
updated_at
string<date-time>

The timestamp of the update.

Example:
04-20-2024 14:00:00
guid
integer

The unique PayMe customer identifier.

Parameters
guid*
:
Authorization*
:
Body
1
{
2
  "customer_remote_id": "string",
3
  "first_name": "string",
4
  "middle_name": "string",
5
  "last_name": "string",
6
  "email": "string",
7
  "phone_number": "string",
8
  "additional_phone_number": "string",
9
  "country": "string",
10
  "city": "string",
11
  "street": "string",
12
  "building_number": "string",
13
  "floor": "string",
14
  "zip": "string",
15
  "company_name": "string",
16
  "gender": "string",
17
  "description": "string",
18
  "birthday": "2019-08-24",
19
  "payment_tokens": {
20
    "payment_method": "string",
21
    "token_guid": "string"
22
  }
23
}
Send API Request
Sandbox
Request Sample: Shell / cURL
curl --request PATCH \
  --url https://sandbox.payme.io/api/customers/ \
  --header 'Accept: application/json' \
  --header 'Authorization: ' \
  --header 'Content-Type: application/json' \
  --data '{
  "customer_remote_id": "string",
  "first_name": "string",
  "middle_name": "string",
  "last_name": "string",
  "email": "string",
  "phone_number": "string",
  "additional_phone_number": "string",
  "country": "string",
  "city": "string",
  "street": "string",
  "building_number": "string",
  "floor": "string",
  "zip": "string",
  "company_name": "string",
  "gender": "string",
  "description": "string",
  "birthday": "2019-08-24",
  "payment_tokens": {
    "payment_method": "string",
    "token_guid": "string"
  }
}'
Response Example
1
{
2
  "created_at": "04-20-2024 13:00:00",
3
  "updated_at": "04-20-2024 14:00:00",
4
  "guid": 0
5
}
