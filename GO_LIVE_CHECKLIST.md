# Go-Live Checklist — מעבר מנתוני דמו לנתונים אמיתיים

> **מטרת הקובץ:** רשימה אחת ומרוכזת של כל מקום באתר שמשתמש היום בכתובת דמה / מזהה ריק /
> נתון מזויף (mock) / קובץ JSON מקומי, ומה בדיוק צריך להחליף כדי לעלות לאוויר.
> כל שורה: **מה המצב היום → מה לשנות (קובץ) → על מה זה משפיע.**
>
> כשמחברים משהו אמיתי — מוצאים אותו כאן, מבצעים, ומסמנים `[x]`. אם מוסיפים בקוד עוד
> תלות בנתון אמיתי, **מוסיפים אותה כאן מיד** (יש תזכורת על כך ב-`AI_INSTRUCTIONS.md`).

---

## 1. דומיין ו-SEO (הכי בסיסי — הרבה דברים תלויים בזה)

- [ ] **הדומיין האמיתי** — היום `url: 'https://example.com'` ב-`src/config/store.config.ts`.
  משפיע על: canonical, קישורי OG/רשתות חברתיות, כתובות ב-sitemap, וכל קישור מוחלט באתר.
  ברגע שמשנים כאן — רובם מתעדכנים לבד.
- [ ] **`public/robots.txt` — עדכון ידני!** יש שם שתי שורות `Sitemap: https://example.com/...`
  שלא מתעדכנות אוטומטית מה-config. צריך להחליף את הדומיין ביד.
- [ ] **שפת/מיקום התוכן** — היום `language: 'en'`, `direction: 'ltr'`, `locale: 'en_US'` ב-`store.config.ts`
  למרות שהאתר כולו עברית RTL. לשנות ל-`he` / `rtl` / `he_IL`. משפיע על תגי OG ו-SEO.
- [ ] **אימות + חיבור Google Search Console** (אחרי שהדומיין באוויר) — כדי לקבל נתוני חיפוש אורגני.
- [ ] **סימן מסחרי "Dezabin" — רישום ב-ILPO** (רשות הפטנטים והסימנים המסחריים, ilpo.gov.il). מגן על השם כמותג כדי שאף אחד לא יוכל להשתמש בו נגדך. עלות ~1,600₪ למחלקה אחת + תוספת לכל מחלקה, כמה חודשים לאישור. **לא חוסם השקה** — רושמים כשלמותג כבר יש ערך (לקוחות/תנועה/הכנסה), לא ברגע בחירת השם. הדומיינים (dezabin.com + dezabin.co.il) כבר בבעלותך; זו שכבת ההגנה החוקית מעליהם.

---

## 2. פרסום ומעקב (Marketing / Tracking)

- [ ] **מזהה GTM אמיתי** — היום `ads.googleTagId: ''` (ריק) ב-`store.config.ts`.
  הקוד שמזריק אותו לכל עמוד כבר קיים ומחכה למספר.
- [ ] **מזהה Meta Pixel אמיתי** — היום `ads.metaPixelId: ''` (ריק) ב-`store.config.ts`.
- [ ] **חיבור פיד המוצרים לחשבון פרסום** — הפיד `/api/feed/products.xml` כבר בנוי ומתעדכן לבד.
  צריך רק לפתוח חשבון Google Merchant Center / Meta Catalog ולחבר אליו את כתובת הפיד (דורש דומיין אמיתי).
- [ ] **הגדרת container ב-GTM עצמו** (בממשק של גוגל, לא בקוד) — לחבר אירועי צפייה במוצר / הוספה לסל ל-GA4.
- [ ] **אירוע "התחלת תשלום" (InitiateCheckout)** בעמוד הצ'ק-אאוט — עדיין לא נשלח.
- [ ] **אירוע "רכישה הושלמה" (Purchase)** — צריך להישלח מתוך webhook אישור התשלום (סעיף 3), רק אחרי תשלום אמיתי.
- [ ] **Meta Conversions API** — שליחת אירוע רכישה מהשרת (לא רק מהדפדפן). דורש access token + pixel id סודיים.
- [ ] **קמפייני "בוסט" אמיתיים** — היום מוכר מגדיר תקציב אבל אין חיבור API לגוגל/מטא. הנתונים בלשונית פרסום (חשיפות/קליקים/הוצאה) הם **mock** (ראה `src/lib/ad-metrics.ts` → `campaignLifetimeStats` / `campaignStatsInRange`).
- [ ] **חשיפות/הוצאה של הקמפיין הבסיסי** בדשבורד האדמין הן גם **mock** (`getMockBaselineImpressions` + `src/lib/admin-ads.ts`). יתחלפו בנתונים אמיתיים כשיחובר חשבון פרסום.
- [ ] **קמפייני פרסום המותג** (פרסום הפלטפורמה עצמה, `src/lib/brand-campaigns.ts`) — הקריאייטיב נשמר אמיתי (כולל תמונה ב-Cloudinary), אבל אין חיבור API לגוגל/מטא, והנתונים (`getMockBrandStats`) הם mock. צריך לחבר לחשבון פרסום אמיתי כדי שירוצו בפועל.
- [ ] **כל מדדי החשיפה/PPC בלשונית פרסום** (`src/lib/admin-ads.ts` → `ExposureOverview`/`PpcSnapshot`) הם הערכה מ-mock. יתחלפו בנתונים אמיתיים מ-API הדיווח של גוגל/מטא.
- [ ] **בורר טווח תאריכים בפרסום** — לשונית האדמין כבר יש בורר פונקציונלי (`src/lib/date-range.ts` + `ad-metrics.ts`, ברירת מחדל 7 ימים, השוואה לתקופה קודמת) מעל **נתוני דמו יומיים דטרמיניסטיים**. כשיחובר API אמיתי: להחליף את מקור הנתונים ב-`ad-metrics.ts` בשאילתת טווח אמיתית מגוגל/מטא (ה-UI והחיווט נשארים). **לשונית הפרסום של המוכר עדיין משתמשת ב-mock הישן (lifetime) — צריך להחיל עליה את אותו בורר.**

> **קידום שקט של חנויות** (לשונית פרסום / טאב חנויות) עובד **באתר בלבד** ולא תלוי בשום חיבור חיצוני — הוא אמיתי כבר עכשיו, אין מה לחבר.

---

## 3. תשלומים / סליקה (ישראל בלבד)

- [ ] **לבחור ספק סליקה** — SUMIT או תקבול (Takbull). לברר עמלות + אם ה-API פותח חשבון-מוכר אוטומטית.
- [ ] **`store.config.ts` → `checkout.provider`** — היום `'stripe'` (placeholder). Stripe לא בשימוש בישראל למודל הזה.
- [ ] **webhook `/api/payment/confirm`** — עדיין לא קיים. **קריטי:** היום `src/lib/orders.ts` קובע `paymentStatus: 'paid'` מיד עם יצירת ההזמנה (stub זמני, יש הערה בקוד בשורה ~69). ברגע שה-webhook קיים — **רק הוא** רשאי לקבוע "שולם". חובה טסט Vitest שמכסה את זה.
- [ ] **מעקב יתרות מוכרים** (`SellerBalance`, לתצוגה בלבד) — עדיין לא קיים.
- [ ] **`checkout.commissionPercent`** — היום `10`, משמש לדוחות בלבד. הניכוי בפועל קורה אצל ספק הסליקה.
- [ ] **`ads.boostCommissionPercent`** — היום `null` (לא הוחלט). זהו **המרווח שלך מתוך הוצאת הפרסום של המוכר** (בוסט) — המוכר משלם על מה שרץ, ואתה גוזר אחוז מזה, השאר קונה פרסום אמיתי (מוצג למוכר כ"דמי פלטפורמה"). כשמחליטים על האחוז: לעדכן כאן ב-`store.config.ts`, להציג את המספר בהערת "דמי פלטפורמה" בלשונית הפרסום, ולחשב אותו ב-Capture בפועל (auth/capture, ראה מודל התמחור). כרגע כל הפרסום mock — אין גבייה אמיתית.

---

## 4. מיילים

**המנוע כבר בנוי (code-complete).** ספק נבחר: **Resend** (מחקר 2026-07-24 — SDK נקי ל-Node/TS, free 3,000/חודש, $20/50K; מעבר ל-Amazon SES רק בנפח ~500K+/חודש). מודול `src/lib/email/` הוא adapter נקי — החלפת ספק = קובץ אחד. אוטומציה ראשונה שרצה: **אישור הזמנה לקונה + התראה למוכר** מיד אחרי צ׳קאאוט (מקובץ לפי חנות, תמונות, RTL). בלי מפתח → נופל ל-console (dev). מה שחסר כדי שישלח **באמת** בעלייה לאוויר:

- [ ] **דומיין שולח מאומת ב-Resend** — תלוי בדומיין האמיתי (סעיף 11/§SEO; כרגע `example.com`). לפתוח חשבון Resend, להוסיף את הדומיין, ולהעתיק את רשומות ה-DNS שהם נותנים.
- [ ] **להזין רשומות DNS: SPF + DKIM + DMARC** אצל רשם הדומיין. **זה הדבר שבאמת קובע הגעה לתיבות ישראליות** (Gmail/Walla/Outlook) — בלי אימות תקין המיילים ייפלו לספאם. זהה בכל ספק.
- [ ] **למלא ב-`.env`: `RESEND_API_KEY`** (מ-Resend) **ו-`EMAIL_FROM`** (כתובת על הדומיין המאומת, למשל `Dezabin <no-reply@dezabin.co.il>`).
- [ ] **לבדוק בפועל** — לבצע הזמנת-בדיקה ולוודא שהמיילים מגיעים לתיבה (לא ספאם) ונראים תקין במובייל.

> **חשבוניות/קבלות — לא באחריות המייל הזה.** מייל אישור-ההזמנה מצהיר במפורש שהוא *אינו* חשבונית מס. הקבלה/חשבונית החוקית מונפקת בנפרד ע"י **המוכר** (העוסק של המכירה) — בפועל אוטומטית דרך **ספק הסליקה** (SUMIT/תקבול מנפיקים חשבונית מס/קבלה כחלק מהתשלום). ראה סעיף התשלומים + פריט 26 ב-CURRENT_TASK (חשבונית מע"מ על דמי הפלטפורמה למוכר).

**מיילי מעבר-סטטוס — בנוי (code-complete).** "מוכן"/"נשלח" (עם מספר מעקב)/"בוטל" לקונה, נתלים על הצינור source-agnostic הקיים (`notifyOrderStatusChanged`). מגיע לכל קונה כולל **אורחים** (בניגוד להתראה בתוך-האתר, שרק לרשומים). קופסת-הקוד: `email/order-status-email.ts` + copy משותף עם ההתראה הפנימית ב-`order-status-copy.ts`. כשה-Sendit יחובר, ה-webbook שלו קורא לאותה פונקציה → המייל יוצא לבד. **חסר רק אותו חיבור ספק (מפתח+דומיין למעלה) כדי שישלח בפועל.**

---

## 5. משלוחים (ישראל בלבד)

- [ ] **שדה "משקל" למוצר** (בגרמים) — עדיין לא קיים. דרוש לחישוב מחיר משלוח אמיתי לפי משקל.
- [ ] **שילוב עם חברת משלוחים (Sendit)** — מחיר משלוח אמיתי לפי כתובת+משקל במקום המחיר הקבוע הידני.
- [ ] **מסך בדשבורד המוכר להזנת מחיר משלוח / סף חינם** — היום אפשר לשנות רק דרך קוד.
- [ ] **לחבר את ה-webhook של הספק לצינור-הסטטוס** — צינור עדכון-הסטטוס בנוי **אדיש-למקור** בכוונה (אותה פונקציה נקראת בין אם המוכר לחץ ובין אם הספק דיווח). כשמחברים את Sendit: (1) כפתור "צור משלוח" שקורא ל-API → מפיק מספר-מעקב + מדבקת PDF **וגם** מודיע לספק שיש חבילה הממתינה לאיסוף (פעולה אחת). כאן חוזר הסטטוס `ready` = "ממתין לאיסוף שליח" — **מצב שהספק מדווח עליו, לא כפתור ידני** (הוסר מהדרופדאון של המוכר עד אז, כי בלי ספק אין לו כיסוי אמיתי); השליח נאסף → הסטטוס עובר ל-`shipped` דרך ה-webhook; (2) endpoint `/api/shipping/webhook` שמקבל את דיווחי הספק (נאספה→בדרך→נמסרה) וקורא **לאותה** פונקציית עדכון-סטטוס → `delivered` מתעדכן לבד. חובה אימות מקור + idempotency על ה-webhook (כמו webhook התשלום).
- [ ] **שיטות משלוח (`deliveryMethod`) — שלוש, הקונה בוחר בצ'קאאוט:**
  1. **שליח עד הבית** — צריך כתובת; השליח מביא לדלת → `delivered`.
  2. **נקודת איסוף / לוקר** — השליח מביא לנקודה, הקונה אוסף. **רשימת הנקודות מגיעה מ-Sendit** (בורר-נקודה בצ'קאאוט). זרימה: `shipped` → "הגיעה לנקודה — ממתינה לך" → `collected` (נאסף מהנקודה). זול יותר משליח.
  3. **איסוף עצמי מהחנות** — בלי שליח/ספק כלל; הקונה בא למוכר. זרימה: `ready` = "מוכנה לאיסוף" (חיווי "בוא לאסוף" + כתובת/שעות) → המוכר מסמן `collected`.
  נוגע ב: **צ'קאאוט** (בורר-שיטה + בורר-נקודה), **הגדרות המוכר** (אילו שיטות הוא מציע + מחיר לכל אחת), **זרימת הסטטוס** (מסתעפת לפי השיטה). צינור-החיוויים אדיש-למקור כבר בנוי — רק צריך את הסתעפות שיטת-המימוש (`deliveryMethod`) + ה-UI.

---

## 6. מסד נתונים (כל ה-`data/*.json` הם dev-only)

- [ ] **מעבר מ-JSON למסד נתונים אמיתי** (SQLite/Postgres) — כל הקבצים ב-`data/` הם זמניים לפיתוח.
  הפונקציות כתובות כ-adapter נקי (swap-ready), רק הפנימיות משתנות. כולל:
  `stores.json`, `store-products.json`, `orders.json`, `user-carts.json`, `store-pageviews.json`,
  `ad-campaigns.json`, `platform-ads.json`, ועוד.
  בזמן המעבר: לטפל בנעילות/טרנזקציות (ראה `AI_INSTRUCTIONS.md` → Hard rules → Inventory/Scalability).

---

## 7. עדכון גרסה בפרודקשן בלי נפילות (deploy בטוח מול DB אמיתי)

> **הרעיון בבסיס:** בזמן deploy, לכמה שניות **הגרסה הישנה והחדשה רצות יחד** מול אותו DB.
> אם ה-DB, ה-API והקבצים כולם תואמים-לאחור — אף משתמש לא מרגיש את המעבר, גם לא באמצע פעולה.
> דפדפנים לא מרעננים לבד; הסיכון היחיד הוא שהגרסה הישנה *תישבר* אחרי deploy.

**עכשיו (הרגל קוד — נאכף אוטומטית):**
- [x] **שינויי `/api/*` ו-DB תמיד additive** — רק להוסיף שדות, אף פעם לא למחוק/לשנות שדה שגרסה
  קיימת מסתמכת עליו. client ישן שפתוח באמצע פעולה חייב להמשיך לעבוד. breaking אמיתי → `/api/v2/`.
  **מסומן כבוצע כי זה כבר כלל ברזל ב-`AI_INSTRUCTIONS.md` → Hard rules** ("Backward-compatible
  API/DB"), שנקרא בתחילת כל סשן — כך הוא מיושם בכל שינוי מהיום, לא רק לפני go-live.

**רק בחיבור ל-DB + עלייה לפרודקשן:**
- [ ] **Host עם Atomic Deploy** — פלטפורמה שמעלה גרסה חדשה לצד הישנה ורק אז מחליפה
  (Vercel/Netlify/Render). כך טאב פתוח ממשיך למשוך את הקבצים הישנים שלו בלי 404 ובלי רענון כפוי.
  **זה ~80% מהפתרון.** self-host → deploy לתיקייה חדשה + החלפת symlink, והשארת ה-`dist/client`
  הישן חי לזמן מה.
- [ ] **שינויי DB תמיד additive** — רק להוסיף עמודות/טבלאות, אף פעם לא למחוק/לשנות עמודה שהקוד
  הישן עדיין קורא. רוצים למחוק? קודם deploy שמפסיק להשתמש בעמודה → ורק ב-deploy הבא מוחקים אותה.
- [ ] **Migrations רצים לפני הקוד החדש** — כל שינוי מבנה ב-DB רץ לפני שהקוד החדש עולה, ובאופן
  תואם גם לגרסה הישנה (בגלל הכלל למעלה). כך הקוד החדש לא מגיע ל-DB שעוד לא מוכן.
- [ ] **Graceful shutdown** — SIGTERM handler בשרת הישן שמסיים בקשות SSR שבאמצע (למשל checkout)
  לפני שהוא נופל ב-restart. מונע ניתוק באמצע פעולה קריטית. (~10 שורות ב-node adapter.)

---

## 8. AIO — חשיפה למנועי-AI וחיפוש (פעולות אקטיביות אחרי שהאתר באוויר עם דומיין)

> **הבנה קריטית:** להיות באוויר זה תנאי הכרחי אך **לא מספיק**. מנועי-AI (ChatGPT / Perplexity /
> Gemini / Copilot) שולפים בזמן-אמת מאינדקס החיפוש (בעיקר Bing ו-Google). כלומר: אם אתה מדורג
> ב-Google/Bing — אתה נשלף לתשובות ה-AI; אם לא — הם פשוט לא ימצאו אותך. אין קסם שבו "דיפלוי =
> מנועי-AI נוהרים אליך". **הקוד כבר דואג לחצי של "להיות קָריא וציטוט-בר"** (JSON-LD Product+Offer
> מועשר, `/llms.txt` דינמי, `robots.txt` פתוח לזחלני-AI, תוויות-פרסום אוטומטיות). מה שלמטה = החצי
> השני, **"להימָצא"**, שהוא עבודה ידנית/הפצה שקוד לבד לא פותר.

- [ ] **מפתח IndexNow** — היום `store.config.ts → seo.indexNowKey` ריק. לייצר מחרוזת hex (~32 תווים)
  ולהדביק כאן. משיופעל (דורש גם דומיין אמיתי): כל פרסום/עדכון של מוצר או חנות דוחף את ה-URL ל-Bing
  (ומשם ל-ChatGPT/Copilot) תוך דקות במקום לחכות לזחילה. קובץ האימות `/{key}.txt` מוגש לבד מהקונפיג
  (`src/pages/[key].txt.ts`). no-op לגמרי עד שיש מפתח **וגם** דומיין לא-דמה (`src/lib/indexnow.ts`).
- [ ] **Google Search Console** — לאמת דומיין ולהגיש את שני ה-sitemaps (`/sitemap-index.xml` +
  `/sitemap-content.xml`). נותן נתוני חיפוש אורגני ומאיץ אינדוקס. (מופיע גם בסעיף 1.)
- [ ] **Bing Webmaster Tools** — **הכי חשוב ל-AIO:** Bing מזין את ChatGPT/Copilot. לאמת דומיין
  ולהגיש את אותם sitemaps (אפשר לייבא ישירות מ-Search Console).
- [ ] **Google Merchant Center + Google Shopping** — לפתוח חשבון ולחבר את `/api/feed/products.xml`.
  מזין גם את Shopping וגם את AI Overviews / קניות ב-Gemini. (מופיע גם בסעיף 2.)
- [ ] **Meta Catalog** — לחבר את אותו פיד ל-Catalog של מטא (אותה כתובת פיד).
- [ ] **בדיקת אמת אחרי עלייה** — לחפש מוצרים/חנויות ב-ChatGPT/Perplexity ובגוגל, ולעקוב אחרי
  הכיסוי ב-Search Console. **לא לסמוך על "עלה לאוויר = מופיע"** — זו עבודת SEO/הפצה מתמשכת.

---

## איך לוודא שלא שוכחים

1. **הקובץ הזה** הוא הרשימה הרשמית. עוברים עליו לפני כל ניסיון "לעלות לאוויר".
2. כל תלות חדשה בנתון אמיתי שנוספת בקוד — **מוסיפים לכאן באותו רגע**.
3. `AI_INSTRUCTIONS.md` מפנה לקובץ הזה, כך שכל סשן עתידי יודע לבדוק אותו.
