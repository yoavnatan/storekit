# Go-Live Checklist — מעבר מנתוני דמו לנתונים אמיתיים

> **מטרת הקובץ:** רשימה אחת ומרוכזת של כל מקום באתר שמשתמש היום בכתובת דמה / מזהה ריק /
> נתון מזויף (mock) / קובץ JSON מקומי, ומה בדיוק צריך להחליף כדי לעלות לאוויר.
> כל שורה: **מה המצב היום → מה לשנות (קובץ) → על מה זה משפיע.**
>
> כשמחברים משהו אמיתי — מוצאים אותו כאן, מבצעים, ומסמנים `[x]`. אם מוסיפים בקוד עוד
> תלות בנתון אמיתי, **מוסיפים אותה כאן מיד** (יש תזכורת על כך ב-`AI_INSTRUCTIONS.md`).

---

## המסלול הקצר — מה באמת חוסם עלייה לאוויר (נמדד 2026-08-04)

בקובץ יש ~80 סימוני `[ ]` פתוחים, אבל הם **שישה תהליכים**, לא שמונים משימות — ו-10 מהם הם סעיף 7
(deploy בטוח), עבודה מתגלגלת שמתחילה אחרי שהאתר חי. **הרשימה הארוכה היא ארכיון-ידע, לא תור-משימות**
— אל תקרא אותה מלמעלה למטה, קרא רק את השורות הבאות:

| # | מה | סעיף | כמה זמן באמת |
|---|---|---|---|
| 1 | **אחסון + DNS — האתר חי על `dezabin.co.il`** (הכתובת הרשמית; `.com` מפנה אליה). באותו יום גם שתי הפניות 301 ודחיסה בפרוקסי | §1 | יום |
| 2 | **DB בפרודקשן** — `DATABASE_URL` בסביבה, שרת בפרנקפורט, `REVOKE` על `money_events`, והחלטה על שינת Neon | §6 | שעה + החלטה אחת |
| 3 | **מיילים** — דומיין שולח ב-Resend + SPF/DKIM/DMARC + מפתח + הזמנת בדיקה | §4 | ערב |
| 4 | **חנויות התצוגה על הסביבה האמיתית** — `seed:showcase`, החלפת סיסמה, וליטוש התוכן | §6.2 | דקות + ליטוש |
| 5 | **סליקה** — בחירת ספק → webhook → יתרות | §3 | פרויקט |
| 6 | **פרסום מחובר באמת** — חשבונות בגוגל/מטא → מספרים בקונפיג → דיווח אמיתי → אימות §2.5 | §2 | שני ערבים + המתנה לאישורים |

**1-4 הם בערך שבוע עבודה מפוזר. שני הכבדים הם 5 ו-6, והם מקבילים** — אפשר לקדם את הסליקה בזמן
שממתינים לאישורי Merchant Center. שים לב ש-6 יכול להתחיל רק אחרי ש-1 (הדומיין) חי.

**מה לא חוסם:** משלוחים (§5 — המחיר הקבוע עובד), AIO (§8 — דורש דומיין חי וממילא נמשך חודשים),
deploy בטוח (§7 — מתגלגל), וחיפוש סמנטי (נדחה במפורש). לא חודשים — אלא אם מנסים לסמן את כל
80 הסימונים לפני שעולים, וזו בדיוק הטעות שהמסלול הזה נועד למנוע.

---

## אפשר לעשות היום — בלי דומיין, בלי חשבון, בלי ספק (נבדק בקוד 2026-08-04)

הקובץ מסודר לפי **תהליך חיצוני**, ולכן הוא נראה כאילו הכל תקוע וממתין למישהו. זה לא נכון: אלה
פריטים שהם **קוד בלבד**, אין להם שום תלות חיצונית, ואפשר לקחת כל אחד מהם לסשן היום. כל שורה
אומתה מול הקוד — כלומר באמת חסרה, לא רק כתובה כחסרה.

**קשיחוּת ואבטחה (§7) — הכי בעל-ערך, וכולם קוד טהור:**
- ~~הגבלת קצב על ההתחברות~~ — **בוצע 2026-08-04** (§7; `lib/rate-limit.ts`, מיגרציה 0009).
- **טוקן CSRF על מסלולים שמשנים נתונים** — אין `csrf` בכל `src/`.
- ~~`SIGTERM` / graceful shutdown~~ — **בוצע 2026-08-04** (§7; `lib/shutdown.ts`).

**פרסום (§2) — קוד שממתין לחשבון, אבל אפשר לכתוב עכשיו וייכנס לפעולה ביום החיבור:**
- ~~אירוע InitiateCheckout ל-`dataLayer`/`fbq` בצ׳קאאוט~~ — **בוצע 2026-08-04** (§2.3;
  `lib/tracking.ts`). הניתוח הפנימי כבר מדד `begin_checkout` ב-`middleware.ts`; זו הדחיפה החוצה.
- ~~לכידת `gclid` / `fbclid` / `utm_*` ושמירתם על ההזמנה~~ — **בוצע 2026-08-04** (§2.5 שכבה 5;
  `lib/attribution.ts`, מיגרציה 0010). הלכידה בלבד — אין עדיין מסך שקורא אותה, ובכוונה.
- ~~להסתיר את לשונית הפרסום מהמוכר עד אימות §2.5~~ — **ירד מהרשימה 2026-08-04.** זו הייתה הדרך
  לעלות לאוויר בלי להראות למוכר מספרי דמו; משהבעלים החליט שלא עולים בלי פרסום מחובר (§2), אין
  מצב שבו מוכר אמיתי ולשונית לא-מחוברת קיימים באותו זמן, והמנגנון מיותר.

**סליקה (§3) — מה שלא דורש ספק:**
- **מעקב יתרות מוכרים (`SellerBalance`)** — תצוגה בלבד, סכימה של הזמנות ששולמו. אין כזה בקוד.
- **חסימת קנייה עצמית של מוכר מנותק** — היום החסימה עובדת רק על מוכר מחובר; התאמה לפי כתובת מייל
  סוגרת את רוב הפער בלי שום תלות בספק.

**משלוחים (§5):**
- **שדה משקל למוצר (גרם)** — מודל + טופס + פיד. לא קיים; לא דורש את Sendit.

## מפת דרכים — הסימונים למטה הם ~9 תהליכים, לא ~50 משימות

הרבה סימונים הם שלבים בתוך תהליך אחד (למשל כל נתוני ה-mock בפרסום מתמלאים מחיבור **אחד**).

**חוסם השקה — בערך בסדר הזה:**

| # | תהליך | סעיף | גודל |
|---|---|---|---|
| 1 | ✅ **מעבר ל-DB — הושלם 2026-08-03.** Neon/Postgres, `data/*.json` נמחקו, נמדד מול המסד האמיתי. הדבר היחיד שנשאר ממנו הוא `pgvector`+embeddings לחיפוש הסמנטי, שנדחה במפורש (החלטת בעלים) ואינו חוסם השקה | §6 + `DB_MIGRATION_PLAN.md` §8 4ב | — |
| 2 | **אחסון + DNS — האתר חי על הדומיין** | §1 | יום. פותח את כל הצד החיצוני |
| 3 | **סליקה** — בחירת ספק → webhook → יתרות | §3 | פרויקט |
| 4 | **מיילים** — חשבון Resend + DNS + מפתח | §4 | ערב |
| 5 | **חנויות תצוגה על הסביבה האמיתית** | §6.2 | דקות |

**אחרי שהאתר חי — לא חוסם:**

| # | תהליך | סעיף | גודל |
|---|---|---|---|
| 6 | **חשבונות פרסום** → מספרים בקונפיג → חיבור דיווח (מכבה בבת אחת את כל ה-mock) → **אימות** | §2 | שני ערבים + חיבור |
| 7 | **AIO / Search Console / Bing** | §8 | ערב + מעקב מתמשך |
| 8 | **משלוחים (Sendit)** — עד אז המחיר הקבוע עובד | §5 | פרויקט |
| 9 | **דיפלוי בטוח, דומיין מותאם למוכר, סימן מסחרי** | §7 + §1 | מתגלגל |

**סדר כפוי:** רוב הצד החיצוני (Merchant Center, מטא, Resend, Search Console) לא יכול להתחיל לפני
שהדומיין חי. ו-DB לפני סליקה — ה-webhook צריך idempotency אמיתי, לא קובץ JSON.

**תהליך 6 לא חוסם את עליית האתר, אבל כן חוסם שיווק** (הוחלט 2026-07-30). הפרסום הוא הליבה של מה
שהפלטפורמה מוכרת למוכר, ולכן לא מציעים אותו ללקוח לפני שהאימות ב-§2.5 עבר — ועד אז לשונית הפרסום
מוסתרת/מסומנת "בקרוב" ולא מציגה מספרי דמו למוכר אמיתי.

---

## 1. דומיין ו-SEO — 🚀 חוסם השקה (רק שורת האחסון+DNS; השאר בסעיף אחרי ההשקה)

- [x] **הדומיין בקונפיג** — `store.url = 'https://dezabin.co.il'` (`src/config/store.config.ts`).
  ממנו נגזרים canonical, תגי OG, כתובות ב-sitemap, IndexNow וכל קישור מוחלט באתר.
- [x] **`public/robots.txt`** — שתי שורות ה-Sitemap כבר על `dezabin.co.il`. **שים לב:** הקובץ הזה
  *לא* נגזר מהקונפיג — אם הדומיין ישתנה שוב, לערוך אותו ביד.
- [x] **שפת/מיקום התוכן** — `language: 'he'`, `direction: 'rtl'`, `locale: 'he_IL'`.
- [ ] **האתר באוויר על הדומיין בפועל** — אחסון + DNS. כל שאר הסעיף (וגם רוב סעיפים 2 ו-8)
  תלוי בזה בפועל, לא רק בערך שבקונפיג.
- [ ] **⚠️ סיווג מול מסנני התוכן הישראליים — נטפרי / רימון / אתרוג (נוסף 2026-08-05).** נתח קונים
  אמיתי בישראל גולש דרך סינון, ואתר חסום שם הוא לקוחות שלא יכולים לקנות — לא "פחות תנועה" אלא אפס.
  שני חלקים, וההפרדה ביניהם חשובה:
  1. **מונע, לפני ההשקה:** לא להשאיר את הדומיין מצביע על עמוד חניה. מסננים מסווגים עמוד כזה
     כ"דומיין חונה/פרסומות" וחוסמים אותו, **וסיווג נתקע נשאר גם אחרי שעולה אתר אמיתי**. עד שיש
     אחסון — עדיף בלי רשומת A בכלל (האימות ב-DNS והמייל לא תלויים בה).
  2. **אחרי ההשקה:** לפנות לכל אחד מהם לסיווג האתר כמסחר. מה שעוזר לזה הוא ש-`spam-filter.ts` כבר
     חוסם בכתיבה אוצר מילים של תוכן מבוגרים/הימורים/תרופות בשמות מוצרים, תיאורים ותגיות — הדבר
     היחיד שבאמת מסווג מרקטפלייס כתוכן מבוגרים הוא מה שמוכר אחד מעלה, וזה נחסם אוטומטית.
- [ ] **⚠️ שתי הפניות 301 בשרת — לא בקוד, ובלעדיהן יש תוכן כפול (נוסף 2026-08-05).** לפלטפורמה
  יש היום שלוש כתובות שמגיעות לאותו מקום, וה-DNS *חייב* שיהיה כך (מי שיקליד `www` או `.com` צריך
  להגיע). מה שמונע תוכן כפול הוא לא מחיקת רשומת DNS אלא **הפניה בשרת**:
  **⚠️ השתיים לא יושבות באותו מקום — נבדק ב-DNS ב-2026-08-05:** `dezabin.com` מנוהל ב-**Cloudflare**
  (`journey.ns` / `mitch.ns`), ואילו `dezabin.co.il` מנוהל ב-**MyNames** (`ns1/ns2.mynames.co.il`).
  1. `www.dezabin.co.il` → `dezabin.co.il` — הכתובת בלי `www` היא הרשמית: `store.url` בלעדיה,
     ולכן כל canonical, כל `<loc>` בסייטמאפ וכל קישור מוחלט מצביעים לשם. **מוגדר אצל מי שמארח את
     האתר, לא ב-MyNames** — DNS רק מפנה לשרת; מי שמחזיר 301 הוא השרת.
  2. `dezabin.com` → `dezabin.co.il` — שומר את הדומיין רשום ואת התנועה, ומרכז את כוח הדירוג
     בכתובת אחת. הפיך: אם הפלטפורמה תצא מישראל, מחליפים כיוון (memory `project_domain_switch`).
     **מוגדר ב-Cloudflare, ב-Redirect Rule** — ושם זה לא דורש שרת כלל, ולכן זו ההפניה היחידה שאפשר
     להגדיר עוד לפני שהאתר באוויר.
  **בדיקה אחרי ההעלאה:** `curl -sI https://www.dezabin.co.il | grep -i '^location'` — חייב להחזיר
  את הכתובת בלי `www`, וב-`301` (לא 302: זמנית לא מעבירה כוח דירוג).
  נכס מסוג **Domain** ב-Search Console מכסה את כל שלוש הכתובות בנכס אחד — זו הסיבה לבחור בו
  ולא ב-URL prefix.
- [ ] **דומיין מותאם למוכר (Cloudflare for SaaS)** — הקוד מוכן ואגנוסטי (`src/lib/custom-domain.ts` + `custom-domain-cloudflare.ts` + middleware), רץ עם stub בטוח בפיתוח. כדי להפעיל בפרודקשן:
  (1) להקים **Cloudflare for SaaS / SSL for SaaS** על הזון של הפלטפורמה ולהגדיר Fallback Origin שמצביע ל-origin שלנו;
  (2) להזין ENV: `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ZONE_ID`, ו-`CUSTOM_DOMAIN_TARGET` (יעד ה-CNAME שהמוכרים מפנים אליו — ה-Fallback Origin/`customers.dezabin.co.il`);
  (3) לוודא שה-middleware מקבל את ה-Host המקורי מ-Cloudflare (proxied) ולא את של Cloudflare עצמו.
  **הגנת Host (404 לדומיין לא-מחובר):** דומיין חיצוני שמצביע אלינו בלי חנות מחוברת מקבל 404 (לא דף הבית). אם יש הוסטים לגיטימיים נוספים שפוגעים ב-origin ישירות (staging / בדיקות בריאות / ה-Fallback Origin עצמו) — להוסיף אותם ב-ENV `PLATFORM_HOSTS` (מופרד בפסיקים) כדי שלא יקבלו 404. IP-ים והוסטים פנימיים כבר פטורים אוטומטית.
  בלי ה-ENV הזה הכל ממשיך לעבוד — פשוט המוכר לא יכול לאמת דומיין (נשאר `pending`). הכתובת המקומית `/<slug>` (dezabin.co.il/שם-החנות) תמיד חיה. **ה-canonical:** כשדומיין מאומת, הוא הופך ל-canonical (ה-SEO נזקף לדומיין של המוכר, החלטה 2026-07-26); בלי דומיין מאומת — הכתובת בפלטפורמה היא ה-canonical.
- [ ] **דחיסה (gzip/brotli) על הקבצים הסטטיים — בפרוקסי/CDN שלפני האפליקציה.** תגובות ה-SSR (HTML של דף הבית, דפי חנות ומוצר, כל `/api`) כבר נדחסות בקוד — `src/middleware.ts` דוחס אותן בעצמו, ונמדד: דף הבית ירד מ-298KB ל-42KB על החוט, וזמן ההגעה של ה-HTML בחיבור של 1.6Mbps מ-3.5 שניות ל-0.6. **מה שהקוד לא יכול לדחוס** זה מה ש-`dist/client` מגיש ישירות ולא עובר דרך ה-middleware: `BaseLayout.css` (201KB → כ-30KB דחוס), באנדלי ה-JS והפונטים. `@astrojs/node` לא יודע לדחוס אותם, ולכן זו הגדרה בשרת שלפניו — ב-Cloudflare זה דלוק כברירת מחדל, ב-nginx זה `gzip on` + `brotli on` על `text/css` ו-`application/javascript`. בלי זה כל מבקר ראשון מוריד ~200KB מיותרים לפני שהעמוד בכלל מצייר.
- [ ] **אימות + חיבור Google Search Console** (אחרי שהדומיין באוויר) — כדי לקבל נתוני חיפוש אורגני.
- [ ] **`TRUST_PROXY_IP=1` בסביבת השרת — אם ורק אם האפליקציה נגישה רק דרך פרוקסי/Cloudflare.**
  קובע אם מגבלת קצב ההתחברות (§7) סופרת לפי הכתובת האמיתית של הגולש או לפי הפרוקסי. **שתי הטעויות
  שקטות:** להדליק כשה-origin חשוף ישירות = כל אחד מזייף כתובת ועוקף את המגבלה; לא להדליק מאחורי
  פרוקסי = כל המוכרים נספרים כמקור אחד. ההסבר המלא ב-`src/lib/client-ip.ts`.
- [ ] **סימן מסחרי "Dezabin" — רישום ב-ILPO** (רשות הפטנטים והסימנים המסחריים, ilpo.gov.il). מגן על השם כמותג כדי שאף אחד לא יוכל להשתמש בו נגדך. עלות ~1,600₪ למחלקה אחת + תוספת לכל מחלקה, כמה חודשים לאישור. **לא חוסם השקה** — רושמים כשלמותג כבר יש ערך (לקוחות/תנועה/הכנסה), לא ברגע בחירת השם. הדומיינים (dezabin.com + dezabin.co.il) כבר בבעלותך; זו שכבת ההגנה החוקית מעליהם.

---

## 2. פרסום ומעקב (Marketing / Tracking) — 🚀 חוסם השקה (**החלטת בעלים, 2026-08-04**)

> **שינוי החלטה — קרא לפני שאתה מתכנן לפי הסעיף הזה.** עד 2026-08-04 הסעיף הזה היה מסומן
> "חוסם שיווק, לא עלייה לאוויר". הבעלים הפך אותו לחוסם השקה: *"אני לא מתכוון לעלות לאוויר עם
> נתוני דמו או כשזה לא מחובר — זה חלק ממה שהאתר מציע."* הפרסום הוא חלק מהמוצר שנמכר למוכר, ולכן
> אתר שעולה בלעדיו מוכר משהו שאינו קיים.
>
> **מה זה עולה, בכנות:** תאריך ההשקה נקשר עכשיו לקצב של גוגל ומטא — אישור Merchant Center,
> אימות דומיין וחשבון מודעות פעיל הם ימים עד שבועות שלא בשליטתך, והם יכולים להתחיל רק **אחרי**
> שהדומיין חי (§1). זו הייתה הסיבה המקורית להוציא את הסעיף מהמסלול החוסם.
>
> **האימות ב-§2.5 הוא מולך בלבד, לא מול המוכרים.** למוכר אין ולא יהיה חשבון בגוגל או במטא: לכל
> הפלטפורמה יש חשבון GTM אחד, פיקסל אחד, Merchant Center אחד ו-Catalog אחד — בדיוק מה שמאפשר
> את הפרסום הבסיסי לכל מוכר בלי שיצטרך להסכים או להירשם לכלום. "אימות" כאן פירושו: להוכיח לעצמך
> פעם אחת שהמספר שהדשבורד מציג למוכר הוא המספר האמיתי מגוגל/מטא, לפני שגובים ממנו כסף על בוסט.

### 2.1 חשבונות לפתוח — הקמה חד-פעמית שלך, לא קוד

> תלוי בדומיין חי (סעיף 1). **מתחילים בגוגל** — היא לא דורשת שום זהות ציבורית, רק חשבון ואימות
> דומיין. כל החשבונות עצמם חינם; משלמים רק על תקציב הפרסום בפועל.

**גוגל:**
- [ ] **Google Ads** — חשבון הקמפיינים.
- [ ] **Merchant Center** + אימות הדומיין, ולחבר את `/api/feed/products.xml` (הפיד בנוי ומתעדכן לבד).
- [ ] **GA4 + container ב-GTM** (בממשק של גוגל) — לחבר את אירועי הצפייה-במוצר/הוספה-לסל שהאתר כבר שולח.

**מטא** — מטא לא מאפשרת מודעה בלי **דף עסקי**: הדף הוא הזהות המפרסמת (השם והתמונה העגולה בראש כל
מודעה). אצלנו יש דף אחד לכל הפלטפורמה, ולכן בכל מודעה — כולל בוסט של מוכר — הזהות היא **Dezabin**;
החנות עצמה מופיעה בתמונת הקריאייטיב ובעמוד הנחיתה. (בגוגל זה אחרת: שם הלוגו הוא נכס של asset group,
אז בוסט של חנות בודדת כן יכול לרוץ עם הלוגו שלה.)
- [ ] **Meta Business** — התיקייה העסקית שמחזיקה את כל השאר.
- [ ] **דף פייסבוק לדזבין** + תמונת פרופיל, קאבר ותיאור קצר. **לא צריך לתחזק אותו** (בלי פוסטים,
  בלי מענה) — אבל דף ריק לגמרי פוגע באמון, כי אנשים לוחצים על שם המפרסם. אינסטגרם לא חובה:
  מודעות שרצות שם משתמשות בזהות של הדף.
- [ ] **חשבון מודעות** (אמצעי תשלום) + **Catalog** מחובר לאותו פיד + **Pixel**.

> **החלטה (2026-07-26) — קישורי הפיד לחנות עם דומיין מותאם:** הפיד מצביע תמיד ל**כתובת הפלטפורמה**
> (`dezabin.co.il/<slug>/<product>`), לא לדומיין של המוכר — ה-Merchant Center שלנו מאמת את `dezabin.co.il`
> בלבד, ואימות אלפי דומייני-מוכרים לא סקיילבילי. המוכר לא מפסיד: הקליק נוחת אצלנו ומיד **301 לדומיין
> שלו** (הנחיתה + ה-SEO אצלו), והפיד תמיד משתמש ב-slug הנוכחי. **לבדוק בהקמה:** אם MC מסמן
> "mismatched domains" — או לאמת דומייני-מוכרים, או להחריג חנויות-עם-דומיין מהפיד. לא נדרש קוד.

### 2.2 מספרים להזין בקוד (אחרי פתיחת החשבונות)

- [ ] **`ads.googleTagId`** — ריק היום ב-`store.config.ts`. קוד ההזרקה לכל עמוד כבר קיים.
- [ ] **`ads.metaPixelId`** — ריק היום ב-`store.config.ts`.

### 2.3 קוד שעוד חסר

- [x] ~~**אירוע "התחלת תשלום" (InitiateCheckout)** בעמוד הצ'ק-אאוט.~~ **בוצע 2026-08-04** —
  `lib/tracking.ts#trackInitiateCheckout`, נורה פעם אחת לטעינת עמוד ב-`checkout.astro` אחרי שתמחור
  העגלה מתעדכן (הסכום הוא מה שמטא מייעלת מולו, אז הוא חייב להיות המחיר שייגבה). שולח `begin_checkout`
  ל-`dataLayer` ו-InitiateCheckout ל-`fbq`. פריטי חנות ראווה לא נספרים. שקט לגמרי עד שיוזן פיקסל.
- [ ] **אירוע "רכישה הושלמה" (Purchase)** — מתוך webhook אישור התשלום (סעיף 3), רק אחרי תשלום אמיתי.
- [ ] **Meta Conversions API** — אירוע רכישה מהשרת, לא רק מהדפדפן. דורש access token + pixel id סודיים.
- [ ] **חיבור API לקמפייני בוסט** — היום המוכר מגדיר תקציב ולא רץ שום דבר בפועל.

### 2.4 נתוני דמו שיתחלפו כשיחובר חשבון פרסום

כל המספרים בלשונית הפרסום היום הם mock דטרמיניסטי — שום כסף לא זז ושום מודעה לא רצה.

- [ ] **ביצועי בוסט** (`ad-metrics.ts` → `campaignLifetimeStats`/`campaignStatsInRange`).
- [ ] **חשיפות/הוצאה של הקמפיין הבסיסי** (`getMockBaselineImpressions` + `admin-ads.ts`).
- [ ] **מדדי חשיפה/PPC** (`admin-ads.ts` → `ExposureOverview`/`PpcSnapshot`).
- [ ] **קמפייני פרסום המותג** (`brand-campaigns.ts`) — הקריאייטיב עצמו אמיתי ונשמר (כולל תמונה
  ב-Cloudinary); רק הנתונים (`getMockBrandStats`) והריצה בפועל חסרים.
- [ ] **בורר טווח התאריכים** — בלשונית האדמין הוא כבר פונקציונלי (`date-range.ts`, ברירת מחדל 7 ימים
  + השוואה לתקופה קודמת) מעל נתוני דמו; להחליף רק את מקור הנתונים ב-`ad-metrics.ts`.
  **גם לשונית הפרסום של המוכר כבר קיבלה את אותו בורר** (`scripts/dashboard/advertising.ts` +
  `resolveAdRange` ב-`api/seller/ad-campaigns.ts`, נבדק 2026-08-04; ברירת המחדל שם היא `lifetime`
  בכוונה). כלומר בשורה הזאת נשאר רק מקור-הנתונים, לא ה-UI.

> **שני דברים שכבר אמיתיים ולא מחכים לכלום:**
> **(1) קידום שקט של חנויות** — עובד באתר בלבד, בלי שום חיבור חיצוני.
> **(2) תמונות ברמת-חנות לקמפיינים** (נבנה 2026-07-28) — `src/lib/store-image.ts` מחזיר לכל חנות
> ארבעה גדלים (`logo` 512², `square` 1080², `portrait` 1080×1350, `landscape` 1200×628): תמונת המוכר
> דרך Cloudinary בגודל מדויק, ואם אין — הסמל הנוצר של החנות מ-`/api/store-image/<slug>/<format>.png`.
> `storeAdCreative()` מחזיר את כל הסט, כך שחיבור ה-API של הבוסט לא צריך לטפל ב"למוכר אין תמונה".

### 2.5 אימות — איך יודעים שהחיבור באמת עובד

כל מה שלמעלה אומר **מה** לחבר. זה אומר **איך לוודא**. הסעיף הזה לא אופציונלי: היום הדשבורד מייצר
את המספרים בעצמו, ולכן הוא ייראה **זהה לחלוטין** בין חיבור שעובד לחיבור שבור. בלי הבדיקות האלה
"מחובר" הוא הנחה, לא עובדה. שלוש השכבות הראשונות חינמיות ולא דורשות תקציב פרסום.

**שכבה 1 — הפיד** (חינם; מאמת את `product-feed.ts` מול הכללים האמיתיים):
- [ ] להגיש את `/api/feed/products.xml` ל-Merchant Center ולקרוא את דוח האישורים: כמה מוצרים אושרו,
  כמה נדחו ומאיזו סיבה. זו הבדיקה היחידה שמאמתת את מיפוי השדות מול מה שגוגל באמת דורשת.
- [ ] אותו פיד ל-Meta Catalog בנפרד — מטא מקפידה על שדות אחרים, ופיד שעבר בגוגל יכול להידחות שם.
- [ ] לפתוח `image_link` בגלישה אנונימית (בלי קוקי, בלי התחברות). תמונה שדורשת session = מוצר שנדחה.
- [ ] לוודא שמוצר שאזל יוצא `out of stock` בפיד, ושהוא באמת מפסיק להתפרסם — אחרת התקציב נשרף על
  מה שאי אפשר לקנות.

**שכבה 2 — הפיקסלים** (חינם):
- [ ] Google Tag Assistant + Meta Pixel Helper על עמוד מוצר, סל וצ'ק-אאוט: לוודא ש-`view_item` /
  `ViewContent` / `AddToCart` יוצאים עם המזהה, הסכום והמטבע הנכונים (`src/lib/tracking.ts`).
- [ ] אחרי חיבור הסליקה (§3): לוודא ש-`Purchase` נורה **פעם אחת בדיוק** לכל הזמנה ושסכומו שווה
  לסכום ההזמנה. אירוע כפול מנפח את ה-ROAS פי שתיים ומטעה כל החלטת תקציב שתתקבל אחריו.

**שכבה 3 — ה-API של הקמפיינים** (סנדבוקס רשמי, בלי חיוב):
- [ ] Google Ads *test manager account* ו-Meta Marketing API *Sandbox mode* — שתיהן סביבות בדיקה
  רשמיות שבהן יוצרים קמפיינים דרך ה-API בלי שכסף זז.
- [ ] לעבור שם על כל פעולה שהדשבורד כבר מציע: יצירת בוסט, השהיה, חידוש, שינוי תקציב, מחיקה. כולן
  היום כתיבה לטבלת `ad_campaigns` ב-Postgres בלבד (`lib/ad-campaigns.ts`; התיקון של השורה הזאת
  2026-08-04 — היא עדיין דיברה על `data/ad-campaigns.json` שנמחק ב-2026-08-03), ואף אחת מהן לא
  נבדקה מול API אמיתי.

**שכבה 4 — ההצלבה** (הבדיקה היחידה שסוגרת את העניין; סנדבוקס לא מספיק כי הנתונים שם מפוברקים גם הם):
- [ ] קמפיין אמיתי אחד קטן (100–200 ₪) על חנות שלך, ואז להשוות **לאותו טווח תאריכים**: חשיפות,
  קליקים והוצאה בדשבורד שלנו מול Google Ads / Meta Ads Manager. **לא זהה = עוד לא סיימת.** זה מה
  שמפריד בין "ה-API החזיר 200" לבין "הדיווח נכון".
- [ ] לאמת שההוצאה בפועל לא חורגת מהתקציב שהמוכר הגדיר — התקציב הוא **תקרה**, לא תשלום מראש.

**שכבה 5 — ייחוס המכירות: מה בכלל נספר כ"מכירה מהקמפיין"** (נוסף 2026-07-31, מתוך שאלת המשתמש):
המספר "מכירות" בכרטיס הקמפיין הוא **ייחוס (attribution), לא ספירת הזמנות**. הכלל שהאינטגרציה
האמיתית חייבת לדווח לפיו כתוב במלואו ב-`src/lib/ad-metrics.ts` (ה-JSDoc של `RangeStat.conversions`),
וה-glossary שהמוכר קורא נוסח לפיו — שני המקומות חייבים להישאר תואמים.
- [ ] **לקבע את חלון הייחוס בשני החשבונות במפורש** ולרשום מה נבחר: Google Ads ברירת מחדל 30 יום
  מהקליק, Meta 7-day-click (+1-day-view). קונה שלחץ, יצא וחזר לקנות בתוך החלון **כן** נספר — לא רק
  מי שקנה מיד. שינוי החלון משנה למפרע כל מספר בדשבורד.
- [ ] **לוודא מול המספרים האמיתיים שסכום המכירות של כל הקמפיינים לא עולה על מספר ההזמנות בפועל.**
  גוגל ומטא יכולות שתיהן לתבוע את אותה מכירה, ולכן חיבור פשוט שלהן מנפח. אם עולה — לדווח מנוכה.
- [x] ~~**ייחוס first-party** — לקרוא `gclid`/`fbclid`/`utm_*` מכתובת הנחיתה, לשמור בעוגייה
  first-party לאורך החלון, ולחתום את הקמפיין על ההזמנה ב-checkout.~~ **הלכידה בוצעה 2026-08-04** —
  `lib/attribution.ts`, עוגייה `sn_attr` (httpOnly, lax, 30 יום) שנכתבת ב-`middleware.ts`, ונחתמת
  על כל הזמנה ב-`/api/checkout` (עמודת `orders.attribution`, מיגרציה 0010).
  **הוקדם על פני הטריגר המקורי ("כשנפתחים חשבונות הפרסום") בכוונה:** ייחוס אפשר לרשום רק ברגע שהוא
  קורה, ולכן כל חודש שעובר בלי הלכידה הוא חודש הזמנות שלא ניתן לייחס בדיעבד. **שום מסך לא קורא את
  העמודה עדיין** — עד שיהיו חשבונות אמיתיים אף קישור לא נושא את הפרמטרים, ו-0 אמיתי ליד אלפי חשיפות
  דמו גרוע משניהם.
  נלכד גם `gbraid`/`wbraid`: באייפון, כשההסכמה ל-ATT נדחית, גוגל שולחת אותם **במקום** `gclid` —
  בלעדיהם החצי הנייד של קמפיין נראה כאילו לא המיר, לא כאילו לא נמדד.
  מה שנשאר לא מדויק גם אחרי החיבור: קליק במכשיר אחד ורכישה באחר, וניקוי עוגיות.
- [ ] **מה שנשאר בסעיף הזה, ליום החיבור:** דוח שקורא את העמודה (`attribution->>'utmCampaign'`,
  יש אינדקס חלקי), הצלבה מול חלון הקליקים לכל רשת בנפרד — גוגל 30 יום, מטא 7 — דרך `landedAt`
  שנשמר על ההזמנה, והסרת "(משוער)" מהתווית `adConversions` + עדכון ה-glossary בהתאם.
- [x] ~~⚠️ **מזהה המוצר לא זהה בין הפיד לאירועים**~~ — **נמצא ותוקן 2026-08-04.** הפיד שלח
  `g:id` = ה-uuid, והאירועים ל-`dataLayer`/`fbq` שלחו את ה-slug — וגוגל ומטא מצליבות מוצר בקטלוג
  לאירוע גלישה לפי המזהה הזה בדיוק, כך שהשניים לא הצליבו כלום. שום דבר לא נראה שבור (הפיד תקין,
  האירועים נורים, כל צד עקבי בפני עצמו), ולכן זה שרד ארבעה מקומות. ה-slug גם לא היה מזהה תקין
  בפני עצמו: הוא ייחודי בתוך חנות ולא בפלטפורמה (מיגרציה 0001 מדדה **47 slugs משותפים בין חנויות
  שונות**), והוא נגזר מהשם — שינוי שם מוצר היה מוחק מזהה וממציא אחר, כלומר מוצר חדש בלי היסטוריית
  אישורים. **הפיד היה הצד הצודק, והאירועים זזו אליו.** המזהה נקבע עכשיו במקום אחד משותף,
  `src/lib/ad-item-id.ts`, שממנו גם הפיד וגם ארבעת האירועים בונים אותו — ל-מוצר עם וריאציות יש
  שורת פיד לכל צירוף ואין שורת אב, אז המזהה מצטמצם לצירוף שהקונה בחר. שני טסטים נועלים את זה:
  אחד מצליב את מזהה שורת הפיד האמיתית מול מה שאירוע היה שולח, והשני סורק את הקוד ומפיל כל קריאה
  שממציאה מזהה משלה.
- [x] ~~**האירוע הפנימי `add_to_cart` נרשם בלי מוצר**~~ — **נמצא ותוקן באותה בדיקה.**
  `tracking.ts` שלח ל-`/api/analytics/event` את אותו מזהה שנשלח לגוגל, והמסלול הזה מקבל **רק uuid**
  ומשמיט בשקט כל דבר אחר — כלומר כל אירוע "הוספה לסל" נרשם בלי מוצר, ולכן הרשימות
  "המוצרים שנוספו הכי הרבה" ו"ננטשו הכי הרבה" בלשונית **הנתונים באדמין היו ריקות מאז ומתמיד**.
  שום שגיאה לא נרשמה: השלב כן נספר, המוצר פשוט לא. שני המזהים מופרדים עכשיו בשדות נפרדים
  (`id` = מזהה הקטלוג, `productId` = ה-uuid), ושני הצדדים מדווחים בנפרד — פער בצד אחד לא מוחק
  את השני.

**שער לפני שמוכר ראשון נכנס — נסגר בהחלטת הבעלים (2026-08-04):**

הכלל פשוט: **אף מוכר לא משלם על פרסום לפני שחמש השכבות למעלה עברו.** שכבות 1-3 חינמיות לגמרי,
ושכבה 4 — הבדיקה היחידה שסוגרת את העניין — היא קמפיין קטן של 100-200 ₪ **מכספך שלך, על חנות שלך**.
המוכרים לא ממנים את בדיקת התקינות, ולא מקבלים גישה לבוסט לפני שהמספרים בדשבורד הוצלבו מול
Google Ads / Meta Ads Manager ונמצאו זהים.

- [x] ~~להסתיר את לשונית הפרסום או לסמן "בקרוב"~~ — **מיותר מאז שהוחלט שלא עולים לאוויר בלי פרסום
  מחובר** (ראה הכותרת של סעיף 2). זו הייתה הדרך לעלות מוקדם בלי להראות למוכר מספרי דמו; משהסעיף
  כולו חוסם השקה, אין רגע שבו מוכר אמיתי ולשונית לא-מחוברת מתקיימים יחד.

---

## 3. תשלומים / סליקה (ישראל בלבד) — 🚀 חוסם השקה, והפריט הכבד היחיד

- [ ] **לבחור ספק סליקה — PayMe מובילה, SUMIT שנייה (סקירה מלאה 2026-08-03).**
  > **תקבול יורדת.** היא נרשמה כאן כבחירה המובילה ב-2026-07-22 וזו הייתה טעות: מוצר המרקטפלייס שלה הוא
  > **תוסף WooCommerce/WCFM בלבד**, ותיעוד ה-API שלה לא כולל שום endpoint של מרקטפלייס, פיצול או פתיחת
  > חשבון-מוכר. הפרויקט הזה הוא Astro. אל תציעו אותה שוב.
  >
  > **המסגרת הרגולטורית (זה מה שקובע את הארכיטקטורה):** עמדת סגל של רשות ניירות ערך מ-25.11.2024 מבחינה
  > בין **Marketplace** (חייב רישיון שירותי תשלום) לבין **Reseller** (פטור — אבל דורש בעלות על הסחורה
  > וסיכון פיננסי מלא, כלומר הורג את המודל). הדרך השלישית, והיחידה שמתאימה: **ספק טכנולוגי בלבד** — כל
  > מוכר הוא בית עסק של גורם מורשה, והכסף אף פעם לא עובר דרך הפלטפורמה. רשת ביטחון: תקנות פטור מחובת
  > רישוי תשפ"ד-2024 פוטרות מי שהממוצע החודשי של כספים שקיבל מתחת ל-5 מיליון ₪, בכפוף להודעה לרשות.
  > הפטור הזמני שהיה למרקטפלייסים פג ב-31.12.2024.
  >
  > **מה נבדק (נקרא אינדקס ה-API הפומבי של כל אחד, לא רק עמוד השיווק):**
  > - **PayMe — מובילה.** כל המוצר שלה הוא להיות שכבת התשלומים של פלטפורמה. מצהירה במפורש על
  >   "split a transaction between multiple sellers and charge a dynamic fee" — כלומר **העמלה שלנו
  >   נגבית בתוך העסקה**, שאלה שאף ספק אחר לא ענה עליה. **0₪ מנוי, 0₪ הקמה, 0₪ משיכה לבנק**;
  >   סליקה 2.5–3% + 1.2₪; חשבוניות **0.20₪ למסמך** (פר-מסמך, אין רצפה חודשית בשום שכבה).
  >   התעריף **נסגר פר-פלטפורמה** (folyou 3%+1.2₪, מרמלדה 2.5%+1.2₪) — בדיוק היחס הסיטונאי ש-SUMIT
  >   סירבה לתת. מורשית: פאיימי בע"מ, ח.פ 515033553, רישיון שירותים פיננסיים ממשרד האוצר.
  >   רפרנס חי בישראל: folyou. ⚠️ המודל שלה הוא **ארנק/ריכוז חיובים** — הכסף עובר דרכה (היא מורשית לכך),
  >   אז לוודא שהפלטפורמה לא נמצאת בזרימה בשום שלב. ⚠️ התיעוד שלה הוא SPA שלא ניתן לקריאה — **הסכמות
  >   לא אומתו**.
  > - **SUMIT — שנייה, והיחידה שהתנהגותה אומתה בקוד אמיתי** (מקור התוסף שלהם ל-WooCommerce):
  >   `POST /billing/payments/multivendorcharge/` — קריאה **אחת**, כל פריט נושא `CompanyID`+`APIKey`
  >   של המוכר שלו, והתשובה חוזרת כ-`Data.Vendors[]` עם `Payment` ו-`DocumentID` לכל מוכר. REST רגיל,
  >   לא כבול לוורדפרס. `POST /website/companies/create/` פותח בית עסק למוכר דרך API — אף ספק אחר לא
  >   מתעד מקבילה. סליקה דרך Upay ב-1.1%. ⚠️ שתי מגבלות שנראו בקוד: **Bit מושבת כשיש מוכר בעגלה**,
  >   ולכל מוכר נדרש חשבון SUMIT משלו (מסלול חינמי קיים; 19–125₪ מעליו).
  > - **Upay** — אין API פומבי, התוסף רק מפנה לדף מתארח, והתעריף הישיר הוא 1.5% (ה-1.1% הוא של SUMIT).
  >   ה-0₪ מנוי + חשבוניות חינם אמיתיים אבל ללקוח קצה בלבד. אין כלום לפלטפורמות.
  > - **טרנזילה** — הטכנולוגיה הכי טובה (iFrame, Hosted Fields, סולק מורשה של בנק ישראל, מוצר מרקטפלייס),
  >   אבל **70–170₪ לחודש + 300–600₪ הקמה לכל בית עסק** — בלתי אפשרי על פני עשרות מוכרים.
  > - **Paddle** — Merchant of Record, היה פותר את הרגולציה לגמרי, אבל **לא תומך במוצרים פיזיים ולא
  >   במוכרי צד ג׳**. פסול.
  > - **PayPlus / EasyCard / קארדקום / Grow** — אף אחד לא מתעד יצירת בית עסק ב-API ולא פיצול תשלום.
  >   ה-0₪ של EasyCard לא כולל API (69₪+199₪). ל-Grow אין מסלול אפס.
  >
  > **הארכיטקטורה שנסגרה, והיא זהה בכל ספק — אפשר לבנות עליה בלי להכריע:**
  > (1) מוכר = ישות שמחזיקה פרטי סליקה משלה, הפלטפורמה שומרת אותם; (2) הפלטפורמה **אף פעם לא מקבלת
  > כספים** → אינה נש"פ; (3) המוכר משלם לספק **ישירות** — אסור לגבות ממנו ולהעביר הלאה, זו העברת כספים
  > עבור צד שלישי וזה בדיוק טריגר הרישיון; (4) ההטמעה היא **iFrame / Hosted Fields**, אף פעם לא
  > server-to-server (זה היה מכניס אותנו לתחולת PCI-DSS המחמירה); (5) אשף ההרשמה חייב **שני מסלולים** —
  > "כבר יש לי חשבון, חברו אותו" ו"פתחו לי חדש"; בלי הראשון כל מוכר ותיק נאלץ למערכת הנהלת חשבונות
  > שנייה; (6) `multivendorcharge` מוכיח שקליק אחד לכל החנויות נשאר אפשרי — צ׳קאאוט פר-חנות הוא הגיבוי
  > והוא גם בטוח (אין הזמנה משולמת-חלקית).
  >
  > **מה שנשאר פתוח — ולמה אי אפשר לסגור עכשיו:** הספק המדויק והתעריף. אף ספק לא ייתן תנאים אמיתיים
  > בנפח אפס מוכרים. לחזור לזה כשיש **ח.פ + דמו + מוכרים בהמתנה** — ואז יש גם מיקוח. (ח.פ נדרש להשקה
  > בכל מקרה, כדי לגבות 99₪ + 12%, ולא בגלל הסליקה.)
  >
  > **שאלות פתוחות פר-ספק:**
  > *PayMe* — האם ההרשמה יכולה להיות מוטמעת אצלנו ולא באתר שלהם; מה התעריף בעשרות ואז מאות מוכרים;
  > האם החשבוניות מחוברות ל**חשבוניות ישראל / מספר הקצאה**; האם הפלטפורמה נמצאת בזרימת הכספים.
  > *SUMIT* — ההתנהגות האמיתית של `multivendorcharge` (התמיכה סתרה את התיעוד שלהם וטענה "2 בקשות
  > נפרדות"); זמינות `companies/create`; איזה מסלול הוא "מסלול פטור החינמי"; האם אפשר לנכות עמלת
  > פלטפורמה בתוך החיוב; חיבור לחשבוניות ישראל; והאם השבתת Bit היא מגבלת API או רק של התוסף.
  > (תוכנית השותפים שלהם היא **תוכנית הפניה** — 20% ממה שמוכר מפנה משלם, לכל החיים, אך ורק על מוכרים
  > במסלול בתשלום. לא תוכנית ISV.)
- [ ] **`store.config.ts` → `checkout.provider`** — היום `'stripe'` (placeholder). Stripe לא בשימוש בישראל למודל הזה.
- [ ] **webhook `/api/payment/confirm`** — עדיין לא קיים. **קריטי:** היום `src/lib/orders.ts` קובע `paymentStatus: 'paid'` מיד עם יצירת ההזמנה (stub זמני, יש הערה בקוד בשורה ~69). ברגע שה-webhook קיים — **רק הוא** רשאי לקבוע "שולם". חובה טסט Vitest שמכסה את זה.
  > **⚠️ כשבונים אותו — חייב לקרוא ל-`settleStoreClosure(store.slug)` אחרי עדכון סטטוס התשלום**
  > (נמצא 2026-07-31 בסקירת דיף). `orderBlocksStoreClosure` (order-status-rules.ts) נגזר משני
  > השדות — `paymentStatus` **וגם** `shippingStatus` — אבל היום `settleStoreClosure` נקרא רק
  > ממסלול שינוי סטטוס **המשלוח** (`/api/seller/orders`), כי זה השדה היחיד שמישהו כותב אליו
  > אחרי הצ׳קאאוט. **זה נכון היום ונשבר ברגע שה-webhook קיים:** תשלום שעובר ל-`failed` מפסיק
  > לחסום סגירת חנות, ואם אף אחד לא יקרא ל-settle — חנות של מוכר שביקש לסגור תישאר תקועה
  > במצב `closing` לנצח (או עד ששאר ההזמנות ישנו סטטוס משלוח במקרה). לא נבנה עכשיו כי אין ספק
  > סליקה ואין מי שכותב ל-`paymentStatus`.
  > **ואם ה-settle שם באמת סוגר חנות — לשלוח גם את מייל הסגירה** (`sendStoreLifecycleEmail`
  > עם `state: 'closed'`), בדיוק כמו ב-`/api/seller/orders`. אחרת החנות תיסגר בשקט והמוכר
  > יגלה את זה רק כשייכנס אליה.
- [ ] **מעקב יתרות מוכרים** (`SellerBalance`, לתצוגה בלבד) — עדיין לא קיים.
- [ ] **להסיר את `MOCK_DECLINE_MARKER` עם החלפת הספק.** בדב, מייל קונה שמכיל `+decline@` גורם
  לספק המדומה לדחות — כדי שמסלול הכישלון (החזרת מלאי, שחרור המפתח, רישום ביומן, הודעה לקונה)
  יהיה בר-הרצה ולא רק בר-טסט. מגודר ב-`import.meta.env.DEV`. כשהספק האמיתי נכנס הוא מביא
  כרטיסי-בדיקה משלו והמחלקה כולה נמחקת.
- [x] **הגנה מחיוב כפול — נבנתה (2026-07-29).** הלקוח מייצר `idempotencyKey` פר-ניסיון-קנייה
  ושומר אותו ב-sessionStorage (`src/lib/checkout-attempt-key.ts`), כלומר **הוא נשמר גם אחרי
  ניסיון שנכשל** — זה בדיוק המקרה המסוכן: הבקשה הצליחה בשרת, התשובה אבדה, והקונה לוחץ שוב.
  השרת (`src/lib/checkout-idempotency.ts`) עושה claim→complete: קנייה שכבר הושלמה מוחזרת
  כמו שהיא בלי לחייב, ובקשה מקבילה מקבלת 409. `tests/checkout-idempotency.test.ts` מכסה.
  **מה עוד צריך כשהספק מחובר:** (1) להעביר את `idempotencyKey` לספק — השדה כבר קיים ב-
  `PaymentRequest` ומועבר, צריך רק למפות אותו לשדה של הספק, כדי שגם הוא ידחה חיוב חוזר;
  (2) ה-webhook `/api/payment/confirm` צריך idempotency משלו על מזהה העסקה של הספק.
  ~~(3) instance אחד בלבד עד המעבר ל-DB — ההגנה נשענת על `Mutex` בתוך תהליך יחיד.~~
  **בוטל 2026-08-03, והשורה הזאת נשארה מיושנת עד 2026-08-04:** ה-`Mutex` נמחק. ה-claim הוא היום
  משפט `INSERT … ON CONFLICT` יחיד שמספר השורות שהוא משנה **הוא** התשובה, כלומר נכון בכל מספר
  instances — זו בדיוק הייתה מטרת המעבר (`DB_MIGRATION_PLAN.md` §4/§7.5). **אין יותר מגבלת
  instance אחד.**
- [x] **יומן אירועים כספיים — נבנה (2026-07-29).** `src/lib/money-events.ts`, append-only:
  ניסיון חיוב (כולל דחייה), יצירת הזמנה, חיוב כפול שנמנע, שינוי סטטוס, שינוי סכום.
  נצפה בלשונית "יומן כספי" באדמין, יחד עם בדיקת-הצלבה חיה (`src/lib/reconcile.ts`) שמחשבת
  כל מספר בשתי דרכים עצמאיות ומדווחת על פערים. **כשהסליקה חיה:** להוסיף `payment_status_changed`
  מה-webhook ואירוע זיכוי/החזר — הטיפוסים כבר מוגדרים.
- [x] **אחוז העמלה על מכירות** — **אין** ואסור שיהיה מספר גלובלי ב-`store.config.ts` (יש שם הערה מפורשת):
  העמלה היא **פר-מוכר** לפי מדרגת התמחור שלו (`src/lib/pricing.ts` → `Seller.tier`). הניכוי בפועל קורה
  אצל ספק הסליקה.
- [ ] **"הזמנת בדיקה" למוכר — לבנות יחד עם הספק, לא לפניו (הוחלט 2026-07-29).** מוכר לא יכול לקנות מהחנות שלו: `/api/checkout` מחזיר `403 own-store`, והחנות חוסמת את זה גם בצד לקוח (`src/lib/own-store-guard.ts`). זה נכון — הזמנה כזו הייתה אמיתית לכל דבר (מלאי, עמלה, מיילים, יחידות שמזינות את תווית `popular`/`bestseller` בפיד גוגל/מטא, והמכירה הראשונה שמתחילה את החיוב החודשי). אבל למוכר עדיין צריכה להיות דרך לבדוק את מסלול הקנייה מקצה לקצה, וזה מה שחסר. **מה לבנות כשהספק מחובר:** כפתור "הזמנת בדיקה" בדשבורד שיוצר הזמנה בסימון `isTest`, דרך ה-test mode של הספק (בלי חיוב אמיתי), ושהדגל הזה **יוציא אותה מ**: ההכנסות באדמין ובדשבורד, `purchasedUnits` שמזין את תוויות הפרסום, והטריגר של "מכירה ראשונה" שמתחיל את דמי המנוי. לפני שיש ספק אמיתי אין לזה על מה לרוץ — היא תהיה זהה להזמנה רגילה עם דגל, כלומר עבודה שתיזרק.
- [ ] **קנייה עצמית של מוכר מנותק — לסגור בצד הספק.** החסימה למעלה עובדת על מוכר **מחובר**; מי שיתנתק או יפתח חלון פרטי יוכל לקנות מעצמו, ואין שם זהות לבדוק מולה בקוד. כשהסליקה חיה: לזהות התאמה בין הכרטיס/ח״פ של הקונה לאלה של המוכר בצד הספק. לא דחוף — נזק כספי אין (הוא משלם לעצמו פחות עמלה), הסיכון היחיד הוא ניפוח מלאכותי של תוויות הפרסום.
- [ ] **`ads.boostCommissionPercent`** — היום `null` (לא הוחלט). זהו **המרווח שלך מתוך הוצאת הפרסום של המוכר** (בוסט) — המוכר משלם על מה שרץ, ואתה גוזר אחוז מזה, השאר קונה פרסום אמיתי (מוצג למוכר כ"דמי פלטפורמה"). כשמחליטים על האחוז: לעדכן כאן ב-`store.config.ts`, להציג את המספר בהערת "דמי פלטפורמה" בלשונית הפרסום, ולחשב אותו ב-Capture בפועל (auth/capture, ראה מודל התמחור). כרגע כל הפרסום mock — אין גבייה אמיתית.

---

## 4. מיילים — 🚀 חוסם השקה (ערב עבודה)

**המנוע כבר בנוי (code-complete).** ספק נבחר: **Resend** (מחקר 2026-07-24 — SDK נקי ל-Node/TS, free 3,000/חודש, $20/50K; מעבר ל-Amazon SES רק בנפח ~500K+/חודש). מודול `src/lib/email/` הוא adapter נקי — החלפת ספק = קובץ אחד. אוטומציה ראשונה שרצה: **אישור הזמנה לקונה + התראה למוכר** מיד אחרי צ׳קאאוט (מקובץ לפי חנות, תמונות, RTL). בלי מפתח → נופל ל-console (dev). מה שחסר כדי שישלח **באמת** בעלייה לאוויר:

- [ ] **דומיין שולח מאומת ב-Resend — ורשום אותו כ-תת-דומיין, `send.dezabin.co.il` (נוסף 2026-08-05).** לפתוח חשבון Resend, להוסיף את הדומיין, ולהעתיק את רשומות ה-DNS שהם נותנים.
  **⚠️ למה תת-דומיין ולא הדומיין הראשי — מלכודת שנשברת בשקט:** לדומיין מותרת **רשומת SPF אחת בלבד**. ב-DNS של דזבין כבר יושבת אחת עבור תיבות הדואר (`v=spf1 a mx include:_spf.myinbox.co.il ~all`, שירות myinbox שנמכר עם הדומיין), והוספת `v=spf1` שנייה **פוסלת את שתיהן** — גם המייל האישי וגם מיילי האתר מתחילים ליפול לספאם, בלי שום הודעת שגיאה. לתת-דומיין יש SPF משל עצמו, כך שההתנגשות לא קיימת, והמוניטין של הדיוור התפעולי מופרד מהמייל האישי (זו גם ההמלצה של Resend עצמם). האלטרנטיבה — למזג `include:` נוסף לתוך הרשומה הקיימת — עובדת, אבל היא רשומה אחת שתי מערכות נשענות עליה.
  אז `EMAIL_FROM` יהיה על תת-הדומיין (`Dezabin <no-reply@send.dezabin.co.il>`), בעוד `replyTo` ממשיך להיות הכתובת העסקית שאתה קורא.
- [ ] **להזין רשומות DNS: SPF + DKIM + DMARC** אצל רשם הדומיין. **זה הדבר שבאמת קובע הגעה לתיבות ישראליות** (Gmail/Walla/Outlook) — בלי אימות תקין המיילים ייפלו לספאם. זהה בכל ספק.
- [ ] **למלא ב-`.env`: `RESEND_API_KEY`** (מ-Resend) **ו-`EMAIL_FROM`** (כתובת על **תת-הדומיין** המאומת — `Dezabin <no-reply@send.dezabin.co.il>`, ולא על הדומיין הראשי; הסיבה בסעיף שמעל, התנגשות SPF).
- [ ] **לבדוק בפועל** — לבצע הזמנת-בדיקה ולוודא שהמיילים מגיעים לתיבה (לא ספאם) ונראים תקין במובייל.

> **חשבוניות/קבלות — לא באחריות המייל הזה.** מייל אישור-ההזמנה מצהיר במפורש שהוא *אינו* חשבונית מס. הקבלה/חשבונית החוקית מונפקת בנפרד ע"י **המוכר** (העוסק של המכירה) — בפועל אוטומטית דרך **ספק הסליקה** (PayMe ו-SUMIT מנפיקים חשבונית מס/קבלה כחלק מהתשלום — ראה §3). ראה סעיף התשלומים + הפריט "חשבונית מע"מ על דמי הפלטפורמה" ברשימת המשימות ב-CURRENT_TASK (קבוצה א׳).

**מיילי מעבר-סטטוס — בנוי (code-complete).** "מוכן"/"נשלח" (עם מספר מעקב)/"בוטל" לקונה, נתלים על הצינור source-agnostic הקיים (`notifyOrderStatusChanged`). מגיע לכל קונה כולל **אורחים** (בניגוד להתראה בתוך-האתר, שרק לרשומים). קופסת-הקוד: `email/order-status-email.ts` + copy משותף עם ההתראה הפנימית ב-`order-status-copy.ts`. כשה-Sendit יחובר, ה-webbook שלו קורא לאותה פונקציה → המייל יוצא לבד. **חסר רק אותו חיבור ספק (מפתח+דומיין למעלה) כדי שישלח בפועל.**

---

## 5. משלוחים (ישראל בלבד) — ⏭ אחרי ההשקה (המחיר הקבוע עובד עד אז)

- [ ] **⚠️ מדיניות החזרות + זמני אספקה — שתי החלטות עסקיות שלך, וחוסמות שדות שכבר מוכנים בקוד (2026-08-04).** אלה לא משימות פיתוח: צריך את המספרים.
  1. **החזרות:** כמה ימי החזרה, ומי משלם על משלוח ההחזרה. החוק הישראלי (חוק הגנת הצרכן) נותן רצפה, המדיניות בפועל היא שלך. **ברגע שיש מספרים** → להוסיף `hasMerchantReturnPolicy` ל-JSON-LD של המוצר (ליד `shippingDetails` שכבר שם) + עמוד מדיניות באתר. בלי זה Google לא מציגה merchant listing מלא, ומנועי-AI לא ממליצים על מוצר שלא ידוע אם אפשר להחזיר.
  2. **זמן אספקה:** כמה ימי עסקים מהזמנה ועד מסירה (טיפול + משלוח). **מגיע עם Sendit** — ואז להוסיף `deliveryTime` בתוך `offerShippingDetails()` ב-`src/lib/shipping.ts` (יש שם הערה שמסבירה למה הוא חסר בכוונה). **לא להמציא מספר בינתיים:** בנתונים מובנים ניחוש הופך להבטחה פומבית.
- [ ] **שדה "משקל" למוצר** (בגרמים) — עדיין לא קיים. דרוש לחישוב מחיר משלוח אמיתי לפי משקל.
- [ ] **שילוב עם חברת משלוחים (Sendit)** — מחיר משלוח אמיתי לפי כתובת+משקל במקום המחיר הקבוע הידני.
- [x] ~~מסך בדשבורד המוכר להזנת מחיר משלוח / סף חינם~~ — **בוטל 2026-07-27, לא משימה.** המחירים
  פלטפורמתיים (`SHIPPING_RATES` ב-`src/lib/shipping.ts`), המוכר לא מתמחר משלוח ולא מרוויח ממנו;
  `flatRate`/`freeAbove` הוסרו מהמודל. הלֶוֶר היחיד שלו הוא איסוף עצמי, וכבר יש לו צ׳קבוקס בהגדרות.
- [ ] **לחבר את ה-webhook של הספק לצינור-הסטטוס** — צינור עדכון-הסטטוס בנוי **אדיש-למקור** בכוונה (אותה פונקציה נקראת בין אם המוכר לחץ ובין אם הספק דיווח). כשמחברים את Sendit: (1) כפתור "צור משלוח" שקורא ל-API → מפיק מספר-מעקב + מדבקת PDF **וגם** מודיע לספק שיש חבילה הממתינה לאיסוף (פעולה אחת). כאן חוזר הסטטוס `ready` = "ממתין לאיסוף שליח" — **מצב שהספק מדווח עליו, לא כפתור ידני** (הוסר מהדרופדאון של המוכר עד אז, כי בלי ספק אין לו כיסוי אמיתי); השליח נאסף → הסטטוס עובר ל-`shipped` דרך ה-webhook; (2) endpoint `/api/shipping/webhook` שמקבל את דיווחי הספק (נאספה→בדרך→נמסרה) וקורא **לאותה** פונקציית עדכון-סטטוס → `delivered` מתעדכן לבד. חובה אימות מקור + idempotency על ה-webhook (כמו webhook התשלום).
- [ ] **שיטות משלוח (`deliveryMethod`) — שלוש, הקונה בוחר בצ'קאאוט. ⚠️ השלד כבר בנוי (נבדק
  2026-08-04):** בורר-השיטה קיים בצ'קאאוט (`checkout.astro` → `availableDeliveryMethods`),
  המחיר לכל שיטה מגיע מ-`SHIPPING_RATES`, `/api/checkout` מאמת את השיטה מחדש בשרת ושומר
  `deliveryMethod` על ההזמנה, ולמוכר יש צ׳קבוקס איסוף-עצמי. **מה שבאמת חסר כאן = בורר נקודת
  האיסוף (הרשימה מגיעה מ-Sendit) והסתעפות הסטטוסים לפי השיטה.** התיאור למטה הוא היעד המלא:
  1. **שליח עד הבית** — צריך כתובת; השליח מביא לדלת → `delivered`.
  2. **נקודת איסוף / לוקר** — השליח מביא לנקודה, הקונה אוסף. **רשימת הנקודות מגיעה מ-Sendit** (בורר-נקודה בצ'קאאוט). זרימה: `shipped` → "הגיעה לנקודה — ממתינה לך" → `collected` (נאסף מהנקודה). זול יותר משליח.
  3. **איסוף עצמי מהחנות** — בלי שליח/ספק כלל; הקונה בא למוכר. זרימה: `ready` = "מוכנה לאיסוף" (חיווי "בוא לאסוף" + כתובת/שעות) → המוכר מסמן `collected`.
  נוגע ב: **צ'קאאוט** (בורר-שיטה + בורר-נקודה), **הגדרות המוכר** (רק האם הוא מציע איסוף עצמי — המחירים פלטפורמתיים ואינם שלו), **זרימת הסטטוס** (מסתעפת לפי השיטה). צינור-החיוויים אדיש-למקור כבר בנוי — רק צריך את הסתעפות שיטת-המימוש (`deliveryMethod`) + ה-UI.

---

## 6. מסד נתונים (Postgres בלבד — `data/*.json` נמחקו 2026-08-03) — 🚀 חוסם השקה

> **התוכנית המלאה נמצאת ב-`DB_MIGRATION_PLAN.md`** (נכתב 2026-07-27) — בחירת ה-DB ולמה, הסכימה טבלה-טבלה,
> רשימת האינדקסים המדויקת, 12 מקרי קצה שנמדדו מול הדאטה בפועל, סדר העבודה ובדיקות האימות.
> הסעיף כאן נשאר כתזכורת ברשימת העלייה לאוויר; הפירוט שם.

- [x] **הספק נבחר (2026-08-02): Neon, אזור `eu-central-1` (פרנקפורט).** הנימוק המלא + טבלת ההשוואה ב-`DB_MIGRATION_PLAN.md` §2.1.
- [x] **הסכימה והאינדקסים נבנו (2026-08-02), וכל הקוד עבר (שלבים 2+3, 2026-08-03).** כל הקבצים
  ב-`migrations/` (0001 הוא הסכימה; נכון ל-2026-08-04 עד 0009) מורצים ע"י `npm run db:migrate`, ומאומתים
  ב-`tests/db-import.test.ts`. `db:migrate` מריץ תמיד את מה שטרם רץ, כך שאין צעד חדש לזכור
  פר-מיגרציה — אבל **כן צריך להריץ אותו שוב אחרי כל משיכה של קוד חדש**, לא רק בהקמה.
  **`data/*.json` נמחקו ו-`npm run db:import` נמחק איתם** — המסד הוא מקור האמת, וייבוא חוזר היה
  דורס ערכים חיים בישנים ומחייה שורות שהאפליקציה מחקה. אין יותר דרך להריץ אותו בטעות.
- [x] **פרויקט Neon פתוח ובשימוש (2026-08-03).** המיגרציות רצו עליו וכל מדידות §9 ב-`DB_MIGRATION_PLAN.md`
  (אינדקסים, 50 קונים בו-זמנית, עומס בסיסי) בוצעו מולו — לא מול מסד מקומי.
- [ ] ⚠️ **פעולה שלי — אותו חיבור בסביבת השרת בפרודקשן, כשיהיה שרת:**
  1. מחרוזת החיבור ה**מאוגדת** (ה-hostname מכיל `-pooler`) — לא הרגילה. בלי pooler
     השרת ימצה את מגבלת החיבורים תוך דקות תחת עומס אמיתי (`DB_MIGRATION_PLAN.md` §7.16).
  2. `DATABASE_URL=…` בסביבת השרת (ולא בקוד), ואז `npm run db:migrate`. אין שלב ייבוא — הוא נמחק.
- [ ] **שרת האפליקציה חייב לשבת באותו אזור כמו ה-DB** (פרנקפורט). ההשהיה שעולה כסף היא בין השרת ל-DB —
  כל עמוד מריץ 5–15 שאילתות; לגולש זה סיבוב אחד לבקשה. שרת בישראל + DB בפרנקפורט = הגרוע משני העולמות.
- [ ] **להריץ `REVOKE UPDATE, DELETE ON money_events FROM <app_role>` בפרודקשן** — היומן הפיננסי הוא
  append-only, ויומן שאפשר לשכתב לא מוכיח כלום. לא במיגרציה כי שם התפקיד תלוי-סביבה.
- [ ] **אחרי שהמסד בפרודקשן חי (אין שלב ייבוא — הוא נמחק): `EXPLAIN ANALYZE` על ארבע השליפות החמות** (§9.2) ובדיקת 50 רכישות מקבילות (§9.5) —
  שתיהן דורשות שרת אמיתי ולכן לא מכוסות בטסטים.
- [ ] ⚠️ **החלטה שלך — Neon נרדם, והתעוררות ארוכה מ-5 שניות היא 500 לגולש (נמדד 2026-08-03).**
  ביומן השגיאות של האדמין יש היום שתי שגיאות `timeout exceeded when trying to connect`, אחת על
  `/` ואחת על `/seller/dashboard`. הן **לא** מיצוי של ה-pool: `max_connections` בנקודה המאוגדת הוא
  901 ובזמן הבדיקה היו 17 חיבורים בסך הכל. מה שכן — `pg_postmaster_start_time()` הראה שה-compute
  עלה מחדש ב-12:01, כלומר הוא **היה מושהה**, ושתי השגיאות מוקדמות ממנו. זה scale-to-zero:
  הבקשה הראשונה אחרי חוסר פעילות ממתינה להתעוררות, ו-`connectionTimeoutMillis: 5_000` ב-`db.ts`
  מוותר לפניה. חיבור חם נמדד ~500ms, כלומר בזמן ער אין בעיה בכלל.
  **✅ נפתר בקוד (2026-08-03) — אין מה לשלם ואין מה להגדיר.** נבחרה האפשרות שאין לה מחיר: ניסיון
  חוזר אחד **רק על השגת החיבור** (`db.ts#connectWithWakeRetry`). זו הפעולה היחידה בשאילתה שמותר
  לנסות פעמיים — עוד לא נשלח שום משפט, אז שום דבר לא יכול היה לקרות חצי; שאילתה שנכשלה **אחרי**
  שהגיעה לשרת עלולה הייתה להתבצע, וחזרה עליה הייתה כתיבה שנייה. התעוררות הפכה מ-500 לעמוד איטי.
  **ו-pool מלא מקבל טיפול הפוך**: אותה הודעת שגיאה בדיוק, אבל שם ניסיון חוזר רק מכפיל את ההמתנה
  ומבטל את ה"כישלון המהיר" — לכן הוא נדחה כשכל הסלוטים תפוסים.
  *מה שלא נדרש:* לכבות autosuspend ב-Neon עולה כסף ואינו נחוץ — עם תנועה אמיתית ה-compute ממילא
  לא מגיע לחוסר פעילות, וזו בעיקר תופעה של סביבת פיתוח ושל אתר בלי מבקרים. להאריך את ה-timeout
  נדחה: הוא קיים כדי למנוע ערימת בקשות כשה-pool באמת מלא.

---

## 6.1 סנכרון מלאי חיצוני — תזמון אוטומטי (נבנה 2026-07-25, **אוטומטי מאז 2026-08-03**) — ✅ סגור

> נבנה: פאנל "סנכרון מלאי חיצוני" בדשבורד המוכר — מיפוי עמודות מפיד זר + זיהוי לפי מק"ט
> (`feed-mapping.ts`, `store-products-import.ts`, `/api/store-product/feed-sync`, `feed-fetch.ts`).
> עובד דרך **העלאת קובץ**, **כפתור "סנכרן עכשיו"**, ומאז שלב 4א גם **לבד, כל שעה**.

- [x] **תזמון אוטומטי — בוצע 2026-08-03** (`DB_MIGRATION_PLAN.md` §8 שלב 4א). ה-scheduler
  (`src/lib/jobs/`) מושך כל שעה את הפיד של כל חנות עם `feedSync.url` **שרשאית למכור** (חנות חסומה,
  סגורה או מושהית מדולגת). הרצף עצמו ירד ל-`lib/store-feed-sync.ts` כדי שהמסלול וה-job יריצו את
  אותו קוד — job שהיה שולח בקשת HTTP לשרת של עצמו היה צריך עוגיית session שאין לו. מי מבין
  ה-instances מריץ בפועל נקבע בטבלת `job_runs` (מיגרציה 0007), לא בקונפיגורציה, אז אפשר להשאיר את
  ה-scheduler דלוק בכולם. **לא צריך cron חיצוני ולא שירות נוסף.**
- [x] **SSRF — פנייה ל-IP שנבדק (pin) — בוצע 2026-08-03.** הכתובת שעברה את הבדיקה היא הכתובת
  שמתחברים אליה (`lookup` מוצמד ל-socket), כלומר חלון ה-DNS-rebinding נסגר לפני שזה רץ בלי אדם.
  TLS לא נפגע — השם עדיין מה ש-SNI נושא ומה שהתעודה נבדקת מולו. הפניות (3xx) נדחות, יש תקרת גודל
  שעוצרת תוך כדי הזרם ולא נשלח `Accept-Encoding`.

**אין כאן שום צעד ידני, וזה היה תיקון (2026-08-03).** שני הדברים שנרשמו כאן לרגע כמשימות שלך
נסגרו בקוד במקום:
- [x] **מיגרציה 0007 כבר הורצה על ה-Neon החי** (`npm run db:migrate`, 2026-08-03). `db:migrate` הוא
  ממילא חלק מכל דיפלוי (סעיף 6) — אין פה צעד חדש לזכור.
- [x] **`SCHEDULER_ENABLED` אינו צריך טיפול — ברירת המחדל היא ה-build.** `npm start` (השרת הבנוי)
  מריץ jobs, `npm run dev` לא. זה נכתב כך בדיוק כי `.env` המקומי מחזיק את פרטי ה-Neon האמיתיים:
  ברירת מחדל של "דלוק בכל מקום שיש בו `DATABASE_URL`" הייתה אומרת ששרת הפיתוח מושך את הפיד של כל
  מוכר מהרשת הביתית וכותב לקטלוג הפרודקשן — מוגן רק על ידי שורה ברשימה, במכונה היחידה שבה שורות
  ברשימה נשכחות. הדריסה קיימת לשני הכיוונים (`1` להריץ בפיתוח כשעובדים על job, `0` להשתיק instance
  בנוי שחולק את המסד ואינו השרת), ושתיהן נדירות.

---

## 6.2 חנויות תצוגה (מוחלט 2026-07-28, **נבנה 2026-07-28**) — 🚀 חוסם השקה

> **למה בכלל:** מוכר ראשון צריך לראות חנות גמורה כדי להבין למה הוא נכנס, והספוטלייט בדף הבית
> הוא בלוק סטטי בלי לפחות שתי חנויות (החצים + ההחלפה האוטומטית עטופים ב-`feed.spotlight.length > 1`,
> נבדק בקוד). שתי הבעיות נפתרות באותו דבר — לכן **3** חנויות, לא אחת.

**בנוי ונאכף בקוד** (`src/lib/demo-stores.ts` + `tests/demo-stores.test.ts`) — דגל `demo?` על `Store`,
`noindex`, מחוץ ל-sitemap/פיד/IndexNow (מוצרי דמה ב-Merchant Center = בעיית מדיניות מול גוגל),
צ׳קאאוט חסום בצד השרת (הסל דווקא עובד), לא נספרות בסף `isLaunchMode`, ומוסתרות מהקונים לבד ברגע
שיש 5 חנויות אמיתיות (הקישור למוכרים "ראה חנות לדוגמה" נשאר תמיד).

**מה נשאר ידני לפני עלייה לאוויר:**
- [ ] **להריץ `npm run seed:showcase` על הסביבה האמיתית** (דורש `DATABASE_URL` + אינטרנט לתמונות).
  שלוש החנויות (`showcase-fashion` / `showcase-tech` / `showcase-home`, בבעלות
  `showcase@dezabin.co.il`) לא קיימות שם מעצמן. הסקריפט כותב ל-Postgres מאז 2026-08-02
  (`DB_MIGRATION_PLAN.md` §8) — עד אז הוא כתב ארבעה קבצי JSON שכבר לא נקראים, כלומר רץ, דיווח
  הצלחה ולא יצר כלום. הרצה חוזרת מחליפה את הסט הקודם בטרנזקציה אחת, ובלי `DATABASE_URL` הוא
  נעצר עם הודעה במקום להצליח בשקט. הסרה: `npm run seed:showcase -- --clean`.
- [ ] **לשנות את סיסמת חשבון הפלטפורמה** (`showcase1234` בסקריפט).
- [ ] **לוודא שהן אכן נעלמו** אחרי 5 חנויות אמיתיות (לא צריך למחוק אותן ידנית).
- [ ] **ליטוש התוכן והמראה של שלוש החנויות** — הבעלים (2026-08-02): "נראות נורא, אי אפשר לצאת ככה".
  זה הדבר הראשון שמבקר רואה ביום הראשון, והספוטלייט לא יכול לעמד בלעדיהן. עבודת עיצוב/תוכן
  בפני עצמה, לא משימת תשתית — לתת לה סשן משלה לפני שסוגרים את סעיף הזריעה למעלה.

### 6.2.1 איפוס למצב יום-ההשקה — הפער שנמדד (2026-08-02)

**שני ה-`--clean` לא מספיקים כדי לנקות דאטת דמו מסביבה.** נמדד מול Neon: מחיקת חנות גוררת בקסקייד
את המוצרים, התמונות, הווריאנטים, הקטגוריות, הקמפיינים, ההודעות, המועדפים וכל היסטוריית הצפיות
(62,708 שורות), ו-`purgeOrdersOfStores` מוחק את ההזמנות במפורש. אבל **לארבע טבלאות אין FK בכלל
ולכן הן שורדות כל ניקוי:**

| טבלה | נמדד | מה זה נראה ביום ההשקה |
|---|---|---|
| `money_events` | 278 | הכנסות מומצאות ביומן הכספי של האדמין |
| `analytics_visitors` / `_products` / `_daily` | 584 / 215 / 47 | משפך ונתוני תנועה מומצאים בלשונית הנתונים |
| `error_log` | 320 | התראות ישנות (יש "נקה יומן" באדמין — הקל שבהם) |
| `notifications` | 10 | התראות יתומות |

שתי הראשונות הן בדיוק המספרים הגדולים שהדשבורד מציג. **צריך פקודה אחת** שמביאה סביבה למצב
יום-ההשקה: בלי דאטת דמו, עם שלוש חנויות הראווה, ובלי מספר מומצא בשום מסך — ואז לפתוח את דף הבית
ואת האדמין ולוודא בעיניים, כי `launch-mode.ts` קורא את מספר החנויות ה**אמיתיות** והמסך משתנה עם הנתונים.

**נדחה במכוון ולא נשכח:** הדאטה הקיים שימושי לפיתוח (בלי 924 מוצרים ו-207 הזמנות אי אפשר לראות
איך מסכים מתנהגים בעומס), ו-`?preview=` למטה כבר מראה את מצב ההשקה בלי לאפס דבר. הסקריפט גם
יהיה מדויק יותר אחרי ליטוש חנויות הראווה, כי רק אז ידוע מה בדיוק אמור להישאר.

> בדיקה בפיתוח: `?preview=empty` (0 חנויות אמיתיות), `?preview=launch` (2), `?preview=blank` (בלי כלום).

---

## 7. עדכון גרסה בפרודקשן בלי נפילות — ⏭ מתגלגל, מתחיל אחרי שהאתר חי

- [x] **הגבלת קצב על התחברות — נבנה 2026-08-04.** `src/lib/rate-limit.ts` + טבלת `auth_attempts`
  (מיגרציה 0009), מחובר לשלושת המשטחים: כניסת מוכר, הרשמת מוכר וכניסת אדמין. המונה יושב ב-Postgres
  ולא בזיכרון התהליך — מונה מקומי מכפיל את התקרה בכל instance נוסף ומתאפס בכל deploy. שני דליים
  לכל ניסיון: לפי חשבון (8 כשלונות / 15 דק׳) ולפי מקור (30, רופף בגלל NAT משותף); אדמין 5 בלבד.
  הבדיקה קורית **לפני** אימות הסיסמה, וכניסה מוצלחת מוחקת את הספירה. `tests/rate-limit.test.ts`.
  **פעולה שנשארה לך (ביום ההעלאה):** להגדיר `TRUST_PROXY_IP=1` בסביבת השרת אם ורק אם האפליקציה
  נגישה **רק** דרך פרוקסי/Cloudflare — ראה `src/lib/client-ip.ts`, שם כתוב מה נשבר בכל אחד משני
  הכיוונים.
- [ ] **טוקן CSRF על מסלולים שמשנים נתונים** — הקוקי היום הוא `httpOnly` + `sameSite:lax`, מה שחוסם
  POST מאתר זר בכל דפדפן מודרני, אז זה **לא חור פתוח** אלא שכבה שנייה. להוסיף כשיש סליקה אמיתית.
- [ ] **חובה לפני העלייה הראשונה: להגדיר `AUTH_SECRET`** (מחרוזת אקראית ארוכה — למשל `openssl rand -hex 32`)
  במשתני הסביבה של השרת. **מ-2026-07-29 השרת מסרב לרוץ עם ברירת המחדל של הפיתוח בפרודקשן** ולא נופל
  חזרה אליה. הסיבה: ברירת המחדל ההיא היא מחרוזת גלויה בקוד המקור, וכל מי שקורא
  את הריפו היה יכול לחתום קוקי סשן של כל מוכר — עקיפת אימות מלאה. כשלון רועש עדיף על
  חתימה שקטה במפתח ציבורי. (`src/lib/seller-auth.ts#secret`, מכוסה ב-`tests/seller-auth-password.test.ts`.)
  **⚠️ תוקן 2026-07-30 — השורה הזו הבטיחה משהו שלא היה נכון:** הקוד קרא `import.meta.env.AUTH_SECRET`,
  וזו לא קריאה בזמן ריצה אלא **הדבקת טקסט בזמן ה-build**. כלומר build שנעשה בלי המשתנה הקפיא
  `undefined` בתוך החבילה, המהדר קיפל את כל הבדיקה ל-`function secret(){ throw }` קבוע, ומאותו רגע
  **כל בקשה שנגעה בסשן נפלה בשרת החי — לא משנה מה כתוב במשתני הסביבה שלו** (המבקר פשוט הופנה
  ל-/404, בלי שום סימן שמדובר בסוד חסר). נמדד מול `astro build` אמיתי. עכשיו כל משתנה שרתי נקרא דרך
  `serverEnv`/`requiredSecret` (`src/lib/runtime-env.ts`) — ישירות מ-`process.env` של התהליך החי —
  כך שהגדרה במשתני הסביבה של השרת באמת עובדת, והחלפת סוד היא restart ולא build מחדש.
  סריקה על כל `src/` ב-`tests/runtime-env.test.ts` מונעת חזרה לצורה הישנה. (המשתנים שתוקנו:
  `AUTH_SECRET`, `ADMIN_SECRET`, `RESEND_API_KEY`, `EMAIL_FROM`, `GOOGLE_CLIENT_ID`,
  `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ZONE_ID`,
  `CUSTOM_DOMAIN_TARGET`, `PLATFORM_HOSTS`. `PUBLIC_*` נשארים build-time בכוונה — הם נכנסים לחבילת
  הדפדפן.)
- [ ] **הפקודה להרצה בפרודקשן היא `npm start`** (`node --env-file-if-exists=.env dist/server/entry.mjs`,
  אחרי `npm run build`). **תוקן 2026-07-30:** `start` היה alias ל-`astro dev` — וזו הפקודה שכל
  פלטפורמת אירוח מריצה כברירת מחדל, כלומר deploy היה מגיש את שרת הפיתוח, ששם `import.meta.env.PROD`
  הוא false ולכן **כל סוד חסר נופל בשקט לברירת המחדל של הפיתוח** במקום להיכשל. לפיתוח: `npm run dev`.
  **וגם: ההבטחה ש"השרת נופל באתחול" סוף סוף נכונה** — `prestart` מריץ את
  `scripts/check-required-env.mjs`, שמסרב לעלות בלי `AUTH_SECRET`/`ADMIN_SECRET` או כשהם עדיין
  ברירות המחדל של הפיתוח, ואומר איזה חסר. עד כה ההגנות זרקו רק בבקשה הראשונה שנגעה בסשן — מה שנראה
  למבקר כהפניה ולמפעיל כלא-כלום. מכוסה ב-`tests/required-env-gate.test.ts`, שגם מוודא שברירות המחדל
  שהשער דוחה הן אלה שמודולי האימות באמת משתמשים בהן. (`.env` ליד `package.json` עובד גם הוא, ומשתנה
  סביבה אמיתי גובר עליו.)
- [ ] **חובה לפני העלייה הראשונה: להגדיר `ADMIN_SECRET`** (גם היא `openssl rand -hex 32`) — **זו הסיסמה
  לאזור `/admin`, ואין שם משתמש.** **תוקן 2026-07-30, והיה חור אמיתי:** הקובץ נפל חזרה למחרוזת
  `'admin'` בכל סביבה, ו-`.env.example` תיעד `ADMIN_USERNAME`/`ADMIN_PASSWORD` — שני משתנים
  שנקראו **רק** ב-`src/lib/auth.ts`, מודול שאף אחד לא ייבא. כלומר מי שהחליף בקפידה כל סיסמה
  בקובץ הדוגמה עוד היה עולה לאוויר עם אזור אדמין שהסיסמה שלו `admin`. עכשיו: המודול המת נמחק,
  השרת נופל בפרודקשן בלי `ADMIN_SECRET`, והקוקי נושא תפוגה חתומה במקום את הסוד עצמו (קוקי
  שמכיל את הסוד = הרשאת אדמין קבועה לכל מי שראה אותו פעם אחת, בלי דרך לפוג).
  (`src/lib/admin-auth.ts`, מכוסה ב-`tests/admin-auth.test.ts`.)

- [ ] **פס סקירה עצמאי על חבילת ה-auth/סודות לפני ה-deploy הראשון — פעולה שלך, לא של סשן.**
  להריץ **`/code-review`** (או `/code-review ultra` לסריקה מרובת-סוכנים בענן) על הדיף שכולל
  `src/lib/runtime-env.ts`, `src/lib/google-oauth.ts`, `src/lib/seller-auth.ts`, `src/lib/admin-auth.ts`,
  `src/pages/api/auth/*`, `scripts/check-required-env.mjs` ו-`package.json` (ה-`start`/`prestart`).
  **למה דווקא כאן:** זה הקוד היחיד בפרויקט שבו טעות שקטה = עקיפת אימות, ושלוש מהתקלות בסעיף הזה
  היו *בדיוק* מהסוג שבדיקות עוברות עליו בשקט — `import.meta.env` שנדבק ב-build, `start` שהיה
  `astro dev`, ו-`ADMIN_SECRET` שנפל ל-`'admin'` בזמן ש-`.env.example` תיעד שני משתנים מתים.
  בכל שלושתן הטסטים היו ירוקים. סקירה של סשן (`review-diff`) כבר נעשית אוטומטית על כל דיף כזה,
  ו**ב-2026-08-04 היא כן מצאה פגם** — קלט של 5000 תווים בשדה המייל בדף ההתחברות הפיל את מפתח
  הטבלה והחזיר 500 לכל אדם שביקש (תוקן ומכוסה) — כלומר השורה הקודמת כאן, "ולא מצאה פגמים", כבר
  לא נכונה. זה מחזק את הסעיף ולא מחליף אותו: הפס הזה הוא **עין שנייה בלתי-תלויה**, שלכן שווה להוציא עליה כסף פעם אחת,
  ברגע שלפני העלייה ולא על שינויי CSS יומיומיים.
  **שים לב:** `/code-review` ניתן להפעלה **רק על ידך** — סשן לא יכול להריץ אותו (ההארנס חוסם
  הפעלה מהמודל), ולכן זה לא ייקרה מעצמו אם לא תקליד את זה. הפקודה בלי ארגומנטים סוקרת את הענף
  הנוכחי ולא צריכה remote ב-GitHub.

- [x] **✅ בוצע 2026-07-30 — CI קם ורץ ירוק** (`.github/workflows/ci.yml`): `npm ci` → `astro check` → `npm run lint` → `npm test` → `npm run build`, על כל push ל-main ועל כל PR.
  **הוחלט 2026-07-27 להקים כשנצטרך, לא לפני** — כל עוד יש סשן אחד שמריץ בדיקות ידנית לפני כל קומיט
  זה מוסיף מעט. שני הטריגרים שבהם זה כן משתלם: **(א)** תחילת מעבר ה-DB (ענף ארוך, הרבה סשנים —
  ראה `DB_MIGRATION_PLAN.md` שלב 0), **(ב)** הרגע שהאתר באוויר עם מוכרים אמיתיים, כי אז main שבור
  = אתר שבור. מה שזה תופס ובדיקה ידנית לא: התקנה מאפס (תלות חסרה ב-`package.json`), ותיעוד
  מאומת על הקומיט עצמו במקום "נאמר שהטסטים עברו".

> **הרעיון בבסיס:** בזמן deploy, לכמה שניות **הגרסה הישנה והחדשה רצות יחד** מול אותו DB.
> אם ה-DB, ה-API והקבצים כולם תואמים-לאחור — אף משתמש לא מרגיש את המעבר, גם לא באמצע פעולה.
> דפדפנים לא מרעננים לבד; הסיכון היחיד הוא שהגרסה הישנה *תישבר* אחרי deploy.

**עכשיו (הרגל קוד — נאכף אוטומטית):**
- [x] **שינויי `/api/*` ו-DB תמיד additive** — רק להוסיף שדות, אף פעם לא למחוק/לשנות שדה שגרסה
  קיימת מסתמכת עליו. client ישן שפתוח באמצע פעולה חייב להמשיך לעבוד. breaking אמיתי → `/api/v2/`.
  **מסומן כבוצע כי זה כבר כלל ברזל ב-`AI_INSTRUCTIONS.md` → Hard rules** ("Backward-compatible
  API/DB"), שנקרא בתחילת כל סשן — כך הוא מיושם בכל שינוי מהיום, לא רק לפני go-live.

**רק בחיבור ל-DB + עלייה לפרודקשן:**
- [ ] **Host עם Atomic Deploy** — פלטפורמה שמעלה גרסה חדשה לצד הישנה ורק אז מחליפה
  (Vercel/Netlify/Render). כך טאב פתוח ממשיך למשוך את הקבצים הישנים שלו בלי 404 ובלי רענון כפוי.
  **זה ~80% מהפתרון.** self-host → deploy לתיקייה חדשה + החלפת symlink, והשארת ה-`dist/client`
  הישן חי לזמן מה.
- [ ] **שינויי DB תמיד additive** — רק להוסיף עמודות/טבלאות, אף פעם לא למחוק/לשנות עמודה שהקוד
  הישן עדיין קורא. רוצים למחוק? קודם deploy שמפסיק להשתמש בעמודה → ורק ב-deploy הבא מוחקים אותה.
- [ ] **Migrations רצים לפני הקוד החדש** — כל שינוי מבנה ב-DB רץ לפני שהקוד החדש עולה, ובאופן
  תואם גם לגרסה הישנה (בגלל הכלל למעלה). כך הקוד החדש לא מגיע ל-DB שעוד לא מוכן.
- [x] **Graceful shutdown — נבנה 2026-08-04.** `src/lib/shutdown.ts`: בקשות שבאמצע נספרות
  ב-middleware, ו-SIGTERM/SIGINT ממתין להן (עד 10 שניות) וסוגר את ה-pool לפני יציאה, במקום ליפול
  מיד — מה ש-Node עושה כברירת מחדל, כלומר עד היום כל deploy קטע את מי שהיה באמצע צ׳קאאוט.
  **הערה על מה שזה כן ומה שלא:** `@astrojs/node` ב-standalone מחזיק את שרת ה-HTTP ולא חושף אותו
  ל-`src/` (נבדק מול 11.0.2 — אין בו שום מאזין לסיגנל), ולכן זה **מנקז ולא סוגר את המאזין**;
  בפועל ה-load balancer מפסיק לנתב ברגע הסיגנל, והדד-ליין מבטיח יציאה גם אם לא. `tests/shutdown.test.ts`.

---

## 8. AIO — חשיפה למנועי-AI וחיפוש — ⏭ אחרי ההשקה, דורש דומיין חי

> **הבנה קריטית:** להיות באוויר זה תנאי הכרחי אך **לא מספיק**. מנועי-AI (ChatGPT / Perplexity /
> Gemini / Copilot) שולפים בזמן-אמת מאינדקס החיפוש (בעיקר Bing ו-Google). כלומר: אם אתה מדורג
> ב-Google/Bing — אתה נשלף לתשובות ה-AI; אם לא — הם פשוט לא ימצאו אותך. אין קסם שבו "דיפלוי =
> מנועי-AI נוהרים אליך". **הקוד כבר דואג לחצי של "להיות קָריא וציטוט-בר"** (JSON-LD Product+Offer
> מועשר, `/llms.txt` דינמי, `robots.txt` פתוח לזחלני-AI, תוויות-פרסום אוטומטיות). מה שלמטה = החצי
> השני, **"להימָצא"**, שהוא עבודה ידנית/הפצה שקוד לבד לא פותר.

- [x] **מפתח IndexNow — בוצע, המפתח כבר בקונפיג** (`seo.indexNowKey`, נבדק 2026-08-04). **אין כאן
  שום פעולה שנשארה לך** — המנגנון פשוט ממתין לדומיין חי, ואז נדלק לבד. משיופעל: כל פרסום/עדכון
  של מוצר או חנות דוחף את ה-URL ל-Bing
  (ומשם ל-ChatGPT/Copilot) תוך דקות במקום לחכות לזחילה. קובץ האימות `/{key}.txt` מוגש לבד מהקונפיג
  (`src/pages/[key].txt.ts`). no-op לגמרי עד שיש מפתח **וגם** דומיין לא-דמה (`src/lib/indexnow.ts`).
- [ ] **Google Search Console** — לאמת דומיין ולהגיש את שני ה-sitemaps (`/sitemap-index.xml` +
  `/sitemap-content.xml`). נותן נתוני חיפוש אורגני ומאיץ אינדוקס. (מופיע גם בסעיף 1.)
- [ ] **Bing Webmaster Tools** — **הכי חשוב ל-AIO:** Bing מזין את ChatGPT/Copilot. לאמת דומיין
  ולהגיש את אותם sitemaps (אפשר לייבא ישירות מ-Search Console).
- [ ] **Google Merchant Center + Google Shopping** — לפתוח חשבון ולחבר את `/api/feed/products.xml`.
  מזין גם את Shopping וגם את AI Overviews / קניות ב-Gemini. (מופיע גם בסעיף 2.)
- [ ] **Meta Catalog** — לחבר את אותו פיד ל-Catalog של מטא (אותה כתובת פיד).
- [ ] **בדיקת אמת אחרי עלייה** — לחפש מוצרים/חנויות ב-ChatGPT/Perplexity ובגוגל, ולעקוב אחרי
  הכיסוי ב-Search Console. **לא לסמוך על "עלה לאוויר = מופיע"** — זו עבודת SEO/הפצה מתמשכת.

---

## איך לוודא שלא שוכחים

1. **הקובץ הזה** הוא הרשימה הרשמית. עוברים עליו לפני כל ניסיון "לעלות לאוויר".
2. כל תלות חדשה בנתון אמיתי שנוספת בקוד — **מוסיפים לכאן באותו רגע**.
3. `AI_INSTRUCTIONS.md` מפנה לקובץ הזה, כך שכל סשן עתידי יודע לבדוק אותו.
