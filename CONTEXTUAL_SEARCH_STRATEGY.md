# אסטרטגיית חיפוש קונטקסטואלי / סמנטי — גולת הכותרת של הפרויקט

> מסמך אסטרטגיה (לא משימה מיידית). החיפוש הסמנטי **תלוי מעבר ל-DB אמיתי** — ראה "למה לא עכשיו".
> נכתב 2026-07-21. מופנה מ-`CURRENT_TASK.md` סעיף 13.4 ומ-`GO_LIVE_CHECKLIST.md` סעיף 6 (DB).

---

## 1. למה זה — החזון
היום כל חיפוש באתר (וברוב אתרי הסחר) הוא **התאמת מילים**: הקונה כותב "חולצה כחולה" ומקבל מוצרים
שהמילים "חולצה"+"כחול" מופיעות בהם. חיפוש קונטקסטואלי הוא **הבנת כוונה**: הקונה כותב
*"מתנה לילד בן 5 שאוהב דינוזאורים"* ומקבל תוצאות רלוונטיות — גם אם אף מוצר לא מכיל את המילים
"מתנה" או "בן 5". זה ההבדל בין מנוע חיפוש של 2015 לחוויית חיפוש בעידן ה-AI.

זו גולת הכותרת כי היא **בידול אמיתי**: מרקטפלייסים ישראליים (Yad2, My Product) הם התאמת-מילים
פשוטה. חיפוש שמבין שפה טבעית הופך את הפלטפורמה מ"קטלוג שאפשר לחפש בו" ל"עוזר קניות".

---

## 2. מה זה בעצם — ההבחנה בין שתי חזיתות (חשוב לא לבלבל)
"AIO" מכיל **שני דברים נפרדים**, ורק השני הוא המסמך הזה:

- **א. להימצא במנועי-AI חיצוניים** (ChatGPT / Perplexity / Gemini). זה **SEO של שכבת השליפה** —
  נתונים מובנים, `/llms.txt`, robots פתוח, IndexNow, פיד. **כבר בוצע** (CURRENT_TASK 13.2). לא נושא המסמך.
- **ב. חיפוש סמנטי בתוך האתר שלנו** ← **זה המסמך הזה.** חוויית חיפוש חכמה משלנו, שרצה על השרת
  שלנו, על הקטלוג שלנו. לא תלוי בשום מנוע חיצוני.

---

## 3. למה לא עכשיו — התלות ב-DB
חיפוש סמנטי עובד ע"י המרת כל מוצר ל**וקטור** (embedding — רשימת מספרים שמייצגת את המשמעות),
ושמירתו במאגר וקטורים שיודע לחפש "הכי קרוב במשמעות". היום הדאטה ב-`data/*.json` — אין שם
יכולת חיפוש-קרבה וקטורי, וכל פתרון שנבנה מעליו ייזרק במעבר ל-DB. לכן זה נבנה **יחד** עם המעבר
ל-Postgres (GO_LIVE סעיף 6), שם `pgvector` נותן את היכולת נייטיב. לבנות עכשיו על JSON = עבודה לפח.

---

## 4. הארכיטקטורה — איך בונים (כשנגיע)
**מקור הטקסט:** לכל מוצר מרכיבים מחרוזת אחת מ: שם + תיאור + נתיב קטגוריה + תגיות + מפרט
(בדיוק אותו טקסט שכבר מזין את ה-JSON-LD, הפיד ו-`audience-infer` — מקור אחד, לא כפילות).

**יצירת ה-embeddings:**
- מודל embedding **רב-לשוני** (עברית קריטית) — למשל OpenAI `text-embedding-3-small` (זול מאוד,
  תומך עברית), Cohere `embed-multilingual`, או מודל self-hosted (`multilingual-e5`) אם רוצים
  לא לשלוח דאטה החוצה. **החלטה פתוחה** — לבדוק איכות עברית מול עלות מול פרטיות.
- מריצים על כל מוצר פעם אחת, ושומרים את הווקטור בעמודה `vector` ב-Postgres (`pgvector`).
- **רענון:** מחשבים מחדש embedding בכל יצירת/עדכון מוצר — **אותן נקודות hook שכבר קיימות
  ל-IndexNow** (`src/lib/indexnow.ts` נקרא ב-`/api/product` וב-`/api/store`), שם נוסיף גם
  enqueue ל-re-embed. מבנה מוכן.

**זרימת חיפוש (query):**
1. ממירים את שאילתת הקונה לווקטור (אותו מודל).
2. חיפוש שכן-קרוב (nearest-neighbor, cosine/inner-product) עם אינדקס `HNSW` ב-pgvector.
3. **Hybrid search** — משלבים עם חיפוש לקסיקלי (BM25/מילולי) כי חיפוש וקטורי טהור **מפספס
   התאמות מדויקות** (מק"ט, שם מותג, דגם). המשקלול בין השניים = החלטה פתוחה לכיול.
4. סינון לפי חנות/קטגוריה/מלאי כמו היום, ו-re-rank בסוף.

**סקלביליות:** אינדקס HNSW מחזיק חיפוש תת-שנייה גם על מאות אלפי מוצרים; ה-embedding של
השאילתה ניתן ל-cache. עלות ה-API של embeddings זניחה (`text-embedding-3-small` ~ סנטים לאלפי מוצרים).

---

## 5. שלבים (phasing)
- **שלב 0 — עכשיו (ללא DB):** הדאטה כבר עשירה (קטגוריות היררכיות, תגיות, `audience-infer`,
  5 תוויות `product-labels`). זה כבר משפר חיפוש-מילים ומכין את הקרקע. אין מה לבנות מעבר לזה כרגע.
- **שלב 1 — במעבר ל-DB:** embeddings + hybrid search. זה ה-MVP של גולת הכותרת.
- **שלב 2 — אחרי שעובד:** דיוק שיחתי ("יש לך את זה גם באדום?"), התאמה-אישית לפי היסטוריה,
  והזרמת אותו מנוע לחיפוש בתוך חנות בודדת ולדף הבית.

---

## 6. החלטות פתוחות — ורק אחת מהן באמת חוסמת

> **⏸ סטטוס: נדחה במפורש (החלטת הבעלים, 2026-08-03).** לא בגלל תלות טכנית — המעבר ל-Postgres
> הושלם ו-`pgvector` זמין. **הטריגר לפתיחה מחדש הוא החלטה 1 בלבד.** נרשם גם ב-DB_MIGRATION_PLAN.md
> §8 כשלב "4ב".

1. **ספק ה-embedding — זו החוסמת, וזו שאלת פרטיות מול תפעול.** OpenAI `text-embedding-3-small`
   וCohere `embed-multilingual` זולים ואפס-תחזוקה, אבל **טקסט המוצרים ושאילתות הקונים עוזבים
   את השרת**; `multilingual-e5` self-hosted לא עוזב, אבל צריך להריץ מודל, זיכרון וזמן תגובה.
   **למה זה חוסם קוד ולא רק תקציב:** הבחירה קובעת את ממד הווקטור, ולכן את הסכימה של העמודה
   ואת האינדקס — אי אפשר "להתחיל ולהחליף אחר כך" בלי מיגרציית דאטה.
2. משקלול ה-hybrid (סמנטי מול לקסיקלי) — **לא חוסם.** כיול אמפירי על שאילתות אמת, כלומר עבודה
   שנעשית תוך כדי הבנייה ולא לפניה.
3. חוצה-פלטפורמה או per-store — **לא חוסם.** §7 כאן כבר עונה: שניהם, כמו היום.

**מה שכבר נבנה מאז, ומשנה את היקף החצי הלקסיקלי (DB_MIGRATION_PLAN.md §3, 2026-08-03):**
`store_products.search_text` — עמודה מחושבת עם הנרמול העברי המלא (ניקוד, אותיות סופיות, גרש,
פיסוק) ואינדקס trgm מעליה, שכבר משרתת את חיפוש הפלטפורמה. זה **לא** BM25, ולכן נשארת שאלה
פתוחה-אך-לא-חוסמת: האם ה-hybrid נשען על העמודה הזו או על `tsvector` — שאין לו stemmer עברי
בקונפיגורציה סטנדרטית, מה שהופך את היתרון שלו על trgm לשאלה אמפירית ולא לנתון. **למדוד, לא
להחליט מראש.**

---

## 7. איך זה מתחבר לשאר
- **מקור טקסט אחד** משותף עם JSON-LD / פיד / תוויות — עקבי, בלי כפילות.
- **hooks קיימים** (IndexNow) הם המקום הטבעי להוסיף re-embed בעדכון מוצר.
- **משלים את ה-AIO החיצוני:** אותו דאטה עשיר שגורם למנועי-AI חיצוניים להבין אותנו, גם מפעיל
  את החיפוש החכם שלנו — שתי החזיתות מוזנות מאותו מקור.
