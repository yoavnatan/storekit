# AI Instructions

Read this + `CURRENT_TASK.md` at session start. If `CURRENT_TASK.md` has multiple substantial, independent items, check whether to split them across parallel worktree sessions (this session in a worktree + background Agent(s) with `isolation:"worktree"`) before diving in — see memory `feedback_parallel_sessions` for when it's worth it vs. not.

---

## What we're building
Multi-vendor internet mall for the Israeli market. Sellers open stores; shoppers discover across them. Two independent SEO surfaces: platform (discovery) + each store (standalone feel).

**Mission:** Hebrew digital mall, no fixed fees (success-based only), separate checkout per seller. **Sellers = registered businesses only** (עצמאים/עוסק פטור/מורשה, small-medium biz) — not private individuals; matches the split-payment provider's sub-merchant requirement and differentiates from Israeli P2P marketplaces (My Product, Yad2), which serve private sellers via manual escrow, not a branded storefront + shipping + marketing. **Fixed platform template only** — no per-seller site/theme design tooling, ever (deliberate scope boundary). Not competing with Wix/Shopify on design freedom — target seller wants zero design decisions and speed. **Zero-touch self-service** — platform owner has no manual, ongoing involvement with sellers/buyers; every seller/buyer flow (registration, upload, fulfillment, messaging, disputes) defaults to fully automated/self-service, never owner approval — a scale requirement (see Hard rules → Scalability). Any new feature involving admin/manual gatekeeping must default to an automated equivalent instead.

### Every seller gets out of the box
1. **Store** — product pages, SEO, platform discovery
2. **Shipping** — integrated Israeli carrier, per-store config, tracking
3. **Marketing** — GTM + Meta Pixel on every page; dataLayer feeds campaigns; seller picks budget only

### Israeli market — hard constraints
- **Payments:** Split-payment via Israeli marketplace-capable processor — SUMIT or תקבול (Takbull), final pick pending (see "Payment architecture" below). Never Stripe.
- **Shipping:** Sendit / Israel Post / iPost only.
- **Currency:** ILS (₪) always.
- **Language:** Hebrew-first, RTL.

### Payment architecture — decided (2026-07-06)
**Split payment via the payment processor.** Platform never holds seller funds — each seller has a sub-merchant/business account with the processor, which splits payment automatically at checkout (seller's share → seller, commission → platform). Reason: platform holding + manually disbursing seller money would be a regulated PSP service in Israel; split payment keeps the platform a pure software/marketplace layer.
- Candidates: **SUMIT** and **תקבול (Takbull)** explicitly market a marketplace/split-payment model — final pick pending direct confirmation of terms. Cardcom/Payme/Tranzila/PayPlus/Meshulam are common gateways but sub-merchant split support is unconfirmed.
- No manual payout queue — processor pays sellers directly, so admin never marks payouts "paid". `SellerBalance`/admin dashboard is a **reporting** view (totalEarned per seller) only.
- Commission % deducted at processor/order level (`store.config.ts` → `checkout.commissionPercent`).

### Checkout — decided
Guest checkout only. Buyer: name, email, phone, address. No buyer registration/login/dashboard.

### Data models — canonical (add fields, never remove without discussion)
```
Order { id, buyerName, buyerEmail, buyerPhone, buyerAddress,
  items:[{productId,productName,storeSlug,storeName,price,qty,image}],
  shippingAmount, totalAmount, paymentRef, paymentStatus:'pending'|'paid'|'failed',
  shippingStatus:'pending'|'processing'|'shipped'|'delivered', trackingNumber?,
  createdAt, updatedAt, buyerId? }
SellerBalance { sellerId, storeId, totalEarned }  // reporting only — split payment means processor pays seller directly, no totalPaidOut/manual payout
Store (add): shipping:{flatRate,freeAbove,processingDays}, contactEmail
StoreProduct (add): weight? (grams)
```

### Proactive obligations — always
- **dataLayer events** — page_view, view_item, add_to_cart, begin_checkout, purchase → `window.dataLayer` + `window.fbq`. Use `src/lib/tracking.ts`.
- **SEO** — every public page: unique title, description, JSON-LD, canonical via `BaseLayout → Seo.astro`.
- **Shipping** — always read from store settings, never hardcode.

---

## North star: physical mall model
| Physical | Platform |
|---|---|
| Entrance | `/` — discovery, search |
| Storefront | `/store/[slug]` |
| Cross-store | Natural recommendations, not aggressive |
| One bag | Unified cart, grouped by store |
| Cash register | Platform collects, tracks per-seller |
| Directory | Browse + category filters |
| Foot traffic | SEO drives store traffic |

Rules: (1) Each store is sovereign. (2) Platform chrome fades inside a store. (3) Discovery = serendipitous. (4) One bag, grouped by store. (5) Mall benefits when stores succeed.

---

## Core priorities
1. **SEO-first** — static pages, structured data, semantic HTML, fast load, no orphan pages.
2. **Automation-ready** — config-driven, clean serializable data.
3. **Simplicity** — no abstractions beyond task requirements.

## Architecture
- **Stack:** Astro 7 + Node adapter, TypeScript, Tailwind v4, Heebo font, `tokens.css` color system. Dev toolbar disabled (`devToolbar:{enabled:false}`). Astro 7's compiler rejects an HTML comment as the first child right after `{expr && (` — place comments above the expression, not inside it.
- **Split:** content pages = static, seller/admin/api = SSR.
- **Header:** `position:fixed; top:0; left:0; right:0` + `body{padding-top:3.3rem}`. Never change to sticky. Desktop `.header-search` sits flush after the logo (not centered) — user preference, confirmed 2026-07-09; don't re-attempt centering without asking.
- **Data:** `data/*.json` dev-only; pure DB adapter functions, swap-ready for SQLite/Postgres.
- **Scroll:** never use `window.scrollTo({top,behavior:'smooth'})` for a JS-computed target on this RTL site — native smooth-scroll visibly drifts `scrollX` off 0 mid-animation (runs off-thread, un-correctable via rAF). Use `animateScrollTo()` in `products.ts` (self-animates via rAF + explicit `window.scrollTo(0,y)` every frame). `html` also keeps `scrollbar-gutter:stable` + `overflow-x:hidden` in `reset.css`.

---

## Hard rules
- **TypeScript everywhere** — no `.js` in `src/`, no `any`.
- **Tailwind v4 only** — no new CSS files or `<style>` blocks; convert existing CSS **on contact only** (no dedicated conversion sessions — decided 2026-07-15 after 10 rounds showed weak byte-savings ROI; the real payoff is less legacy-CSS context to carry, which only justifies touching a file when a real feature already touches it). Never name a custom class the same as a Tailwind utility (e.g. `.grid`) — unlayered legacy CSS beats `@layer utilities` regardless of source order. A `@media` override doesn't gain specificity — a same-specificity unscoped rule later in the file wins regardless of viewport; qualify with an ancestor ID instead of relying on source order. **Remaining legacy CSS surface** (convert only what you're already touching): `src/styles/pages/dashboard.css` (4 sections left — gallery/preview box, products-table core incl. sticky headers, mobile card layout, and `.dash-tabs`/`.dash-panel`/`.dash-head`/`.card` which `admin.css` depends on directly and must never be touched without checking admin panel components first), `src/styles/pages/store.css` (103 classes, not yet scanned), `seller/dashboard.astro`'s `<style is:global>` (blocked until admin-class-sharing is resolved), `buyer/dashboard.astro`'s own `<style>`, stray hex in `StoreAvatar.astro`/`index.astro`. When converting: keep a class's name in markup as an empty marker (no CSS) if any JS does `querySelector`/`closest` on it — strip only the CSS, never the classname itself.
- **`main.css` is global — every byte loads on every page.** A legacy `src/styles/pages/*.css` file may only be moved out of `main.css` into a direct `import '…css'` on its own page file if every one of its classnames is provably unique to that page (checked programmatically, not by eye — short/generic names like `.field`/`.btn`/`.is-loading`/`.hidden` routinely collide across page files and rely today on `main.css`'s import order to cascade correctly). `admin.css` was split out this way (2026-07-15, admin routes are auth-gated + not indexed, so this also keeps that weight off every public/SEO page); `dashboard.css`/`store.css` were checked and are NOT safe to split — they carry shared-component rules (Header/StoreCard/etc. reused across many routes) and override generic classnames from `components/*.css` on purpose. **Gotcha found converting `checkout.astro` (2026-07-15):** a page-owned `<style>` block (even `is:global`) already gets its own per-route CSS chunk from Astro's bundler regardless of the `is:global` attribute — that keyword only disables scoping/hashing, not bundling. Converting such an already-isolated block to Tailwind utilities moves its weight into the one shared Tailwind output (loaded on every page via `main.css`'s `@import "tailwindcss"`), so it can *increase* the global bundle even though the source `<style>` block disappears. Only genuinely cross-page CSS (`dashboard.css`/`store.css`, already `@import`ed into `main.css`, or a component like `ProductQuickView.astro` that's rendered from `BaseLayout` on every page) shrinks the global bundle when converted — track that distinction before assuming a Tailwind conversion is a net byte win.
- **`<Image />` from `astro:assets`** for all platform images.
- **Colors via `tokens.css`** — never hardcode hex. Use `[color:var(--color-X)]`.
- **No emoji** — inline SVG icons (`aria-hidden="true"`, `currentColor`).
- **Lighthouse 100 target** — semantic HTML, proper alt/width/height, lazy loading.
- **Mobile-first** — 375px viewport, scale up with `sm:`/`md:`/`lg:`.
- **Modular** — UI = display only; DB/API/Cloudinary → `/src/services/` or `/pages/api/`. No file > 200 lines. SRP always.
- **Dropdown design system** — all dropdowns: `background:var(--color-surface)`, `padding:0.3rem` container, items `border-radius:4px` + `padding:0.45rem 0.75rem` + hover `var(--color-bg)`, spring `cubic-bezier(0.34,1.56,0.64,1) 0.13s`.
- **Micro-interactions** — `scale(0.97)` button `:active`; image `scale(1.06)` card hover; shadow growth; spring `cubic-bezier(0.34,1.56,0.64,1)`, 100–180ms hover / 220–320ms springs. No border-darkening; every animation needs a reason. `spawnRipple()` clips to its own dedicated inner layer, never the target button directly (clipping the button itself can swallow a corner badge, e.g. an alert dot, for as long as `overflow:hidden` sticks). **In-flight loading** (delete/save/inline edit): disable the control + show `.dot-pulse` (`utils.css`), not a spinner. `ConfirmModal.onConfirm` may return a `Promise` — dialog stays open (disabled, dots showing) until it resolves. A button whose disabled state is really a brief success confirmation (e.g. add-to-cart checkmark) gets `.btn--confirmed` (`buttons.css`) instead of the sitewide `cursor:not-allowed`.
- **"Tactile depth" design language** (site-wide since 2026-07-07: product/home/store/dashboard/checkout/cart/PQV). **Lone/hero surfaces** (single image wrap, detail card): `border:1.5px` + `--shadow-card` → `--shadow-card-hover` on hover. **Secondary/repeated cards** (grid/list items): lighter `border:1px` + `--shadow-xs` → `--shadow-card-hover` on hover — `shadow-card` at rest reads heavy once multiplied across a grid. **Full-width list rows** (order card, table row): skip shadow-hover, use a `border-color` tint instead — the shadow's blur/spread is calibrated for spaced grid cards and reads excessive flush against neighbors. Qty steppers are always flat (`border:1px`, no inset shadow/fill — a filled pill read as disabled). No `translateY` hover-lift on a card whose image already hover-zooms — **exception:** homepage product cards keep `translateY(-5px)` deliberately as a felt differentiator from in-store product cards. Image containers holding a possibly-transparent (bg-removed) photo must use `background:var(--color-surface)` (white), never `--color-bg`/none, plus a `border-bottom` separating image from body when both are white. Tokens in `tokens.css`. Restrained on purpose — not neumorphism, not brutalist neon borders.
- **Scalability** — stateless API routes; no shared write state. Ask: breaks at 1000 sellers + 10,000 buyers? **Known JSON-file cost to revisit at DB migration:** header's SSR-computed indicators (notif badge, avatar alert dot via `sellerHasAnyAlert`) now run on every page a logged-in seller loads and do a full file-read+parse+filter per store/thread, no cache/index — negligible today, becomes an indexed query at real-DB time (same function signatures, only internals change).
- **Inventory integrity** — atomic `decrementStock(productId,qty):Promise<boolean>`. JSON: async mutex (`src/lib/mutex.ts`). DB: `BEGIN TRANSACTION; UPDATE ... WHERE stock >= qty`. Payment webhooks: idempotency check on `paymentRef`.
- **Testing** — `tsc` after every change, **and `npx astro check` whenever a `.astro` file's `<script>`/frontmatter changed** — plain `tsc --noEmit` can't parse `.astro` syntax and silently skips those files entirely (`@astrojs/check` installed as devDependency to catch this). Known `astro check` false-positives to leave alone: (1) a `const` declared right before `Astro.redirect(...)` inside an `if`/`else if` chain gets wrongly flagged `ts(6133)` "never read" — verify with `grep` before "fixing" (deleting it breaks the redirect); (2) "Unreachable code" on a `<script is:inline>` nested inside a conditionally-rendered branch — real, runs fine (`is:inline` is injected raw). Real TS control-flow gotcha: a `let` reassigned inside `.forEach`, then read via `if(x)`/`x?.` right after the loop, narrows to `never` — rewrite as `filter().map().filter(guard)` instead of manual `let`+closure mutation. Vitest set up (`tests/`, default `node` env, per-file `// @vitest-environment jsdom` only where DOM/localStorage is touched, `npm test`) — see Project structure for the current test file list/coverage. **Standing policy: any change that moves money or decrements stock needs a Vitest test in the same change** — not "extra scope"; applies to `orders.ts`, `cart.ts`, `/api/checkout`, `store-products(-bulk).ts`, `/api/store-product/bulk`, and extends to any future money/stock code. Run `npm test` alongside `tsc` after every such change. Playwright is installed (chromium) but reserved for money/inventory/multi-surface bugs, or a UI/interaction claim that can't be verified by reasoning alone — one-off repro scripts, not a committed suite; not for routine changes.
- **Security review gate** — before closing any session whose diff touched money/auth/inventory/cart/checkout code (`orders.ts`, `seller-*`, `cart.ts`, `user-carts.ts`, `checkout.*`, `/api/checkout`, `/api/payment/*`, `/api/auth/*`), run `/code-review` (free tier, not `ultra`) on the diff before reporting done. `/code-review ultra` is user-triggered only, billed — never launch proactively.
- **Production monitoring — future waypoint, not yet built.** Error/log monitoring will live in the seller's own future admin dashboard (see `CURRENT_TASK.md`) rather than a separate tool — flagged early so the dashboard's design leaves room for it.
- **Product reviews — future waypoint, not yet built.** No review/rating feature exists today. When built, buyer-submitted review text must run through the same spam/keyword-stuffing filter already used for seller product/category content — another free-text UGC surface needing the same automatic gate, not a manual moderation queue.
- **Self-healing errors — future waypoint, not yet built (2026-07-15).** Auto-retry (with backoff) for genuinely transient, idempotent failures, auto-marking those log entries "resolved" so the Alerts tab only surfaces things that actually need a human — fits the zero-touch philosophy. Deliberately not built now: today's errors are almost all logic/data bugs (not retryable) or a `data/*.json` write collision that a real DB's own transactions/row-locks make moot — building JSON-specific retry now is throwaway work. Build this once there's a real external call to retry against (split-payment webhook, Cloudinary, future SendGrid/Sendit) or once the DB migration happens (retry on deadlock/dropped-connection instead). **Never retry a non-idempotent operation** (e.g. checkout) blindly — risks double-charging or double-decrementing stock.
- **Accessibility (WCAG 2.1 AA)** — skip link, focus traps on modals, Escape closes overlays, ARIA labels/roles/`aria-expanded`/`aria-live`, visible labels, 4.5:1 contrast, never color-only state.
- **SEO** — `BaseLayout → Seo.astro` always. JSON-LD Product on product pages, Store/LocalBusiness on store pages. Unique title+description per page.
- **i18n** — all strings via `getT(lang)`. Hebrew-first, RTL. Server-side `lang` conditionals for icon placement.
- **Ads — two-tier model (decided 2026-07-06):** (1) **Baseline**, platform-funded — every product/store, by default, in a platform-wide feed powering Google Performance Max / Meta Advantage+ Catalog, paid from platform's own budget. Needs a bulk feed endpoint (Merchant Center + Meta Catalog) exporting all `StoreProduct`s — not built; today only per-page OG `product:price:*` tags exist (`Seo.astro`). (2) **Boost**, seller-funded — extra pay → more spend on their products; a boost = a **separate dedicated campaign** per store/product (`adCampaigns`, via Google Ads/Meta Marketing API, billed through the split-payment provider) since catalog campaigns can't weight one product inside a shared campaign. Either tier: sellers never touch targeting/creative, budget/duration only. One GTM + one Meta Pixel account for the whole platform (not per-seller) — what makes tier 1 possible with zero seller opt-in. `dataLayer` provides `store_id`, `ecommerce.items`.
- File content in English. Chat in Hebrew.

---

## Features built
- **Auth:** Unified seller+buyer accounts, register=name+email+password, `?next=` on all flows. Google OAuth (`/api/auth/google`+callback): find by googleId → email+link → create; `Seller.googleId?`, empty `passwordHash` for OAuth. Env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Session cookie TTL = 180 days (not 1 day).
- **Dashboard:** Multi-store tabs + switcher (alert dot fires only for a store *other than* the one open; live-updates via `updateSwitcherAlertDot()`). Last-selected store persists via `seller_last_store` cookie (180d) — bare `/seller/dashboard` links fall back to it, not the seller's first store. Switcher preserves the open panel tab (`&panel=`). AJAX product CRUD; inline cell editing (click→input→Enter/Escape); row thumbnail/serial also toggles bulk-select; bulk edit/upload/delete with synced desktop/mobile select-all; kebab menu (view/edit/delete/PQV); store settings save button duplicated top+bottom of a long form, kept in sync via `initSettingsForm()`. Products toolbar/table header sticky under site header (desktop, `ResizeObserver`-measured), unsticks itself while a product's edit row is open (`:has()`) in favor of that row's own sticky header. `.dash-head` sticky was tried and fully reverted (2026-07-15, user didn't like it) — don't rebuild without being asked again. Cancel on an edit row restores the last-saved snapshot immediately. 11 modules in `src/scripts/dashboard/` incl. `store-image.ts` (banner/profile crop, generalized `{vpWidth,aspect}` shared by gallery/avatar/banner). AJAX row-build (`buildRows`) mirrors server row structure/classes exactly. `.thumb-wrap` has a border (not just bg) so transparent/white photos don't blend into the row. 401 on AJAX save shows a translated session-expired message; `showStatus()` always `scrollIntoView`s.
- **Dashboard sort/filter/search/pagination (2026-07-15):** Products/Orders/Messages tabs are server-paginated+searched (15/page; Products also gets a 10/20/50/100 page-size picker) via **AJAX fetch + DOM patch, not a full nav** (unlike admin's own pagination) — matches the storefront's own "load more" pattern (`fetchProductsPage`/`renderProductCardHTML` in `store/[slug].astro`) so the page never visibly reloads. `src/lib/seller-{products,orders,messages}-query.ts` (mirror `admin-orders-filter.ts`) run search+sort+filter server-side before `paginate()` (`src/lib/pagination.ts`, shared with admin); each tab's own `/api/seller/{products,orders,messages}.ts` returns the paginated JSON, and the SSR initial render (page 1) calls the exact same lib functions so results never drift. Client rebuilds rows via each tab's own row-builder (`buildRows`/`buildOrderCard`/`buildMessageRow`) + re-binds per-row listeners (delegated listeners need no re-bind). New product/CSV-import/delete no longer patch DOM rows manually — they just re-trigger the same paginated fetch, since only one page's rows exist in the DOM at a time. A single-row state change (order status save, message mark-read) does a lightweight local filter-match recheck instead of a full re-fetch. Products gained a sortable **"נרכשו"** (units purchased) column (`getPurchasedCountsByStoreSlug` in `orders.ts`). Messages' pinned "הודעות מערכת" row stays outside pagination entirely (fixed, single thread). Distinct filter-dropdown values (Products' category, Messages' product/sender) must come from a full-list source embedded server-side (category tree / `msg-filter-values-data` JSON), not scanned off DOM rows — only one page's rows exist in the DOM now.
- **Dashboard sort/filter toolbar:** one consistent pattern site-wide. Desktop: `.sort-btn`+funnel filter buttons live in real table headers (products, seller/buyer messages); orders (cards-only, no header) get an always-visible `.orders-toolbar`; mobile gets `.table-toolbar` (shown only once the table header disappears into card layout). A dedicated always-visible search input (all 3 paginated tabs) sits outside `.table-toolbar` since — unlike sort/filter — it has no separate desktop header equivalent. Dropdown content renders into a shared floating portal per surface (`src/lib/toolbar-portal.ts`'s `createFloatingPortal()`), position `getBoundingClientRect()`-computed + viewport-clamped on open. Outside-click-to-close uses `e.composedPath()`, never `target.contains()` — a portal click that re-renders `innerHTML` can detach `e.target` mid-bubble, breaking `.contains()`. Filter state is `Map<col,Set<value>>` (OR within column, AND across). Orders defaults to the active-status preset + a fixed "newest first" sort option.
- **Products table responsive (640–999px):** real `<table>` markup, no header to shrink into — `table-layout:fixed` + explicit width on every column except name (widths measured from real rendered content, not guessed); name absorbs the squeeze via single-line ellipsis (don't override `white-space`, breaks truncation). `row-num` dropped at this breakpoint. A horizontal-scroll wrapper was tried and reverted: `overflow-x:auto` also forces `overflow-y:auto` (CSS Overflow spec), making it a new sticky containing block that detaches the table's own sticky header. Mobile card grid repeats `actions` in both template rows so kebab spans full card height; price+stock share one line.
- **CSV bulk import/export:** `src/lib/csv-bulk.ts` (pure/isomorphic) + `/api/store-product/bulk.ts` + dashboard `csv-import.ts`/`csv-preview.ts`. Export current catalog or a fixed sample template; import → server-side dry-run preview → confirm → commit. Blank cell on an update row means "leave unchanged", not clear (`stock`/`category`/`tags`/`sku` all preserve-on-blank in `bulkUpsertProducts` — the opposite once caused real data loss). Row's `id` (blank=create, filled=update) is the only match key; `sku` validated unique (catalog + within-batch); export cells sanitized against formula injection; row count capped (`MAX_IMPORT_ROWS`). Update-row preview shows resolved product's current→new name as the only guard against a scrambled/reordered row updating the wrong product (the `id` is an internal UUID, not eyeball-verifiable). Products also gained optional `sku` (unique per store, case-sensitive).
- **Images:** Cloudinary upload; BG removal Web Worker (`@imgly` `segmentForeground`+`applySegmentationMask` — mask computed on a 1024px copy, applied to full-res original); crop output sized to actual pixels selected (capped 2048); manual cleanup modal (erase/restore brush, independent zoom+pan, brush stays fixed screen size so zoom = more precision); crop and bg-removal keep separate undo snapshots. Up to 5 images/product. `passthroughImageService()`. `cdnSrc(url,w?)` → `f_auto,q_auto,w_N`; `thumbUrl(url,w?,h?)` → `c_fill,f_auto,q_auto`; `initThumbs` needs an ancestor of `.thumb-wrap`, never the wrap itself. Widths: card=400, carousel=300, thumb=128, main=800. Crop modal backdrop-click-to-close requires the *mousedown itself* to start on the backdrop (a pan gesture ending over the backdrop shouldn't close it). **Drag-and-drop + multi-select:** every gallery slot accepts a dropped file; multi-file pick/drop fills consecutive empty slots. One shared off-screen file input drives click-to-choose (a real per-slot `<input>` is still a native drop target even hidden, causing double-processing). Each image gets a "Cancel" beside "Done" reverting just that slot. **Gotcha:** `galleryWidgetHtml()` already returns its own `.gallery-widget` root — never wrap its output in another one (caused a hard-to-diagnose duplicate-file bug when multiple galleries mount at once, e.g. bulk-upload panel). **Bulk image-upload panel:** "Save all" saves every open product's gallery in one click; sticky header via the same `:has()` unstick pattern as the edit-row header.
- **Store page `/store/[slug]`:** Product grid; product modal (pushState URL sync); lightbox; filter+sort bar (category chips, sort dropdown); product card image carousel (scroll-snap, dots, RTL, touch); skeleton shimmer. **Search:** two independent searches — header search bar (dropdown, recent searches, `?q=` sync) always searches the *whole store*, ignoring category filter (Enter resets category to "all", remembers the prior category via `preGlobalSearchTag` so clearing search restores it). A separate **local search** (`#product-search`, underline field) only exists once a category is selected, scoped to it. `#search-results-heading` shows result count/empty state with its own "−" clear button (`clearActiveSearch()` routes to whichever search is active). Header search bar is wide + truly centered via a 3-column grid (`.container:has(>.header-search)`) — flex+`margin:auto` only centers in the logo/actions leftover gap, visibly off when their widths differ. Header logo = store name only (no icon/platform-escape link), truncates with ellipsis; `.header-actions` stays `flex-shrink:0`. Tactile-depth pass on cards/chips (see Hard rules); image containers use `--color-surface`+`border-bottom`. Back-navigation restores exact scroll+filter/sort/search state (`src/lib/scroll-restore.ts`), gated to real returns only (`document.referrer` must be this store's product page or `/checkout`). Quick-view→full-page history is collapsed (`history.back()`) before the real navigation so one back reliably returns to the store grid with scroll+filters intact. Lightbox sizes off the real loaded image's own aspect ratio (no hardcoded `width`/`height` — those forced non-square photos to letterbox). **Store banner:** optional banner+profile image (`StoreAvatar.astro`), two plain stacked blocks (no overlay/glass — reverted per design philosophy), image locked to `aspect-ratio:3/1` matching the dashboard crop tool's fixed `aspect:3` exactly (a mismatch lets `object-fit:cover` re-crop the seller's already-cropped image at the wrong ratio per viewport). **Product-modal stock parity:** quick-view's qty stepper respects the selected variant combo's own `variantStock` override (`data-variant-stock`, `pmEffectiveStock()`/`pmRemainingFor()`, mirroring the full product page), not just the flat `stock` field. Variant option buttons disable+strikethrough per combo (`pmUpdateVariantAvailability()`). Always-visible stock note (green/amber/red, `--color-success`/`--color-warning` tokens). **Category nav:** drill-down breadcrumb above the chip+sort row; sort menu anchors `inset-inline-end` (RTL-safe). Chip row is single-line with `overflow-x:auto`+snap, edge arrows floating over the chips (`position:absolute`, keeps row size stable). A chip with subcategories reveals them in a shared flyout on hover (desktop, `matchMedia('(hover:hover)...')`) — must be `position:fixed` since the chip row's own `overflow-x:auto` clips vertical overflow of an absolute descendant regardless of z-index; drilling deeper is click-only and needs `stopPropagation()`. Mobile (`hover:none`/`pointer:coarse`) gets a hamburger + right-opening side drawer with the tree as a single-branch accordion, matching the dashboard's `category-picker.ts`. Chevrons need `scaleX(-1)` under `[dir="rtl"]`. **Filter-transition jump, fixed (2026-07-15):** `renderCategoryNav()` (breadcrumb+chips) used to run synchronously on click, well before `refreshFromServer()`'s grid swap (which waits on a fetch) — two reflows at two separate moments read as the layout "resetting" mid-transition, worst switching to "All" (always the largest, most different result set). Now called from inside `refreshFromServer()` itself, gated behind a `pendingCategoryNav` flag (only true when the category actually changed, so a sort/search-only refresh doesn't rebuild the chip DOM for nothing) — both changes land in one reflow. Outgoing grid dims via opacity during the fetch, fades back in. **Never manually compensate scroll position for a height change here** — tried once (offsetting `window.scrollY` by the breadcrumb's height delta) and it visibly moved `.store-banner` above the filter bar, which never itself changed height — moving `scrollY` moves everything on screen, not just the intended target. The category-chip hover flyout now also tracks the chip's live position on page scroll (was frozen at open-time position — looked detached once the page scrolled), closes if the chip scrolls off-screen entirely, on any click outside it (`composedPath()`, including re-clicking its own trigger chip) or via the shared `.dropdown--closing` fade (`header.css`) instead of an instant hide.
- **Product page `/store/[storeSlug]/[productSlug]`:** Image gallery+lightbox; qty stepper; add-to-cart (`#add-to-cart-btn` reveals `#to-checkout-btn` next to it on click, same pattern as store page's quick-view); wishlist; related products; BreadcrumbList+Product JSON-LD; specs table; category chip; tags. Grid `grid-cols-1 md:grid-cols-[minmax(0,440px)_1fr]`. Out-of-stock variant options disabled (dimmed+strikethrough), not hidden, computed server-side then recomputed client-side per click. Mobile (<768px): `#product-cta-row` mirrors into `#sticky-cart-bar` (fades in via IntersectionObserver once the inline row scrolls away, ids kept in sync via shared `qty`/`updateQty`). Color swatches `border-radius:3px` (not circles). Tactile-depth pass; qty stepper flat. Sticky footer appears once `#add-to-cart-btn` is off-screen; collapses to buttons-only then expands with mini image+name once the real ones scroll away too. Thumbnail swap reveals on `load`/`error`, not a timer. **Variant-selection gating:** add-to-cart on every variant picker (product page, related-item modal, store quick-view) requires an active choice per dimension first (single-option dimensions auto-select); a blocked click shakes + neutral-ring-highlights the missing dimension(s) — ring lives on each option button, never the wrapping row (`width:fit-content` can't size to "widest wrapped line"). **Product-card CTA:** unified pill (`.product-card__qtycart`) — minus/plus flank a center segment that's both live qty and add-to-cart trigger; variant products open the quick-view instead of adding blind, auto-firing the missing-variant shake ~260ms after open. Whole card opens quick-view on click via `e.composedPath()` (not `closest()` — a button's innerHTML swap on click can detach `e.target` mid-bubble). Add-to-cart success uses `spawnBurst()` not `spawnRipple()` (ripple clips to the button's own shape, broken once it's a thin pill-segment). `formatPrice()` is number-then-symbol site-wide ("100 ₪").
- **Cart & Wishlist:** Cart: per-store localStorage (`store_cart_v2_{slug}`), CartDrawer (grouped by store, PQV on item click), server-side `data/user-carts.json`+`/api/user-cart`+`cart-sync.ts` (merge guest→user on login, debounced save). **Stock ceiling:** `addItem()` caps accumulated qty at `product.stock`; every add-to-cart surface disables its button once cart qty reaches stock instead of blindly re-enabling after the confirm-animation timeout. `addItem`'s `stock:` param must stay the raw total (it does its own prev-qty-in-cart math internally) — a separate net-of-cart "remaining" value drives the UI ceiling only; conflating the two double-subtracts. Wishlist: localStorage+server sync, WishlistDrawer (renders grouped list immediately, no generic skeleton first; per-thumbnail shimmer + `img.decode()` fade-in). Wishlist removal is optimistic with rollback-on-failure + error toast. Cart-line qty-stepper flat + explicit `dir="ltr"`. Count badge `position:absolute` in a wrapper outside `<button>`, never expands button width. `.cart-line img` gets `--color-surface`+border. Mobile multi-store sticky pay-all bar sits flush with the drawer's true bottom (a scroll container's own `padding-bottom` can't be occupied by a sticky descendant — 1rem moved onto `.cart-drawer__lines > *:last-child` instead). `.cart-drawer__lines`/`#wishlist-lines` get `overscroll-behavior:contain` (scroll-chaining into the page behind was a real bug). Wishlist qty-stepper shares `.cart-line__qty`'s pattern + enforces `WishlistItem.stock?`. Wishlist image URLs run through `cdnSrc(url,400)` at write- and render-time (was storing raw full-res, re-decoding a multi-MB photo every open). `render()` on `wishlist:change` used to destroy+recreate every `<img>` (whole-panel flicker on any unrelated add) — a `loadedImageSrcs` Set now skips the fade for already-decoded images.
- **Homepage `/`:** Bounded, personalized feed (`src/lib/home-feed.ts`'s `buildHomeFeed()`) instead of one carousel per store — liked/buy-again/new-stores/category/discovery shelves (`HomeShelf.astro`, `StoreCard.astro`) + a limited rotating spotlight (full per-store carousels, subset of stores) — the real scaling fix for hundreds of stores. Hero: plain white banner (platform name + static tagline) on `--color-surface`, rest of page on `--color-bg`. (An earlier animated-gradient hero with glow blobs/random phrases was built, iterated on, then fully reverted as over-complicated — don't rebuild without being asked again.) Category browse row (`/stores?category=`, no counts by design). `StoreCard`: fixed 3-slot image-preview row (placeholder squares for missing photos) + always-rendered tagline/tags rows with explicit `min-height` (prevents height jump vs. shelf siblings). Edge-fade masks shelves/category row, not the spotlight's per-store carousels (masking forces a compositing layer, broke hover-shadow buffer + arrow-visibility timing — reverted there). Header search is a **dropdown-only preview** (stores+products via `/api/search`→`site-search.ts`) that no longer mutates the page's own shelves in place; Enter/a recent chip/"view all" navigates to a dedicated **`/search?q=` results page** (`search.astro`) with higher caps. `/stores` directory: SSR search/category-filter/pagination, `noindex` on filtered/paginated views. `isHome` prop suppresses the back-to-store pill here and clears `last-store` sessionStorage.
- **Checkout `/checkout`:** Guest+session; email pre-fill; client-side order summary + shipping calc; `/api/checkout` re-validates prices server-side, saves `buyerId`; PQV modal; `patchQty()` (no re-render on +/−); stock enforcement; qty-stepper reuses `.cart-line__qty`. Remove-item is icon-only (no label text — was overflowing once per-item checkboxes ate into row width). `/checkout/success` shows order ID, header has full site parity (dashboard link + search bar). **Per-item/per-store selection:** custom checkbox per item + one tri-state per store (default = everything checked); unchecking dims the card + live-updates total; a store with nothing checked hides its own shipping row. Both "לתשלום" and "בצע הזמנה" disable when nothing's selected. Only checked items are sent to `/api/checkout` and removed from the cart after success (matched by `storeSlug`+`cartKey`, not a wholesale wipe) — unchecked items stay in the cart.
- **Orders — seller:** "הזמנות" tab; accordion cards; sort/filter/search via `.orders-toolbar` (server-paginated, see Dashboard pagination bullet above); live poll 15s inserts a new card only when the seller is on page 1 (sorted-by-recency assumption only holds there — same reasoning as admin's own Messages-tab poll), also trims page 1 back to 15 rows; toast/badge fire regardless of page. Status dropdown; order edit modal (buyer details, item soft-delete, discount %/₪); red dot on pending; tab badge. Card auto-collapses ~700ms after a successful save. **Fixed (2026-07-15):** `pollOrders()` was seeding its known-order-ids from the DOM (page-1 only, 15 rows) instead of a full fetch — any store with >15 orders re-toasted old orders as "new" on every page load.
- **Performance & Advertising tabs (seller dashboard, 2026-07-15):** ביצועים — revenue/orders/visitors KPIs + charts + top products over a date range (`seller-performance.ts`, `store-pageviews.ts` for the new visitor counter, `chart-svg.ts` for the isomorphic SVG bar chart). פרסום — seller-controlled ad budget per store/product (`ad-campaigns.ts`), **not wired to a real Google/Meta API yet** — stats shown are clearly-labeled demo data, no real spend.
- **Orders — buyer / buyer dashboard:** Filtered by `buyerId||buyerEmail`; accordion with thumbnail strip (real border, no extra shadow). Collapsed header shows store name(s) as plain text; clickable store link only in the expanded per-store breakdown. No payment-status badge (an order existing already implies paid — `createOrder` defaults `paymentStatus:'paid'`); shipping-status badge only. Tactile-depth pass on profile card/nav/order-card/message-card (border-color-tint hover on rows, not shadow-lift). Seller dashboard (`dashboard.css`) still has ~9 un-converted shadow-hack declarations — next in line for the tactile-depth rollout. Mobile: topbar collapsed to one sticky bar (hamburger+title, title read live from each panel's real `<h1>`); "עגלות"/Carts tab relocated from the homepage, reuses `home.css`'s cart-chip logic. **Desktop overflow, fixed (2026-07-15):** `.my-content-wrap`'s `min-width:0` (guards against `.o-items`' `white-space:nowrap` order-list text forcing the 2-col grid's `1fr` track past the viewport) was scoped only to the mobile `@media` block — the same grid is active at every width above 640px, so a long order list could still force horizontal overflow on desktop/tablet. Moved to an unconditional base rule.
- **Messaging:** `MessageCompose.astro` on store/product pages. Seller + buyer dashboard tabs, both with the sort/filter toolbar (seller side also server-paginated+searched, see Dashboard pagination bullet above — `buildMessageRow` only embeds the buyer's own opening message, never existing replies, since expanding a thread always live-refetches its replies fresh via `loadThread()` anyway). Thread expand in-place. Live poll 15s + immediate poll on load. URL sync (`?panel=messages`/`?tab=messages`). No SMTP — buyer sees seller email directly. Mobile: msg-table → grid card layout. Unread detection checks reply messages too (a reply carries only `toSellerId`, checked via `getMessageReplies(rootId)` per thread — otherwise a follow-up in an already-read thread wasn't flagging). Mobile cards show a live last-message preview + date, synced on poll without opening the thread. Unread accent is a `::before` bar (not `border`, which bends with radius), `#ef4444` — reads as "new" more clearly than the old blue accent. Opening a thread scrolls the reply box into view + focuses it. **Multi-store scoping:** seller messages tab now filters by both seller *and* the open store (`toStoreId===store.id`) at both the SSR panel and the 15s unread-poll — was showing every store's threads regardless of which was open. Replies (no `toStoreId` of their own) are matched to their thread root instead.
- **Notifications:** Bell shows a numeric unread-count badge (red, `.cart-count` shape); polls 30s + immediate refetch on `visibilitychange`; routes to correct tab; deleted on read; X close. **Stock alerts:** `low_stock` (crosses `LOW_STOCK_THRESHOLD`, exported from `variant-combo.ts` so client-bundled code can read it without pulling in `node:fs`) and `out_of_stock`, fired from `/api/checkout` only after the order commits, mutually exclusive per item, names the specific variant combo when relevant. Fully cleared once the seller actually updates stock (full edit, inline stock-cell quick-edit, or a CSV row with a non-blank stock value). Seller alert dot (avatar dropdown, `/api/seller/alerts`+`seller-alerts.ts`): persistent live indicator for pending orders/unread messages across all stores, derived fresh each poll so it can't go stale. Toast (`ToastContainer.astro`) fires site-wide, deduped via `toastOnce(key)` keyed to `relatedId` so the site poller and a page's own live poll can't double-toast the same event. Mobile: centered bottom bar. **SSR-render both indicators' initial state** (`getUnreadCountForUser`/`sellerHasAnyAlert`, see Hard rules → Scalability for cost) — avoids a hidden-then-fetched flash on load, matching how cart/wishlist badges already compute synchronously from localStorage. **Toast-on-fresh-navigation race, fixed:** the poller's inline script can resolve before `ToastContainer`'s deferred module script has attached its listener — fixed with an early `is:inline` buffer in `BaseLayout.astro` (`window.__toastQueue`) that `ToastContainer` drains once ready. Also: the "don't toast a stale backlog on first check" guard was suppressing everything on every fresh page nav (not just once ever) — now only notifications older than 30s are skipped, `localStorage`-persisted per-user. **Store tag + manual delete:** `Notification` gained optional `storeSlug`/`storeName`, shown as a tag above the title; each row has its own delete (×) + a "נקה הכל" clear-all in the dropdown head, both optimistic (no refetch needed).
- **Navigation context:** `src/lib/last-store.ts` (sessionStorage) saved on every store/product page view; `Header.astro` renders a persistent "back to store" pill on any non-store page — survives detours through notifications/checkout. Suppressed + cleared on the homepage (`isHome` prop) since landing there is a deliberate exit. Avatar dropdown's "בית" (home) is the only way back to the platform home now that the store header's own escape link is gone.
- **Mobile header:** ≤640px — store search + homepage search hidden → magnifying-glass opens fixed panel (DOM-teleport). Notif+wishlist panels: `position:fixed; left:8px; right:8px` at button rect. All dropdowns mutually close with fade-close animation (`.dropdown--closing`, opacity+scale 0.13s). Flash-on-resize fix: `visibility:hidden` on closed drawers + dropdown animations gated on `:not([hidden])` + `matchMedia` transition suppressor on buyer sidebar.
- **PQV (`ProductQuickView.astro`):** Native `<dialog>`; `pqv:open {storeSlug,productSlug,storeName,newTab?}`; lightbox inside. Main image = scroll-snap carousel + dots only (no thumbnail strip — redundant), mirrored in the store page's own duplicate modal (`.pm-*` classes, `openProductModal`). Scrollable body + a separate pinned CTA footer (qty/add-to-cart/wishlist/link always reachable). Description uses `src/lib/read-more.ts` (shared char-based truncation, real newlines survive expansion), 2-line clamp everywhere. Variant option buttons: a selected non-color option gets border+bold only, never a solid fill. "לתשלום" → checkout accordion or `/checkout?pay=1`. PQV qty-stepper: shared SVG icons, flat, no inset shadow.
- **Categories (hierarchical):** Per-store tree (max 3 levels, `StoreCategory{id,storeId,name,parentId,order}`, `data/store-categories.json`, `src/lib/store-categories.ts`). Dashboard Settings gets a tree editor (`category-tree.ts`); product forms get a nested dropdown-in-dropdown picker (`category-picker.ts`) that creates a category/subcategory inline. `resolveCategoryId` in `/api/product.ts` re-validates a submitted `categoryId` belongs to the caller's store. **Gotcha:** `store-categories.ts` uses `node:fs` — never let a client-bundled script value-import it even transitively (crashed the whole dashboard once via `csv-bulk.ts`, moved to server-only `store-products-bulk.ts`). CSV uses 3 separate columns (category/subcategory1/subcategory2), auto-creates missing categories; a filled subcategory with an empty parent is a hard validation error, never silently dropped. Store page filter is a drill-down breadcrumb+chip nav, matches descendants too, product filtering/pagination stays server-side (never ships a full 2000-product catalog to the browser). `Store.categories?:string[]` (flat tags, powers the platform homepage's browse-by-category shelves) deliberately kept separate from the per-product tree. `tags?:string[]` (SEO/search) unchanged.
- **Variants & inventory (dashboard, inline in product edit):** `StoreProduct.variants?:{name,options:string[]}[]` + `variantStock?:Record<comboKey,number>` (`src/lib/variant-combo.ts` → `comboKey()`/`generateCombos()`, shared by dashboard+storefront). Dashboard combo grid: sortable/filterable column per dimension + sticky Stock column + sticky Total row. Top-level "מלאי" becomes read-only/auto-summed once variants exist; legacy products get rows seeded via even-split (never silently changes the saved sum). Color recognition (`src/lib/color-variants.ts`, He/En singular+plural) — an unrecognized color gets a native color-picker chip stored as `"name #hex"`, always route through `resolveVariantColor()` before display. A variant type name can't duplicate another, synonym-aware (`canonicalDimName()`), flagged live and enforced at save both client+server. Storefront: per-combo stock gates add-to-cart max-qty + low/out-of-stock text (falls back to total `stock` when unset), disables the specific out-of-stock option button. **Checkout-time atomic enforcement:** `decrementStock()`/`restockProduct()`/`getEffectiveStock()` in `store-products.ts`, serialized via `src/lib/mutex.ts` so concurrent checkouts can't both read-then-write a stale count. `/api/checkout` reserves stock per item as it validates the cart; an insufficient-stock item rolls back everything already reserved and rejects the whole checkout (409). `decrementStock` returns `{ok,before,after}` from inside the same mutex-protected write. `schema.org` `availability` (`isProductInStock()`) now checks every combo, not just the flat `stock` field. **Variant-image linking:** a color-variant value can be assigned one of the product's uploaded photos; clicking that color on the storefront swaps the main image.
- **i18n:** `he`/`en` dictionaries; `getLang`/`getDir`/`getT`; lang cookie; RTL throughout. `buyerDashboard` section complete (all visible strings + JS strings via data attrs). **SEO/A11y:** `Seo.astro`, JSON-LD, sitemap, skip link, focus traps, ARIA, keyboard nav. **Tracking:** `src/lib/tracking.ts` — `trackViewContent` + `trackAddToCart` → dataLayer + fbq. GTM+Pixel on every page. Product OG tags. Sellers never touch ad config.
- **Image skeleton fixes:** two real bugs. Search-results product cards never had `initImageSkeletons()` wired to their wrap class. Product page's main-image/thumb skeleton CSS was silently defeated by an inline `style="background:..."` on the same element — inline styles always beat a stylesheet rule regardless of specificity (same bug class as the header-search-bar `display` fix — never put `background`/layout properties an `.is-loading` class needs to override into an inline style). Also fixed a real stored-XSS: the store-header product-search dropdown inserted `p.name`/img `alt` raw into `innerHTML` (unescaped, unlike the home-search dropdown right next to it) — routed through `escH()`.
- **Admin dashboard (`/admin`):** Rebuilt on the seller dashboard's own shell/tabs language (`dash-tabs`/`.dash-panel`, `initDashTabs()` reused unchanged). Tabs: **overview** (platform stat cards, revenue = `paymentStatus:'paid'` orders only, reporting-only per split-payment architecture), **sellers** (accordion per seller, per-store product counts, "שלח הודעה" modal, block/unblock, server-side search over seller/store name+email), **stores** (flat one-row-per-store list across all sellers — Sellers tab buries stores 3 levels deep — with the same block/unblock+search), **orders** (card list mirroring the seller dashboard's own UX incl. toolbar; filters cover shipping/payment status+store, no filter on phone), **attention** (stores with 0 products or no shipping config — **view-only, never gates a seller**, per zero-touch), **messages**, **alerts** (shows which store/actor an error happened to + an optional `resolutionHint`, groundwork for future auto-notify — nothing actually sent yet, no SMTP). `sellerMode={true}` reuses the existing header flag (hides buyer icons) rather than new route-detection. `src/lib/admin-auth.ts` centralizes the admin-cookie check; fixed a real bug where `admin_token` was `path:'/admin'` and never reached `/api/admin/*`. `admin-stats.ts`'s aggregation functions take pre-fetched data as params so each JSON source is read once per request. **Pagination:** all 6 list tabs are server-paginated (`src/lib/pagination.ts`, 15/page, arrow nav via query params, `AdminPager.astro` — a `position="top"|"bottom"` prop shows the pager both above and below each list). Orders' search/sort/filter moved server-side (`src/lib/admin-orders-filter.ts`); Sellers/Stores search moved to `admin-stats.ts`'s `filterSellerCards`/`filterStoreRows`. Multi-value filter params (e.g. Orders' store filter) must go through `encodeList`/`decodeList` (`src/lib/admin-nav.ts`) — a plain comma-join breaks on a value that itself contains a comma. Messages tab's live-poll (15s) only inserts/reorders rows when viewing page 1 (sorted-by-recency assumption only holds there); a trim keeps page 1 capped at 15 rows even as new threads arrive mid-session. **In-panel AJAX nav (2026-07-15):** search/sort/filter/pager in Sellers/Stores/Orders no longer do a full `window.location.href` reload — `swapPanel()` (`admin-nav.ts`) fetches the same URL a full nav would load, swaps only that tab's `#dash-panel-*` innerHTML, and updates the address bar via `history.pushState`; `wirePanelLinks()` intercepts same-tab pager `<a>` clicks the same way, `wirePopstateReload()` just does a real reload on back/forward rather than tracking swap history client-side. `createFloatingPortal()` calls (Orders' sort/filter dropdown) must stay a module-level singleton, not created inside the re-run-per-swap init function — it wires its own `document`-level listeners on every call, so creating it per-init would pile up duplicates on every keystroke. **Revenue (2026-07-15):** `getStoreRevenueMap()` (`admin-stats.ts`) computes per-store total+current-month revenue from paid orders net of any seller-applied discount; shown on Sellers/Stores cards. **Fixed-column card grids:** `.admin-seller-card__summary`/`.admin-store-row` use CSS Grid with fixed (not content-sized) column widths, not flex — each card/row is a separate DOM subtree, so content-sized (flex or grid `auto`) columns drift left-right per row depending on that row's own digit count, breaking the "columns line up down the list" scan pattern; a mobile breakpoint collapses to fewer columns. Alerts tab has a small icon-only copy button per row (next to "פרטים נוספים") that copies all of that entry's details as formatted text via `navigator.clipboard`.
- **Content moderation / spam prevention:** `src/lib/spam-filter.ts` — two automatic checks (no manual queue, per zero-touch), wired into product/category create-edit + CSV import: word-boundary blocklist (not substring — avoids false positives like "XXX-Large") and keyword-stuffing detection (word repeats ≥8 times AND ≥30% of significant words — both conditions together, tuned conservatively, revisit once real seller data exists). Admin-only store/product block (`/api/admin/moderation.ts`, `Store.blocked`/`StoreProduct.blocked`) is the one deliberately manual gate — a judgment call on a specific bad actor, not routine ops. Every public surface must gate through `isStoreVisible`/`isProductVisible`/`getVisibleStores`/`getVisibleProductsByStoreId`, never an inline `!x.blocked` filter — a code-review pass found 3 real surfaces that had forgotten the inline check. `404.astro` sets a real `Astro.response.status=404` so a blocked listing de-indexes. **Blocking always notifies the seller** (notification + system message) — the seller's reply *is* the appeal channel, no separate UI. Open/deferred: thin/duplicate content across near-identical stores (decided to monitor Search Console, not build a similarity detector), review/UGC spam (no reviews feature yet — must reuse this filter when built), new-seller rate limiting (not needed — processor's KYC already gates it).
- **Admin↔seller messaging:** `src/lib/admin-messages.ts` (`data/admin-messages.json`) — flat one-conversation-per-seller (not subject-threaded like buyer↔seller `messages.ts` — admin only ever has one relationship per seller). Admin side: seller-picker+compose collapsed behind "צור הודעה חדשה"; also reachable via a per-seller-card "שלח הודעה" modal. Seller side: **no separate tab** — the admin thread renders as a pinned "הודעות מערכת" row inside the seller's regular messages tab, using that tab's existing expand/reply/sort/filter. `'admin_message'` notification type routes to it (title "הודעת מערכת חדשה" — a system channel, not personal). Both sides poll 15s: tab dot + row preview update live, an open thread auto-marks-read on a new message.
- **Operational error logging:** `src/middleware.ts` logs to `data/error-log.json` (capped 500) only on a genuinely thrown/uncaught server exception — deliberately not any route returning `>=500` (an intentional 503 like `/api/auth/google`'s "OAuth not configured" would otherwise be a content-free false-positive). A route wanting its own caught error visible in Alerts calls `logError()` directly (`/api/checkout`'s catch block). Client-side `src/scripts/error-reporter.ts` (`window.onerror`/`unhandledrejection`, capped 5/session) posts to unauthenticated `/api/log-client-error` (body-size capped) into the same log. Alerts tab rows have a "פרטים נוספים" toggle showing `stack`/`statusCode`. **Identity enrichment (2026-07-15):** `ErrorLogEntry` gained `storeSlug`/`storeName`/`actorRole`/`actorId`/`actorLabel` + `resolutionHint`, so an Alerts row shows which store/who an error happened to. `resolveErrorContext(pathname,cookies)` (error-log.ts) does the best-effort lookup — wraps its own body in try/catch (must never throw, since it runs inside a caller's own catch block before `logError`'s internal try/catch even starts). `route` on the client-report endpoint is unauthenticated client input — spoofable for store attribution, accepted as a low-impact limit since this is a reporting-only surface, not something gating money/access.

---

## Project structure
```
src/i18n/translations.ts + index.ts ← he+en string dictionaries; getLang/getDir/getT
src/config/store.config.ts      ← platform config + formatPrice + cdnSrc
data/sellers.json               ← seller accounts
data/stores.json                ← store records
data/store-products.json        ← per-store products (storeId field)
data/user-carts.json            ← server-side cart + wishlist per user
data/messages.json              ← message threads
data/notifications.json         ← notifications (message-thread ones deleted on read; any notif also deletable one-by-one or all-at-once by the user)
data/orders.json                ← orders
data/store-categories.json      ← per-store category tree (max 3 levels)
data/admin-messages.json        ← flat admin↔seller conversations (one per seller, no reply-tree)
data/error-log.json             ← ErrorLogEntry log (capped 500 entries), server+client sources
data/store-pageviews.json       ← daily page-view bucket per store slug (store-pageviews.ts) — dev-only, swap-ready for a real DB counter
data/ad-campaigns.json          ← seller-owned "boost" ad campaigns (ad-campaigns.ts) — baseline campaign has no row here, it's automatic/not seller-owned
src/layouts/BaseLayout.astro    ← page shell (GTM+Pixel injection)
src/components/Seo.astro        ← all meta/OG/JSON-LD
src/components/Header.astro     ← nav; notifications bell (polls 30s); back-to-store pill; avatar dropdown = app menu
src/components/CartDrawer.astro
src/components/WishlistDrawer.astro
src/components/ConfirmModal.astro      ← global <dialog>, event-driven (confirm:open); onConfirm may be async (busy-state dot-pulse until resolved)
src/components/ProductQuickView.astro  ← global PQV + lightbox; pqv:open event
src/components/MessageCompose.astro    ← buyer contact form
src/components/StoreAvatar.astro       ← store profile-image circle + gradient-initial fallback; shared by banner/homepage/messages
src/lib/cart.ts                 ← CartItem, localStorage, events, syncCartImages, getCartQty (stock-ceiling lookup)
src/lib/wishlist.ts + wishlist-counts.ts ← WishlistItem, localStorage; count badge sync
src/lib/ripple.ts + burst.ts    ← spawnRipple(); spawnBurst() (radial dot burst, fixed-position so it escapes rounded overflow:hidden containers)
src/lib/read-more.ts            ← initReadMore() (inline read-more/less)
src/lib/seller-auth.ts          ← Seller interface, register/login/session, Google OAuth
src/lib/last-store.ts           ← saveLastStore/getLastStore/clearLastStore (sessionStorage) — back-to-store pill state
src/lib/scroll-restore.ts       ← saveStoreScroll/applyStoreScrollQuery/restoreStoreScroll (sessionStorage, self-clearing) — store page scroll+filter restore on back-nav
src/lib/stores.ts               ← Store interface + CRUD, getStoreById, isStoreVisible/getVisibleStores (blocked-store gate)
src/lib/store-products.ts       ← StoreProduct interface + CRUD + isSkuTaken + decrementStock/restockProduct/getEffectiveStock (mutex-serialized atomic stock adjust, returns {ok,before,after}), isProductVisible/getVisibleProductsByStoreId (blocked-product gate) — bulk upsert moved out, see below
src/lib/spam-filter.ts          ← findSpamKeyword (word-boundary blocklist) + findKeywordStuffing (repetition/density heuristic), both pure
src/lib/store-products-bulk.ts  ← bulkUpsertProducts (CSV batch create/update, preserve-on-blank) + productsToCsv (moved from csv-bulk.ts — needs store-categories.ts, so must stay server-only)
src/lib/store-categories.ts     ← StoreCategory tree CRUD, resolveCategoryFilterIds, resolveOrCreateCategoryPaths (fs-backed, node:fs — never let client-bundled code value-import this)
src/lib/mutex.ts                ← Mutex — serializes async read-modify-write JSON ops (used by store-products.ts's stock functions)
src/lib/csv-bulk.ts             ← CSV parse/stringify (3 category columns), header i18n mapping, row validation incl. orphan-subcategory check, templateCsv (pure, isomorphic)
src/lib/product-form.ts         ← parseImages/Category/Sku/Tags/Specs/VariantsPayload — shared by /api/product.ts
src/lib/variant-combo.ts        ← comboKey()/generateCombos()/canonicalDimName()/isProductInStock()/LOW_STOCK_THRESHOLD — shared by dashboard variant editor + storefront combo-stock lookups + checkout notification threshold (pure/isomorphic, no `node:fs`, so both server and browser-bundled scripts can import it)
src/lib/color-variants.ts       ← resolveVariantColor()/isColorVariant(); exports COLOR_VARIANT_NAME_GROUP (reused by variant-combo.ts's canonicalDimName)
src/lib/gallery-widget.ts       ← gallery HTML helper
src/lib/tracking.ts             ← trackViewContent, trackAddToCart
src/lib/user-carts.ts           ← getUserCart / saveUserCart
src/lib/cart-sync.ts            ← merge guest→user, debounced save
src/lib/orders.ts               ← Order interfaces + CRUD + buyerId + getPurchasedCountsByStoreSlug (productId→units, Products tab's "נרכשו" column)
src/lib/messages.ts             ← Message interface + CRUD + markRead
src/lib/seller-alerts.ts        ← getSellerStoreAlerts/sellerHasAnyAlert (pending orders + unread msgs incl. replies)
src/lib/notifications.ts        ← Notification interface + CRUD; NotificationType incl. 'admin_message'
src/lib/admin-auth.ts           ← isAdminRequest/requireAdmin/setAdminCookie/clearAdminCookie (admin_token cookie, path:'/')
src/lib/admin-stats.ts          ← getPlatformOverview/getSellerCards/getStoresNeedingAttention/getProductCountsByStore/getStoreRows/filterSellerCards/filterStoreRows/getStoreRevenueMap/orderNetForStore (exported — reused by seller-performance.ts) — pure, take pre-fetched data as params
src/lib/admin-messages.ts       ← AdminMessage (flat per-seller conversation) + summarizeAdminThreads (pure) + getAdminThreadSummaries
src/lib/admin-orders-filter.ts  ← filterAndSortOrders/parseOrderQuery/getOrderStoreNames — Orders tab's server-side search/sort/filter, mirrors product-listing.ts's pattern
src/lib/admin-nav.ts            ← buildAdminUrl()/encodeList()/decodeList()/debounce()/swapPanel()/wirePanelLinks()/wirePopstateReload() — shared by admin/index.astro (server) + admin scripts (client) so pager/nav URLs never drift out of sync; swapPanel/wirePanelLinks/wirePopstateReload are the in-panel-AJAX-nav mechanics (see Features built). encodeList/decodeList/debounce also reused by the seller dashboard's own Products/Orders/Messages pagination.
src/lib/pagination.ts           ← paginate()/parsePage() — generic page slicing, shared by all 6 admin list tabs (15/page) and the seller dashboard's Products/Orders/Messages tabs (Products' page size is caller-chosen: 10/20/50/100)
src/lib/seller-products-query.ts ← filterAndSortSellerProducts/parseSellerProductQuery — Products tab's server-side search/sort(incl. purchased count)/category-filter
src/lib/seller-orders-query.ts  ← filterAndSortSellerOrders/parseSellerOrderQuery — Orders tab's server-side search/sort/status-filter, scoped to one store's own subtotal
src/lib/seller-messages-query.ts ← filterAndSortSellerMessages/buildSellerMessageRows/parseSellerMessageQuery — Messages tab's server-side search/sort/filter (pinned "הודעות מערכת" row excluded)
src/lib/seller-performance.ts   ← buildPerformanceSummary()/pickGranularity() — Performance tab's revenue+orders+visitors aggregation, day/month bucketed
src/lib/store-pageviews.ts      ← recordPageView()/getDailyPageViews()/getPageViewsInRange() — daily page-view counter per store, mutex-protected
src/lib/chart-svg.ts            ← buildBarChartSvg() — isomorphic inline-SVG bar chart, shared by SSR initial render and client-side re-render
src/lib/ad-campaigns.ts         ← AdCampaign CRUD + getMockCampaignStats()/getMockBaselineImpressions() (seeded-stable mock data, no real ad API yet)
src/lib/error-log.ts            ← ErrorLogEntry (+storeSlug/storeName/actorRole/actorId/actorLabel/resolutionHint), logError()/getRecentErrors()/clearErrorLog()/truncateStack()/resolveErrorContext() (pure)
src/lib/format-date.ts          ← formatHeDateTime() — isomorphic, safe from both server frontmatter and client scripts
src/lib/html-escape.ts          ← escapeHtml() — client-only (DOM), for innerHTML-building client scripts
src/middleware.ts               ← onRequest — logs only thrown/uncaught server errors, never a route's own deliberate status code; also taps every GET to `/store/[slug]*` into store-pageviews.ts's recordPageView()
src/lib/cloudinary.ts           ← thumbUrl server-side
src/lib/toolbar-portal.ts       ← createFloatingPortal() — shared floating dropdown (position, outside-click) for sort/filter toolbars
src/lib/home-feed.ts            ← buildHomeFeed()/getCategoryCounts() — homepage shelf-selection strategy (liked/buyAgain/new/category/discovery/spotlight), seeded shuffle
src/lib/img-skeleton.ts         ← clearSkeletonOnLoad()/initImageSkeletons() — shared skeleton-shimmer-clear, homepage + /stores
src/lib/site-search.ts          ← searchSite() — platform-wide store+product search; configurable storeLimit/productLimit/imageWidth (header dropdown preview vs. /search page)
src/workers/bg-removal.ts       ← BG removal Web Worker (@imgly, mask-on-small + apply-to-full-res)
src/scripts/dashboard/          ← 16 modules: bg-worker, cleanup-modal, cloudinary, status, crop-modal, gallery, products, ui, store-image, csv-import, csv-preview, category-tree (Settings tab tree editor), category-tree-cache (shared tree cache, avoids products.ts↔category-picker.ts circular import), category-picker (nested dropdown-in-dropdown for product forms), performance (Performance tab range-picker+chart fetch), advertising (Advertising tab campaign CRUD)
src/components/HomeShelf.astro  ← homepage shelf section (title + StoreCard row)
src/components/StoreCard.astro  ← compact store card — homepage shelves + /stores directory
src/pages/index.astro           ← SSR homepage
src/pages/stores.astro          ← SSR /stores directory — search/category-filter/pagination
src/pages/search.astro          ← SSR dedicated /search results page — stores (StoreCard) + products, back button (history.back with same-origin-referrer check)
src/pages/store/[slug].astro    ← SSR store page
src/pages/store/[storeSlug]/[productSlug].astro ← SSR product page
src/pages/seller/               ← register, login, dashboard (?panel=X URL sync)
src/pages/buyer/dashboard.astro ← buyer area (?tab=X URL sync)
src/pages/checkout.astro + checkout/success.astro ← SSR checkout + confirmation
src/pages/admin/index.astro     ← admin dashboard shell, 7 tabs, per-tab pagination/filter query-param parsing (see Features built)
src/pages/admin/login.astro + logout.ts ← admin auth (ADMIN_SECRET env, default 'admin' in dev)
src/components/admin/           ← AdminOverviewPanel/AdminSellersPanel/AdminStoresPanel/AdminOrdersPanel/AdminAttentionPanel/AdminMessagesPanel/AdminAlertsPanel (display-only, one per /admin tab) + AdminPager (shared arrow-pager, all 6 list tabs)
src/scripts/admin/               ← orders-filter, admin-messages, admin-msg-seller-dropdown, sellers (block toggle + message modal + search), stores (block toggle + search), alerts — client wiring for /admin; search/sort/filter navigate server-side (debounced) rather than DOM-filter
src/scripts/error-reporter.ts   ← initErrorReporter() — window.onerror/unhandledrejection → /api/log-client-error, capped 5/session
src/pages/api/messages.ts       ← GET/POST messages
src/pages/api/notifications.ts  ← GET/POST notifications
src/pages/api/admin-messages.ts ← seller-facing admin-thread GET/POST (session-guarded, own sellerId only)
src/pages/api/admin/messages.ts ← admin-facing GET/POST (admin-guarded), api/admin/errors.ts ← GET/clear error log
src/pages/api/admin/moderation.ts ← admin-guarded block/unblock store/product; notifies + messages the seller (appeal channel)
src/pages/api/log-client-error.ts ← unauthenticated client-error intake, body-size + field-length capped
src/pages/api/seller/alerts.ts  ← GET {hasAlert} for header avatar dot
src/pages/api/seller/orders.ts  ← GET: no `?page` = legacy full unpaginated list (poll's own new-order detection needs the whole set regardless of page/filter); `?page` = paginated+searched+sorted JSON. PATCH: order edit (status/tracking/buyer details/item soft-delete/discount)
src/pages/api/seller/products.ts ← GET paginated+searched+sorted+filtered JSON (Products tab AJAX)
src/pages/api/seller/messages.ts ← GET paginated+searched+sorted+filtered JSON (Messages tab AJAX, buyer threads only)
src/pages/api/seller/performance.ts ← GET {storeSlug,from,to,granularity?} → PerformanceSummary JSON, range capped at 731 days
src/pages/api/seller/ad-campaigns.ts ← GET list+mock stats, POST create, PATCH budget/status, DELETE
src/pages/api/search.ts         ← GET site-wide search (stores+products) for the header dropdown, wraps site-search.ts
src/pages/api/                  ← product, store, wishlist, lang, user-cart, checkout
src/pages/api/store-product/bulk.ts ← GET export CSV / POST dry-run preview + commit import
src/pages/api/store-category.ts ← POST create/rename/delete/move-category (authorizes via getStoresBySellerId, returns fresh tree)
src/pages/api/auth/google.ts    ← OAuth initiation
src/pages/api/auth/google/callback.ts ← OAuth callback
src/styles/main.css             ← single CSS entry point
src/styles/base/tokens.css      ← CSS variables
src/styles/base/reset.css
src/styles/layout/container.css
src/styles/components/          ← buttons, cards, forms, header, footer, cart-drawer, store-card, etc.
src/styles/pages/               ← home, auth, dashboard, store, product, checkout, stores, search
src/styles/utilities/utils.css  ← .muted, .badge, skeleton-shimmer, msg-thread, notif-dot, dot-pulse loader
src/env.d.ts                    ← global Window types (dataLayer, fbq, __sessionUserId, etc.)
_backup/store-slug.astro        ← store page pre-modal snapshot (revert if needed)
.claudeignore                   ← tells Claude to skip node_modules, dist, _backup, pics, fonts
vitest.config.ts                ← node env default; per-file `// @vitest-environment jsdom` where DOM/localStorage is touched
tests/cart.test.ts              ← addItem stock-ceiling clamp, per-variant qty, makeCartKey
tests/checkout.test.ts          ← /api/checkout server-side price/shipping re-validation, signed-in buyer cart cleanup, atomic stock decrement/rollback, low-stock/out-of-stock notification crossing+mutual-exclusivity
tests/csv-bulk.test.ts          ← CSV parse/stringify round-trip, sku-duplicate detection, formula-injection sanitization
tests/store-products-bulk.test.ts ← bulkUpsertProducts create/update/preserve-on-blank/cross-store isolation, isSkuTaken
tests/store-products.test.ts    ← decrementStock/restockProduct/getEffectiveStock — variant-combo bucket vs shared pool, concurrent-decrement serialization
tests/variant-combo.test.ts     ← isProductInStock — shared-pool fallback vs per-combo override
tests/admin-stats.test.ts       ← isStoreIncomplete — the "needs attention" heuristic
tests/admin-messages.test.ts    ← summarizeAdminThreads — grouping/ordering/unread-count
tests/spam-filter.test.ts       ← findSpamKeyword word-boundary cases + findKeywordStuffing (real stuffing vs. legit-repetition non-cases)
tests/error-log.test.ts         ← truncateStack
```

---

## Workflow
1. Read this file + `CURRENT_TASK.md`.
2. Do only what is in `CURRENT_TASK.md → Your instruction`.
3. Before reporting any change done: `tsc` clean, `npm test` clean (add/extend a test if the change touched money/auth/inventory/cart/checkout — see Hard rules → Testing), and run `/code-review` on the diff if it touched that same surface (see Hard rules → Security review gate). Then mark a `✅ בוצע (date) — ...` line right under the specific numbered item in `CURRENT_TASK.md` it belongs to (not just at end-of-session) — this is how the user tracks what's done across parallel sessions, so it must never be skipped once a numbered item is actually finished.
4. **Parallel sessions (2026-07-14):** two Claude sessions on this repo at once is fine with **no worktree** as long as the two tasks share zero files (check `git status`/the actual code paths first, don't assume). Only if there's real file overlap: use a git worktree — but always start it from a **fresh** session, never by pulling an already-long session into one (that drags its whole history along and saves nothing). One task per worktree; merge then `ExitWorktree remove` as soon as it's done — don't leave one open "just in case". `CURRENT_TASK.md` is always shared/live — never write to it while the user is actively editing it in their IDE (ask/wait instead of racing the save).
5. **End of session** (trigger: "next session" / "we're done" / "end of session" / "סגור את הסשן") — do ALL of:
   - Update **Features built**: merge new items into existing bullets (never add a bullet if the feature already exists). One line per feature max. No implementation details — names + key gotchas only.
   - Update **Project structure**: add new files, remove deleted ones. One line per file.
   - Update `CURRENT_TASK.md → Next` and `Recommended next step` (one plain line, the recommendation only — never a recap of what was done this session).
   - Keep this file lean (~200 lines is a trigger to compress, not a hard ceiling): merge bullets, drop details already recoverable from the code. Don't force-cut real, non-recoverable context (a decision, a gotcha) just to hit the number.
   - **Never change `Your instruction`** — only the user changes that.
