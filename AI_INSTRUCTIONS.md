# AI Instructions

Read this + `CURRENT_TASK.md` at session start. If `CURRENT_TASK.md` has multiple substantial, independent items, check whether to split them across parallel worktree sessions (this session in a worktree + background Agent(s) with `isolation:"worktree"`) before diving in — see memory `feedback_parallel_sessions` for when it's worth it vs. not.

---

## What we're building
Multi-vendor internet mall for the Israeli market. Sellers open stores; shoppers discover across them. Two independent SEO surfaces: platform (discovery) + each store (standalone feel).

**Mission:** Hebrew digital mall. No fixed fees; success-based only. Separate checkout per seller. **Sellers = registered businesses only** (עצמאים/עוסק פטור/מורשה, עסקים קטנים-בינוניים) — not private individuals; matches the split-payment provider's sub-merchant requirement (needs a registered business) and differentiates from Israeli P2P marketplaces (My Product, Yad2) which serve private sellers via manual escrow, not a branded storefront + shipping + marketing. **Fixed platform template only** — no per-seller site/theme design tooling, ever (deliberate scope boundary, not a gap to fill later). Not competing with Wix/Shopify on design freedom; target seller explicitly wants zero design decisions and speed, not a website builder. **Zero-touch self-service (2026-07-12)** — the platform owner has no manual, ongoing involvement with sellers or buyers. Every seller/buyer-facing flow (registration, product upload, order fulfillment, messaging, disputes) defaults to fully automated/self-service, never a step requiring the owner's manual approval or intervention — a scale requirement, not a preference (see Hard rules → Scalability). Split-payment architecture, self-service seller registration, and direct buyer↔seller messaging already follow this; any new feature involving admin/manual gatekeeping (seller approval, dispute mediation, etc.) must default to an automated equivalent instead.

### Every seller gets out of the box
1. **Store** — product pages, SEO, platform discovery
2. **Shipping** — integrated Israeli carrier, per-store config, tracking
3. **Marketing** — GTM + Meta Pixel on every page; dataLayer feeds campaigns; seller picks budget only

### Israeli market — hard constraints
- **Payments:** Split-payment via Israeli marketplace-capable processor — SUMIT or תקבול (Takbull), final pick pending (see "Payment architecture" below). Never Stripe.
- **Shipping:** Sendit / Israel Post / iPost only.
- **Currency:** ILS (₪) always.
- **Language:** Hebrew-first, RTL.

### Payment architecture — decided (revised 2026-07-06)
**Split payment via the payment processor.** Platform never holds seller funds — each seller has their own sub-merchant/business account with the processor; the processor splits the payment automatically at checkout: seller's share → seller, commission → platform. Reason: if the platform held seller money and disbursed it manually, that's a regulated financial/payment service in Israel (PSP license). Split payment avoids this — platform is a pure software/marketplace layer, sellers own product/fulfillment responsibility.
- Candidate providers (need direct confirmation of marketplace/split-payment support + terms): **SUMIT** and **תקבול (Takbull)** explicitly market a marketplace model (separate business account per seller). Cardcom/Payme/Tranzila/PayPlus/Meshulam are common Israeli gateways but their sub-merchant split support wasn't confirmed via search — contact them directly before committing.
- No manual payout queue — since the processor pays sellers directly, admin does **not** mark payouts as "paid". `SellerBalance`/admin dashboard becomes a **reporting** view (totalEarned per seller, for platform's own visibility), not a payout-management tool.
- Commission % still deducted at the processor/order level (`store.config.ts` → `checkout.commissionPercent`).

### Checkout — decided
Guest checkout only. Buyer: name, email, phone, address. No buyer registration/login/dashboard.

### Data models — canonical (add fields, never remove without discussion)
```
Order { id, buyerName, buyerEmail, buyerPhone, buyerAddress,
  items:[{productId,productName,storeSlug,storeName,price,qty,image}],
  shippingAmount, totalAmount, paymentRef, paymentStatus:'pending'|'paid'|'failed',
  shippingStatus:'pending'|'processing'|'shipped'|'delivered', trackingNumber?,
  createdAt, updatedAt, buyerId? }
SellerBalance { sellerId, storeId, totalEarned }  // reporting only — split payment means processor pays seller directly, no totalPaidOut/manual payout
Store (add): shipping:{flatRate,freeAbove,processingDays}, contactEmail
StoreProduct (add): weight? (grams)
```

### Proactive obligations — always
- **dataLayer events** — page_view, view_item, add_to_cart, begin_checkout, purchase → `window.dataLayer` + `window.fbq`. Use `src/lib/tracking.ts`.
- **SEO** — every public page: unique title, description, JSON-LD, canonical via `BaseLayout → Seo.astro`.
- **Shipping** — always read from store settings, never hardcode.

---

## North star: physical mall model
| Physical | Platform |
|---|---|
| Entrance | `/` — discovery, search |
| Storefront | `/store/[slug]` |
| Cross-store | Natural recommendations, not aggressive |
| One bag | Unified cart, grouped by store |
| Cash register | Platform collects, tracks per-seller |
| Directory | Browse + category filters |
| Foot traffic | SEO drives store traffic |

Rules: (1) Each store is sovereign. (2) Platform chrome fades inside a store. (3) Discovery = serendipitous. (4) One bag, grouped by store. (5) Mall benefits when stores succeed.

---

## Core priorities
1. **SEO-first** — static pages, structured data, semantic HTML, fast load, no orphan pages.
2. **Automation-ready** — config-driven, clean serializable data.
3. **Simplicity** — no abstractions beyond task requirements.

## Architecture
- **Stack:** Astro 7 (upgraded from 5, 2026-07-10 — closed 3 real CVEs, 0 config changes needed) + Node adapter, TypeScript, Tailwind v4, Heebo font, `tokens.css` color system. Dev toolbar disabled (`devToolbar:{enabled:false}` in `astro.config.mjs`). Astro 7's stricter compiler rejects an HTML comment as the first child right after `{expr && (` — place comments above the expression, not inside it (bit us in the product page's sticky-cart-bar block).
- **Split:** content pages = static, seller/admin/api = SSR
- **Header:** `position:fixed; top:0; left:0; right:0` + `body { padding-top:3.3rem }`. Never change to sticky. Desktop `.header-search` sits flush after the logo, not centered in the header bar — tried `margin-inline:auto` centering (2026-07-09), user reverted: prefers it snug against the icons cluster. Don't re-attempt without asking.
- **Data:** `data/*.json` dev-only; pure DB adapter functions, swap-ready for SQLite/Postgres
- **Scroll:** never use `window.scrollTo({top, behavior:'smooth'})` (native CSS smooth-scroll) for a JS-computed target on this RTL site — confirmed via direct frame-by-frame measurement that it visibly nudges `window.scrollX` away from 0 and back mid-animation on a tall page, reading as a diagonal scroll; pinning `left` explicitly and even an rAF loop forcing `scrollX` back to 0 did not stop it (looked like the native animation runs off-thread). Fixed by never delegating to native smooth-scroll: `animateScrollTo()` in `products.ts` self-animates via `requestAnimationFrame` + positional `window.scrollTo(0, y)` every frame, explicit x always 0. `html` also gets `scrollbar-gutter:stable` (prevents a *different*, real layout-shift-from-scrollbar-appearing issue) and `overflow-x:hidden` (defense-in-depth, no page should ever scroll horizontally) in `reset.css` — keep both when touching global scroll CSS.

---

## Hard rules
- **TypeScript everywhere** — no `.js` in `src/`, no `any`.
- **Tailwind v4 only** — no new CSS files or `<style>` blocks. Convert existing CSS on contact. Never name a custom CSS class the same as a Tailwind utility (e.g. `.grid`, `.flex`) — unlayered legacy CSS beats Tailwind's `@layer utilities` regardless of source order, silently breaking it (bit us: `.grid` in `container.css` was overriding `grid-cols-*`/`gap-*`; renamed to `.auto-grid`). Also: a `@media` override doesn't gain specificity — a same-specificity unscoped rule *later in the file* silently wins regardless of viewport (bit us with `.dash-panel`/`.dash-tabs` mobile padding); qualify with an ancestor ID (e.g. `#dash-main-card .dash-panel`) instead of relying on source order.
- **`<Image />` from `astro:assets`** for all platform images.
- **Colors via `tokens.css`** — never hardcode hex. Use `[color:var(--color-X)]`.
- **No emoji** — inline SVG icons (`aria-hidden="true"`, `currentColor`).
- **Lighthouse 100 target** — semantic HTML, proper alt/width/height, lazy loading.
- **Mobile-first** — 375px viewport, scale up with `sm:`/`md:`/`lg:`.
- **Modular** — UI = display only; DB/API/Cloudinary → `/src/services/` or `/pages/api/`. No file > 200 lines. SRP always.
- **Dropdown design system** — all dropdowns: `background:var(--color-surface)`, `padding:0.3rem` container, items `border-radius:4px` + `padding:0.45rem 0.75rem` + hover `var(--color-bg)`, spring `cubic-bezier(0.34,1.56,0.64,1) 0.13s`.
- **Micro-interactions** — `scale(0.97)` button `:active`; image `scale(1.06)` card hover; shadow growth; spring `cubic-bezier(0.34,1.56,0.64,1)`. 100–180ms hover, 220–320ms springs. No border-darkening. Every animation needs a reason. `spawnRipple()` clips to its own dedicated inner layer, never to the target button directly (bit us: clipping the button itself silently swallowed any corner badge it carried — e.g. the avatar's alert dot — for as long as `overflow:hidden` stuck around). **In-flight loading** (deletes/saves/inline edits): disable the control + show `.dot-pulse` (`utils.css`, 3 breathing dots, `currentColor`) — not a spinning-ring SVG (reads dated, 2026-07-08 call). `ConfirmModal`'s `onConfirm` may return a `Promise`; the dialog stays open (buttons disabled, dots showing) until it resolves, instead of closing immediately. A button whose `disabled` state is really a brief success confirmation (add-to-cart's checkmark, all 5 instances site-wide) gets `.btn--confirmed` (`buttons.css`) instead of the sitewide `cursor:not-allowed` — a blocked-cursor reads as negative right when the action just succeeded.
- **"Tactile depth" design language** (piloted on the product page; rolled out to homepage, store page, buyer dashboard, checkout, cart drawer, PQV as of 2026-07-07) — **lone/hero surfaces** (single image wrap, detail card) get `border:1.5px solid var(--color-border)` + `box-shadow:var(--shadow-card)` → `--shadow-card-hover` on hover. **Secondary/repeated cards** (grid or list items — related-card, product-card, order-card) get the *lighter* `border:1px` + `--shadow-xs` → `--shadow-card-hover` on hover — using `shadow-card` at rest on a repeated card reads as heavy once multiplied across a grid (learned the hard way, see `src/styles/pages/product.css`'s `.related-card` for the correct reference, not the written rule alone). **Full-width list rows** (order card, table row — not a grid card) skip the shadow-hover entirely and use a `border-color` tint instead — `shadow-card-hover`'s blur/spread is calibrated for spaced-out grid cards and reads as excessive on a row stacked flush against its neighbors. Qty steppers (every instance site-wide) are flat, not embossed — just `border:1px`, no inset shadow, no fill (a filled pill read as a disabled control, tried+reverted 2026-07-10) — `--shadow-inset` token deleted from `tokens.css` (removed 2026-07-10: read as a dated skeuomorphic gimmick once repeated across a product grid). Pressed/active chips still get `--shadow-xs`. No `translateY` hover-lift on a card whose image already has its own hover-zoom (double motion) — **exception:** the homepage's product cards keep `translateY(-5px)` deliberately, as a felt differentiator from an individual store's (flatter) product cards. Image containers that can hold a bg-removed (transparent) product photo must use `background:var(--color-surface)` (white), never `--color-bg` or `none` — plus a `border-bottom` separating the image from the card body when both are white. Tokens in `tokens.css`. Restrained on purpose — not neumorphism, not brutalist neon borders.
- **Scalability** — stateless API routes; no shared write state. Ask: breaks at 1000 sellers + 10,000 buyers? **Known JSON-file cost to revisit at DB migration (2026-07-12):** the header's SSR-computed indicators (notif badge via `getUnreadCountForUser`, avatar/dropdown alert dot via `sellerHasAnyAlert`) now run on *every* page a logged-in seller loads (not just the dashboard), and `sellerHasAnyAlert` does a full `fs.readFileSync`+`JSON.parse`+filter per store (`getOrdersByStoreSlug`) and per message thread (`getMessageReplies`) — no cache, no index. Negligible at today's data size; will need to become an indexed query (`WHERE sellerId=... AND status=...`) instead of a full-file scan once this is a real DB — same function signatures, only the internals change, so no dedicated migration task needed beyond the DB swap itself.
- **Inventory integrity** — atomic `decrementStock(productId,qty):Promise<boolean>`. JSON: async mutex (`src/lib/mutex.ts`). DB: `BEGIN TRANSACTION; UPDATE ... WHERE stock >= qty`. Payment webhooks: idempotency check on `paymentRef`.
- **Testing** — `tsc` after every change **and `npx astro check` whenever a `.astro` file's `<script>`/frontmatter changed** (2026-07-12): plain `tsc --noEmit` cannot parse `.astro` syntax at all and silently skips those files entirely — it reported "clean" all session while `.astro` script blocks sat on 5 real type errors (`@astrojs/check` installed as devDependency to catch this; `tsc` alone is still fine for `.ts` files). Note a real `astro check` false-positive found the same day: a `const` declared right before `return Astro.redirect(...)` inside an `if`/`else if` chain in frontmatter gets wrongly flagged `ts(6133)` "never read" even though it plainly is — don't "fix" those by deleting the variable (breaks the redirect); verify with a `grep` for the variable's usage before trusting that specific warning. Also a real (unrelated) TS control-flow limitation: a `let` reassigned inside a `.forEach` closure, then read via `if(x)`/`x?.` right after the loop, narrows to `never` at the read site — rewrite as `array.filter().map().filter(is-guard)` instead of manual `let` + closure mutation, don't just re-shuffle the guard form (tried, didn't help). One more `astro check` false-positive to leave alone: "Unreachable code detected" on a `<script is:inline>` block nested inside a conditionally-rendered template branch (e.g. `{!sellerMode && (<script is:inline>...)}`) — real, runs fine in the browser (`is:inline` is injected raw, untouched by the checker's own control-flow view of the surrounding JSX). Vitest set up (2026-07-10, `tests/`, default `node` env with a per-file `// @vitest-environment jsdom` docblock only where a real DOM/localStorage is touched, `npm test`) with unit/integration coverage for the money/inventory-critical code that exists today: `tests/cart.test.ts` (stock-ceiling clamping in `addItem`, per-variant-combo qty, `makeCartKey`), `tests/checkout.test.ts` (`/api/checkout` — server-side price re-validation ignores client-sent price, shipping/`freeAbove` calc, rejects unknown product/empty cart/bad email/qty≤0, seller notification routing, signed-in buyer's server-side cart cleanup), `tests/csv-bulk.test.ts` (CSV parse/stringify round-trip, header i18n matching, row validation, sku-duplicate detection, formula-injection sanitization), `tests/store-products-bulk.test.ts` (`bulkUpsertProducts` create/update/preserve-on-blank/cross-store isolation, `isSkuTaken`). **Principle (standing policy, not a one-off): any change that moves money or decrements stock needs a Vitest test in the same change** — this is a verification requirement on work already being done, not "extra scope" under the do-only-what's-asked rule below. Applies today to `orders.ts`, `cart.ts`, `/api/checkout`, `store-products(-bulk).ts`, `/api/store-product/bulk`; extend to `seller-balances.ts`, `decrementStock`, `/api/payment/confirm`, commission/shipping calc once those exist — don't treat their absence from this sentence as a green light. Run `npm test` alongside `tsc` after every such change. Playwright is installed (devDependency + chromium) but reserved for money/inventory/multi-surface bugs crossing that threshold — installed+run silently as part of the task when warranted, not for routine changes, and not as a permanent suite (one-off verification scripts, not committed test files). UI/interaction bugs (not money/inventory) still warrant a one-off Playwright repro when a claim can't be verified by reasoning alone — e.g. the gallery drag-drop duplicate-file bug below was only pinned down by scripting the exact multi-widget scenario, not by code review.
- **Security review gate** — before closing any session whose diff touched money/auth/inventory/cart/checkout code (`orders.ts`, `seller-*`, `cart.ts`, `user-carts.ts`, `checkout.*`, `/api/checkout`, `/api/payment/*`, `/api/auth/*`), run `/code-review` (the in-conversation free tier, not `ultra`) on the diff before reporting the task done. `/code-review ultra` (multi-agent cloud review) is user-triggered only, billed — never launch it proactively; mention it as an option for larger/riskier changes instead.
- **Production monitoring — future waypoint, not yet built.** Error/log monitoring will live in the seller's own future admin dashboard (planned, see `CURRENT_TASK.md`) rather than a separate tool — flagged early so the dashboard's design leaves room for it, not an immediate ask.
- **Accessibility (WCAG 2.1 AA)** — skip link, focus traps on modals, Escape closes overlays, ARIA labels/roles/`aria-expanded`/`aria-live`, visible labels, 4.5:1 contrast, never color-only state.
- **SEO** — `BaseLayout → Seo.astro` always. JSON-LD Product on product pages, Store/LocalBusiness on store pages. Unique title+description per page.
- **i18n** — all strings via `getT(lang)`. Hebrew-first, RTL. Server-side `lang` conditionals for icon placement.
- **Ads — two-tier model (decided 2026-07-06):** (1) **Baseline**, platform-funded — every product/store, by default, in a platform-wide feed powering Google Performance Max / Meta Advantage+ Catalog, paid from platform's own budget. Needs a bulk feed endpoint (Merchant Center + Meta Catalog) exporting all `StoreProduct`s — not built; today only per-page OG `product:price:*` tags exist (`Seo.astro`). (2) **Boost**, seller-funded — extra pay → more spend on their products; since catalog campaigns can't weight one product inside a shared campaign, a boost = a **separate dedicated campaign** per store/product (`adCampaigns`, via Google Ads/Meta Marketing API, billed through the split-payment provider). Either tier: sellers never touch targeting/creative, budget/duration only. One GTM + one Meta Pixel account for the whole platform (not per-seller) — what makes tier 1 possible with zero seller opt-in. `dataLayer` provides `store_id`, `ecommerce.items`.
- File content in English. Chat in Hebrew.

---

## Features built
- **Auth:** Unified seller+buyer accounts. register=name+email+password, `?next=` on all flows. Google OAuth (`/api/auth/google` + callback): find by googleId → email+link → create. `Seller.googleId?`, empty `passwordHash` for OAuth. Env: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`. Seller session cookie/token TTL = 180 days (was 1 day, was silently logging sellers out daily) — stays signed in until explicit logout.
- **Dashboard:** Multi-store tabs + switcher (alert dot on switcher btn fires only when a store *other than* the currently-open one has a pending order/unread msg — points the seller TO the alert, not at what they're already looking at; per-store dot in dropdown for all stores incl. current; both update live via `updateSwitcherAlertDot()` as orders/messages get handled, no reload needed). Last-selected store persists across visits (2026-07-10) via `seller_last_store` cookie (180d, same TTL as the session cookie) — a bare `/seller/dashboard` link (header avatar dropdown, notification routing) previously always fell back to the seller's first store; now `?store=` missing from the URL falls back to the cookie instead. Switcher links also carry `&panel=${initialPanel}` so switching stores keeps whichever tab (products/orders/messages/settings) was open instead of resetting to products. AJAX product CRUD; inline cell editing (click name/price/stock → input → Enter commits, Escape cancels); click a row's thumbnail or serial number also toggles its bulk-select checkbox; bulk actions (edit/upload/delete, select-all lives in the table's `.check-col` header on desktop with a synced mobile-toolbar twin — the table header itself is `display:none` in the mobile card layout, so that's the one reachable there); kebab menu (view/edit/delete, PQV `newTab?`); store settings + save spring animation (save button duplicated at both top and bottom of the settings form, 2026-07-12 — a long form otherwise needed a scroll-to-top to save; both buttons stay in sync via `initSettingsForm()` iterating every `[type="submit"]` in the form, not just the first found); pagination (20/page); products toolbar + table header sticky under the site header on scroll (desktop; heights measured live via `ResizeObserver` so it stays pixel-flush) — both unstick themselves the moment a product's edit row is open (`:has()`), replaced by that row's own sticky header (thumb+name+Save/Cancel) so only one relevant thing stays pinned while editing; opening edit also smooth-scrolls the page so that header lands right under the site header, with a short `ResizeObserver` re-aim window afterward to correct for gallery images that finish loading late. Cancel restores the row's last-saved-state snapshot immediately (not just hide-and-leave-mutated) — any unsaved edit, including a still-blank variant dimension, is discarded right away instead of only becoming visible after a full page reload; snapshot is refreshed after every successful save. 11 modules in `src/scripts/dashboard/` incl. `store-image.ts` (store banner/profile upload widget — manual crop via `crop-modal.ts`, generalized to accept any `{vpWidth,aspect}` instead of a hardcoded square, so gallery/avatar/banner crops all share one modal). Newly-added product now sorts to the top (server list ordered by `createdAt` desc; AJAX insert `prepend`s instead of `append`s) — was landing at the bottom until reload. AJAX row-build (`buildRows`) now matches the server row's exact structure (incl. `price-col`/`stock-col`/`actions-col` classes added 2026-07-10, see below) + edit-row `colspan` fixed to match. `.thumb-wrap` gets a `border` in addition to its `--color-surface` bg — was background-only, so a transparent/white product photo blended into the white table row. A 401 on AJAX save now shows a translated "session expired, log in again" message instead of the raw server string, and `showStatus()` always `scrollIntoView`s itself — it renders after `.products-header` near the top, easy to miss if the add-form is scrolled far down.
- **Dashboard sort/filter toolbar (2026-07-10):** replaces the old single category-chip bar + basic column sort everywhere with one consistent pattern. Desktop puts `.sort-btn` + funnel filter buttons directly in real table headers (products: name/category/price/stock/wishlist/date, filter on category only; seller+buyer messages: date/status/product, filter on status/product/from(seller)-or-store(buyer)). Orders (cards-only at every width, no header to fall back to) gets an always-visible `.orders-toolbar` instead; mobile gets `.table-toolbar` (`display:none` above 640px — only shown once the table's own header disappears into the card layout). All dropdown content renders into one shared floating portal per surface (`src/lib/toolbar-portal.ts`'s `createFloatingPortal()`, reused fresh per page/table — not literally shared across products/messages/orders since each owns its row shape) — position is `getBoundingClientRect()`-computed and viewport-clamped on every open (a plain `position:absolute` overflowed the screen when the trigger sat anywhere but a guaranteed edge). Outside-click-to-close uses `e.composedPath()`, never `target.contains()` — a portal click that re-renders `innerHTML` (drilling into a filter column) detaches the original `e.target` mid-bubble, so a `.contains()` check done afterward wrongly reads "outside" and closes the portal the instant it opens its next level. Filter state is `Map<col, Set<value>>` (OR within a column, AND across columns); multi-column filters get a column-picker level whose row checkbox is real (not decorative) — unchecking it clears that column directly without drilling in. Orders' filter defaults to the old active-status preset; its sort adds a fixed workflow-order "newest orders first" option (not alphabetical). Status-only sort/filter (messages) is entirely omitted from the DOM when there's no read/unread mix to act on, not just hidden.
- **Products table responsive fix (2026-07-10):** between the 640px mobile-card breakpoint and ~999px the table is still real `<table>` markup with no header to shrink into. Fix is `table-layout:fixed` + an explicit width on every column *except* name — widths were measured from real rendered content (Playwright, live session), not guessed twice before that (guessed widths either overflowed or looked visibly cramped/misaligned). Name alone absorbs the squeeze via `.product-desc`'s pre-existing single-line ellipsis (don't override its `white-space` — did that once, which broke truncation into unlimited wrap instead); `.product-name` got the same single-line ellipsis treatment to match. `row-num` is dropped outright at this breakpoint (same as full mobile). A horizontal-scroll wrapper was tried first and reverted: `overflow-x:auto` on a wrapper forces `overflow-y:auto` too (CSS Overflow spec), which makes it a new `position:sticky` containing block — the table's own sticky header started tracking the wrapper's (non-)scroll instead of the page's, detaching and floating mid-table instead of following scroll. Mobile card grid (`grid-template-areas`) has `actions` repeated in both template rows so the kebab button spans the full card height (reads as "menu for this row", not specifically adjacent to stock) with price+stock now sharing one line instead of stock sitting alone underneath it.
- **CSV bulk import/export (2026-07-12):** `src/lib/csv-bulk.ts` (pure, isomorphic — no `fs`) + `src/pages/api/store-product/bulk.ts` + `src/scripts/dashboard/csv-import.ts`/`csv-preview.ts`. Export current catalog or a fixed sample template (works with 0 products — a bare export gives just a header row); import → server-side preview (dry-run) → confirm → commit. Blank cell on an update row means "leave unchanged", not clear (`stock`/`category`/`tags`/`sku` all preserve-on-blank in `bulkUpsertProducts`) — the opposite behavior caused real data loss (silently zeroing stock) before the fix. Row's `id` column (blank = create, filled = update) is the only match key; `sku` is validated unique (existing catalog + within the same import batch) same as the single-product form's `isSkuTaken`, CSV export cells are sanitized against Excel/Sheets formula injection, row count capped (`MAX_IMPORT_ROWS`). Update-row preview shows the resolved product's current→new name — the `id` is an internal UUID the seller has no way to verify by eye, and CSV has no cell-locking, so this is the only guard against a scrambled/reordered row silently updating the wrong product. Products also gained an optional `sku` field (unique per store, case-sensitive) — table column hidden ≤999px (same treatment as wishlist/date, avoids the already-tuned tablet width budget).
- **Images:** Cloudinary upload; BG removal Web Worker (`@imgly` `segmentForeground`+`applySegmentationMask` — mask computed on a 1024px copy for speed, applied back onto the full-res original so quality isn't lost); crop output sized to the actual pixels selected (capped 2048, never a fixed size); manual cleanup modal (`cleanup-modal.ts`: erase/restore brush, independent zoom+pan — brush stays a fixed screen size so zooming in gives more precision, not a bigger brush); crop and bg-removal each keep a separate pristine snapshot + own undo so either can be redone after the other; saving a product resets+closes the image editor like a fresh reload (`finalizeGallery`/`closeGalleryPanel`). Up to 5 images/product. `passthroughImageService()`. `cdnSrc(url,w?)` → `f_auto,q_auto,w_N`. `thumbUrl(url,w?,h?)` → `c_fill,f_auto,q_auto` (server+client; thumb skeleton needs an ancestor of `.thumb-wrap` passed to `initThumbs`, never the wrap itself — bit us twice, once in the original gallery code and again in the bulk-upload panel's own thumbnail-refresh call, 2026-07-12). Consistent widths: card=400, carousel=300, thumb=128, main=800. Crop modal's backdrop-click-to-close now requires the *mousedown itself* (not just where the click resolves) to have started on the backdrop — a pan/zoom gesture that began on `crop-viewport` but ended with the mouse released over the surrounding backdrop was closing the modal mid-edit (click's target resolves to the nearest common ancestor of mousedown/mouseup). **Drag-and-drop + multi-select (2026-07-12):** every gallery slot accepts a dropped file, and a multi-file pick/drop fills consecutive empty slots in order (not just the one clicked) — up to 5 in one native picker action. One shared, off-screen (`position:fixed;top:-9999px`) file input per widget drives click-to-choose, not one input per slot — a real per-slot `<input>` was still a valid native drag-drop target even visually hidden, so a real OS-level drop double-processed the same files (once via the app's own handler, once via the browser's native "drop onto a file input" default action); `pointer-events:none`/capture-phase listeners did not reliably suppress that, moving the input off-screen did. Each image also gets a "Cancel" beside "Done" (`revertSlot`) that discards just that slot's in-session pick, reverting to whatever URL it had when the widget was rendered. **Gotcha:** `galleryWidgetHtml()` already returns its own `<div class="gallery-widget">` root — wrapping its output in another one (as the bulk-upload panel's template briefly did) creates two nested `.gallery-widget` elements sharing the same slots, each independently initialized with its own listeners; this was the real cause of a hard-to-diagnose duplicate-file bug that only reproduced with multiple galleries mounted at once (the bulk panel), never in a single-gallery form — never wrap `galleryWidgetHtml()`'s output in an extra `.gallery-widget` div. **Bulk image-upload panel:** a "Save all" button saves every open product's gallery in one click (`resolveGalleryUrls` is a no-op for a gallery nobody touched, so it's safe to call unconditionally on all of them); its own header is sticky (same `:has()` unstick-everything-above pattern as the edit-row header, so only it stays pinned while the panel is open) and the toggle button that opens it flips to a close-label and scrolls the panel's header into view (`animateScrollTo`, see Scroll gotcha above) when opened, closes back on a second click.
- **Store page `/store/[slug]`:** Product grid; product modal (pushState URL sync); lightbox; filter+sort bar (category chips `[aria-pressed]`, sort button with `[aria-expanded]` styling); product card image carousel (scroll-snap, dots, RTL, touch); skeleton shimmer. **Search (redesigned 2026-07-14):** two independent searches. The header search bar (dropdown, recent searches, `?q=` sync) always searches the *whole store*, ignoring any category filter — Enter dispatches `store-search:global`, which resets the category chip to "all"; clearing it (native ×/backspace-to-empty) restores whichever category was active before via `preGlobalSearchTag`, and a direct category click discards that remembered state (`clearGlobalSearch()`) so it never overwrites a filter the user just picked on purpose. A separate **local search** (`#product-search`, plain underline field — no box, no open/close animation, just a leading icon + trailing × that appears once there's text) only exists once a category is selected (`.category-nav-row`, shares its own visibility with the breadcrumb) — scoped to that category, auto-cleared when navigating back to "all". `#search-results-heading` shows "תוצאות חיפוש ל_X_"/"לא נמצאו תוצאות" above the grid with its own borderless **minus**-icon clear button (`clearActiveSearch()` — routes to whichever of the two searches is currently in effect; kept as "−" not "×" to stay visually distinct from the local field's own ×). Header search bar itself is wide (`min(480px,38vw)`, shrinks smoothly) and *truly* centered via a 3-column grid (`.container:has(> .header-search){display:grid;grid-template-columns:1fr auto 1fr}`) — flex+`margin:auto` only ever centered it in the logo/actions leftover gap (visibly off whenever their widths differ, e.g. vs. the homepage hero title), and needed the container's `display` moved out of an inline style into a real CSS rule since nothing external can override an inline style. storeMode pages with no search bar at all (checkout, admin) keep the original `flex:1 1 0` logo/actions behavior (icons pushed to the row's far edge) — the grid only activates via `:has()` when a `.header-search` is actually present, and the back-to-store pill (the one page it can coexist with a search bar on, `/stores`) is hidden in that combination rather than breaking the row onto two lines. Header logo = store name only, no icon, no platform-escape link (removed 2026-07-08); truncates with ellipsis — `.header-actions` in store mode must stay `flex-shrink:0` (never `flex:1`, which makes icons themselves shrink), only the logo column gives way. `.store-name-link` carries both the `.logo` and `.store-name-link` classes, each with their own rules — don't let both also set `overflow:hidden`; two independently-rounded flex-width clip boundaries on the same element is redundant and (2026-07-12) was mistaken for a real glyph-clipping bug before the actual report turned out to be the reporter's Safari zoom not being reset to 100% (Cmd+0) — keep this a single clip boundary (`.logo.store-name-link{overflow:visible}` overrides the outer one) as good hygiene regardless. Tactile-depth pass: product cards + active category chip on tokens (see Hard rules), no hover-lift (homepage differentiator); image containers use `--color-surface` (not `--color-bg`) + a `border-bottom` separator; card+modal qty-steppers are flat (border only, see Hard rules). Back-navigation restores exact scroll position + filter/sort/search state (`src/lib/scroll-restore.ts`, anti-flash `visibility:hidden` script, `behavior:'instant'`) — gated to real returns only (2026-07-09): restore only fires when `document.referrer` is this store's own product page or `/checkout`, so arriving via homepage/an unrelated link/another store lands at the top instead of replaying a stale earlier scroll position. **Quick-view → full product page history fix (2026-07-13):** the modal's own `pushState` (below) put a history entry at the exact same URL as the "view full page" link's real target — one plain `<a>` click stacked a real navigation on top of that soft entry, so back from the full page hit the leftover entry first (same URL, no live document) and just reloaded the product page again instead of returning to the store grid. Fixed by collapsing the modal's entry (`history.back()`) before navigating for real (`.pm-full-link` click handler), so one back reliably lands on the store page with scroll+filters intact. Lightbox (`.lightbox__img`/PQV's `.co-lb__img`) sizes itself off the real loaded image's own aspect ratio (`width:auto;height:auto;max-width/height:100%`, no `width`/`height` HTML attrs, 2026-07-12 — those used to hardcode a `1200x1200` square hint for CLS purposes, forcing every non-square photo to letterbox inside a square box whose padding was filled by `background:var(--color-surface)`, showing as unwanted white bars on ordinary photos) — the surface-color background now only ever shows through actual transparent pixels (a bg-removed product photo), never as box letterboxing. **Store banner:** optional seller-uploaded banner image + profile image (`StoreAvatar.astro`, shared w/ homepage card + buyer-dashboard messages, `border:1px`+`shadow-xs` ring); two plain stacked blocks (image on top, details below, no overlay/glass/floating-card — all tried + reverted per design-philosophy "no gimmicks"), image locked to `aspect-ratio:3/1` (2026-07-10, was a height `clamp(160px,20vw,220px)`) — must exactly match the dashboard crop tool's own fixed `aspect:3` (`store-image.ts`/`crop-modal.ts`, real pixel crop, see Dashboard bullet), or `object-fit:cover` silently re-crops the seller's already-cropped image a second time at a *different* ratio per viewport width (was landing anywhere from ~2:1 to ~5:1, never the 3:1 the seller actually selected — WYSIWYG bug); `.store-banner__identity` carries a `margin-bottom` that collapses with whatever follows so missing tagline/description doesn't leave the action buttons flush against the title. **Product-modal stock parity (2026-07-12):** the grid's own quick-view (`openProductModal`, `.pm-*`) used to know only the flat `stock` field — its qty stepper capped against that instead of the selected combo's own `variantStock` override, letting a buyer add more to cart than that specific combo actually had (found by live Playwright repro, not code review alone). Fixed via a new `data-variant-stock` attribute per card + `pmEffectiveStock()`/`pmRemainingFor()`, mirroring the full product page's `effectiveStockFor()`/`remainingFor()`. `.pm-variant-opt` buttons now disable+strikethrough per combo the same way the full product page's do (`pmUpdateVariantAvailability()`, re-run on every variant click). New always-visible stock note on both surfaces (green dot "במלאי" / amber "נותרו X בלבד" / red "אזל מהמלאי", was previously hidden entirely unless stock was already low) — new `--color-success`/`--color-warning` tokens in `tokens.css`. **Category nav + sort-bar layout (2026-07-13):** the drill-down breadcrumb (only rendered when a category is selected) now sits in its own full-width `.category-breadcrumb` row above the chips+sort row (`.store-controls`/`.store-controls__row`) — previously the whole nav was one unstyled `#category-nav` div with zero dedicated CSS, colliding with the sort dropdown. Sort menu now anchors via `inset-inline-end:0` (was a literal `right:0` under both `ltr:`/`rtl:` — wrong for RTL once the trigger sits at the row's visual left edge, was pushing the menu off-screen) plus a `max-width:calc(100vw - 1.5rem)` safety net. **Category chip row, second pass (2026-07-13):** chip row is single-line (`overflow-x:auto` + `scroll-snap-align:start` per chip, `.store-controls__row` itself is `nowrap` — was `wrap`, which let a flex-item arrow toggling visible/hidden resize the row and push the sort button onto its own line) with edge arrows floating semi-transparently *over* the chips (`position:absolute`, not a flex sibling — the only thing that keeps the row's own box size stable as arrows show/hide; no reserved padding, which read as disproportionate). A chip with subcategories reveals them in one shared flyout on real mouse hover only (`matchMedia('(hover:hover) and (pointer:fine)')`) — the flyout must be `position:fixed`, not `absolute`: the chip row's own `overflow-x:auto` silently clips vertical overflow of an absolutely-positioned descendant regardless of z-index (rendered but invisible, easy to miss). A flyout option that itself has children drills one level deeper on **click** (hover only opens the first level) inside the same shared panel with a back row — mirrors the dashboard's own toolbar filter dropdown (`products.ts`'s single-panel replace, not a side-by-side cascade); the drill click must `stopPropagation()` or it also bubbles into the outer delegated `[data-category-id]` select handler and applies itself as the filter. Selecting the actual leaf closes the flyout immediately (was staying open until mouse-leave). Mobile (`@media (hover:none),(pointer:coarse)` — capability, not viewport width) gets a hamburger + side drawer instead (`.category-drawer-*`), opening from the *right* in Hebrew/RTL, with the full tree as a single-branch accordion (only one branch open at a time, plain "הכל" row inside an open group selects that branch — same shape as the dashboard's `category-picker.ts`). Chevrons in both the flyout and the drawer are drawn LTR and need their own `scaleX(-1)` under `[dir="rtl"]` — bit us twice, once per chevron.
- **Product page `/store/[storeSlug]/[productSlug]`:** Image gallery + lightbox; qty stepper; add-to-cart (`#add-to-cart-btn` is `flex-auto` — fills the CTA row on every screen size, reveals `#to-checkout-btn` ("לתשלום", links to `/checkout`) next to it on click and shrinks to make room, same reveal pattern as the store page's `.pm-add-to-cart`/`.pm-to-checkout`); wishlist; related products row; BreadcrumbList+Product JSON-LD; SEO; specs table; category chip; tags. Grid `grid-cols-1 md:grid-cols-[minmax(0,440px)_1fr]` (image capped 440px). Out-of-stock variant option buttons are disabled, not hidden (dimmed + strikethrough) — computed server-side for the default selection, recomputed client-side per click against the buyer's current picks for every other dimension. Mobile (<768px): image `aspect-ratio:4/3`; tight vertical gaps; qty+add-to-cart row (`#product-cta-row`) stays inline below the color picker AND mirrors into `#sticky-cart-bar` (fixed bottom, fades in via IntersectionObserver once the inline row scrolls away; own ids kept in sync via shared `qty`/`updateQty`; no qty stepper of its own — image+name+price+add-to-cart only, so it covers less of the description; `#sticky-add-to-cart-btn` reveals its own `#sticky-to-checkout-btn` the same way; thumbnail deferred via `data-src`, only fetched on first reveal). Color-variant swatches: `border-radius:3px` (matches header badge, not circles). Tactile-depth pass (see Hard rules): image wrap/detail-card/related-cards get shadow-token border+shadow; qty stepper is flat (border only). Sticky footer: appears once `#add-to-cart-btn` itself is off-screen (even pre-scroll, if it starts below the fold); shows buttons-only+centered while the real image/name are still visible, mini image+name join in via an elegant flex-basis+opacity collapse (no dead gap) only once those scroll away too; inline+sticky `toCheckoutBtn`s always stay in sync regardless of which add-to-cart button was clicked. Thumbnail swap reveals on the new image's `load`/`error`, not a fixed timer (which could flash the old image back); points at the same `cdnSrc(...,800)` as the main image (was wrongly using the raw original) + idle-time prefetch of the rest of the gallery. Title enlarged slightly, price shrunk below it for a calmer price/title hierarchy. `#sticky-add-to-cart-btn` gets `flex:1` (`.sticky-btn--solo`) whenever the mini-summary is collapsed (real name/image still visible up top) so it fills the bar instead of hugging its own content, same flex-auto-fills-then-shrinks-for-checkout pattern as the inline row — not conditioned on the checkout button's own visibility, or it snaps back to a tiny fixed width the moment checkout joins it instead of just making room. Image skeleton shimmer on main image + thumbnails + the mobile sticky-footer thumb (same `is-loading`/`skeleton-shimmer` pattern as the store page), removed via `load`/`error` listeners once each image actually loads. **Variant-selection gating (2026-07-09):** add-to-cart on every variant picker (product page, its related-item modal, store page's own quick-view — `.variant-group`/`.variant-opt`/`.pm-variant-opt`) requires an active choice per dimension first — no option pre-selected by default (a single-option dimension is the one exception: auto-selected, since there's no real choice to make). A blocked click shakes + neutral-ring-highlights (`color-mix(...,var(--color-text)...)`, not red) whichever dimension(s) are missing instead of a modal/alert. Ring lives on each `.variant-opt`/`.pm-variant-opt` button, never the wrapping `[role=group]` row — `width:fit-content` can't size itself to "widest wrapped line": it sizes off max-content then just falls back to full available width the moment options wrap onto 2 lines on mobile. Missing-variant auto-scroll checks the mobile sticky add-to-cart bar's real covered region (`.sticky-bottom-bar.is-visible`), not just raw viewport height, or the flagged group could land hidden behind it. **Product-card CTA rework (2026-07-10):** the separate qty-stepper + add-to-cart-button pair became one unified pill (`.product-card__qtycart`) — minus/plus flank a center segment (`.product-card__add-btn`, `flex:1`, fills the pill) that's both the live qty number and the add-to-cart trigger (bare cart+plus icon, no box, `currentColor`, internal mesh-grid lines); the "1" qty stays hidden until >1 so it's not repeated on every idle card. Variant products don't add blind from the card anymore — `openProductModal(card, true)` opens the quick-view instead and auto-fires its `pmFlagMissingVariants()` shake/ring ~260ms after open (past the entrance transition) so the redirect isn't a dead end. Whole card opens the quick-view on click (delegated listener uses `e.composedPath()`, not `e.target.closest()` — a button's own innerHTML swap on click, e.g. icon→checkmark, can detach `e.target` mid-bubble and silently break `closest()`'s ancestor walk); `.product-card{cursor:pointer}` needed an explicit `.product-card button:disabled{cursor:default}` override since cursor inherits. Add-to-cart success uses `spawnBurst()` (`src/lib/burst.ts`) instead of `spawnRipple()` — the old ripple clipped to the button's own shape, which broke once the button became a thin flexible pill-segment instead of a square/box. Price: not bold, same size as the name (`0.9375rem`) so weight alone carries hierarchy; description removed from cards entirely (keeps the grid height-uniform, avoids dead space in stretched rows). Image-to-body seam is a plain `border-bottom` — tried an inset shadow, then a soft outer drop-shadow instead, both reverted as an unnecessary third depth layer on top of the card's own border+shadow (reads as noise once repeated across a grid); a crisp line is more deliberate/minimal. `formatPrice()` (`store.config.ts`) is number-then-symbol site-wide now ("100 ₪", was symbol-first) — also fixed several hand-rolled duplicates in seller dashboard (incl. a regex that parses a formatted string back out, updated to match the new order).
- **Cart & Wishlist:** Cart: per-store localStorage (`store_cart_v2_{slug}`), CartDrawer (grouped by store, PQV on item click), server-side `data/user-carts.json`+`/api/user-cart`+`src/lib/cart-sync.ts` (merge guest→user on login, debounced save 1.2s). **Stock ceiling (2026-07-10):** `addItem()` now caps accumulated qty at `product.stock` — was unbounded, so repeated add-to-cart clicks (button re-enables after its 1.4s confirm animation) could push a cart past real stock indefinitely. New `getCartQty(storeSlug,slug,selectedVariants?)` helper. Every add-to-cart surface (product-card grid, both quick-view `.pm-` modals, product page + its mobile sticky duplicate, product page's related-item modal, wishlist drawer) now also disables its own button/qty-stepper once that combo's cart qty reaches stock, instead of blindly re-enabling on the timeout. Gotcha: the value passed to `addItem`'s `stock:` param must stay the *raw* total — it does its own prev-qty-in-cart math internally — while a separate net-of-cart "remaining" value drives the UI ceiling only; conflating the two double-subtracts (hit this once on the product page, caught by a Playwright pass before shipping). Global `ProductQuickView.astro` doesn't call `addItem` at all (checkout-review-only), so it was never affected. Wishlist: localStorage+server sync, WishlistDrawer (renders the real grouped list immediately on open — no generic skeleton first, since a mismatched swap for the real (differently-shaped) content caused a visible jump/shrink; each thumbnail gets its own shimmer (`.wl-img-wrap`, reuses `.skel-bar`) + `img.decode()` fade-in instead of gating the whole list on every image loading first, 2026-07-08). Wishlist removal is optimistic (heart/row update instantly) but rolls back on server failure (2026-07-08) — item data is captured from the sibling `data-wl-cart` button before the row is wiped, so a failed `/api/wishlist` POST re-adds it + shows an error toast (`i18n.wishlist.removeFailed`). Cart-line qty-stepper is flat (border only, tactile depth) + explicit `dir="ltr"` (was missing it — RTL mirrored +/- vs. every other qty-stepper on the site). Count badge: `position:absolute` in `.cart-btn-wrap` wrapper (outside `<button>`), never expands button width; pixel-precise centering via `line-height:1` + fixed px + flexbox centering. `.cart-line img` gets `--color-surface` bg + border (was unstyled — a transparent/white product photo blended into the drawer's `--color-bg`). Mobile multi-store sticky pay-all bar (`.cart-grand-total`, `position:sticky;bottom:0`) now sits flush with the drawer's true bottom — a scroll container's own `padding-bottom` is fixed trailing space no sticky descendant can ever occupy regardless of its own margin/inset, so that 1rem moved from `.cart-drawer__lines` onto `.cart-drawer__lines > *:last-child` instead (whichever block ends up last, sticky bar or plain store group). `.cart-drawer__lines` gets `overscroll-behavior:contain` (2026-07-09) — scrolling to the end of the drawer's item list was chaining into the page behind it. Wishlist qty-stepper (2026-07-10) now shares the same visual/behavioral pattern as `.cart-line__qty` (was plain separate-square-buttons with no disabled state) and enforces the item's real stock ceiling — `WishlistItem.stock?` added, threaded through every add-to-wishlist call site (product card, product page, both PQV-style modals) via a `data-wishlist-stock` attribute. Disabled qty-stepper buttons site-wide (cart drawer, checkout, wishlist, product cards) dropped `cursor:not-allowed` for opacity-only dimming; wishlist's own add-to-cart button now gets `.btn--confirmed` during its success state like the other 5 instances (was still showing the blocked cursor). **Wishlist drawer image/layout fixes (2026-07-10):** `.wl-img-wrap`/`.wl-img` moved out of `WishlistDrawer.astro`'s own scoped `<style>` into `cart-drawer.css` — the drawer's rows are built via client-side `innerHTML`, not Astro's own template, so Astro's scoping attribute never lands on them and a component-scoped style silently no-ops on that markup; now match `.cart-line img` exactly (56px, border+`--color-surface`, was 40px unstyled), same reason `.cart-line__qty`/`.cart-line__rm` (already global) worked when reused here. Wishlist image URLs now run through `cdnSrc(url,400)` at both write-time (every add-to-wishlist call site) and again at render-time in the drawer — was storing the raw full-res original, so every open re-decoded a multi-MB photo even off a warm HTTP cache; the render-time transform also covers stale pre-fix `localStorage` entries. `skel-bar`'s own `border-radius:4px` was silently beating `.wl-img-wrap`'s `8px` (same-specificity classes, `utils.css` loads later) — fixed via an inline `style` override on the wrap, the convention every other `skel-bar` instance already follows. Row layout: two stacked rows (image+name/price, then a full-width `.wl-controls` row — qty-stepper + add-to-cart `flex:1` + remove) instead of one squeezed row, which was crushing the add-to-cart label to ellipsis; `.wl-controls` also pins qty-stepper/remove/add-to-cart to a shared 30px height (cart drawer's/checkout's own reuse of those same classes at their original size is untouched, scoped via the wrapper class). `#wishlist-lines` gets `overscroll-behavior:contain` (matches `.cart-drawer__lines`) — scrolling to the list's end was chaining into the page behind it. `.wl-img` sizes itself `100%`/`100%` of `.wl-img-wrap`, not a duplicated hardcoded `56px` — the wrap's own `border` eats into its box under `box-sizing:border-box`, shrinking its real content area below 56px, so a same-56px child image overflowed and got clipped asymmetrically by `overflow:hidden` (border visibly detached from the image). `render()` rebuilding the whole list on every `wishlist:change` while open (even from an unrelated add elsewhere on the page) was destroying+recreating every `<img>`, resetting them all to their opacity:0 skeleton and re-fading in at once (whole-panel flicker) — a module-level `loadedImageSrcs` Set now remembers which src's already decoded, so a later `render()` paints those already-loaded from the first frame (`loaded` class + inline `opacity:1` baked into the initial HTML string) instead of replaying the fade; only a genuinely new item's image still shimmers in. **Single-store total wording (2026-07-14):** `CartDrawer`'s per-store footer shows generic "סה"כ בעגלה"/"לתשלום" (no store name appended) when the cart only has one store, instead of the always-per-store "סכום לחנות"/"לתשלום — שם חנות" — the multi-store grand-total block (`carts.length>1`) was already correctly conditional, only the per-store-group footer wasn't.
- **Homepage `/` (feed rebuilt 2026-07-13):** Bounded, personalized feed (`src/lib/home-feed.ts`'s `buildHomeFeed()`) instead of one full carousel per store — liked/buy-again/new-stores/category/discovery shelves (`HomeShelf.astro`, `StoreCard.astro`) plus a limited rotating spotlight (full per-store product carousels, only a subset of stores) — the real scaling fix once there are hundreds of stores, not just a style choice. Hero: plain white banner (`--color-surface`, same pattern as a store page's own `.store-banner`) — platform name (H1) + one static tagline (`t.home.heroTagline`), then the rest of the page continues on the standard gray `--color-bg`. **This replaced an earlier fixed-position animated gradient wash** (glow blobs, per-visit random hero phrases, multi-round hue/color-mix iteration) that was built, iterated on extensively, and fully reverted the same session as over-complicated for what the design needed — don't rebuild anything like it without being explicitly asked again. Category browse row (`/stores?category=`, plain pills, edge-fade on scroll, no counts — decided against showing them). `StoreCard`: fixed 3-slot image-preview row (placeholder squares for missing photos) + always-rendered tagline/tags rows, tags row has an explicit `min-height` — a store missing a tagline or category used to have its preview row visibly "jump up" vs. shelf siblings that had one, since an empty/absent row collapses to 0 height by default. Edge-fade (`initEdgeFade()` in index.astro) masks the shelves and the category row — not the spotlight's per-store `.home-carousel`s, which deliberately kept their plain prev/next-arrow behavior (edge-fade was tried there too and reverted 2026-07-13: masking forces an element into its own compositing layer, which needs a real padding/negative-margin buffer for hover shadows and made the arrow-visibility timing flaky before any real scrolling — not worth it for a row that already has explicit arrow controls). `--fade-start`/`--fade-end` are JS-set, RTL-safe via `[dir]` CSS split. Header search (`#home-header-search`, sticky header, recent searches `home_search_recent_v1`, works on buyer dashboard too; `<form role="search">` + empty `<datalist>` to block browser credential/history suggestions) is a **dropdown-only preview** (2026-07-14) — stores+products via `/api/search`→`site-search.ts#searchSite()`, each product hit shows its own store's name; it no longer reaches into the page's own shelves/cards to filter them in place (dropped — was silently mutating the layout under an open dropdown). Enter, a recent-search chip, or the dropdown's "צפו בכל התוצאות" row all navigate to the dedicated **`/search?q=` results page** instead (`src/pages/search.astro`) — stores rendered via the same `StoreCard` + real preview images as this page/`/stores`, products in a purpose-built card grid (`.search-product-card`), both capped higher than the dropdown's short preview (`searchSite(q,{storeLimit:24,productLimit:60,imageWidth:300})`); a "back" button returns via `history.back()` when there's a same-origin referrer, else falls back to `/`. `/stores` directory page (`stores.astro`): SSR search/category-filter/pagination, its own independent `<form>` search is untouched by any of the above, `noindex` on filtered/paginated views, shares `img-skeleton.ts`'s skeleton-clear logic with the homepage. `isHome` prop passed to `BaseLayout`→`Header` suppresses the back-to-store pill here and clears its sessionStorage (`clearLastStore()`) — landing on the mall entrance is a deliberate exit from any store context.
- **Checkout `/checkout`:** Guest+session; email pre-fill; client-side order summary + shipping calc; `/api/checkout` re-validates prices server-side, saves `buyerId`; PQV modal; `patchQty()` (no re-render on +/−); stock enforcement; qty-stepper reuses the shared `.cart-line__qty` class (cart drawer/wishlist) instead of its own bespoke style, same `dir="ltr"` fix; its number gets a fixed (not min-) width so it doesn't resize between 1- and 2-digit qty, and the stepper itself gets `flex-shrink:0` — otherwise it's a flex item in `.co-card__row` that gets squeezed whenever the price column (a sibling, not even in that row) grows wide enough to tighten `.co-card__body`'s available width. Remove-item button is icon-only (trash SVG, no "הסר פריט" text) on both mobile and desktop — was overflowing the row once per-item checkboxes ate into the available width (2026-07-10). `/checkout/success`: shows order ID; header now gets full site parity (`hasStore`+`showSearch`, was silently missing both — dropped the seller-dashboard link + search bar). **Per-item/per-store selection (2026-07-09):** custom checkbox (site's accent color + spring pop, not a generic input) on every item card and one per store (tri-state — `indeterminate` when only some of that store's items are checked); default = everything checked. Unchecking dims the card (`opacity:.5`) and live-updates the grand total; a store with nothing checked hides its own shipping row (no shipping charged on nothing bought). "לתשלום" **and** "בצע הזמנה" (they're in different sections — the accordion can already be open when the last item gets unchecked) both disable the moment nothing is selected, each with its own "לא נבחרו פריטים"-style hint text. On submit, only checked items are sent to `/api/checkout` and only those are removed from the cart after success (localStorage + server-side `user-carts.json`, patched by matching `storeSlug`+derived `cartKey` rather than wiped wholesale) — unchecked items stay in the cart. `success.astro` no longer blanket-clears every store cart; that's now precise per-item removal done by the checkout page + API itself.
- **Orders — seller:** "הזמנות" tab; accordion cards; sort (date/amount/status) + filter (shipping status, see dashboard sort/filter toolbar above) via `.orders-toolbar`; live poll 15s (re-applies current sort+filter after prepending a new order, which otherwise always landed at the top regardless of sort); status dropdown (pill); order edit modal (buyer details, item soft-delete, discount %/₪); red dot on pending; tab badge. Card auto-collapses ~700ms after a successful save (2026-07-12) — long enough to see the "נשמר" confirmation first.
- **Orders — buyer / buyer dashboard:** Filtered by `buyerId || buyerEmail`; accordion with thumbnail strip (`.o-thumb` gets a real `border:1px solid var(--color-border)`, 2026-07-08 — was an invisible bg-matching stacking-separator border, no box-shadow added on top since the card's own `--shadow-xs` already signals depth and a per-thumbnail shadow read as messy). Collapsed card header also shows the order's store name(s) as plain, non-link, comma-joined text (wraps if multiple, no icon) — the clickable store-name link only lives in the expanded per-store breakdown (`/store/[slug]`). No payment-status badge (2026-07-08, removed as redundant — an order existing already implies paid): `createOrder` now defaults `paymentStatus:'paid'` at creation instead of a `pending` stub nothing ever flipped (no live gateway yet); shipping-status badge only. Header = `showSearch={true}` (homepage-style). Full i18n via `t.buyerDashboard.*`; JS strings via `#buyer-i18n` data attrs; date locale-aware. Tactile-depth pass: `border:1px` + `--shadow-xs` tokens on profile card/nav/order-card/message-card; order-card hover uses a border-color tint, not a shadow-lift (too heavy on a full-width stacked row, see Hard rules); order thumbnails use `--color-surface`. Seller dashboard (`dashboard.css`) still has ~9 of the same un-converted shadow-hack declarations — do that page next if continuing the tactile-depth rollout. Mobile (2026-07-08): sidebar's own "עמוד הבית" link removed (redundant with the avatar dropdown's home item); topbar collapsed to one sticky bar — hamburger + title together (was two stacked bars: a static "האזור שלי" row above each panel's own duplicate `<h1>`), title text read live from each panel's real `<h1 class="panel-head">` (not the shorter nav-tab label, so it says e.g. "הודעות ממוכרים" not "הודעות") and hidden per-panel `<h1>` on mobile only; both the hamburger and the title (not the rest of the bar) open the drawer; sticky offset measured via `getBoundingClientRect()` + `ResizeObserver`, not a hardcoded rem, to avoid a 1-2px seam. "עגלות"/Carts tab (`#panel-carts`) relocated from the homepage — same `getActiveStoreCarts()`/`getSubtotal()` render logic + `.home-cart-chip` markup, reused via the still-global `home.css` (not a homepage-only stylesheet).
- **Messaging:** `MessageCompose.astro` on store/product pages. Seller + buyer dashboard tabs, both with the sort/filter toolbar (see dashboard bullet above). Thread expand in-place. Live poll 15s (+ immediate poll on load, not just after the first interval tick). URL sync (`?panel=messages` / `?tab=messages`). No SMTP — buyer sees seller email directly. Mobile: msg-table → grid card layout (grid-template-areas: status/from/date/actions/subject/preview). Unread detection (tab dot, alert dots) checks reply messages too, not just the thread root — a reply carries no `toStoreId` of its own, only `toSellerId`, so it's checked via `getMessageReplies(rootId)` per thread (bit us: a buyer follow-up in an already-read thread wasn't flagging anything until the next poll). Mobile cards show a live last-message preview (`(את/ה)`/`(You)` tag when self-sent) + last-message date, kept in sync on poll without opening the thread (`refreshRowPreview`, shared helper). Unread accent = a straight `::before` bar, not a `border` (a border bends with the card's `border-radius`); per-row red dot removed for both roles (2026-07-08, seller matched to buyer's existing bold-text treatment) — unread state now reads from `.msg-table__row--unread`'s side bar alone, recolored `#ef4444` (was `var(--color-accent)`/blue, easy to mistake for a neutral UI accent rather than "new"); desktop `msg-table` cell padding tightened + made symmetric (status/actions columns balanced to the same outer inset). Opening a thread scrolls the reply box (textarea+buttons) into view and focuses the textarea (`preventScroll`); a close-conversation button next to Send collapses it back with a subtle (`block:'nearest'`) scroll. Reply textarea focus border uses `--color-text`, not accent blue. Reply-send button text simplified to "שלח"/"Send" (was "שלח תשובה"/"Send reply") + recolored to the site's accent-blue CTA gradient (`.seller-msg-reply-send` in `utils.css`, shared by seller + buyer dashboards, 2026-07-09) — matches `MessageCompose`'s own send button rather than an arbitrary color. **Multi-store scoping (2026-07-10):** seller dashboard's messages tab was showing threads from *every* store the seller owns regardless of which one was open — `getMessagesBySeller()` only ever filtered by seller, never by store. Fixed at the SSR panel (`storeMessages` now also requires `toStoreId===store.id`) and the 15s unread-poll (`/api/messages?...&storeId=`, sourced from a new `data-store-id` on `#upload-config`) so the tab dot doesn't light up for another store's unread thread. Replies are exempt from the store filter — they always carry `toStoreId:''` (see above), so they're matched to their thread root instead of filtered directly.
- **Notifications:** Bell shows a numeric unread-count badge (red, same `.cart-count` shape as cart/wishlist — not a plain dot); polls 30s + immediate refetch on `visibilitychange` (background-tab timers get throttled by the browser, this closes the gap the moment the tab regains focus); routes to correct tab; deleted on read. X close button. **Stock alerts (2026-07-12):** `low_stock` (crosses `LOW_STOCK_THRESHOLD`, exported from `variant-combo.ts` — not `store-products.ts`, which pulls in `node:fs` — so the browser-bundled dashboard script can read the same constant) and `out_of_stock` (crosses 0), fired from `/api/checkout` only after the triggering order actually commits. Mutually exclusive per item — a single order that empties stock straight from above-threshold only gets the more severe `out_of_stock`, not both (two separate real depletion events over time still each get their own alert). Body names the specific variant combo when one was selected, not just the product name. Routes to `?panel=products`. Bell-dropdown click now actually calls `mark-read` (was wired to nothing before — every notification of every type stayed unread forever regardless of clicking it, `keepalive:true` so the POST survives the immediate navigation). Fully cleared (not just read) once the seller actually updates the product's stock — full edit form, the inline stock-cell quick-edit specifically (not a name/price/sku-only edit), or a CSV import row with an explicit non-blank stock value. Dashboard products-table stock number itself renders in `--color-danger` at/below `LOW_STOCK_THRESHOLD` (same constant). Tab title badge `(N)`. Seller alert dot (avatar + "sellers dashboard" dropdown link, header-wide via `/api/seller/alerts` + `src/lib/seller-alerts.ts`): persistent live indicator for pending orders/unread messages across all of a seller's stores — derived from real state each poll, so it can't go stale like a one-shot notification would if an order gets flipped back to "pending". Toast (`ToastContainer.astro`; `.toast-card`/`.toast-close`/`.toast-container` in `utils.css`) fires from any page incl. both dashboards, deduped via `toastOnce(key)` keyed to the notification's `relatedId` so the site-wide poller and a page's own live poll (messages/orders) can't double-toast the same event — a page's own poll only fires its own toast when a thread is already open and the poll (not the user opening it) finds new replies, keyed inside `loadThread`'s reply-fetch to the exact reply id (firing it earlier, before the mark-read call, raced the notification's deletion and either double-toasted or silently lost it). Click navigates via `href` in the `toast:show` detail, same per-type routing as a bell-dropdown click (`Header.astro`). Mobile (≤640px): centered bottom bar (`left/right:1rem`), not side-anchored. Bell dropdown shows a `.dot-pulse` next to the "התראות" title while `loadNotifs()` is in flight (2026-07-08) — existing list stays visible (no wipe/flash), just a subtle refresh cue. **Toast redesign (2026-07-09):** no icon (was a clock circle, read generic/oversized) — a colored `::before` accent bar instead (same pattern as msg unread state, not a `border` since that bends with radius); lighter `border:1px`+`shadow-xs`→`shadow-card-hover` by default, heavier `border:1.5px`+`shadow-card` "lone surface" treatment on desktop only (≥641px) for more presence, mobile untouched. **Fixed two real badge/toast bugs:** (1) `Header.astro`'s badge was recomputed from `items.filter(unread).length` off whatever list a caller passed — the poller's `notifications:update` event carries only the *incremental* since-filtered batch, so the badge reset to that batch's own small size instead of the true total; fixed via a dedicated `setBadge(unreadCount)` always fed the server's authoritative count, never derived from the list. (2) the site-wide poller's `since` cursor (`BaseLayout.astro`) was the client's wall-clock `now()` instead of the newest notification's own `createdAt` — a notification created during the fetch's network round-trip landed chronologically before that cursor and was silently excluded from every future incremental fetch forever (toast never fires, though `unreadCount` still counted it since that's always recomputed fresh) — fixed by anchoring the cursor to `notifs[0].createdAt`. **SSR-render both indicators' initial state (2026-07-12):** the bell badge and the seller alert dot both used to start `hidden` and only reveal after an async fetch resolved, flashing empty-then-filled on every page load/nav — unlike the cart/wishlist badges, which compute synchronously from localStorage before first paint. Fixed by computing each one server-side (`getUnreadCountForUser`/`sellerHasAnyAlert`, both cheap at today's JSON-file scale — see Hard rules → Scalability for the cost once that's a real DB) and passing the result into `Header.astro` as an `initialUnreadCount`/inline `sellerHasAnyAlert` call, rendering the badge/dot already-correct in the HTML; the client fetch still runs after to keep it live, just without ever needing to reveal anything. **Toast-on-fresh-navigation race (2026-07-12):** the notifications poller is a synchronous `is:inline` script whose first `fetch` can resolve in a few ms on localhost — often before `ToastContainer.astro`'s own listener (a bundled, deferred `type="module"` script) has attached, so a toast fired on a brand-new page load (landing on `/checkout/success` right after your own store's "new order" notification is the common case) used to dispatch into a void and silently vanish. Fixed with a tiny early `is:inline` buffer script in `BaseLayout.astro` that always captures `toast:show` into `window.__toastQueue`, which `ToastContainer` drains (deduped via `toastOnce`) once its real listener is ready. Also: the poller's "don't toast a stale backlog on the very first check ever" guard used to suppress *every* notification on that first check (a plain page nav resets script state, so this happened far more often than "once ever") — a notification created literally a second earlier (e.g. your own order) got swallowed with it; now only notifications older than 30s are skipped on that first check, `localStorage`-persisted per-user so it truly only applies once per browser. **Store tag + manual delete (2026-07-14):** `Notification` gained optional `storeSlug`/`storeName`, set at every seller-relevant `createNotification` call site that has one (`new_order`/`low_stock`/`out_of_stock` in `/api/checkout`, `new_message`/`seller_reply` in `/api/messages`; `admin_message` deliberately has none — it's seller-wide, not store-specific) — shown as a small accent tag above the title in the bell dropdown and prefixed onto the toast body. Each notification row now has its own delete (×) button, plus a "נקה הכל" clear-all button in the dropdown head (hidden when the list is empty) — both call new `deleteNotification`/`deleteAllNotificationsForUser` in `notifications.ts` via new `delete`/`delete-all` POST actions on `/api/notifications`, DOM updated optimistically (no reload/full refetch needed for the delete itself).
- **Navigation context (2026-07-08):** `src/lib/last-store.ts` (sessionStorage key, `{slug,name,url}`) is saved on every store/product page view; `Header.astro` renders a persistent, subtle "back to store" pill (back-arrow icon, RTL-aware) on any page that isn't a store's own header — survives detours through notifications/toasts/messages/checkout so a buyer never loses the thread back to the store they were shopping in. Suppressed + cleared (`clearLastStore()`, gated by an `isHome` prop threaded `BaseLayout`→`Header`) specifically on the homepage, since landing there is a deliberate exit — otherwise a stale pill would follow into checkout/dashboard. Avatar dropdown reframed as the app's own menu: "בית" (home) is now the first item, ahead of "האזור שלי"/seller dashboard/logout — this is the only way back to the platform home now that the store header's own escape link is gone.
- **Mobile header:** ≤640px — store search + homepage search hidden → magnifying-glass opens fixed panel (DOM-teleport). Notif+wishlist panels: `position:fixed; left:8px; right:8px` at button rect. All dropdowns mutually close with fade-close animation (`.dropdown--closing`, opacity+scale 0.13s). Flash-on-resize fix: `visibility:hidden` on closed drawers + dropdown animations gated on `:not([hidden])` + `matchMedia` transition suppressor on buyer sidebar.
- **PQV (`ProductQuickView.astro`):** Native `<dialog>`; `pqv:open {storeSlug,productSlug,storeName,newTab?}`; lightbox inside. Main image = scroll-snap carousel + dots only (thumbnail strip removed 2026-07-07 as redundant with the carousel) — mirrored in the store page's own duplicate modal (`openProductModal` in `[slug].astro`, `.pm-*` classes). Both split into a scrollable body + a separate pinned CTA footer (qty/add-to-cart/wishlist/link always reachable regardless of content length; tried also freezing the modal's overall height so "show more" wouldn't resize it — reverted, kept causing gap/spacing bugs, modal just hugs its content naturally again). Description uses `src/lib/read-more.ts` (shared, char-based binary-search truncation so real newlines survive expansion) — 2-line clamp everywhere, incl. the full product page (was 4, shortened 2026-07-08); "עוד" sits inline right after the ellipsis, "פחות" gets its own line. Full product page's `#product-desc` also gets `text-align:justify` + `overflow-wrap:break-word` + `min-width:0` on its wrapper (2026-07-08 — a long unbroken run of text could otherwise force the flex-column wrapper wider than `.details-card`/`.container`, overflowing past the edges). Variant option buttons: a selected non-color (text) option gets border+bold only, never a solid fill (color swatches keep their light tint) — same fix in all three variant-picker instances (PQV, store modal, product-page's related-items mini-modal). Name→price→desc→variants spacing unified to ~8px steps in both modals (was an inconsistent 6/14/16px jump). "לתשלום" → checkout accordion or `/checkout?pay=1`. PQV qty-stepper: shared SVG minus/plus icons + continuous border (was plain text glyphs with an extra divider, inconsistent with the rest of the site), flat — no inset shadow; main-image area `--color-surface`.
- **Categories (hierarchical, 2026-07-13):** Replaced flat `category?:string` with a per-store tree (max 3 levels, `StoreCategory{id,storeId,name,parentId,order}`, `data/store-categories.json`, `src/lib/store-categories.ts` — tree CRUD, `resolveCategoryFilterIds` self+descendant filter match, `resolveOrCreateCategoryPaths` batch CSV resolve-or-create). Dashboard Settings gets a tree editor (`category-tree.ts`); product forms get a nested dropdown-in-dropdown picker (`category-picker.ts`, `.order-status-dropdown` visual language) that creates a new category/subcategory inline without navigating away — create doesn't auto-select/close (a separate decision from assigning the product), nested "+ add subcategory" names its parent. `resolveCategoryId` in `/api/product.ts` re-validates a submitted `categoryId` belongs to the caller's store. **Gotcha:** `store-categories.ts` uses `node:fs` — never let a client-bundled script value-import it even transitively (crashed the whole dashboard once via `csv-bulk.ts`→`productsToCsv`, moved to server-only `store-products-bulk.ts`; verify via recursively fetching a client entry point's import graph and grepping for `node:` leaks if this resurfaces). CSV uses 3 separate columns (category/subcategory1/subcategory2, not a delimited path), auto-creates any named category/subcategory that doesn't exist yet; a filled subcategory with an empty parent column is a hard `category-orphan-subcategory` validation error, never silently dropped/promoted. Store page filter is a drill-down breadcrumb+chip nav, not a flat list — matches descendants too, and product filtering/pagination stays server-side (`fetchProductsPage()`→`/api/store-products?category=...`, same server-paginated "load more" as search/sort — a 2000-product store never ships more than one page to the browser); only the small category *tree* itself (names/hierarchy, not products) is embedded in the initial page load. `Store.categories?:string[]` (flat tags, unrelated — powers the *platform homepage's* browse-by-category shelves via `home-feed.ts`) deliberately kept separate, relabeled in Settings ("קטגוריות חנות (לעמוד הבית של הפלטפורמה)") to avoid confusion with the per-product tree. `tags?:string[]` (SEO/search) unchanged — added as chips via the same collapsed-trigger→input→Enter pattern as variant values (hidden hoisted-comma input keeps `parseTags` server-side unchanged), × arms a shared inline delete-confirm (`initRemoveConfirm()`, also resolves variant dim/chip removal; must be registered before the editors that arm it). Specs editor (`פרטים ומידות`) label/value inputs are fixed proportional widths (170px/220px, `flex:0 0 auto`) instead of `flex:1`.
- **Variants & inventory (dashboard, inline in product edit — not a separate tab, deliberate):** `StoreProduct.variants?:{name,options:string[]}[]` (seller-defined dimensions, e.g. Color/Size/anything) + `variantStock?:Record<comboKey,number>` (optional per-combo stock; `src/lib/variant-combo.ts` → `comboKey()`/`generateCombos()`, shared by dashboard + storefront). Dashboard combo grid: one sortable + filterable column per dimension (filter = funnel dropdown, multi-select, AND across columns) + sticky Stock column (sticky both axes) + sticky Total row whose label always states what it's summing ("All" or the active filter values). Top-level "מלאי" field becomes read-only/auto-summed the instant variants exist (no more editable-independently-of-the-breakdown confusion); legacy products without `variantStock` yet get rows seeded via even-split of the current total (never silently changes the saved sum). Products-table stock column gets a read-only per-variant breakdown dropdown (quick glance, no need to open edit). Color recognition (`src/lib/color-variants.ts`) matches singular+plural He/En names; an unrecognized color gets an inline native color-picker chip stored as `"name #hex"` — always route through `resolveVariantColor()` before display or the hex leaks into labels. A variant type name can't duplicate another, synonym-aware (`canonicalDimName()` in `variant-combo.ts` — e.g. "צבע"/"צבעים" or "מידה"/"מידות" count as the same type, reusing `color-variants.ts`'s `COLOR_VARIANT_NAME_GROUP`): flagged live (red border) and reinforced at save (client `readVariantDims` + server `parseVariantsPayload` both dedupe); a still-unnamed dimension blocks adding values under it (would otherwise silently vanish on save since both layers require a name). Storefront product page: per-combo stock gates add-to-cart max-qty + low/out-of-stock text for the selected combo (falls back to total `stock` when unset), and disables (dimmed+strikethrough, not hidden) the specific out-of-stock option button itself. **Checkout-time atomic enforcement (2026-07-12, closes the gap noted above):** `decrementStock()`/`restockProduct()`/`getEffectiveStock()` in `store-products.ts`, serialized via a new `src/lib/mutex.ts` so two concurrent checkouts on the same product/combo can't both read-then-write the same stale count. `/api/checkout` reserves stock per item as it validates the cart, not after building every order — an insufficient-stock item rolls back everything already reserved and rejects the whole checkout (409); the same rollback also covers a downstream order-creation failure after stock was already reserved (try/catch around order+notification+cart-cleanup). `decrementStock` returns `{ok,before,after}` — before/after read from inside the same mutex-protected write, not a separate call, which is also what the low/out-of-stock notification crossing-check below relies on to stay race-free. Also fixed: the product page's `schema.org` `availability` tag (what Google reads for in-stock/out-of-stock in search results) used to check only the flat `stock` field — `isProductInStock()` (`variant-combo.ts`) now checks every combo, falling back to the shared pool only for combos with no override. Gotchas: filter dropdowns render in one shared `document.body` portal (position:fixed, repositioned via `getBoundingClientRect()` on open) since a dropdown nested in the grid gets clipped by its own `overflow:auto` — any handler for portal content must skip the usual "no `.variants-editor` ancestor → return" guard; the mobile `.products-table tr.edit-row td{display:block}` rule is a descendant selector that also flattens this nested real `<table>`, needed a higher-specificity revert scoped to `.variant-combos`; `position:sticky` + `border-collapse:collapse` detaches borders, this table uses `separate`. **Variant-image linking (2026-07-12):** a color-variant value can be assigned one of the product's uploaded photos in the dashboard editor; clicking that color on the storefront (product page, PQV, store-page quick-view) swaps the main image to the assigned one. Enter-key flow in the dimension/value adder: Enter on the dimension name opens the value-adder, Enter on a value commits it and keeps the adder open for the next value — fixed a real reentrancy bug underneath this (`expandValueAdder()` was destroying+rebuilding a focused input via `innerHTML`, whose synchronous blur triggered a reentrant handler that wrote to the same now-detached node) by reusing the existing input instead of tearing it down when one's already present. Remove-confirm on a dimension/value now correctly refreshes the stock-combo table immediately (was stale until the next save) — the bug was looking up the enclosing `[data-variants-editor]` via `.closest()` *after* the node was already removed from the DOM, so the lookup silently failed on the detached node; fixed by resolving it before removal.
- **i18n:** `he`/`en` dictionaries; `getLang`/`getDir`/`getT`; lang cookie; RTL throughout. `buyerDashboard` section complete (all visible strings + JS strings via data attrs). **SEO/A11y:** `Seo.astro`, JSON-LD, sitemap, skip link, focus traps, ARIA, keyboard nav. **Tracking:** `src/lib/tracking.ts` — `trackViewContent` + `trackAddToCart` → dataLayer + fbq. GTM+Pixel on every page. Product OG tags. Sellers never touch ad config.
- **Image skeleton fixes (2026-07-14):** two real bugs, not missing features. Search-results product cards (`/search`) never had `initImageSkeletons()` wired to their own wrap class (only the store-card one was). Product page's main-image/thumb skeleton CSS was already correct but silently defeated by an inline `style="...background:var(--color-surface)"` on the same element — inline styles always beat a stylesheet rule regardless of specificity, so the `.is-loading` shimmer gradient never painted; fixed by moving `background` out of the inline style into the existing CSS rule (same class of bug as the header-search-bar `display` fix noted above — leave `background`/layout-affecting properties out of inline styles on any element a `.is-loading` class needs to override). Also fixed a real stored-XSS found in passing: the store-header product-search dropdown (`Header.astro`) inserted `p.name`/img `alt` raw into `innerHTML` with zero escaping (unlike the home-search dropdown right next to it, which already used `escH()`) — now routed through the same `escH()`.
- **Admin dashboard rebuilt (`/admin`, 2026-07-14):** full rebuild from a bare inline-styled prototype into the seller dashboard's own shell/tabs language — `dash-tabs`/`.dash-panel` markup + `initDashTabs()` reused unchanged, just new `data-panel` ids. Tabs: **overview** (platform stat cards — revenue is `paymentStatus:'paid'` orders only, reporting-only per the split-payment architecture, see below), **sellers** (one accordion card per seller — mirrors the seller dashboard's own `.order-card` pattern — with per-store product-count breakdown), **orders** (full table + client-side substring search by id/name/email/phone — fine at today's scale, not real pagination/search), **attention** (stores with 0 products or no shipping config — **view-only, never gates a seller**, per the zero-touch-self-service rule — confirmed with the user), **messages**, **alerts** (see below). `src/lib/admin-auth.ts` centralizes the admin-cookie check and fixes a real bug: the old `admin_token` cookie was `path:'/admin'`, so it silently never reached `/api/admin/*`. `src/lib/admin-stats.ts`'s aggregation functions take pre-fetched sellers/stores/orders/productCounts as params rather than each re-reading their own JSON file — `admin/index.astro` reads each source file exactly once per request.
- **Admin↔seller messaging (2026-07-14):** `src/lib/admin-messages.ts` (`data/admin-messages.json`) — deliberately a flat one-conversation-per-seller model, not subject-based reply-threads like buyer↔seller `messages.ts` (the admin only ever has one ongoing relationship with a given seller, no need for a thread hierarchy). New tab in both dashboards (admin's has a seller-picker + compose box + existing-thread list; seller's is a single continuous thread under "הודעות מהמנהל"); new `'admin_message'` `NotificationType` routes the seller's existing bell/toast to it, no new polling plumbing needed. Admin-side thread row shows an unread dot and re-sorts to the top on reply (matches `getAdminThreadSummaries()`'s own "most recent first" ordering).
- **Operational error logging (2026-07-14):** `src/middleware.ts` logs to `data/error-log.json` (capped 500 entries) only on a genuinely thrown/uncaught server exception — **deliberately not** any route that returns `>=500` itself (a route like `/api/auth/google`'s intentional 503 "OAuth not configured" produced a content-free false-positive alert under that broader heuristic). A route that wants its own caught error visible in the admin Alerts tab calls `logError()` directly instead (`/api/checkout`'s catch block is the first example — logs the real message/stack, still restocks + returns the same response as before). Client-side `src/scripts/error-reporter.ts` (`window.onerror`/`unhandledrejection`, capped 5 reports/session) posts to the intentionally-unauthenticated `/api/log-client-error` (body-size capped, rejects before parsing) into the same log, shown in the Alerts tab.

---

## Project structure
```
src/i18n/translations.ts + index.ts ← he+en string dictionaries; getLang/getDir/getT
src/config/store.config.ts      ← platform config + formatPrice + cdnSrc
data/sellers.json               ← seller accounts
data/stores.json                ← store records
data/store-products.json        ← per-store products (storeId field)
data/user-carts.json            ← server-side cart + wishlist per user
data/messages.json              ← message threads
data/notifications.json         ← notifications (message-thread ones deleted on read; any notif also deletable one-by-one or all-at-once by the user)
data/orders.json                ← orders
data/store-categories.json      ← per-store category tree (max 3 levels)
data/admin-messages.json        ← flat admin↔seller conversations (one per seller, no reply-tree)
data/error-log.json             ← ErrorLogEntry log (capped 500 entries), server+client sources
src/layouts/BaseLayout.astro    ← page shell (GTM+Pixel injection)
src/components/Seo.astro        ← all meta/OG/JSON-LD
src/components/Header.astro     ← nav; notifications bell (polls 30s); back-to-store pill; avatar dropdown = app menu
src/components/CartDrawer.astro
src/components/WishlistDrawer.astro
src/components/ConfirmModal.astro      ← global <dialog>, event-driven (confirm:open); onConfirm may be async (busy-state dot-pulse until resolved)
src/components/ProductQuickView.astro  ← global PQV + lightbox; pqv:open event
src/components/MessageCompose.astro    ← buyer contact form
src/components/StoreAvatar.astro       ← store profile-image circle + gradient-initial fallback; shared by banner/homepage/messages
src/lib/cart.ts                 ← CartItem, localStorage, events, syncCartImages, getCartQty (stock-ceiling lookup)
src/lib/wishlist.ts + wishlist-counts.ts ← WishlistItem, localStorage; count badge sync
src/lib/ripple.ts + burst.ts    ← spawnRipple(); spawnBurst() (radial dot burst, fixed-position so it escapes rounded overflow:hidden containers)
src/lib/read-more.ts            ← initReadMore() (inline read-more/less)
src/lib/seller-auth.ts          ← Seller interface, register/login/session, Google OAuth
src/lib/last-store.ts           ← saveLastStore/getLastStore/clearLastStore (sessionStorage) — back-to-store pill state
src/lib/scroll-restore.ts       ← saveStoreScroll/applyStoreScrollQuery/restoreStoreScroll (sessionStorage, self-clearing) — store page scroll+filter restore on back-nav
src/lib/stores.ts               ← Store interface + CRUD
src/lib/store-products.ts       ← StoreProduct interface + CRUD + isSkuTaken + decrementStock/restockProduct/getEffectiveStock (mutex-serialized atomic stock adjust, returns {ok,before,after}) — bulk upsert moved out, see below
src/lib/store-products-bulk.ts  ← bulkUpsertProducts (CSV batch create/update, preserve-on-blank) + productsToCsv (moved from csv-bulk.ts — needs store-categories.ts, so must stay server-only)
src/lib/store-categories.ts     ← StoreCategory tree CRUD, resolveCategoryFilterIds, resolveOrCreateCategoryPaths (fs-backed, node:fs — never let client-bundled code value-import this)
src/lib/mutex.ts                ← Mutex — serializes async read-modify-write JSON ops (used by store-products.ts's stock functions)
src/lib/csv-bulk.ts             ← CSV parse/stringify (3 category columns), header i18n mapping, row validation incl. orphan-subcategory check, templateCsv (pure, isomorphic)
src/lib/product-form.ts         ← parseImages/Category/Sku/Tags/Specs/VariantsPayload — shared by /api/product.ts
src/lib/variant-combo.ts        ← comboKey()/generateCombos()/canonicalDimName()/isProductInStock()/LOW_STOCK_THRESHOLD — shared by dashboard variant editor + storefront combo-stock lookups + checkout notification threshold (pure/isomorphic, no `node:fs`, so both server and browser-bundled scripts can import it)
src/lib/color-variants.ts       ← resolveVariantColor()/isColorVariant(); exports COLOR_VARIANT_NAME_GROUP (reused by variant-combo.ts's canonicalDimName)
src/lib/gallery-widget.ts       ← gallery HTML helper
src/lib/tracking.ts             ← trackViewContent, trackAddToCart
src/lib/user-carts.ts           ← getUserCart / saveUserCart
src/lib/cart-sync.ts            ← merge guest→user, debounced save
src/lib/orders.ts               ← Order interfaces + CRUD + buyerId
src/lib/messages.ts             ← Message interface + CRUD + markRead
src/lib/seller-alerts.ts        ← getSellerStoreAlerts/sellerHasAnyAlert (pending orders + unread msgs incl. replies)
src/lib/notifications.ts        ← Notification interface + CRUD; NotificationType incl. 'admin_message'
src/lib/admin-auth.ts           ← isAdminRequest/requireAdmin/setAdminCookie/clearAdminCookie (admin_token cookie, path:'/')
src/lib/admin-stats.ts          ← getPlatformOverview/getSellerCards/getStoresNeedingAttention/getProductCountsByStore — pure, take pre-fetched data as params
src/lib/admin-messages.ts       ← AdminMessage (flat per-seller conversation) + summarizeAdminThreads (pure) + getAdminThreadSummaries
src/lib/error-log.ts            ← ErrorLogEntry, logError()/getRecentErrors()/clearErrorLog()/truncateStack() (pure)
src/lib/format-date.ts          ← formatHeDateTime() — isomorphic, safe from both server frontmatter and client scripts
src/lib/html-escape.ts          ← escapeHtml() — client-only (DOM), for innerHTML-building client scripts
src/middleware.ts               ← onRequest — logs only thrown/uncaught server errors, never a route's own deliberate status code
src/lib/cloudinary.ts           ← thumbUrl server-side
src/lib/toolbar-portal.ts       ← createFloatingPortal() — shared floating dropdown (position, outside-click) for sort/filter toolbars
src/lib/home-feed.ts            ← buildHomeFeed()/getCategoryCounts() — homepage shelf-selection strategy (liked/buyAgain/new/category/discovery/spotlight), seeded shuffle
src/lib/img-skeleton.ts         ← clearSkeletonOnLoad()/initImageSkeletons() — shared skeleton-shimmer-clear, homepage + /stores
src/lib/site-search.ts          ← searchSite() — platform-wide store+product search; configurable storeLimit/productLimit/imageWidth (header dropdown preview vs. /search page)
src/workers/bg-removal.ts       ← BG removal Web Worker (@imgly, mask-on-small + apply-to-full-res)
src/scripts/dashboard/          ← 14 modules: bg-worker, cleanup-modal, cloudinary, status, crop-modal, gallery, products, ui, store-image, csv-import, csv-preview, category-tree (Settings tab tree editor), category-tree-cache (shared tree cache, avoids products.ts↔category-picker.ts circular import), category-picker (nested dropdown-in-dropdown for product forms)
src/components/HomeShelf.astro  ← homepage shelf section (title + StoreCard row)
src/components/StoreCard.astro  ← compact store card — homepage shelves + /stores directory
src/pages/index.astro           ← SSR homepage
src/pages/stores.astro          ← SSR /stores directory — search/category-filter/pagination
src/pages/search.astro          ← SSR dedicated /search results page — stores (StoreCard) + products, back button (history.back with same-origin-referrer check)
src/pages/store/[slug].astro    ← SSR store page
src/pages/store/[storeSlug]/[productSlug].astro ← SSR product page
src/pages/seller/               ← register, login, dashboard (?panel=X URL sync)
src/pages/buyer/dashboard.astro ← buyer area (?tab=X URL sync)
src/pages/checkout.astro + checkout/success.astro ← SSR checkout + confirmation
src/pages/admin/index.astro     ← admin dashboard shell, 6 tabs (see Features built)
src/pages/admin/login.astro + logout.ts ← admin auth (ADMIN_SECRET env, default 'admin' in dev)
src/components/admin/           ← AdminOverviewPanel/AdminSellersPanel/AdminOrdersPanel/AdminAttentionPanel/AdminMessagesPanel/AdminAlertsPanel — display-only, one per /admin tab
src/scripts/admin/               ← orders-filter, admin-messages, alerts — client wiring for /admin
src/scripts/error-reporter.ts   ← initErrorReporter() — window.onerror/unhandledrejection → /api/log-client-error, capped 5/session
src/pages/api/messages.ts       ← GET/POST messages
src/pages/api/notifications.ts  ← GET/POST notifications
src/pages/api/admin-messages.ts ← seller-facing admin-thread GET/POST (session-guarded, own sellerId only)
src/pages/api/admin/messages.ts ← admin-facing GET/POST (admin-guarded), api/admin/errors.ts ← GET/clear error log
src/pages/api/log-client-error.ts ← unauthenticated client-error intake, body-size + field-length capped
src/pages/api/seller/alerts.ts  ← GET {hasAlert} for header avatar dot
src/pages/api/search.ts         ← GET site-wide search (stores+products) for the header dropdown, wraps site-search.ts
src/pages/api/                  ← product, store, wishlist, lang, user-cart, checkout
src/pages/api/store-product/bulk.ts ← GET export CSV / POST dry-run preview + commit import
src/pages/api/store-category.ts ← POST create/rename/delete/move-category (authorizes via getStoresBySellerId, returns fresh tree)
src/pages/api/auth/google.ts    ← OAuth initiation
src/pages/api/auth/google/callback.ts ← OAuth callback
src/styles/main.css             ← single CSS entry point
src/styles/base/tokens.css      ← CSS variables
src/styles/base/reset.css
src/styles/layout/container.css
src/styles/components/          ← buttons, cards, forms, header, footer, cart-drawer, store-card, etc.
src/styles/pages/               ← home, auth, dashboard, store, product, checkout, stores, search
src/styles/utilities/utils.css  ← .muted, .badge, skeleton-shimmer, msg-thread, notif-dot, dot-pulse loader
src/env.d.ts                    ← global Window types (dataLayer, fbq, __sessionUserId, etc.)
_backup/store-slug.astro        ← store page pre-modal snapshot (revert if needed)
.claudeignore                   ← tells Claude to skip node_modules, dist, _backup, pics, fonts
vitest.config.ts                ← node env default; per-file `// @vitest-environment jsdom` where DOM/localStorage is touched
tests/cart.test.ts              ← addItem stock-ceiling clamp, per-variant qty, makeCartKey
tests/checkout.test.ts          ← /api/checkout server-side price/shipping re-validation, signed-in buyer cart cleanup, atomic stock decrement/rollback, low-stock/out-of-stock notification crossing+mutual-exclusivity
tests/csv-bulk.test.ts          ← CSV parse/stringify round-trip, sku-duplicate detection, formula-injection sanitization
tests/store-products-bulk.test.ts ← bulkUpsertProducts create/update/preserve-on-blank/cross-store isolation, isSkuTaken
tests/store-products.test.ts    ← decrementStock/restockProduct/getEffectiveStock — variant-combo bucket vs shared pool, concurrent-decrement serialization
tests/variant-combo.test.ts     ← isProductInStock — shared-pool fallback vs per-combo override
tests/admin-stats.test.ts       ← isStoreIncomplete — the "needs attention" heuristic
tests/admin-messages.test.ts    ← summarizeAdminThreads — grouping/ordering/unread-count
tests/error-log.test.ts         ← truncateStack
```

---

## Workflow
1. Read this file + `CURRENT_TASK.md`.
2. Do only what is in `CURRENT_TASK.md → Your instruction`.
3. Before reporting any change done: `tsc` clean, `npm test` clean (add/extend a test if the change touched money/auth/inventory/cart/checkout — see Hard rules → Testing), and run `/code-review` on the diff if it touched that same surface (see Hard rules → Security review gate). Then mark a `✅ בוצע (date) — ...` line right under the specific numbered item in `CURRENT_TASK.md` it belongs to (not just at end-of-session) — this is how the user tracks what's done across parallel sessions, so it must never be skipped once a numbered item is actually finished.
4. **Parallel sessions (2026-07-14):** two Claude sessions on this repo at once is fine with **no worktree** as long as the two tasks share zero files (check `git status`/the actual code paths first, don't assume). Only if there's real file overlap: use a git worktree — but always start it from a **fresh** session, never by pulling an already-long session into one (that drags its whole history along and saves nothing). One task per worktree; merge then `ExitWorktree remove` as soon as it's done — don't leave one open "just in case". `CURRENT_TASK.md` is always shared/live — never write to it while the user is actively editing it in their IDE (ask/wait instead of racing the save).
5. **End of session** (trigger: "next session" / "we're done" / "end of session" / "סגור את הסשן") — do ALL of:
   - Update **Features built**: merge new items into existing bullets (never add a bullet if the feature already exists). One line per feature max. No implementation details — names + key gotchas only.
   - Update **Project structure**: add new files, remove deleted ones. One line per file.
   - Update `CURRENT_TASK.md → Next` and `Recommended next step` (one plain line, the recommendation only — never a recap of what was done this session).
   - Keep this file lean (~200 lines is a trigger to compress, not a hard ceiling): merge bullets, drop details already recoverable from the code. Don't force-cut real, non-recoverable context (a decision, a gotcha) just to hit the number.
   - **Never change `Your instruction`** — only the user changes that.
